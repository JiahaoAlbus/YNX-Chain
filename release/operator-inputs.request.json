{
  "schemaVersion": 1,
  "productId": "ynx-oracle-market-data",
  "requestedAt": "2026-07-27T03:17:46Z",
  "status": "limited_source_public_testnet_activated",
  "activationMode": "limited",
  "inputs": [
    {
      "id": "public_service",
      "required": false,
      "satisfied": true,
      "evidence": "release/evidence/oracle-public-testnet-f71d5ca.json",
      "request": "Provide the approved Oracle Testnet server host/user, an SSH key path available outside chat, the Oracle API hostname with DNS pointed to that server, and Caddy ingress authority. Deployment variables: SERVER_HOST, SERVER_USER, SSH_KEY_PATH, ORACLE_API_DOMAIN.",
      "secret": true
    },
    {
      "id": "oracle_state_integrity_key",
      "required": false,
      "satisfied": true,
      "evidence": "Provisioned owner-only on the Testnet host; value was not transmitted or committed.",
      "request": "Provision /etc/ynx-oracle/oracle.env on the server as owner-only mode 0600 with YNX_ORACLE_STATE_HMAC_KEY_HEX containing at least 32 random bytes encoded as hex. Do not transmit the value in chat or commit it.",
      "secret": true
    },
    {
      "id": "oracle_web_origin",
      "required": false,
      "satisfied": true,
      "evidence": "Exact HTTPS Oracle Web origin is configured at the public API boundary.",
      "request": "Provide the approved HTTPS Oracle Web origin for exact read-only CORS. Deployment variable: ORACLE_PUBLIC_ORIGIN.",
      "secret": false
    },
    {
      "id": "provider_rights",
      "required": false,
      "requiredFor": "authoritative_price_publication",
      "request": "Executed rights or written approval for at least three independent YNX-capable market-data sources, including redistribution, benchmark/valuation, retention, regions, quotas, costs, and decommission terms.",
      "secret": false
    },
    {
      "id": "reporter_signers",
      "required": false,
      "requiredFor": "authoritative_price_publication",
      "request": "Secure signer or HSM paths and public reporter identities for each approved provider. Do not transmit private keys in chat or commit them.",
      "secret": true
    }
  ],
  "prohibitedChatInputs": ["Private key", "SSH private key", "Oracle state HMAC key", "Seed phrase", "PEM", "Full API secret", "Validator key", "Payment card data"]
}
