# YNX Chain Python SDK

Dependency-free Python client for YNX Chain REST status and EVM JSON-RPC. The package is prepared for local installation and validation; this repository does not claim that it is published to PyPI.
