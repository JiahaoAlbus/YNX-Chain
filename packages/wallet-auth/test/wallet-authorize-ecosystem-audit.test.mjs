import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { test } from "node:test";
import { verifyWalletAuthorizeConsumers } from "../scripts/verify-no-bare-wallet-authorize.mjs";

const root = fileURLToPath(new URL("../../..", import.meta.url));
const audit = JSON.parse(await readFile(new URL("../../../release/integration/wallet-authorize-ecosystem-source-runtime-audit-20260821.json", import.meta.url), "utf8"));
const auditV2 = JSON.parse(await readFile(new URL("../../../release/integration/wallet-authorize-ecosystem-source-runtime-audit-v2-20260821.json", import.meta.url), "utf8"));
const auditV3 = JSON.parse(await readFile(new URL("../../../release/integration/wallet-authorize-ecosystem-owner-runtime-matrix-v3-20260821.json", import.meta.url), "utf8"));
const providerRecovery = JSON.parse(await readFile(new URL("../../../release/integration/wallet-provider-discovery-connect-state-p0-handoff-20260821.json", import.meta.url), "utf8"));
const pendingOwnerHandoffs = JSON.parse(await readFile(new URL("../../../release/integration/wallet-provider-connect-pending-owner-handoffs-20260821.json", import.meta.url), "utf8"));
const ownerActivityCheckpoint = JSON.parse(await readFile(new URL("../../../release/integration/wallet-provider-connect-owner-activity-checkpoint-20260821.json", import.meta.url), "utf8"));
const publicOwnerRecheck = JSON.parse(await readFile(new URL("../../../release/integration/wallet-provider-public-owner-recheck-20260821.json", import.meta.url), "utf8"));
const ownerPublicCompletion = JSON.parse(await readFile(new URL("../../../release/integration/wallet-provider-owner-public-completion-checkpoint-20260821.json", import.meta.url), "utf8"));
const walletWebIdentityDrift = JSON.parse(await readFile(new URL("../../../release/integration/wallet-web-public-identity-drift-recheck-20260822.json", import.meta.url), "utf8"));
const registry = JSON.parse(await readFile(new URL("../product-session-registry.json", import.meta.url), "utf8"));

test("ecosystem authorize audit covers every registered client exactly once", () => {
  assert.equal(audit.registeredClientCount, 13);
  assert.equal(audit.requiredProductCount, 12);
  assert.equal(audit.nonProductRegistryClientCount, 1);
  assert.deepEqual(audit.products.map(({ productId }) => productId).sort(), registry.products.map(({ productId }) => productId).sort());
  assert.equal(new Set(audit.products.map(({ productId }) => productId)).size, audit.products.length);
});

test("v1 consumer audit remains an immutable historical checkpoint", () => {
  const frozenFindings = [...audit.scanner.registeredProductFindings, ...audit.scanner.otherEcosystemFindings];
  assert.equal(frozenFindings.length, audit.scanner.findingCount);
  assert.equal(new Set(frozenFindings.map(({ file, line, code }) => `${file}:${line}:${code}`)).size, frozenFindings.length);
});

test("no registered product is promoted without the three owner evidence segments", () => {
  assert.equal(audit.productsConnected, 0);
  assert.equal(audit.productsMigratedV2, 0);
  assert.equal(audit.truth.registryV3Public, false);
  assert.equal(audit.truth.deployedPublic, false);
  assert.deepEqual(audit.requiredOwnerEvidenceSegments.map(({ segment }) => segment), ["runtime-source", "public-gateway-v2", "visible-platform-lifecycle"]);
  for (const product of audit.products) {
    assert.equal(product.runtime.connected, false, product.productId);
    assert.equal(product.runtime.productSessionV2, false, product.productId);
    assert.ok(product.blocker.length > 20, product.productId);
    assert.ok(product.migrationHandoff.length >= 3, product.productId);
  }
});

test("every EVM product handoff addresses the exact 0x1917 MetaMask flow", () => {
  for (const product of audit.products.filter(({ evmCompatible }) => evmCompatible)) {
    assert.notEqual(product.audit.metaMask0x1917, "not-applicable", product.productId);
    assert.ok(product.migrationHandoff.join(" ").includes("0x1917"), product.productId);
  }
});

test("v2 ecosystem audit consumes every exact owner source without promoting runtime", () => {
  assert.equal(auditV2.ownerInputs.length, registry.products.length);
  assert.deepEqual(auditV2.products.map(({ productId }) => productId).sort(), registry.products.map(({ productId }) => productId).sort());
  assert.equal(auditV2.productsConnected, 0);
  assert.equal(auditV2.productsMigratedV2, 0);
  assert.equal(auditV2.truth.macComputerControl, false);
  for (const product of auditV2.products) {
    assert.equal(product.runtime.productSessionV2, false, product.productId);
    assert.equal(product.runtime.computerControl, false, product.productId);
    assert.ok(product.blocker.length > 40, product.productId);
    assert.equal(product.handoff.length, 3, product.productId);
  }
});

test("v2 baseline scanner evidence matches the recovery branch without claiming owner-current source", async () => {
  const findings = await verifyWalletAuthorizeConsumers(root);
  assert.equal(auditV2.scanner.baselineScope, "RECOVERY_BRANCH_SNAPSHOT_NOT_OWNER_CURRENT_STATE");
  assert.equal(findings.length, auditV2.scanner.findingCount);
  assert.deepEqual(Object.fromEntries([...new Set(findings.map(({ code }) => code))].sort().map((code) => [code, findings.filter((finding) => finding.code === code).length])), auditV2.scanner.findingCountsByCode);
  for (const finding of auditV2.scanner.registeredBaselineFindings) {
    assert.ok(findings.some(({ file, line, code }) => file === finding.file && line === finding.line && code === finding.code), `${finding.productId}:${finding.file}:${finding.code}`);
  }
  assert.deepEqual(auditV2.scanner.verifiedOwnerSourceSupersessions, [{
    owner: "explorer-monitor",
    file: "apps/monitor/src/App.tsx",
    baselineLine: 558,
    baselineCode: "NONCANONICAL_WALLET_AUTHORIZE_URI",
    ownerCommit: "70defaf9667b6513fa1e02530e69a172c6de97f5",
    directSourceVerification: "No ynx-wallet://, ynxwallet://, location.href, or window.location match; baseline line is a password input.",
    runtimeOrMigrationPromotion: false,
  }]);
});

test("safe-launcher and MetaMask counts are derived from product rows", () => {
  const products = auditV2.products.filter(({ nonProductRegistryClient }) => !nonProductRegistryClient);
  assert.equal(products.filter(({ safeLauncherV2Consumed }) => safeLauncherV2Consumed).length, auditV2.safeLauncherV2SourceConsumedProductCount);
  assert.equal(products.filter(({ sourceAudit }) => sourceAudit.metaMaskAddSwitch0x1917 === true).length, auditV2.productsWithCompleteMetaMaskSourcePath);
  for (const product of products.filter(({ sourceAudit }) => sourceAudit.standardWalletIndependentFromProductSession !== true)) {
    assert.match(product.blocker, /standard|provider|Product Session/i, product.productId);
  }
});

test("v3 owner/runtime matrix tracks all twelve products and preserves false authority gates", () => {
  assert.deepEqual(auditV3.registeredProducts.map(({ productId }) => productId).sort(), auditV2.products.filter(({ nonProductRegistryClient }) => !nonProductRegistryClient).map(({ productId }) => productId).sort());
  assert.equal(auditV3.counts.registeredProducts, 12);
  assert.equal(auditV3.counts.productsConnected, 0);
  assert.equal(auditV3.counts.productsMigratedV2, 0);
  assert.equal(auditV3.truth.macComputerControl, false);
  for (const product of auditV3.registeredProducts) {
    assert.equal(product.runtime.productSessionV2, false, product.productId);
    assert.ok(product.ownerHandoff.length > 80, product.productId);
  }
  assert.deepEqual(auditV3.registeredProducts.filter(({ runtime }) => runtime.realInstalledApproval === true).map(({ productId }) => productId), ["calendar"]);
});

test("E2E execution is centrally gated across all products instead of treating source checkpoints as completion", () => {
  assert.equal(auditV3.standardWalletE2EExecution.plan, "release/integration/wallet-standard-wallet-e2e-execution-plan-p0-20260822.json");
  assert.equal(auditV3.standardWalletE2EExecution.status, "OWNER_DISPATCH_READY_DIRECT_E2E_REQUIRED");
  assert.deepEqual(auditV3.standardWalletE2EExecution.dappCoverage.external, ["uniswap-interface-reference", "opensea-reference", "safe-reference"]);
  assert.equal(auditV3.standardWalletE2EExecution.dappCoverage.directRuntimeCount, 0);
  assert.equal(auditV3.standardWalletE2EExecution.walletConnect.realProjectIdAndRelay, false);
  assert.equal(auditV3.standardWalletE2EExecution.sourceCheckpointIsCompletion, false);
  assert.equal(ownerActivityCheckpoint.standardWalletE2EOrchestration.mode, "EXECUTION_ORCHESTRATION_NOT_RECEIPT_CONSUMPTION");
  assert.equal(ownerActivityCheckpoint.standardWalletE2EOrchestration.directRuntimeEvidenceRecorded, false);
});

test("Protocol Owner public recheck records regressions without promoting product completion", () => {
  assert.equal(publicOwnerRecheck.sharedProviderConnectAuthority.sourceCommit, auditV3.sharedProviderConnectRecovery.sourceCommit);
  assert.equal(publicOwnerRecheck.sharedProviderConnectAuthority.sourceTree, auditV3.sharedProviderConnectRecovery.sourceTree);
  assert.equal(publicOwnerRecheck.method.newSensitiveRequestTriggered, false);
  assert.equal(publicOwnerRecheck.products.shop.existingMetaMaskAccountRestored, true);
  assert.equal(publicOwnerRecheck.products.shop.connectedAccountActionOpenedConnectionChooser, true);
  assert.equal(publicOwnerRecheck.products.shop.connectedAccountActionOpenedConnectionDetails, false);
  assert.equal(publicOwnerRecheck.products.shop.exclusiveCausalityToShopClickProven, false);
  assert.equal(publicOwnerRecheck.products.calendar.privateProductSessionFailureRemovedStandardConnection, false);
  assert.equal(publicOwnerRecheck.products.calendar.connectedAccountActionOpenedGuestLocalDataDialog, true);
  assert.equal(publicOwnerRecheck.products.calendar.disconnectActionVisible, false);
  assert.equal(publicOwnerRecheck.products.card.metaMaskRestoredInShopAndCalendarSameChrome, true);
  assert.equal(publicOwnerRecheck.products.card.cardMetaMaskDetectionSucceeded, false);
  assert.equal(publicOwnerRecheck.truth.productsConnected, 0);
  assert.equal(publicOwnerRecheck.truth.productsMigratedV2, 0);
  for (const productId of ["shop", "calendar", "card"]) {
    const product = auditV3.registeredProducts.find((entry) => entry.productId === productId);
    assert.equal(product.runtime.protocolOwnerPublicRecheck, "release/integration/wallet-provider-public-owner-recheck-20260821.json");
    assert.equal(product.runtime.productSessionV2, false);
  }
});

test("latest iOS second-launch malformed callback proof remains a fail-closed non-public slice", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.iosSecondLaunchMalformedCallback;
  assert.equal(evidence.evidenceCommit, "714c91e758d9f08d3c9ee25bf2a9543f0046ed15");
  assert.equal(evidence.secondLaunchMalformedCallbackDelivered, true);
  assert.equal(evidence.secondLaunchMalformedCallbackBoundToSecondPid, true);
  assert.equal(evidence.visibleFailClosed, "INVALID_DEEP_LINK");
  assert.equal(evidence.overallPassed, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
  assert.equal(evidence.public, false);
  assert.equal(evidence.productionSigned, false);
  assert.equal(evidence.appStore, false);
});

test("macOS notarized manifest command repair remains source-only", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosNotarizedManifestCommandFix;
  assert.equal(evidence.implementationCommit, "04074a1f921693659b81111b9df638553c1b8ad4");
  assert.equal(evidence.evidenceCommit, "637e0bf2d5b6f9815fd6769a1d5611df30c44a5e");
  assert.equal(evidence.notarizedManifestCommandSourceRepaired, true);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.productionWorkflowDispatched, false);
  assert.equal(evidence.productionCredentialsRead, false);
  assert.equal(evidence.developerId, false);
  assert.equal(evidence.notarized, false);
  assert.equal(evidence.notarizedDmg, false);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
  assert.equal(evidence.artifactDownloadInspected, false);
});

test("owner public completion checkpoint rejects local, historical and source-only substitutes", () => {
  assert.equal(auditV3.ownerPublicCompletionCheckpoint, "release/integration/wallet-provider-owner-public-completion-checkpoint-20260821.json");
  assert.deepEqual(Object.keys(ownerPublicCompletion.ownerReadback).sort(), registry.products.filter(({ productId }) => productId !== "wallet-web-companion").map(({ productId }) => productId).sort());
  assert.equal(ownerPublicCompletion.completionRule.ownerDeploymentRequired, true);
  assert.equal(ownerPublicCompletion.completionRule.ownerMustPersonallyOpenRealPublicEntry, true);
  assert.equal(ownerPublicCompletion.completionRule.sourceOrPrOrTestsAloneComplete, false);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.ownerOpenedLocalEntry, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.ownerOpenedRealPublicEntryAfterRegression, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.ownerFixDeployedPublic, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.regressionClosed, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.connectionDetailsVisible, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.disconnectControlVisible, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.switchAccountControlVisible, true);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.disconnectExecuted, false);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.switchAccountExecuted, false);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.newApproveRejectCallbackProved, false);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.productSessionV2, false);
  assert.equal(ownerPublicCompletion.ownerReadback.calendar.complete, false);
  assert.equal(ownerPublicCompletion.ownerReadback.card.ownerOpenedRealPublicEntryAfterFix, false);
  assert.equal(ownerPublicCompletion.ownerReadback.shop.ownerOpenedRealPublicEntryAfterRegressionFix, false);
  assert.equal(ownerPublicCompletion.counts.ownersWithPostFixPublicOpenEvidence, 1);
  assert.equal(ownerPublicCompletion.counts.ownerFixesDeployedPublic, 1);
  assert.equal(ownerPublicCompletion.counts.completeProducts, 0);
  assert.equal(ownerPublicCompletion.truth.deployedPublicAggregate, false);
});

test("Calendar P0-143 closes connection-details regression without claiming full lifecycle", () => {
  const calendar = auditV3.registeredProducts.find(({ productId }) => productId === "calendar");
  assert.equal(calendar.sourceCommit, "d2044bd9a8ec6bdc6235dfb625a62c3dd5c58b04");
  assert.equal(calendar.sourceTree, "b11b1e65b0026ed2bfd5d3306bd85a5c63ab3f53");
  assert.equal(calendar.ownerEvidenceCommit, "2426c2e2184f685467886fb7427950f78d089024");
  assert.equal(calendar.runtime.sourceBoundPublic, true);
  assert.equal(calendar.runtime.connectedAccountActionOpenedConnectionDetails, true);
  assert.equal(calendar.runtime.connectedAccountActionOpenedGuestLocalDataDialog, false);
  assert.equal(calendar.runtime.disconnectControlVisible, true);
  assert.equal(calendar.runtime.switchAccountControlVisible, true);
  assert.equal(calendar.runtime.refreshRestore, true);
  assert.equal(calendar.runtime.chooserClosedAfterSuccess, true);
  assert.equal(calendar.runtime.privateCalendarSyncDegradedIndependent, true);
  assert.equal(calendar.runtime.disconnectExecuted, false);
  assert.equal(calendar.runtime.switchAccountExecuted, false);
  assert.equal(calendar.runtime.callback, false);
  assert.equal(calendar.runtime.productSessionV2, false);
  assert.equal(calendar.runtime.computerControl, false);
});

test("iOS ASC upload copy cleanup stays non-production", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.iosAscUploadKeyCopyCleanup;
  assert.equal(evidence.implementationCommit, "42bcd3c1996db64830d91ac1a0e182cfa2bb720b");
  assert.equal(evidence.evidenceCommit, "4bc080c4831d0d5e749b7ec518dd26a3ebb72200");
  assert.equal(evidence.ascUploadKeyCopyCleanupSourceImplemented, true);
  assert.equal(evidence.runtimeVerifiedWithoutProductionSecrets, true);
  assert.equal(evidence.expectedKeychainSearchListSha256, evidence.restoredKeychainSearchListSha256);
  assert.ok(evidence.temporaryMaterialsDeleted.includes("asc-upload-key-copy"));
  assert.equal(evidence.productionDistributionSkipped, true);
  assert.equal(evidence.productionCredentialsUsed, false);
  assert.equal(evidence.ipa, false);
  assert.equal(evidence.appStoreConnectUpload, false);
  assert.equal(evidence.appStore, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
});

test("macOS native engineering DMG stays an inspected Gatekeeper-rejected CI artifact", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosNativeEngineeringDmg;
  assert.equal(evidence.implementationCommit, "b1f7bff5732a5ab85fa87345dd9ea3b59c8d9431");
  assert.equal(evidence.evidenceCommit, "a00397cdf669bd71cd452af617ce6debf53a1680");
  assert.equal(evidence.supplementImplementationCommit, "af8dba8b88a187f92d149ac5b6d84db35e564ee3");
  assert.equal(evidence.supplementEvidenceCommit, "af275bbe5586feb442976a0ebf88f1569dd08774");
  assert.equal(evidence.engineeringDmgCreated, true);
  assert.equal(evidence.engineeringDmgMounted, true);
  assert.equal(evidence.containedBundleId, "com.ynxweb4.wallet.macos");
  assert.equal(evidence.canonicalCallbackScheme, "ynxwallet");
  assert.equal(evidence.deepAdHocCodesignVerified, true);
  assert.equal(evidence.spctlExit, 3);
  assert.equal(evidence.gatekeeperBoundaryVerified, true);
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.artifactDownloadInspected, false);
  assert.equal(evidence.innerDmgFactsSource, "PUBLIC_ACTIONS_LOG");
  assert.equal(evidence.innerDmgSha256, "81a635598fee01d4fe12d617866df7c52b09acc09ea447826a2f1bee6e6ac662");
  assert.equal(evidence.innerDmgBytes, 307402);
  assert.equal(evidence.minimumMacOS, "13.0");
  assert.deepEqual(evidence.architectures, ["x86_64", "arm64"]);
  assert.equal(evidence.signingClass, "adhoc");
  assert.equal(evidence.teamIdentifierPresent, false);
  assert.equal(evidence.hdiutilChecksumValid, true);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.systemApplicationsInstalled, false);
  assert.equal(evidence.developerId, false);
  assert.equal(evidence.notarized, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
});

test("macOS engineering DMG prerelease gate is source-ready and default-off", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosEngineeringDmgPrereleaseGate;
  assert.equal(evidence.implementationCommit, "93439d277505cfeceacb953e789a630c4fe2e7d9");
  assert.equal(evidence.evidenceCommit, "1d37a63eff1b7da2d96ff881c31ad5cb0435f6a0");
  assert.equal(evidence.sourceReady, true);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.defaultOff, true);
  assert.equal(evidence.publicationJobSkipped, true);
  assert.equal(evidence.defaultMainWorkflowPresent, false);
  assert.equal(evidence.tagCreated, false);
  assert.equal(evidence.releaseCreated, false);
  assert.equal(evidence.dispatch, false);
  assert.equal(evidence.runDmgSha256, "2286abc11fe85c09986478cee602e956b6ff387e6c0f1d7bd999ab5d5b26fe69");
  assert.equal(evidence.signingClass, "adhoc");
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.publicArtifact, false);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.developerId, false);
  assert.equal(evidence.notarized, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
});

test("macOS release manifest is truthful source with local-only runtime proof", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosTruthfulReleaseManifestSource;
  assert.equal(evidence.implementationCommit, "ae553387b67c3dc6643f1f3ea4b7dc2d2dfe96d5");
  assert.equal(evidence.evidenceCommit, "1654fb467efa0bd2e31f43e11c09cebd4205c4cf");
  assert.equal(evidence.sourceReady, true);
  assert.equal(evidence.localRuntimeVerified, true);
  assert.equal(evidence.localManifestJsonParsed, true);
  assert.equal(evidence.localManifestExactInputsReadBack, true);
  assert.equal(evidence.localManifestReleaseCreated, false);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.hostedManifestRuntimeVerified, false);
  assert.equal(evidence.publicationJobSkipped, true);
  assert.equal(evidence.defaultMainRegistration, false);
  assert.equal(evidence.operatorDispatch, false);
  assert.equal(evidence.releaseCreated, false);
  assert.equal(evidence.signingClass, "adhoc");
  assert.equal(evidence.teamIdentifier, null);
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.publicArtifact, false);
  assert.equal(evidence.downloadHosted, false);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
});

test("macOS anonymous public-download roundtrip stays source-only", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosAnonymousPublicDownloadRoundtripSource;
  assert.equal(evidence.implementationCommit, "912e79642e805a35004a02312ee2b7f1ca17f3e8");
  assert.equal(evidence.evidenceCommit, "ae0c3a8074e20961530b3da962bbebacf2e76f27");
  assert.equal(evidence.anonymousPublicRoundtripSourceReady, true);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.transportMechanismObserved, true);
  assert.deepEqual(evidence.observedTransportStatusChain, [302, 200]);
  assert.equal(evidence.observedTransportExactContentLength, true);
  assert.equal(evidence.observedTransportAssetPlatform, "android");
  assert.equal(evidence.observedTransportPromotesMacosPublic, false);
  assert.equal(evidence.publicationJobSkipped, true);
  assert.equal(evidence.defaultMainRegistration, false);
  assert.equal(evidence.operatorDispatch, false);
  assert.equal(evidence.anonymousMacosRoundtripRuntimeVerified, false);
  assert.equal(evidence.releaseCreated, false);
  assert.equal(evidence.publicArtifact, false);
  assert.equal(evidence.downloadHosted, false);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.authorization, false);
  assert.equal(evidence.productSession, false);
});

test("macOS public-artifact isolated lifecycle remains a source contract", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosPublicArtifactIsolatedLifecycleSource;
  assert.equal(evidence.implementationCommit, "fb15b05b3da5743d0a55b7c0d53cbad6705250d8");
  assert.equal(evidence.evidenceCommit, "f9c24c919b82170d21884588332e9cb92dceb995");
  assert.equal(evidence.publicArtifactIsolatedInstallLifecycleSourceReady, true);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.publicationJobSkipped, true);
  assert.equal(evidence.isolatedInstallRoot, "RUNNER_TEMP/Public Applications");
  assert.equal(evidence.systemApplicationsTargetForbidden, true);
  assert.equal(evidence.sameRunLifecycleSource, "NON_PUBLIC_RELEASE");
  assert.equal(evidence.mainRegistration, false);
  assert.equal(evidence.operatorDispatch, false);
  assert.equal(evidence.publicRoundtripRuntimeVerified, false);
  assert.equal(evidence.isolatedLifecycleRuntimeVerified, false);
  assert.equal(evidence.releaseCreated, false);
  assert.equal(evidence.publicArtifact, false);
  assert.equal(evidence.downloadHosted, false);
  assert.equal(evidence.websitePublished, false);
  assert.equal(evidence.systemApplicationsInstalled, false);
  assert.equal(evidence.browserQuarantineAccepted, false);
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.authorizationSuccess, false);
  assert.equal(evidence.productSession, false);
});

test("macOS /Applications install remains a source contract without runtime", () => {
  const evidence = ownerActivityCheckpoint.walletPlatformTracking.macosSystemApplicationsInstallSource;
  assert.equal(evidence.implementationCommit, "14630843459e1ae41c007eee8dfb34789a0e4443");
  assert.equal(evidence.evidenceCommit, "5a5359c917b1de3d093b58d3bc417b1c1943e9a2");
  assert.equal(evidence.systemApplicationsInstallSourceReady, true);
  assert.equal(evidence.hostedSourceContractVerified, true);
  assert.equal(evidence.systemInstallTarget, "/Applications/YNX Wallet.app");
  assert.equal(evidence.requiresTargetAbsent, true);
  assert.equal(evidence.requiresExactAnonymousPublicDmgFirst, true);
  assert.equal(evidence.requiresNoQuarantineAttribute, true);
  assert.equal(evidence.browserQuarantineAcceptedByAbsence, false);
  assert.equal(evidence.sameRunLifecycleSource, "NON_PUBLIC_NATIVE_PACKAGE");
  assert.equal(evidence.publicationJobSkipped, true);
  assert.equal(evidence.operatorDispatch, false);
  assert.equal(evidence.anonymousPublicRoundtripRuntimeVerified, false);
  assert.equal(evidence.systemApplicationsInstallRuntimeVerified, false);
  assert.equal(evidence.releaseCreated, false);
  assert.equal(evidence.systemApplicationsInstalled, false);
  assert.equal(evidence.browserQuarantineAccepted, false);
  assert.equal(evidence.gatekeeperAccepted, false);
  assert.equal(evidence.authorizationSuccess, false);
  assert.equal(evidence.productSession, false);
});

test("v3 counts and precise owner blockers are derived without aggregate promotion", () => {
  const products = auditV3.registeredProducts;
  assert.equal(products.filter(({ runtime }) => runtime.sourceBoundPublic === true).length, auditV3.counts.sourceBoundPublicProducts);
  assert.equal(products.filter(({ web }) => web.metaMaskAddSwitch0x1917 === true).length, auditV3.counts.completeMetaMaskSourcePaths);
  assert.equal(products.filter(({ web }) => web.topLevelOrHandwrittenYnxwallet === true).length, auditV3.counts.topLevelOrHandwrittenWebBlockers);
  assert.equal(products.filter(({ web }) => web.standardProvider === false).length, auditV3.counts.missingStandardProviderProducts);
  assert.equal(products.filter(({ web }) => String(web.guest).startsWith("unproven")).length, auditV3.counts.guestUnprovenProducts);
  for (const product of products.filter(({ web }) => web.metaMaskAddSwitch0x1917 === false)) assert.match(product.ownerHandoff, /MetaMask|switch|0x1917/i, product.productId);
});

test("Trust Center public guest evidence remains outside registered migration counts", () => {
  const trust = auditV3.otherEcosystem.find(({ surfaceId }) => surfaceId === "trust-center");
  assert.ok(trust);
  assert.equal(trust.runtime.deployedPublic, true);
  assert.equal(trust.runtime.guestReadOnlyVerified, true);
  assert.equal(trust.runtime.guestWriteHttp, 401);
  assert.equal(trust.runtime.installedWallet, false);
  assert.equal(trust.runtime.accountApproval, false);
  assert.equal(trust.runtime.productSessionV2, false);
  assert.equal(trust.runtime.computerControl, false);
  assert.equal(auditV3.truth.deployedPublicAggregate, false);
});

test("Wallet Web/PWA and macOS DMG publication remain outside product authority", () => {
  const companion = auditV3.nonProductRegistryClient;
  assert.equal(companion.productId, "wallet-web-companion");
  assert.equal(companion.webShellSourceCommit, "7032948364c507f898a2b0cdba2a62d4039388fc");
  assert.equal(companion.publicEvidenceCommit, "21e55d85a36b632f1d56a57777e37783b9afa76c");
  assert.equal(companion.deploymentId, "dpl_98S2czECu4YvPWnbpHUctKtJxDQD");
  assert.equal(companion.runtime.sourceBoundPublic, true);
  assert.equal(companion.runtime.currentSourceBoundPublic, false);
  assert.equal(companion.runtime.currentPublicIdentityDrift, "release/integration/wallet-web-public-identity-drift-recheck-20260822.json");
  assert.equal(companion.runtime.metaMaskEip6963Detected, true);
  assert.equal(companion.runtime.dedicatedProject, "ynx-wallet-web");
  assert.equal(companion.runtime.metaMaskApprovalTriggered, false);
  assert.equal(companion.runtime.pwaCacheVersion, 10);
  assert.equal(companion.runtime.pwaInstallabilityPassed, true);
  assert.equal(companion.runtime.cacheTamperRollbackRecoveryPassed, true);
  assert.equal(companion.runtime.v10DirectVisibleVerified, true);
  assert.equal(companion.runtime.visibleFinalUrl, "https://wallet.ynxweb4.com/");
  assert.equal(companion.runtime.visibleConnectionStatus, "Not connected");
  assert.equal(companion.runtime.noCustomSchemeBlankTab, true);
  assert.equal(companion.runtime.forgedApprovalCallbackError, "CALLBACK_MISMATCH");
  assert.equal(companion.runtime.forgedApprovalAuthorityGranted, false);
  assert.equal(companion.runtime.accountApproval, false);
  assert.equal(companion.runtime.providerChainId1917, false);
  assert.equal(companion.runtime.callback, false);
  assert.equal(companion.runtime.productSessionV2, false);
  const vaultSigner = auditV3.standardWalletParityConformance.walletWebExtensionVaultSignerP0152;
  assert.equal(vaultSigner.sourceAndBuiltArtifactAccepted, true);
  assert.equal(vaultSigner.directNodeRpcChainId, "0x1917");
  assert.equal(vaultSigner.currentSourceBoundPublic, false);
  assert.equal(vaultSigner.installedProvider, false);
  assert.equal(vaultSigner.approveReject, false);
  assert.equal(vaultSigner.transaction, false);
  assert.equal(vaultSigner.parityComplete, false);
  const packagingGate = auditV3.standardWalletParityConformance.walletWebSourceIdentityPackagingGate;
  assert.equal(packagingGate.sourceCommit, "2357ae576abb14a25db261f94f63b3d4c71add83");
  assert.equal(packagingGate.rejectsUncommittedFallback, true);
  assert.equal(packagingGate.currentSourceBoundPublic, false);
  assert.equal(packagingGate.releaseBlocked, true);
  assert.equal(packagingGate.installedParity, false);
  const providerIdentity = auditV3.standardWalletParityConformance.walletWebIndependentProviderIdentity;
  assert.equal(providerIdentity.eip6963.name, "YNX Wallet");
  assert.equal(providerIdentity.eip6963.rdns, "com.ynx.wallet");
  assert.equal(providerIdentity.eip6963.isYNXWallet, true);
  assert.equal(providerIdentity.eip6963.isMetaMask, false);
  assert.equal(providerIdentity.internalBridgeMarkerIsProviderAuthority, false);
  assert.equal(providerIdentity.installedProvider, false);
  assert.equal(providerIdentity.parityComplete, false);
  const interopConsumption = auditV3.standardWalletParityConformance.walletWebRouterInteropAuthorityConsumption;
  assert.equal(interopConsumption.routerAuthority.commit, "9ab9cd8c8deac8563acff9ffd7e277553e20383e");
  assert.equal(interopConsumption.fixtureProfiles, 5);
  assert.equal(interopConsumption.requiredExternalDappsBeforePromotion, 3);
  assert.equal(interopConsumption.privateServiceDegradedPreservesStandardConnection, true);
  assert.equal(interopConsumption.fixtureOnly, true);
  assert.equal(interopConsumption.webInstallConfirmationGranted, false);
  assert.equal(interopConsumption.externalDappRuntimeCount, 0);
  assert.equal(interopConsumption.parityComplete, false);
  const publicConsumerHandshake = auditV3.standardWalletParityConformance.publicConsumerHandshake;
  assert.equal(publicConsumerHandshake.ownerSourceCommit, "f418a8dbc36aa4855396741fed8cafac5d498858");
  assert.equal(publicConsumerHandshake.subpath, "@ynx-chain/wallet-auth/standard-wallet-public-consumer");
  assert.equal(publicConsumerHandshake.contractSha256, "0b20c0188e86b707de6c0110415cfb82a60592b2bdf6368ebe87d6477ce95952");
  assert.equal(publicConsumerHandshake.implementationSha256, "b35c6d5837c1fa652c73593dda0efeb6b67a23f917deffada2d67e3061e55570");
  assert.deepEqual(publicConsumerHandshake.requiredForDirectE2EProducts, ["calendar", "card", "creator-studio", "developer", "dex", "exchange", "finance", "pay", "quant", "shop", "social", "video"]);
  assert.equal(publicConsumerHandshake.requiredForResponsibilityRegistry.domainDenominator, 36);
  assert.equal(publicConsumerHandshake.soleProviderMethod, "eth_chainId");
  assert.equal(publicConsumerHandshake.account, false);
  assert.equal(publicConsumerHandshake.productSession, false);
  assert.equal(publicConsumerHandshake.sourceOnly, true);
  assert.equal(publicConsumerHandshake.productsConnected, 0);
  assert.equal(publicConsumerHandshake.productsMigratedV2, 0);
  assert.equal(companion.macosDmgPublication.websitePublished, true);
  assert.equal(companion.macosDmgPublication.artifactSha256, "ad6e4077bc001743cf1a4163ceaca5a009a3c8a4d8a809cc4896798976cf56c0");
  assert.equal(companion.macosDmgPublication.isolatedTempApplicationsCopyVerified, true);
  assert.equal(companion.macosDmgPublication.directBinaryColdLaunch.chainId, "0x1917");
  assert.equal(companion.macosDmgPublication.launchServicesSecond.ppid, 1);
  assert.equal(companion.macosDmgPublication.identityGapEvidenceCommit, "b7f20a717b22ed279146ac7ab2b41c272a79acb0");
  assert.equal(companion.macosDmgPublication.ynxwalletSchemeRegistered, false);
  assert.equal(companion.macosDmgPublication.associatedDomainsPresent, false);
  assert.equal(companion.macosDmgPublication.publicArtifactMatchesOwnerNativeIdentity, false);
  assert.equal(companion.macosDmgPublication.publicNativeWalletPublished, false);
  assert.equal(companion.macosDmgPublication.publicArtifactCallbackDelivered, false);
  assert.equal(companion.nativeMacosProductionPipeline.sourceReady, true);
  assert.equal(companion.nativeMacosProductionPipeline.ciRun, 32486864907);
  assert.equal(companion.nativeMacosProductionPipeline.defaultMainRegistrationCount, 0);
  assert.equal(companion.nativeMacosProductionPipeline.productionRelease, false);
  assert.equal(companion.nativeMacosKeychainCleanup.originalSearchListAlwaysRestored, true);
  assert.equal(companion.nativeMacosKeychainCleanup.isolatedKeychainP12NotaryKeyDeleted, true);
  assert.equal(companion.nativeMacosKeychainCleanup.ciRun, 32487661461);
  assert.equal(companion.nativeMacosKeychainCleanup.artifactDownloadBytes, 0);
  assert.equal(companion.nativeMacosKeychainCleanup.productionCleanupRuntimeProof, false);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.failedRun, 32488308883);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.successRun, 32489069803);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.cleanupReceiptExpectedSha256, companion.nativeMacosKeychainCleanupRuntime.cleanupReceiptRestoredSha256);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.sharedCleanupRuntimeWithoutProductionSecrets, true);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.productionSigningCleanupRuntime, false);
  assert.equal(companion.nativeMacosKeychainCleanupRuntime.iosGatePromoted, false);
  assert.equal(companion.nativeCiTriggerIsolation.macosOnlyChangesExcludedFromWalletIosPushPr, true);
  assert.equal(companion.nativeCiTriggerIsolation.macOnlyWalletIosRunCount, 0);
  assert.equal(companion.nativeCiTriggerIsolation.workflowSelfChangeTriggered, true);
  assert.equal(companion.nativeCiTriggerIsolation.iosSimulatorPromoted, false);
  assert.equal(companion.nativeIosProductionDistribution.sourceReady, true);
  assert.equal(companion.nativeIosProductionDistribution.hostedSourceContractVerified, true);
  assert.equal(companion.nativeIosProductionDistribution.distributionJobSkipped, true);
  assert.equal(companion.nativeIosProductionDistribution.defaultMainWorkflowCount, 0);
  assert.equal(companion.nativeIosProductionDistribution.ipa, false);
  assert.equal(companion.nativeIosProductionDistribution.appStoreReleased, false);
  assert.equal(companion.nativeIosDistributionCleanupRuntime.cleanupRuntimePassed, true);
  assert.equal(companion.nativeIosDistributionCleanupRuntime.expectedSearchListSha256, companion.nativeIosDistributionCleanupRuntime.restoredSearchListSha256);
  assert.equal(companion.nativeIosDistributionCleanupRuntime.sharedDistributionCleanupRuntimeVerifiedWithoutProductionSecrets, true);
  assert.equal(companion.nativeIosDistributionCleanupRuntime.productionCleanupRuntime, false);
  assert.equal(companion.appleProductionOperatorInputs.implementationCommit, "2b11c771a7d34f852e3028057b52f560d7498bdc");
  assert.equal(companion.appleProductionOperatorInputs.evidenceCommit, "07ca1a4a437a8f24dcae345d2f92344b40600f00");
  assert.equal(companion.appleProductionOperatorInputs.requestSha256, "579846e6d6120fc861dd93dfa16bb86e4cbc9cafede34cb1e41b063db35ef0a9");
  assert.equal(companion.appleProductionOperatorInputs.operatorInputRequestHostedSourceVerified, true);
  assert.equal(companion.appleProductionOperatorInputs.secretValuesRequested, false);
  assert.equal(companion.appleProductionOperatorInputs.universalLinkExcludedUntilCoreFrozen, true);
  assert.equal(companion.appleProductionOperatorInputs.operatorInputsProvided, false);
  assert.equal(companion.appleProductionOperatorInputs.ipa, false);
  assert.equal(companion.appleProductionOperatorInputs.developerId, false);
  assert.equal(companion.nativeMacosArtifactBackread.evidenceCommit, "d970aa3f229371ee300e2ec5f647699928f1dd44");
  assert.equal(companion.nativeMacosArtifactBackread.innerArchiveSha256, "08cbb2f177e0a82e597d5aa1063722e15dafc5a5e130b0d9b00fc0e00dd0bd41");
  assert.equal(companion.nativeMacosArtifactBackread.actionsArtifactDownloaded, true);
  assert.equal(companion.nativeMacosArtifactBackread.strictDeepCodesignVerified, true);
  assert.equal(companion.nativeMacosArtifactBackread.signature, "adhoc");
  assert.equal(companion.nativeMacosArtifactBackread.deviceRecoveryScreenshotUnobscured, true);
  assert.equal(companion.nativeMacosArtifactBackread.canonicalBridgeScreenshotUnobscured, false);
  assert.equal(companion.nativeMacosArtifactBackread.gatekeeperAccepted, false);
  assert.equal(companion.nativeMacosArtifactBackread.authorizationSuccess, false);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.implementationCommit, "07634c308bd553702cf5ea3837cfcff88ba6e2b2");
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.evidenceCommit, "eb916f843c6433d0011dac3dc7b5bad6fb3d1708");
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.chainId, "0x1917");
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.walletStateDigestUnchanged, true);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.coldGatewayFailClosedLogVerified, true);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.secondGatewayFailClosedLogVerified, true);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.sensitiveInputProvided, false);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.authorizationSuccess, false);
  assert.equal(companion.nativeMacosPublicGatewayFailClosed.productSessionVerified, false);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.implementationCommit, "f7669a663370f7c2e5a34743e39024dfd05ed333");
  assert.equal(companion.nativeIosPublicGatewayFailClosed.evidenceCommit, "b87ff7d0d913045f4b8c77801aba288a54c2516c");
  assert.equal(companion.nativeIosPublicGatewayFailClosed.hostedOverallPassed, false);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.buildInstallGatewayAndNegativeStepsPassed, true);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.nativeKeychainAddReadDelete, true);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.observedStateUnchanged, true);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.biometricSuccess, false);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.recoverySuccess, false);
  assert.equal(companion.nativeIosPublicGatewayFailClosed.productSession, false);
  assert.equal(companion.macosDmgPublication.systemApplicationsInstallVerified, false);
  assert.equal(companion.macosDmgPublication.browserQuarantineAcceptanceVerified, false);
  assert.equal(companion.macosDmgPublication.wholeAppUnsigned, true);
  assert.equal(companion.macosDmgPublication.notarized, false);
  assert.equal(companion.macosDmgPublication.rollbackVerified, false);
  assert.equal(companion.macosDmgPublication.productionSigned, false);
  assert.equal(auditV3.counts.productsConnected, 0);
  assert.equal(auditV3.counts.productsMigratedV2, 0);
});

test("current Wallet Web identity drift blocks public source binding without treating no-provider fallback as a connection", () => {
  assert.equal(walletWebIdentityDrift.method.accountRequestTriggered, false);
  assert.equal(walletWebIdentityDrift.observedPublic.buildIdentity.sourceCommit, "uncommitted-source-tree");
  assert.equal(walletWebIdentityDrift.conclusion.currentSourceMatchesExpectedV10, false);
  assert.equal(walletWebIdentityDrift.conclusion.currentSourceBoundPublic, false);
  assert.equal(walletWebIdentityDrift.conclusion.currentNoProviderFallbackVisible, true);
  assert.equal(walletWebIdentityDrift.conclusion.productsConnected, 0);
  assert.equal(walletWebIdentityDrift.conclusion.deployedPublicAggregate, false);
});

test("shared Provider/connect recovery hands off to all products without promoting runtime", () => {
  assert.equal(auditV3.sharedProviderConnectRecovery.sourceCommit, providerRecovery.source.commit);
  assert.equal(auditV3.sharedProviderConnectRecovery.registeredProductConsumers, 7);
  assert.equal(auditV3.sharedProviderConnectRecovery.publicRuntimeConsumers, 2);
  assert.equal(auditV3.sharedProviderConnectRecovery.realAccountApprovalProducts, 1);
  assert.deepEqual(providerRecovery.registeredProductHandoffs.map(({ productId }) => productId).sort(), auditV3.registeredProducts.map(({ productId }) => productId).sort());
  assert.deepEqual(providerRecovery.registeredProductHandoffs.filter(({ consumed }) => consumed).map(({ productId }) => productId), ["calendar", "developer", "dex", "exchange", "pay", "quant", "shop"]);
  const quantRecovery = providerRecovery.registeredProductHandoffs.find(({ productId }) => productId === "quant");
  assert.equal(quantRecovery.sourceCommit, "301b680ac8bec297108a75920b1c34354345b574");
  assert.equal(quantRecovery.ownerAuditRemotePublished, false);
  assert.equal(quantRecovery.publicSourceBound, false);
  const calendarRecovery = providerRecovery.registeredProductHandoffs.find(({ productId }) => productId === "calendar");
  assert.equal(calendarRecovery.publicSourceBound, true);
  assert.equal(calendarRecovery.realProviderApproval, true);
  assert.equal(calendarRecovery.refreshRestore, true);
  assert.equal(calendarRecovery.disconnect, false);
  assert.equal(calendarRecovery.callback, false);
  const shopRecovery = providerRecovery.registeredProductHandoffs.find(({ productId }) => productId === "shop");
  assert.equal(shopRecovery.sourceCommit, "e35c950d57a6f9a4477877d3806cf1e4566ce74e");
  assert.equal(shopRecovery.evidenceCommit, "35dc239546c2bf963534d5031d4c35c4e22c2d1c");
  assert.equal(shopRecovery.evidenceSha256, "3b11fd760b52dfa4c33960f2f892abb926fea6576e1b1c082b6012f7c64ae9f6");
  assert.equal(shopRecovery.publicationType, "static-only");
  assert.equal(shopRecovery.fullRuntimeIdentityMatched, false);
  assert.equal(shopRecovery.refresh.chainId, "0x1917");
  assert.equal(shopRecovery.refresh.accountPresent, true);
  assert.equal(shopRecovery.refresh.connectDialogOpen, false);
  assert.equal(shopRecovery.newAccountApprovalObserved, false);
  assert.equal(providerRecovery.directChromeEvidence.shop.accountApprovalObserved, false);
  assert.equal(providerRecovery.directChromeEvidence.card.accountApprovalObserved, false);
  assert.equal(providerRecovery.standardConnectStateContract.rpcProbe.endpointBindingObserved, "Public Wallet Companion declares rpc.cors=false.");
  assert.match(providerRecovery.standardConnectStateContract.rpcProbe.connectionAuthority, /provider\.request eth_chainId=0x1917/);
  assert.equal(auditV3.sharedProviderConnectRecovery.directBrowserRpcIsConnectionPrerequisite, false);
  assert.equal(auditV3.sharedProviderConnectRecovery.acceptedCorsSafeRpcProbeRequired, true);
  assert.equal(providerRecovery.truth.productsConnected, 0);
  assert.equal(providerRecovery.truth.threeProductChromeAcceptance, false);
  assert.equal(providerRecovery.truth.officialInstallersReplaced, false);
  assert.equal(providerRecovery.truth.websiteDirectLinksRestored, false);
});

test("pending Provider/connect handoffs cover the remaining five owners and preserve connection authority", () => {
  const consumed = providerRecovery.registeredProductHandoffs.filter(({ consumed }) => consumed).map(({ productId }) => productId);
  const pending = providerRecovery.registeredProductHandoffs.filter(({ consumed }) => !consumed).map(({ productId }) => productId).sort();
  assert.deepEqual(consumed, ["calendar", "developer", "dex", "exchange", "pay", "quant", "shop"]);
  assert.equal(pendingOwnerHandoffs.consumed.count, 7);
  assert.deepEqual(pendingOwnerHandoffs.consumed.products, consumed);
  assert.equal(pendingOwnerHandoffs.pending.length, 5);
  assert.deepEqual(pendingOwnerHandoffs.pending.map(({ productId }) => productId).sort(), pending);
  assert.ok(pendingOwnerHandoffs.pending.every(({ currentSourceCommit, handoff }) => /^[0-9a-f]{40}$/.test(currentSourceCommit) && handoff.length > 80));
  assert.deepEqual(pendingOwnerHandoffs.connectionAuthority.successRequires, ["selected-provider", "approved-account", "provider-request-chain-0x1917"]);
  assert.equal(pendingOwnerHandoffs.connectionAuthority.directBrowserRpcFetchRequired, false);
  assert.equal(pendingOwnerHandoffs.connectionAuthority.rpcProbeDegradedEffects.connectionPreserved, true);
  assert.equal(pendingOwnerHandoffs.connectionAuthority.rpcProbeDegradedEffects.chooserReopened, false);
  assert.equal(pendingOwnerHandoffs.connectionAuthority.rpcProbeDegradedEffects.classifiedAsNoProvider, false);
  assert.equal(pendingOwnerHandoffs.completionBoundary.sourceCheckpointIsProductCompletion, false);
  assert.equal(pendingOwnerHandoffs.acceptanceEvaluator.export, "evaluateProductWalletMigrationEvidence");
  assert.equal(pendingOwnerHandoffs.acceptanceEvaluator.authority, "evidence-evaluation-only");
  assert.equal(pendingOwnerHandoffs.acceptanceEvaluator.failClosed, true);
  const finance = pendingOwnerHandoffs.pending.find(({ productId }) => productId === "finance");
  assert.match(finance.stalledReason, /69ba84ea.*51a60a36/);
  assert.equal(auditV3.registeredProducts.find(({ productId }) => productId === "finance").candidateEvidenceSourceTreeMismatch, true);
  assert.equal(pendingOwnerHandoffs.truth.newProductConsumptionRecorded, true);
  assert.equal(pendingOwnerHandoffs.truth.aggregateConnected, false);
  assert.equal(pendingOwnerHandoffs.consumed.quantOwnerAuditRemotePublished, false);
  const pendingCard = pendingOwnerHandoffs.pending.find(({ productId }) => productId === "card");
  assert.equal(pendingCard.currentSourceCommit, "345e0bdb41e9ad7cd5b208ffb1d144bc1b3b328b");
  assert.equal(pendingCard.sharedProviderConnectConsumed, false);
  assert.equal(pendingCard.noProviderTopLevelTabDelta, 0);
});

test("owner activity checkpoint derives counts and consumes Faucet recovery without product promotion", () => {
  const products = ownerActivityCheckpoint.products;
  const card = products.find(({ productId }) => productId === "card");
  assert.equal(card.sourceCommit, "345e0bdb41e9ad7cd5b208ffb1d144bc1b3b328b");
  assert.equal(card.sourceConsumed, false);
  assert.equal(card.topLevelTabDelta, 0);
  assert.deepEqual(products.map(({ productId }) => productId).sort(), registry.products.filter(({ productId }) => productId !== "wallet-web-companion").map(({ productId }) => productId).sort());
  assert.equal(products.filter(({ sourceConsumed }) => sourceConsumed).length, ownerActivityCheckpoint.summary.sourceConsumers);
  assert.equal(products.filter(({ sourceConsumed, publicRuntime }) => sourceConsumed && publicRuntime).length, ownerActivityCheckpoint.summary.sourceBoundPublicConsumers);
  assert.equal(products.filter(({ realProviderApproval }) => realProviderApproval).length, ownerActivityCheckpoint.summary.realProviderApprovalProducts);
  assert.equal(products.filter(({ complete }) => complete).length, ownerActivityCheckpoint.summary.threeSegmentCompleteProducts);
  assert.deepEqual(products.filter(({ sourceConsumed }) => !sourceConsumed).map(({ owner }) => owner).sort(), [...ownerActivityCheckpoint.stalledOwners].sort());
  assert.equal(ownerActivityCheckpoint.summary.productsConnected, 0);
  assert.equal(ownerActivityCheckpoint.summary.productsMigratedV2, 0);
  assert.match(products.find(({ productId }) => productId === "finance").blocker, /69ba84ea.*51a60a36/);
  assert.equal(ownerActivityCheckpoint.faucetTracking.runtimeSource, "64efa498fa9982e12ffda888391d67082dc898bb");
  assert.equal(ownerActivityCheckpoint.faucetTracking.ownerEvidenceCommit, "87d953b4");
  assert.equal(ownerActivityCheckpoint.faucetTracking.runtimeRelease, "ynx-chain-64efa498fa99");
  assert.equal(ownerActivityCheckpoint.faucetTracking.vercelReady, true);
  assert.equal(ownerActivityCheckpoint.faucetTracking.identityPromoted, true);
  assert.equal(ownerActivityCheckpoint.faucetTracking.websitePage.bundle, "assets/index-D0C2uU3-.js");
  assert.equal(ownerActivityCheckpoint.faucetTracking.websitePage.sameBundle, true);
  assert.equal(ownerActivityCheckpoint.faucetTracking.visibleBrowser.kind, "REAL_CHROME_BROWSER");
  assert.equal(ownerActivityCheckpoint.faucetTracking.visibleBrowser.consoleErrors, 0);
  assert.equal(ownerActivityCheckpoint.faucetTracking.visibleBrowser.computerControl, false);
  assert.deepEqual(ownerActivityCheckpoint.faucetTracking.chain, { decimal: 6423, hex: "0x1917" });
  assert.equal(ownerActivityCheckpoint.faucetTracking.transfers.length, 1);
  assert.equal(ownerActivityCheckpoint.faucetTracking.transfers[0].hash, "0xb15d2de15bdf899f7dab05d108385bb3bece5db0a104a5c46f6dfd60f3c4b1e9");
  assert.equal(ownerActivityCheckpoint.faucetTracking.transfers[0].recipient, "0x2626262626262626262626262626262626262626");
  assert.equal(ownerActivityCheckpoint.faucetTracking.rateLimit429Verified, true);
  assert.equal(ownerActivityCheckpoint.faucetTracking.duplicateTransferObserved, false);
  assert.equal(ownerActivityCheckpoint.faucetTracking.runtimeEndpoints.requestOptionsAcao, "*");
  assert.equal(ownerActivityCheckpoint.faucetTracking.explorerTransactionPageHttp, 200);
  assert.deepEqual(ownerActivityCheckpoint.faucetTracking.boundaries, { walletPlatformFaucet: false, computerControl: false, registeredProductConsumer: false });
  assert.equal(ownerActivityCheckpoint.faucetTracking.modifiedByProtocolOwner, false);
  assert.deepEqual(auditV3.remoteRecheck.faucetSourceIdentityTracking.transfers, ownerActivityCheckpoint.faucetTracking.transfers);
  assert.deepEqual(auditV3.remoteRecheck.faucetSourceIdentityTracking.boundaries, ownerActivityCheckpoint.faucetTracking.boundaries);
  assert.equal(auditV3.remoteRecheck.faucetSourceIdentityTracking.runtimeIdentityPromoted, true);
  assert.equal(auditV3.remoteRecheck.faucetSourceIdentityTracking.matrixAuthority, false);
  assert.equal(ownerActivityCheckpoint.truth.aggregateConnected, false);
});

test("owner checkpoint separates Wallet public artifacts from approval and Product Session authority", () => {
  const platform = ownerActivityCheckpoint.walletPlatformTracking;
  assert.equal(platform.webPwa.sourceCommit, "7032948364c507f898a2b0cdba2a62d4039388fc");
  assert.equal(platform.webPwa.evidenceCommit, "21e55d85a36b632f1d56a57777e37783b9afa76c");
  assert.equal(platform.webPwa.deploymentId, "dpl_98S2czECu4YvPWnbpHUctKtJxDQD");
  assert.equal(platform.webPwa.sourceBoundPublic, true);
  assert.equal(platform.webPwa.walletAliasRestoredToDedicatedProject, true);
  assert.equal(platform.webPwa.v10DirectVisibleVerified, true);
  assert.equal(platform.webPwa.visibleConnectionStatus, "Not connected");
  assert.equal(platform.webPwa.forgedApprovalCallbackError, "CALLBACK_MISMATCH");
  assert.equal(platform.webPwa.forgedApprovalAuthorityGranted, false);
  assert.equal(platform.webPwa.accountApproved, false);
  assert.equal(platform.webPwa.callback, false);
  assert.equal(platform.webPwa.productSessionV2, false);
  assert.equal(platform.macosDmg.websitePublished, true);
  assert.equal(platform.macosDmg.artifactSha256, "ad6e4077bc001743cf1a4163ceaca5a009a3c8a4d8a809cc4896798976cf56c0");
  assert.equal(platform.macosDmg.isolatedTempApplicationsCopyVerified, true);
  assert.equal(platform.macosDmg.directBinarySecond.chainId, "0x1917");
  assert.equal(platform.macosDmg.launchServicesCold.ppid, 1);
  assert.equal(platform.macosDmg.identityGapEvidenceCommit, "b7f20a717b22ed279146ac7ab2b41c272a79acb0");
  assert.equal(platform.macosDmg.ownerNativeSource.ynxwalletSchemeRegistered, true);
  assert.equal(platform.macosDmg.publicArtifactMatchesOwnerNativeIdentity, false);
  assert.equal(platform.macosDmg.publicNativeWalletPublished, false);
  assert.equal(platform.macosDmg.publicArtifactCanonicalCallbackFailClosedVisible, false);
  assert.equal(platform.macosDmg.nativeProductionPipeline.sourceCommit, "07cab0839e55a2a4f65b4813345d35fb5460c5f8");
  assert.equal(platform.macosDmg.nativeProductionPipeline.sourceReady, true);
  assert.equal(platform.macosDmg.nativeProductionPipeline.productionDmg, false);
  assert.equal(platform.macosDmg.keychainCleanup.evidenceCommit, "e277f5c6c023cdef39de9a73691565e1c653fa22");
  assert.equal(platform.macosDmg.keychainCleanup.originalSearchListAlwaysRestored, true);
  assert.equal(platform.macosDmg.keychainCleanup.innerHashesInspected, false);
  assert.equal(platform.macosDmg.keychainCleanup.productionCleanupRuntimeProof, false);
  assert.equal(platform.macosDmg.keychainCleanupRuntime.fixCommit, "99fa2c7db61ac23717743103cced76001ea4160b");
  assert.equal(platform.macosDmg.keychainCleanupRuntime.productionCredentialsUsed, false);
  assert.equal(platform.macosDmg.keychainCleanupRuntime.sharedCleanupRuntimeWithoutProductionSecrets, true);
  assert.equal(platform.macosDmg.keychainCleanupRuntime.productionSigningCleanupRuntime, false);
  assert.equal(platform.macosDmg.ciTriggerIsolation.evidenceCommit, "69009ba5abbbd57739bdd6a08936dbddbe5e842b");
  assert.equal(platform.macosDmg.ciTriggerIsolation.macOnlyWalletIosRunCount, 0);
  assert.equal(platform.macosDmg.ciTriggerIsolation.iosSimulatorPromoted, false);
  assert.equal(platform.iosProductionDistribution.evidenceCommit, "77225b3ca830b3edaf7ae4cd5f7fbb3b619f380e");
  assert.equal(platform.iosProductionDistribution.sourceReady, true);
  assert.equal(platform.iosProductionDistribution.iosSimulatorPromoted, false);
  assert.equal(platform.iosProductionDistribution.appStoreConnectUploadSubmitted, false);
  assert.equal(platform.iosProductionDistribution.websitePublished, false);
  assert.equal(platform.iosDistributionCleanupRuntime.evidenceCommit, "cf8f56782d6831e11b57be600efa0382b7ef4045");
  assert.equal(platform.iosDistributionCleanupRuntime.productionCredentialsUsed, false);
  assert.equal(platform.iosDistributionCleanupRuntime.sharedDistributionCleanupRuntimeVerifiedWithoutProductionSecrets, true);
  assert.equal(platform.iosDistributionCleanupRuntime.productionCleanupRuntime, false);
  assert.equal(platform.appleProductionOperatorInputs.evidenceCommit, "07ca1a4a437a8f24dcae345d2f92344b40600f00");
  assert.equal(platform.appleProductionOperatorInputs.operatorInputRequestImplemented, true);
  assert.equal(platform.appleProductionOperatorInputs.operatorInputRequestHostedSourceVerified, true);
  assert.equal(platform.appleProductionOperatorInputs.secretValuesRequested, false);
  assert.equal(platform.appleProductionOperatorInputs.operatorInputsProvided, false);
  assert.equal(platform.appleProductionOperatorInputs.universalLinkExcludedUntilCoreFrozen, true);
  assert.equal(platform.macosNativeArtifactBackread.evidenceCommit, "d970aa3f229371ee300e2ec5f647699928f1dd44");
  assert.equal(platform.macosNativeArtifactBackread.actionsArtifactDownloaded, true);
  assert.equal(platform.macosNativeArtifactBackread.minimumMacOS, "13.0");
  assert.deepEqual(platform.macosNativeArtifactBackread.architectures, ["x86_64", "arm64"]);
  assert.equal(platform.macosNativeArtifactBackread.signature, "adhoc");
  assert.equal(platform.macosNativeArtifactBackread.teamIdentifierPresent, false);
  assert.equal(platform.macosNativeArtifactBackread.gatekeeperAccepted, false);
  assert.equal(platform.macosNativePublicGatewayFailClosed.evidenceCommit, "eb916f843c6433d0011dac3dc7b5bad6fb3d1708");
  assert.equal(platform.macosNativePublicGatewayFailClosed.nativePublicRpcChainIdVerified, true);
  assert.equal(platform.macosNativePublicGatewayFailClosed.nativePublicAppGatewayReachable, true);
  assert.equal(platform.macosNativePublicGatewayFailClosed.walletStateDigestUnchanged, true);
  assert.equal(platform.macosNativePublicGatewayFailClosed.walletApprovalCompleted, false);
  assert.equal(platform.macosNativePublicGatewayFailClosed.callbackEmitted, false);
  assert.equal(platform.iosNativePublicGatewayFailClosed.evidenceCommit, "b87ff7d0d913045f4b8c77801aba288a54c2516c");
  assert.equal(platform.iosNativePublicGatewayFailClosed.hostedOverallConclusion, "failure");
  assert.equal(platform.iosNativePublicGatewayFailClosed.simulatorInstallColdSecondLaunch, true);
  assert.equal(platform.iosNativePublicGatewayFailClosed.nativeKeychainAddReadDelete, true);
  assert.equal(platform.iosNativePublicGatewayFailClosed.biometricPositiveStepPassed, false);
  assert.equal(platform.iosNativePublicGatewayFailClosed.authorizationSuccess, false);
  assert.equal(platform.macosDmg.systemApplicationsInstallVerified, false);
  assert.equal(platform.macosDmg.browserQuarantineAcceptanceVerified, false);
  assert.equal(platform.macosDmg.developerId, false);
  assert.equal(platform.macosDmg.rollbackVerified, false);
  assert.equal(platform.macosDmg.productionSigned, false);
  assert.equal(platform.latestMacosOwnerEvidence.quarantineTranslocation.commit, "8133b1ca9aa77dd52b17818c784303c96b9c53e5");
  assert.equal(platform.latestMacosOwnerEvidence.quarantineTranslocation.visibleQuarantinedLaunch, false);
  assert.equal(platform.latestMacosOwnerEvidence.browserDownloadFailure.actualBrowserDownload, false);
  assert.equal(platform.latestMacosOwnerEvidence.browserDownloadFailure.browserQuarantineAcceptance, false);
  assert.equal(platform.latestMacosOwnerEvidence.keychainRoundTrip.addReadDeleteVerified, true);
  assert.equal(platform.latestMacosOwnerEvidence.keychainRoundTrip.authorization, false);
  assert.equal(platform.latestMacosOwnerEvidence.lockedStateBackreadLimitation.actualLockedDeviceReadDeniedVerified, false);
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.policyEvidenceCommit, "4765c9e7476f89fee653abb44f04bef825419ccf");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.policyTests, "5/5");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.localStateImplementationCommit, "25fc6771acd437c4b2a78c4ee279758f5bbd45da");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.localStateEvidenceCommit, "58076b0154196349699696f64a616f5727c7c9cd");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.localStateTests, "5/5");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.macCoreTests, "27/27");
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.pendingProposalAndRequestRestartPersistenceSourceVerified, true);
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.realProjectId, false);
  assert.equal(platform.latestMacosOwnerEvidence.walletConnectV2Policy.realRelayOrPairing, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricPositiveFix.repairStatus, "IN_PROGRESS");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricPositiveFix.repairImplementationCommit, "560d801b9a2d3cd856b3dfdbdc8d66aa3114d1b5");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricPositiveFix.failureEvidenceSha256, "c7e8d102e8edd8dffc94d54703323360b1bdad202e7deed9769e8ed8aefbfa79");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricPositiveFix.biometricPositivePromoted, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailure.evidenceCommit, "cdac464e9fd09c42ad921e5a9d7f5304b912348d");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailure.sha256, "56f519c592563a5ca1f7235e2c4c913d733399636f740352b942e5932ba2fc00");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailure.terminalStatus, "FAILURE");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailure.biometricPositivePromoted, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectV2DecisionState.implementationCommit, "e829f38b9f35d47be1afd924cf609cf2e59e3be7");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectV2DecisionState.hostedRunStatus, "IN_PROGRESS");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectV2DecisionState.walletJsTests, "40/40");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectV2DecisionState.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectV2DecisionState.approveReject, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailureSecond.evidenceCommit, "c1b630bd2d6d101b3705b0d6ebdf0eec86f08672");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailureSecond.terminalStatus, "FAILURE_BIOMETRIC_ONLY");
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailureSecond.walletConnectXCUITestPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailureSecond.walletConnectSourceSignpost.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosBiometricTerminalFailureSecond.sign, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectUriCapability.implementationCommit, "aba683d5dc90ee6a8df339240510e77680c9fa9c");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectUriCapability.projectIdUnavailableVisibleFailClosed, "PROJECT_ID_UNAVAILABLE");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectUriCapability.validProjectIdAdapterMissingVisibleFailClosed, "RELAY_UNAVAILABLE");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectUriCapability.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.ownerCommit, "0ba02e5f6b218e59e23169b35588b1baebf36540");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.runTerminal, "FAILURE_BIOMETRIC_ONLY");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.canonicalWalletConnectUriDeliveredToInstalledSimulator, true);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.visibleFailClosedCode, "WALLETCONNECT_PROJECT_ID_UNAVAILABLE");
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.callbackEmitted, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.iosWalletConnectCallbackSimulatorFailClosed.authorization, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.ownerCommit, "5d57ba0706b0b77c1b323c5eebdad4dddbb83e63");
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.isolatedTempApplicationsLifecycle, true);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.canonicalWalletConnectFailClosedSameSecondPid, "WALLETCONNECT_PROJECT_ID_UNAVAILABLE");
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.callbackEmitted, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.gatekeeperAccepted, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectCallbackFailClosed.public, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.ownerCommit, "932d2d23c4293a1c5fa73af1de09825e73e771b5");
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.isolatedTempApplicationsColdStart, true);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.coldStartCallbackPid, 6472);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.coldStartVisibleFailClosed, "WALLETCONNECT_PROJECT_ID_UNAVAILABLE");
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.callbackEmitted, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.macosWalletConnectColdStartFailClosed.public, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.ownerCommit, "7431e002e017127ee7012354f123fa7cf31289b7");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.macCoreTests, "31/31");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.approvedTopicDappClassAndNameSubstitutionRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.persistedUnsupportedMethodFailsClosedAsCorrupt, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappIdentityBinding.authorization, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.ownerCommit, "bc82f2e4c7d18f1c2c0e229abf45512d55c7f24b");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.macCoreTests, "32/32");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.walletConnectStateTests, "8/8");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.existingSessionRejectsSameTopicProposalIdentityReplacement, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.persistedValidationFailsClosedOnSameTopicIdentityReplacement, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSameTopicReproposalIdentityBinding.authorization, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.ownerCommit, "a6c71a3159f99cb313db5f332fef1447d8503d7b");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.macCoreTests, "33/33");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.walletConnectStateTests, "9/9");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.runtimeSecondPendingProposalError, "WALLETCONNECT_REQUEST_DUPLICATE");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.persistedSameTopicTwoProposalError, "WALLETCONNECT_PERSISTENCE_CORRUPT");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.localFullSwiftTestPassed, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectSinglePendingTopic.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.ownerCommit, "3355c5804c851f30462d75722dac6a3114eefca8");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.macCoreTests, "34/34");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.walletConnectStateTests, "10/10");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.leadingTrailingWhitespaceRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.controlCharactersRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.persistedAmbiguousDisplayNameFailsClosedAsCorrupt, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.normalUnicodeNamesSupported, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectDappDisplayNameAmbiguity.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.ownerCommit, "4f5ab74142f08f6ac5d518f3a3bca9b13f00397f");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.macCoreTests, "35/35");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.walletConnectStateTests, "11/11");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.canonicalTopicFormat, "64 lowercase hex");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.uppercaseRuntimeTopicRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.persistedUppercaseTopicFailsClosedAsCorrupt, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalLowercaseTopic.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.ownerCommit, "212a9c6fd3877d843cc933106776a02c87260218");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.macCoreTests, "36/36");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.walletConnectStateTests, "12/12");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.uppercaseRuntimeDigestRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.persistedUppercaseApprovedAccountFailsClosedAsCorrupt, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.relay, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectCanonicalDigestAndApprovedAccount.authorization, false);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.ownerCommit, "a3f27e542b9606d4dd3c15a4a4173de53a23e74e");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.hostedPassed, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.macCoreTests, "37/37");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.walletConnectStateTests, "13/13");
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.runtimeControlCharacterRequestIdRejected, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.persistedControlCharacterRequestIdFailsClosedAsCorrupt, true);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.existingMaxRequestIdBytes, 128);
  assert.equal(platform.latestMacosOwnerEvidence.nativeWalletConnectControlCharacterRequestId.relay, false);
  assert.equal(platform.modifiedByProtocolOwner, false);
  assert.equal(ownerActivityCheckpoint.summary.threeSegmentCompleteProducts, 0);
});

test("Central released Finance and Explorer paths plus P0-150 parity are tracked without creating a lease or product completion", () => {
  assert.deepEqual(ownerActivityCheckpoint.centralP0145P0146, {
    commit: "4556da2ea6dac6e11c7c62a4f53b542ea1556081",
    finance: "SOURCE_BUILD_ACCEPTED_LEASE_RELEASED_AWAITING_PRODUCTION_BINDING",
    explorer: "DEPLOYMENT_REJECTED_ARTIFACT_INTEGRITY_ACCEPTED_LEASE_RELEASED",
    heavyOwner: null, lightOwner: null, pathsReleasedAndNotWritable: true,
    leaseIssuedByProtocolOwner: false, publicOrProductCompletionPromoted: false,
  });
  assert.equal(ownerActivityCheckpoint.products.find(({ productId }) => productId === "finance").centralStatus, "SOURCE_BUILD_ACCEPTED_LEASE_RELEASED_AWAITING_PRODUCTION_BINDING");
  assert.equal(auditV3.centralP0145P0146.publicOrProductCompletionPromoted, false);
  assert.equal(auditV3.standardWalletParityConformance.centralP0150.commit, "f92757f7491c3e28106664aacc2066663bbd52bb");
  assert.equal(auditV3.standardWalletParityConformance.fixtureOnly, true);
  assert.equal(auditV3.standardWalletParityConformance.sharedCoreProviderSource.evidenceCommit, "0e5af17d4f784a30390452b5c2b750047e93727d");
  assert.equal(auditV3.standardWalletParityConformance.sharedCoreProviderSource.mergedIntoRouterBranch, false);
  assert.equal(auditV3.standardWalletParityConformance.externalDappDirectRuntimeCount, 0);
  assert.equal(auditV3.standardWalletParityConformance.productsConnected, 0);
  assert.equal(ownerActivityCheckpoint.sharedCoreStandardWalletProvider.evidenceBlob, "db781971cd557b75de9ac88bef4dad3c24599069");
  assert.equal(ownerActivityCheckpoint.sharedCoreStandardWalletProvider.platformConsumed, false);
  assert.equal(auditV3.standardWalletParityConformance.macosWalletConnectV2Policy.sourceAndHostedPolicy, true);
  assert.equal(auditV3.standardWalletParityConformance.macosWalletConnectV2Policy.realRelayOrPairing, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappIdentityBinding.ownerCommit, "7431e002e017127ee7012354f123fa7cf31289b7");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappIdentityBinding.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappIdentityBinding.approvedTopicDappClassAndNameSubstitutionRejected, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappIdentityBinding.persistedUnsupportedMethodFailsClosedAsCorrupt, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappIdentityBinding.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSameTopicReproposalIdentityBinding.ownerCommit, "bc82f2e4c7d18f1c2c0e229abf45512d55c7f24b");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSameTopicReproposalIdentityBinding.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSameTopicReproposalIdentityBinding.existingSessionRejectsSameTopicProposalIdentityReplacement, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSameTopicReproposalIdentityBinding.approvalFailsClosedOnSameTopicIdentityReplacement, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSameTopicReproposalIdentityBinding.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.ownerCommit, "a6c71a3159f99cb313db5f332fef1447d8503d7b");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.concurrentAndPersistedSameTopicProposalsFailClosed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.runtimeSecondPendingProposalError, "WALLETCONNECT_REQUEST_DUPLICATE");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.persistedSameTopicTwoProposalError, "WALLETCONNECT_PERSISTENCE_CORRUPT");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectSinglePendingTopic.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappDisplayNameAmbiguity.ownerCommit, "3355c5804c851f30462d75722dac6a3114eefca8");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappDisplayNameAmbiguity.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappDisplayNameAmbiguity.persistedAmbiguousDisplayNameFailsClosedAsCorrupt, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappDisplayNameAmbiguity.normalUnicodeNamesSupported, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectDappDisplayNameAmbiguity.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.ownerCommit, "4f5ab74142f08f6ac5d518f3a3bca9b13f00397f");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.uppercaseRuntimeTopicRejected, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.persistedUppercaseTopicFailsClosedAsCorrupt, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.centralAcceptance.commit, "0cf9b0ac5f9f0ca328094fb1e0719067e9f0faca");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalLowercaseTopic.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalDigestAndApprovedAccount.ownerCommit, "212a9c6fd3877d843cc933106776a02c87260218");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalDigestAndApprovedAccount.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalDigestAndApprovedAccount.uppercaseRuntimeDigestRejected, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalDigestAndApprovedAccount.persistedUppercaseApprovedAccountFailsClosedAsCorrupt, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectCanonicalDigestAndApprovedAccount.realDappRuntime, false);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectControlCharacterRequestId.ownerCommit, "a3f27e542b9606d4dd3c15a4a4173de53a23e74e");
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectControlCharacterRequestId.hostedPassed, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectControlCharacterRequestId.runtimeControlCharacterRequestIdRejected, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectControlCharacterRequestId.persistedControlCharacterRequestIdFailsClosedAsCorrupt, true);
  assert.equal(auditV3.standardWalletParityConformance.nativeWalletConnectControlCharacterRequestId.realDappRuntime, false);
  const hardening = auditV3.standardWalletParityConformance.crossPlatformReownRuntimeHardening;
  assert.equal(hardening.ownerHead, "bbfbd9441005694981902c3be7d02b4bf09d141e");
  assert.equal(hardening.implementationCommit, "9f8e6aca883af7d31875e04ca01b8b20fac47892");
  assert.equal(hardening.supersedes.implementationCommit, "b25c8c565c1e83687bafd957c199b51a8e92eab0");
  assert.equal(hardening.supersedes.evidenceCommit, "5d66e08c79a175e739d277f215e352b2a8d50dab");
  assert.equal(hardening.evidenceBlob, "4b64f282d13f8c0d1382cf327ff124261efe59c5");
  assert.equal(hardening.evidenceSha256, "f03272df8ed45d4be50f5db905c7d2263b61b93b80aaa4dd6f65f00e3af6b918");
  assert.equal(hardening.malformedAddSwitchRequestRejectedBeforeApproval, true);
  assert.equal(hardening.sourceOnly, true);
  assert.equal(hardening.hostedCITriggered, false);
  assert.equal(hardening.relay, false);
  assert.equal(hardening.parityComplete, false);
});

test("Central P0-155 makes installed E2E the only interoperability completion tier", () => {
  const central = auditV3.centralP0155InteroperabilityMatrix;
  assert.equal(central.commit, "f3e4a07247f198f2a2fdd558efd7e9706b4075cf");
  assert.equal(central.standardWalletIndependentFromGateway, true);
  assert.equal(central.productSessionFailureEffect, "PRIVATE_SERVICE_DEGRADED_ONLY");
  assert.equal(central.desktopWindows.evidenceTier, "PUBLIC_ARTIFACT_IDENTITY_ONLY");
  assert.equal(central.desktopWindows.installedAuthority, false);
  assert.equal(central.completionEvidenceTier, "DIRECT_INSTALLED_E2E");
  assert.equal(central.sourceCiLocalArtifactOrDetectionAloneCounts, false);
  assert.equal(central.executionLease, false);
  assert.equal(central.productionLease, false);
  assert.equal(central.webInstallConfirmationGranted, false);
  assert.equal(central.productsConnected, 0);
  assert.equal(central.parityComplete, false);
});

test("Central P0-156 through P0-158 accept Router source evidence without granting an execution tier", () => {
  const accepted = auditV3.centralP0156P0158RouterAcceptance;
  assert.equal(accepted.commit, "a23feb5e31dd89d1812f9c2211dac7efdeceeb23");
  assert.equal(accepted.matrixSha256, "7fc4fd177f8ded9907d76a8401de2e7ee0c3cb571c81fbe382e69c76798bdae4");
  assert.equal(accepted.p0157.routerCommit, "2e2eb37277613e52208f3e42b450a3a28a2ed08b");
  assert.equal(accepted.p0157.interopAuthority.sha256, "c59cc18de86a304be8de6ef7056e3e260e62156fe36fb0b76e021e38e096a2fe");
  assert.equal(accepted.p0158.routerCommit, "cd7c3466b2b97876733c536b5c6ca4185ca06024");
  assert.equal(accepted.p0158.iosTerminalRun, false);
  assert.equal(accepted.p0150VerifierPassed, true);
  assert.equal(accepted.p0155VerifierPassed, true);
  assert.equal(accepted.leaseIssued, false);
  assert.equal(accepted.chromeLaunchOrInstall, false);
  assert.equal(accepted.deployedOrDistributed, false);
  assert.equal(accepted.installedProvider, false);
  assert.equal(accepted.productsConnected, 0);
  assert.equal(accepted.productsMigratedV2, 0);
});

test("Central P0-159 binds Router regression evidence without lifting an installed-wallet gate", () => {
  const accepted = auditV3.centralP0159RouterRegressionBinding;
  assert.equal(accepted.commit, "347a202564035e902e945b7308bbb5c5397964a4");
  assert.equal(accepted.tree, "36e53d4405e57fe0a3093a38eb952f8790e3e2f2");
  assert.equal(accepted.acceptanceSha256, "7fcf3cb29a3f754d8a90255f4cac3ee0e9c71a434f2f9786d73facf31415f374");
  assert.equal(accepted.matrixSha256, "deee43f5f12197411ae12757aa939ab687a52615650e4688c66b2fc2b128a026");
  assert.equal(accepted.routerRegressionCommit, "762ebb08810af807a8626ca59aea02e7a39c56e8");
  assert.equal(accepted.decision, "SOURCE_EVIDENCE_ONLY_REGRESSION_BINDING");
  assert.equal(accepted.p0150VerifierPassed, true);
  assert.equal(accepted.p0155VerifierPassed, true);
  assert.equal(accepted.leaseIssued, false);
  assert.equal(accepted.chromeLaunchOrInstall, false);
  assert.equal(accepted.deployedOrDistributed, false);
  assert.equal(accepted.installedProvider, false);
  assert.equal(accepted.realDapp, false);
  assert.equal(accepted.productsConnected, 0);
  assert.equal(accepted.productsMigratedV2, 0);
});
