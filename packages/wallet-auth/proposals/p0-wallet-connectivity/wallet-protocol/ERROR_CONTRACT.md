# Error contract — Candidate

Provider errors retain EIP-1193 numeric codes. Enhanced authorization errors retain their protocol-specific stable code, HTTP status, retryability and user action. Gateway unavailability is not a provider disconnect and must not be rendered as an offline Wallet Connection.
