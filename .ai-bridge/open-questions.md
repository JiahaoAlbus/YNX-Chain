# YNX Cloud open questions

These are owner or operator inputs, not requests to expose credentials in chat.

- Which reviewed object-storage provider, regions, durability source, KMS boundary, lifecycle idempotency contract, CDN policy and exit terms will be used?
- Will Wallet/Auth approve separate native and Web registration tuples under the canonical registry?
- Which deployment and DNS authority will provide staging/public endpoints and immutable artifact hosting?
- Which owner will accept the frozen Cloud canonical events, including storage lifecycle events, into Data Fabric and shared Testnet?
- Which Security/SRE path will retain container image scan, provenance and cross-region restore evidence for the exact release commit?
