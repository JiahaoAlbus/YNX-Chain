# YNX Cloud active plan

Phase: **EXPAND**, with central **INTEGRATE** acceptance still pending.

1. Keep commit `7759586` and evidence checkpoints protected on `origin/codex/final-cloud`.
2. Obtain a green CI container build/cold-start result, then add container image scan evidence.
3. Implement the next autonomous storage gap: versioned hot/cold/archive lifecycle state, fail-closed transition rules, provider adapter contract and migration tests.
4. Continue queue/worker separation and persisted concurrent/sharded capacity evidence.
5. Submit the frozen Wallet, object-scope, Data Fabric and Resource Market contracts for owner acceptance without enabling unreviewed registrations.
6. When provider, central and deployment inputs exist, execute real Object Storage and shared Testnet flows; keep staging, public, hosted, production-signed and store states false until direct proof.
