# YNX Search Current Plan

Phase: `FREEZE` → `INTEGRATE`  
Protected runtime source: `88ee867322ec11a243a483c04bab99676cc3416e`

1. Keep Source Registry v4, Search result v4, data-policy v1.0.0, canonical
   entities, explainable ranking, source-use rights, recovery format and public
   feed schemas frozen and fail-closed.
2. Preserve Request, Trace and Error correlation, bounded structured logs and
   the protected metrics contract; do not claim central Monitor acceptance or
   staging evidence before direct proof exists.
3. Implement the provider-neutral external Search adapter with explicit
   unavailable, rate-limit, retention, health, `asOf` and source-separation
   semantics before requesting a provider credential.
4. Complete Wallet, AI, Trust, Browser, Data Fabric, Monitor, Website,
   Integration and Security owner acceptance; do not replace missing owners
   with local substitutes.
5. Deploy the protected runtime source to staging only after recovery and
   release gates pass, then verify exact health SHA, restart, metrics, empty or
   approved corpus behavior, backup, restore and rollback.
6. Advance to public release only after immutable artifacts, SBOM, provenance,
   Website `/search`, public hashes, current-source deployment and direct public
   evidence pass.
