# YNX Search Current Plan

Phase: `FREEZE` → `INTEGRATE`  
Protected runtime commit: `52c70f74220df06208b6a415580a5a879c4a8cb8`

1. Freeze the Search integration contract, dependency acceptance, release record,
   public metadata, and full-goal coverage matrix.
2. Implement canonical YNX entity aliases and negative `YNX != Lynx` behavior.
3. Add versioned, machine-readable ranking explanations covering exact entity,
   phrase, lexical, authority, freshness, language, diversity, duplicate and
   source-quality factors. Vector remains explicitly Candidate until real
   embeddings and quality evidence exist.
4. Generate the six deterministic public feed artifacts with schemas and hashes.
5. Add public-only data-class ingestion gates, v3 backup/restore and migration
   evidence, structured observability and capacity measurements.
6. Complete Wallet, AI, Trust, Browser, Data Fabric, Website, Integration and
   Security owner acceptance, then deploy the current source commit to staging.
7. Advance to public release only after current-source staging, immutable
   artifacts, public route and direct evidence pass.
