# YNX Cloud active plan

Phase: **PROTECT**, then **FREEZE**.

1. Protect implementation commit `6e101f9` by normal push when GitHub recovers; verified bundle already exists.
2. Freeze `release/integration/cloud-contract.json` and obtain owner acceptance without enabling unreviewed registrations.
3. Obtain a green CI container build/cold-start result, then add container scan evidence.
4. Continue autonomous runtime gaps: product-isolated content-addressed dedup, storage lifecycle contract, queue/worker and persisted concurrent capacity tests.
5. When provider, central and deployment inputs exist, execute real Object Storage and shared Testnet flows; keep public and production states false until direct proof.
