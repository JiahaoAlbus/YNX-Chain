import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const address=/^0x[0-9a-fA-F]{40}$/;const gitCommit=/^[0-9a-f]{40}$/;
const release=JSON.parse(await readFile(new URL("../../release/dex/product-release.json",import.meta.url),"utf8"));
for(const key of ["productId","name","sourceBranch","sourceCommit","recoveryTipCommit","integrationBranch","version","surfaces","implementedLocal","testedLocal","installedLocal","integratedCentral","deployedStaging","deployedPublic","downloadHosted","productionSigned","storeReleased","publicUrls","healthUrls","artifactUrls","sha256","bytes","signingClass","minOS","installEvidence","centralIntegration","knownLimitations","generatedAt"])assert(Object.hasOwn(release,key),`missing release field ${key}`);
assert.equal(release.productId,"ynx-dex");assert.equal(release.sourceBranch,"codex/ecosystem-dex");assert.equal(release.integrationBranch,"codex/integrate-finance-suite");assert.equal(release.integratedCentral,true);assert.equal(release.deployedPublic,false);assert.equal(release.productionSigned,false);assert.equal(release.storeReleased,false);assert(gitCommit.test(release.sourceCommit),"sourceCommit must be exact Git SHA");assert(gitCommit.test(release.recoveryTipCommit),"recoveryTipCommit must be exact Git SHA");
const wallet=JSON.parse(await readFile(new URL("../../apps/dex/wallet-client.json",import.meta.url),"utf8"));assert.deepEqual(Object.keys(wallet).sort(),["bundleId","callbacks","integrationStatus","maxScopes","productClientId","productDeviceAlgorithms","requestingProduct","schemaVersion","scopes"].sort());assert.equal(wallet.schemaVersion,2);assert.equal(wallet.productClientId,"ynx-dex-web-v1");assert.equal(wallet.bundleId,"com.ynxweb4.dex.web");assert.deepEqual(wallet.scopes,["account:read","dex:positions:read","dex:transaction:request"]);assert.equal(wallet.integrationStatus,"accepted-in-local-central-registry-not-publicly-deployed");
const integration=JSON.parse(await readFile(new URL("../../dex/ynx-testnet.integration.json",import.meta.url),"utf8"));assert.equal(integration.network.chainId,6423);for(const field of ["wrappedYNXT","factory","router","quoter","multicall"])assert(integration.deployments[field],`missing ${field}`);for(const value of Object.values(integration.deployments))if(value.address)assert(address.test(value.address));
console.log("YNX DEX manifests: PASS");
