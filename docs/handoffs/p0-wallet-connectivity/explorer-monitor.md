# Explorer and Monitor Handoff

Provide public probes for RPC, EVM RPC, Gateway, app APIs, endpoint identity,
release source identity, and error aggregation. Evidence must distinguish
Gateway rejection, Product Session degradation, RPC/API/relay failure, wrong
chain, expiry, and client retirement.
