# Project State

Explorer remote update (2026-07-12): release `4ec8a807344c` is live at `https://explorer.ynxweb4.com`. The Explorer now uses one shared two-second RPC/indexer sampler for all SSE clients, parallelizes its core snapshot reads, isolates optional validator/resource failures from core block and transaction rendering, and exposes an animated finalized-block ribbon. `go test ./...` and `make explorer-check` passed. Desktop/mobile browser checks showed live data and no horizontal overflow. Singapore, Silicon Valley, and Seoul each returned three consecutive HTTP 200 responses and verified the deployed build plus `rpc-and-indexer-backed` status; a Singapore SSE check observed height growth from `10656` to `10658` in four seconds. The authoritative public network still has only 13 indexed transactions, so unchanged transaction rows are truthful low-activity behavior rather than synthetic data. This scoped Explorer deployment is not public BFT proof and does not change the consensus candidate/cutover status below.

Remote BFT candidate update (2026-07-12): production validator/node keys were generated under owner-controlled, mode-restricted host-local directories on the four assigned servers and matched a public-key-only role manifest. A WireGuard overlay at `10.77.42.1-4/32` provided three encrypted private peers per role. Fresh authoritative state at height `16165` contained 14 accounts, 4 validators, and the funded owner address with `1000 YNXT`; package `tmp/consensus-candidate-production/package-0b28e7da47a9` bound that state to `ynx_6423-1`. The remote parallel candidate reached common height/hash with four signers, committed owner-signed transaction `E3A6D8BCBF30609BC50DFCD954BCD1B024B32E4D1B6111330173C6FB6BC85548` at height `16277`, and produced identical sender/recipient account state on all four applications. The one-validator fault drill advanced with three signers and recovered Seoul to height `16325`. Approval-gated rollback then removed all candidate services/state; the post-rollback deploy gate passed on all four roles, authoritative `ynx-chaind` and public Explorer remained active, and `publicCutoverAuthorized=false` throughout. Host-local validator secrets and owner transaction keys were not printed, copied into Git, or transmitted to the servers beyond their intended custody boundaries. This supersedes older statements below that say production keys, private networking, or remote candidate proof do not exist. Public endpoints still run the authoritative replication model, so public BFT is not yet claimed.

BFT Gateway update (2026-07-12): commits through `18ca392` add and extend `ynx-bft-gatewayd`, a loopback-only CometBFT/ABCI compatibility service. It maps candidate status, retained history, blocks, accounts, the exact validator set, node identity, native signed transaction broadcast/history, Faucet funding, signed AI permission/action/audit state, `eth_chainId`, and `eth_blockNumber` into canonical YNX API shapes. Broadcast paths verify canonical envelopes, chain ID/signature, Comet commit hash, block membership, signer/action/record identity, and final ABCI state. `/health` now reports ten implemented and five missing capabilities while deliberately keeping `publicCutoverReady=false`. All temporary services and candidate state were removed after proof; authoritative RPC and Explorer remained online. This is real remote candidate evidence, not public BFT deployment or cutover approval.

BFT Faucet update (2026-07-12): commit `c735ada` gives `ynx-faucetd` explicit rollback-compatible `authoritative` and canonical signed `bft` modes. BFT mode derives and verifies the configured address from exactly one process-local hex or mode-`0600` raw key source, serializes balance/nonce query plus signing/broadcast, validates every committed response field, and fails closed on insufficient funds, stale/rejected upstream state, custody mismatch, oversized input, or inconsistent hash/sender/recipient/amount/fee/nonce/height. Race-enabled tests prove concurrent nonces `1` and `2` without reuse. For remote proof, a local loopback Faucet retained the owner key on the local machine and reached the remote loopback Gateway only through strict SSH forwarding. A real `25 YNXT` Faucet request committed at candidate height `16234` with hash `0x33be80389374e9d5e422c26fd8727416a8afa82960ba7dcba1659dc0ff668ec0`; Gateway lookup/history matched, sender balance became `974` with nonce `1`, and four-signer evidence passed at height `16246`. The local Faucet, tunnel, remote Gateway, and candidate were then stopped/removed. This proves the BFT funding path against a remote candidate, not persistent remote or public Faucet deployment.

BFT Indexer/Explorer update (2026-07-12): commit `7d4c83d` exposes CometBFT's validated earliest retained height/hash/time through Gateway status and upgrades Indexer persistence to schema v2. Empty stores start at the exact source boundary; resumes verify stored tip integrity and every next parent hash, while pruned resume points, source regression/identity changes, and earliest/latest hash mismatch fail with explicit rebuild guidance. Authoritative sources without an earliest boundary retain height-`0` behavior. Against a fresh remote candidate, Gateway reported earliest height `16166` and the BFT Faucet transaction committed at height `16211`. An empty local Indexer reached `16224` by indexing exactly 59 retained blocks and one real transaction, then a restarted process resumed at `16225` and added 12 blocks through `16236` without duplication. The polling Indexer later matched source height `16295` with lag `0`; a temporary Explorer returned the exact Faucet transaction, four validators, and SSE growth from `16295` to `16299`. Browser checks at `1440x1000` and `390x844` showed live real data, no mobile horizontal overflow, and no console warning/error. Four-signer candidate evidence passed at height `16389`. All local temporary services/tunnels and remote Gateway/candidate state were removed, the four-host post-rollback gate passed, and public authoritative RPC/Explorer remained online. This is remote candidate compatibility proof, not public BFT deployment.

BFT AI action update (2026-07-12): commits `b63e7d5`, `310ac0e`, and `18ca392` add a canonical domain-separated `application_action` envelope with secp256k1 signer ownership, chain ID, shared account nonce, typed canonical payload/hash, fixed `1 YNXT` fee, one AI resource unit, and strict size/type/unknown-field/signature checks. ABCI state version 2 persists sorted AI permissions/actions plus append-only block-time audit events; permission, proposal, approval, rejection, query/list, AppHash, restart recovery, signer/session/requester/scope binding, expiry, and resource accounting are deterministic. `ynx-ai-gatewayd` now has explicit rollback-compatible `authoritative` and process-local signer `bft` modes with serialized nonce selection and response verification. Against a temporary four-validator candidate, the local signer key stayed on the local machine while permission `449f1c033dbfe4d4ef92ea85`, action `988787c58a51ac68ea56a841`, and approval committed at heights `16324`, `16325`, and `16326`; an invalid permission approval returned `422` without consuming nonce. All four ABCI applications returned identical approved action, permission, three audit events, and signer account state (`nonce=3`, `balance=997`, AI/bandwidth usage `3`). Four-signer evidence passed at height `16375`; updated Gateway health at height `16447` reported ten implemented and five missing capabilities with cutover false. Local AI/provider/tunnel, remote Gateway, and all candidate state were removed. The first post-rollback read-only gate hit a transient Seoul SSH disconnect; the immediate retry passed all four roles, and public authoritative Explorer/RPC remained healthy at observed RPC height `25405`. This is remote candidate proof only, not public AI/BFT deployment.

BFT Pay update (2026-07-12): commit `ba2f72e` adds canonical signed intent, invoice, refund, and webhook metadata actions through the shared signer/chain/nonce/fee envelope. ABCI state version 3 persists merchant-owned Pay objects, global idempotency records, append-only block-time events, Pay resource usage, request hashes, and audit metadata in AppHash. It rejects unsupported currency, unsafe amounts, unauthorized object access, cumulative over-refund, changed-input idempotency reuse, malformed hashes, stale webhook timestamps, and inconsistent committed evidence. Webhook HMAC remains process-local in `ynx-payd`; neither its key nor raw webhook bodies enter chain state. Local race/integration checks include 12 concurrent identical requests producing one broadcast and no replay nonce/fee. A fresh authoritative migration at height `26615` produced a four-validator candidate; temporary local `ynx-payd` reached its loopback Gateway only through strict SSH forwarding while the owner signer and webhook keys stayed local. Intent `ab6cf45373292eab55a0130a`, invoice `17bffe51e4d82571a082c3f8`, refund `eef561bafef98730df55a205`, and webhook event `3165e3925ec4e79b97dc29c2` committed. Identical intent replay returned the same object without nonce/fee and changed input returned `409`; four ABCI applications agreed on the same Pay/account digest with nonce `4`, balance `996`, Pay credits `4`, one intent/invoice/refund/webhook, four events, and four idempotency records. Four-signer evidence passed at height `26822`. Gateway commit `ce5ad6e` then reported eleven implemented/four missing with cutover false. Local Pay/tunnel secrets and processes, remote Gateway, and all candidate services/state were removed; the four-host post-rollback gate passed and public authoritative RPC/Explorer remained healthy at observed RPC height `27000`. This is temporary remote candidate proof only; no public Pay/BFT cutover is claimed.

BFT Trust/Chain Law update (2026-07-12): commit `a36e1a1` adds signed governance request creation, explicit review/rejection, Trust appeal creation, and appeal resolution. ABCI state version 4 persists signer-bound governance requests, appeals, advisory-only false-positive corrections, and append-only transparency records in AppHash using consensus block time; every action charges `1 YNXT`, one Trust unit, and one bandwidth unit through the shared nonce. Governance creation reuses the authoritative Request Validity classifier, so illegal, overbroad, evidence-free, unsupported-asset, and direct native YNXT freeze requests persist as rejected evidence without asset action. Local race/integration tests cover restart persistence, native protection, review/reject transitions, appeal correction, transparency audit hashes, terminal-decision replay, signer binding, eight concurrent nonces, unsafe key permissions, path/payload ID mismatch, and inconsistent commit evidence. A fresh authoritative migration at height `27707` produced a temporary four-validator candidate. Nine signed actions created five classified governance requests, one appeal, one advisory correction, and nine transparency entries; signer account nonce changed `0->9`, balance `1000->991`, and Trust usage `0->9`. All four ABCI applications returned governance/appeal/account digest `3a3c4e6f76056f7f3f0e6049522eaa13812884e16ca56203809bb293962a35b4` and AppHash `88501017f4ca588d3263c952bbee84a6f3a81096f730d689f690c66b845a3b20`. Four-signer evidence passed at height `27935`. The temporary Trust service, SSH tunnel, runtime key environment, remote Gateway, and candidate were removed; the four-host post-rollback gate passed, and public RPC advanced from height `28021` to `28022` while Explorer health remained true. This governance/appeal-only phase is superseded by the complete Trust proof below. It remains remote candidate evidence only, not persistent or public BFT Trust deployment.

BFT Trust full-surface update (2026-07-12): commit `82c8a3e` adds canonical signed advisory-label, evidence-packet, and tracking-review actions to the previously proven governance/appeal slice in ABCI state version 5/app version 6. Labels require a signer-bound issuer, canonical subject type, evidence hash, confidence, bounded expiry, appeal availability, explicit non-criminal legal status, and `none_advisory_only` asset effect. Label-linked and tracking-review appeals resolve against committed targets; accepted/removed/reduced outcomes deterministically correct labels without freezing or moving assets. Evidence packets snapshot committed lot lineage, labels, correction state, risk summaries, deterministic JSON hashes, and bounded PDF exports without copying private evidence or transaction bodies into consensus state. Tracking reviews reuse the authoritative evidence, minimum-necessary, overbreadth, sensitive-review, and low-confidence rules. A fresh authoritative migration at height `28701` produced a private four-validator candidate. Nine successful actions created one label, two evidence packets, two tracking reviews, two appeals, two corrections, and nine transparency entries. An unknown-subject evidence request failed without consuming nonce/fee; the next valid evidence action reused nonce `9`. The signer account moved from nonce `0`/balance `1000`/Trust usage `0` to `9`/`991`/`9`. All four ABCI applications returned complete Trust/account digest `9e906f5ec6a9f7bc7d1008ac1bed073e469755c7fe2a16a0a0405e30d27e9a38` and AppHash `d22c2259552616327896ab3494b47e9111d2d6d089aee9a9cebfd675892ab17e`; four-signer evidence passed at height `28911`. Promotion commit `f8f64eb` then reported twelve implemented and three missing capabilities with `publicCutoverReady=false`. All temporary Trust/Gateway/tunnel/secrets/candidate state were removed, the four-host rollback gate passed, and public RPC advanced from `29068` to `29069` while Explorer stayed healthy. Trust is now candidate-proven, not persistently deployed or publicly routed.

BFT Resource update (2026-07-12): commit `4ca2204` adds canonical signed `resource_delegation_create` and `resource_rental_create` actions in ABCI state version 6/app version 7. Delegation converts provider liquid YNXT into beneficiary stake; rental binds policy hash, deterministic expiry/quote, maximum price, provider boundary, and idempotency, then settles renter YNXT to provider/protocol treasury. Quote commitments, delegations, rentals, provider/protocol income, audit events, and idempotency records persist in AppHash; validation recomputes every quote/settlement/link, requires migration stake plus delegations to equal account stake, and conserves total liquid plus staked YNXT. A fresh authoritative migration at height `30344` produced a private four-validator candidate after one transient Seoul SSH timeout triggered complete automatic cleanup and a clean-gate retry. Initial four-signer evidence passed at height `30358`. Through a strict local tunnel, the owner-local mode-`0600` signer committed delegation `7aa8a41ceef2cad269cecbb4` at height `30509` and rental `1fa8fe971d0fe8dc6cde5a68` at height `30511`, using quote `4a18703f599f8d5a0dfe9aa1`. Exact delegation/rental replay returned the same objects without nonce/fee, changed input returned `409`, and an undelegated-provider rental returned `422` without changing the post-delegation account. Owner state moved from balance/stake/nonce `1000/0/0` to `896/100/2`; the `6 YNXT` rental reconciled as provider `4` plus protocol `2`, with one delegation, one quote, one rental, two income records, two events, and two idempotency records. All four ABCI applications returned Resource/account digest `f885cb50ca2c7559152528df0110eab274b5fe50834154370335e1d204cefde4` and AppHash `400d460c809bf2c3ed6c31e9120f348796870675eee504d6b873df2200b14f5e`; post-action four-signer evidence passed at height `30567`. Promotion commit `e597d0d` then reported thirteen implemented and two missing capabilities at height `30645`, with `publicCutoverReady=false`; post-promotion four-signer evidence passed at height `30650`. The local Resource daemon/tunnel, remote Gateway, candidate services/state, archives, and release directories were removed. The four-host post-rollback gate passed; public authoritative RPC advanced `30821->30833` with chain ID `6423`/`YNXT`, and Explorer remained healthy as `rpc-and-indexer-backed`. Resource is candidate-proven, not persistently deployed or publicly routed.

BFT EVM receipt/log update (2026-07-12): commit `59957e5` adds Comet-backed `eth_getTransactionByHash`, `eth_getTransactionReceipt`, and bounded `eth_getLogs` without expanding the local opcode/Counter/Hardhat/IDE subset. Receipts bind canonical payload hash, block membership/hash/height, transaction index, `gas_used`, cumulative `/block_results` gas, status, zero bloom, and truthful empty logs because current BFT envelopes execute no EVM `LOG`. A fresh authoritative migration at height `34597` produced a private candidate. Owner-signed native transfer `0x7970333552e261324280008557edeb621d8e5af2ede0b2cf6e04c5b2ad0033df` committed at height `34932`; Resource application action `0xe6293740d1914fdc90a92a1b5d73fcfc725b73c34174533cb833b401bda7c134` committed at height `35131`. Remote proof exposed two real defects: Comet unknown-tx errors arrive as structured HTTP 500 responses, fixed by `b335b39`, and application-action transaction recipients must be `null`, fixed by `999bd5d`. Both receipts then matched direct Comet transaction/block/block-results evidence with gas/cumulative gas `1`, zero logs, unknown receipt `null`, canonical malformed/pending/address/topic/outside-history rejection, and a real `1036`-block overbroad rejection. A temporary Silicon host load spike caused 3-of-4 progress and SSH banner timeouts; it recovered without restart, caught up to one block, and rejoined signing. Four ABCI applications agreed on AppHash `256eccdf2ea98bb602bee4efadcf7c4ccf433a578adedd1a006581c2f17ecfae`, semantic digest `dde88dafe26dabacae6b2ed8d6a8156c6cdd7c78c54847ba08ecf69f33832c6d`, owner balance/stake/nonce `978/10/2`, recipient balance `10`, and one Resource delegation. Final four-signer evidence passed at height `35828`. Temporary signer daemon/tunnel/Gateway/candidate state/releases were removed, the four-host absence gate passed, public RPC advanced `36088->36090`, and Explorer remained `rpc-and-indexer-backed`. EVM receipt/log compatibility is candidate-proven and promoted to fourteen implemented/one missing; it is not persistently/publicly routed and public cutover remains false.

BFT bounded IDE complete candidate update (2026-07-12): commits `d9682d4` and `437d365` add deterministic `ide_contract_deploy`/`ide_contract_call` actions restricted to the two existing pinned artifacts already covered by the bounded local interpreter; no opcode, Counter behavior, Hardhat artifact, sample contract, or arbitrary IDE surface was added. ABCI state version 7/app version 8 persists deterministic addresses, source/deployed-bytecode identity, constructor storage, SSTORE writes, real LOG evidence, receipts, audit hashes, and idempotency in AppHash. A fresh authoritative migration at height `40952` contained 14 accounts, four validators, and owner balance/stake/nonce `1000/0/0`; bound package migration hash `a7e6478eab688b493775a945e69bb425e073e520af1dec526dca212e2d964626` reached four-signer height `41031`. Through a strict local tunnel, the owner-local mode-`0600` key committed contract `0x281731f8549c41932069d32a8babb6ace1fe62ae` at height `41097` with tx `0x3c41c97144d7b510ee6463755ef1f0add8d73c9c12f13e52f6226eb34d276f69`, then `increment(5)` at height `41098` with tx `0xfdbabc9184e8b3171df2f9743677195c9c280bb5201b372fe39de56180938532`. Storage changed from `7` to `12`; the call produced one SSTORE, one `CountChanged` LOG, encoded result `12`, and a nonzero receipt bloom. Direct Comet tx/block/block-results evidence matched height `41098`, index `0`, block hash `3A5627FF4C6754DD85E82657847C160CC768CFA55F1413C5AA8470610AE648FE`, gas/cumulative gas `1`, and the Gateway receipt/filter result. The first proof exposed Comet duplicate-cache errors being mapped to HTTP `502`; `437d365` maps them to `422`. Exact replay, changed-input idempotency reuse, and unsupported execution then all returned `422` without changing owner `998/0/2`, receipt/log counts, or total supply `1,090,000,000`. Four ABCI applications returned identical contract, receipt, log, and account bytes, AppHash `7a7c836fb1745353d328a5cd2d2553be0d4b34e60e27279cb37198f7e92bfbe7`, and height-neutral semantic digest `6c76df655fe48dbedc2e83c599d2e9b1c7353ea4105438945d17e4a784631548`. Final four-signer evidence passed at height `41389`. Temporary tunnel/Gateway/candidate state/services/releases/archives were removed, the four-host absence gate passed, public authoritative RPC advanced `41496->41518`, and Explorer remained `rpc-and-indexer-backed`. Gateway capability is promoted to fifteen implemented/zero missing, but `publicCutoverReady=false`; this is complete private candidate proof, not persistent/public BFT deployment or public cutover authorization.

Public BFT cutover transaction update (2026-07-12): `ynx-bft-gatewayd` now requires a separate default-false runtime authorization plus fifteen implemented capabilities, a canonical 12-character commit, a release exactly bound to that commit, and a valid UTC build time before reporting `publicCutoverReady=true`. The transaction engine defaults to a non-mutating plan and requires a clean `main` worktree, mode-`0600` approval valid for at most 24 hours, exact commit/release binding, explicit authorization, automatic-rollback consent, and an executable phase driver. Once mutation freeze starts, any failure runs every idempotent reverse phase because a remote action can mutate and fail before returning. A clean temporary-repository self-test verified the success path and injected failures after ten mutation/post-mutation phases; every failure restored byte-identical authoritative fixture state and wrote a passed rollback journal. This is local orchestration evidence only. The production Tencent driver, real custody approval, remote freeze/snapshot/service/ingress rehearsal, persistent BFT deployment, and public proof remain incomplete; no public route changed.

Runtime mutation-freeze update (2026-07-12): a shared marker-based middleware now protects the authoritative API, BFT Gateway, Faucet, AI, Pay, Trust, and Resource service entrypoints. It is disabled when unconfigured and can be toggled atomically without restarting services. While active, safe HTTP reads, supported read-only EVM JSON-RPC calls, and AI chat remain available; malformed/batch EVM payloads and state-changing requests fail closed with retryable HTTP `503`. Unit tests verify request-body restoration and immediate unfreeze, while deployment generation binds all authoritative services to `/var/lib/ynx-chain/mutation-freeze.json`. This code is local and not deployed. A 2026-07-12 read-only Tencent status check found primary height `42260`, Singapore `42260`, Silicon Valley `41743`, and Seoul `41161`, with repeated replication timeouts on Silicon Valley and Seoul. That lag blocks a cutover rehearsal until follower connectivity and convergence recover; no remote mutation was attempted.

Authoritative follower convergence fix (2026-07-12): read-only diagnosis found `/var/lib/ynx-chain/testnet/devnet-state.json` at about `14.1 MB`; a cross-region authenticated snapshot probe timed out after 30 seconds with only about `2.6 MB` received, while the deployed follower client timeout was fixed at 10 seconds. Authenticated replication responses now negotiate gzip while retaining the HMAC over uncompressed JSON, so transparent follower decompression preserves the existing authenticity and decompressed-size checks. The follower timeout defaults to a bounded 45 seconds and deployment generation records that value. On the live primary state file, gzip produced about `2.5-2.7 MB` (`17%-19%` of raw size). Local protocol tests pass, but this fix is not deployed and follower convergence is not yet re-proven.

Restricted upgrade gate update (2026-07-12): the prior deploy gate required remote smoke to already report the current target release, creating a circular dependency for every normal upgrade. `YNX_UPGRADE_DEPLOY=1` now accepts only a fixed allowlist of source-to-target release identity differences, the separately hosted legacy Web4 health mismatch, and the resulting skipped mutation smoke. Wrong chain identity, stalled height, validator, EVM, service, governance, source-evidence, or SSH failures still block. Self-tests prove an expected old-to-new release passes and a height-growth failure remains blocked. This mode is explicit and is not a general readiness bypass.

Scoped predeploy backup update (2026-07-12): the first restricted upgrade attempt stopped before installation because the release backup included an unrelated legacy `/home/ubuntu/.ynx-v2` tree of about `15 GB`; its partial archive reached about `4.8 GB`. The remote tar was terminated, the partial was removed, all eight authoritative public services remained active, and RPC still reported the old release. The YNX Chain release backup now covers only `/etc/ynx`, the eight current service units, Caddy/Nginx ingress configuration, and `/var/lib/ynx-chain`. It does not copy legacy V2 state, observer data, or append-only runtime logs that this deployment does not modify. Manual full-stack backup remains separate.

Updated: 2026-07-12

- Explorer UI update: `internal/explorer/web.go` now provides a denser Apple-inspired operational interface over the existing real RPC/indexer APIs, with TRONSCAN-like chain-navigation depth without copying its visual design. The first viewport exposes live height, calculated indexed-window TPS, observed block time, transaction count, validator count, index lag, recent activity, and chain identity. Live block and transaction feeds support type filtering, arrival feedback, structured block/transaction/address search, a keyboard-dismissible detail drawer, copy actions, validator/resource views, explicit testnet boundaries, and MetaMask configuration. `GET /api/stream` still provides two-second SSE snapshots with a ten-second fallback; the UI now exposes connected/stale/reconnecting state, and the HTML shell is `no-store` so an old page cannot hide a deployed UI update. `go test ./...`, `make test`, `make explorer-check`, `make no-placeholder-check`, `make secret-scan`, and `make objective-state-check` pass. Desktop `1440x1000` and mobile `390x844` browser QA verified live height growth, filtering, detail interaction, zero horizontal overflow, and no console errors. Scoped Explorer release `6cd65238c77b` is live at `https://explorer.ynxweb4.com`; public verification observed SSE IDs `5766-5765-12` through `5769-5768-12`, four validators, one-block index lag, and matching health build identity. The previous binary is retained at `/var/backups/ynx-chain/ynx-explorerd-before-6cd65238c77b-20260711T145456Z`. This remains a scoped Explorer deployment, not proof of BFT consensus, mainnet, full current-HEAD release convergence, or complete ecosystem public proof.
- Wallet ownership boundary: deployed `ynx_remote_smoke_<timestamp>` accounts remain verifier-generated legacy string identifiers and `ynx_faucet` remains a runtime reserve identifier; those identifiers have no mnemonic or private key to transfer. Two owner-controlled EVM-compatible secp256k1 testnet accounts now exist as mode-`0600` local raw-key files: the funded candidate sender and a recipient used for remote signed-transaction proof. Four production validator/node key pairs exist only in mode-restricted host-local candidate directories. These prove testnet custody mechanics but are not a complete treasury/foundation/team/deployer/faucet handover. Mainnet-grade custody still requires an offline owner ceremony, independent backup/recovery verification, role separation, rotation policy, and signed handover evidence. Secrets and mnemonics must never be committed, logged, or delivered in plaintext chat.
- BFT migration boundary: `ConsensusMigrationState` exports deterministic sorted YNXT account state, core validator voting state, resource policy, last committed height/hash, liquid/staked totals, and a tamper-evident SHA-256 state hash while excluding peer operational evidence. Commit `b1275c4` adds an exact validator identity-to-CometBFT ed25519 public-key/address binding, rejects missing, duplicate, malformed, mismatched, or inactive-validator bindings, and recomputes the migration hash after binding. `ynx-chaind --export-consensus-state` and the generated bound state use atomic/mode-`0600` files. This is locally verified migration evidence, not a remote key ceremony or cutover.
- CometBFT quorum boundary: commit `b1275c4` pins the CometBFT module at `v0.38.23`, generates four isolated ephemeral homes and ABCI state paths outside Git, writes byte-identical genesis files, connects every node to the other three peers, and fixes the fresh persistent ABCI handshake so CometBFT runs `InitChain` before the migration anchor is committed. `make consensus-quorum-check` started four official CometBFT processes plus four ABCI processes, observed a common height-6 block hash, at least three precommit signatures per checked commit, participation by all four genesis validators, and exact validator-address agreement across all RPC nodes. It then broadcast a real signed YNXT transfer through CometBFT RPC, verified sender balance `874`, recipient balance `125`, nonce `1`, stopped one validator plus its ABCI process, observed the remaining three advance four blocks, restarted the stopped pair, and verified catch-up to the common height/hash with durable initialized application state. All generated keys are disposable local fixtures, mode restricted, omitted from logs/manifests, and forbidden for remote/custody use.
- Production BFT candidate boundary: commit `ec2f691` implements a schema-bound public-key-only manifest for the exact four server roles, requires RFC1918 P2P addresses, and rejects wrong role bindings, public/loopback hosts, duplicates, malformed keys, address mismatches, inactive validators, unsafe ports, and noncanonical fields. `ynx-consensus-package` creates a complete hash-covered candidate package with a common bound migration/genesis, per-role loopback RPC/ABCI/metrics, private P2P configuration, hardened candidate-only systemd units, and install/backup/rollback scripts. `ynx-consensus-keycheck` locally derives and compares host private-key identities without printing secrets. Strict-SSH deploy, common-height/hash/validator/precommit/peer evidence, approval-gated one-validator fault/recovery, owner-signed transaction convergence, and candidate rollback paths exist. `make consensus-production-package-check` generates a disposable package, rejects semantic tampering even with recomputed hashes, builds Linux ABCI/keycheck/CometBFT binaries, and dry-runs all four deploy/verify/fault/rollback roles. Evidence self-tests reject divergent hashes and divergent post-transaction balances and always keep `publicCutoverAuthorized=false`.
- Remaining BFT gap: all fifteen Gateway compatibility capabilities have private candidate proof and the candidate is intentionally rolled back. `publicCutoverReady=false` remains because persistent service transition, current-anchor migration timing, production signer custody/recovery, Indexer/Explorer continuity, ingress rebinding, failure thresholds, and automatic rollback still need one approval-gated public-cutover transaction and live evidence. The public network stays on authoritative producer/follower replication. No public BFT, complete owner wallet handover, production Byzantine-fault readiness, or mainnet status is claimed.
- State snapshot baseline commit: `ef88ed2 Fix candidate signed transaction sender lookup`
- Last pushed commit before this acceptance update: `999bd5d Return null EVM recipient for application actions`
- Current deployment override: the current verifier release is deployed across the primary, Singapore, Silicon Valley, and Seoul validator-role nodes. Strict SSH, release-manifest checksums, chain ID `6423`, native `YNXT`, block growth, four configured validators, public RPC/EVM/Faucet/Indexer/Explorer/AI/Pay/Trust/Resource health, bounded Explorer index-lag polling, Anti-Illegal Request rejection, native YNXT protection, governance review/reject, appeal resolution, false-positive correction evidence, transparency reporting, and Resource policy/delegation/rental/income flows were observed remotely. Authenticated authoritative replication is also remotely verified: primary is the only producer, all three followers disable production and report follower mode, and all returned the same hash for fixed height `3898`. Full public proof is not complete because the AI provider returns `429 insufficient_quota` and `web4.ynxweb4.com` still serves the legacy `ynx_9102-1` Hub. The replication design provides deterministic state convergence, not BFT consensus, validator voting, or Byzantine fault tolerance. This current override supersedes older predeployment blocker statements retained below as historical implementation detail.
- Authoritative replication is implemented, locally tested across restart, and remotely verified for fixed-height/hash convergence. One producer exposes an authenticated HMAC-signed snapshot; followers disable local production, reject mutations, validate and atomically persist producer state, and preserve node-local peer evidence. The next chain-runtime gap is migration to real BFT consensus rather than extending the authoritative model.
- Chain repo state: `/Users/huangjiahao/Desktop/YNX Chain`, branch `main`, remote `https://github.com/JiahaoAlbus/YNX-Chain.git`, currently focused on core chain construction while remote public deployment is blocked by SSH/host-key and public-ingress evidence. The current local slice aligns the deploy package with the actual public endpoint contract used by `remote-smoke-test`: `REST_DOMAIN` and `INDEXER_DOMAIN` are now required deploy inputs, persisted into the generated chain env, `nginx/ynx-chain.conf` generates public ingress routes, and Caddy-backed hosts receive a packaged `caddy/ynx-chain.caddy` snippet installed through a managed import block in `/etc/caddy/Caddyfile` instead of overwriting the whole Caddyfile. The deploy bundle also includes `scripts/check-local-services.sh`; after systemd restarts, deployment calls it on the target host to verify localhost `/health`, `/status`, `/node/identity`, expected commit, expected release name, numeric chain ID, and `YNXT`. Primary-node full-stack mode now also requires indexer, explorer, and faucet `/health` responses to expose the same expected build commit and release, so a restarted primary cannot pass local deploy checks with stale auxiliary service binaries. `deploy-dry-run` and `caddy-ingress-check` verify these domains, proxy targets, Caddy snippet packaging, candidate validation command path, backup path, managed import installer, local execution of the generated installer against an existing Caddyfile fixture, local-service checker packaging, checker self-test, generated post-restart checker commands for the primary and all validators, and release strings embedded in `ynx-chaind`, `ynx-indexerd`, `ynx-explorerd`, and `ynx-faucetd`. This does not claim remote deployment. `remote-smoke-test` treats Chain Law APIs, AI permission/action audit, validator peer readiness, validator peer discovery, node identity, release/build identity, release manifest checksum evidence, validator peer sync freshness, and validator peer sync records as public proof requirements and will verify request validity rules, transparency reports, sensitive AI action proposal/permission/approval/lookup/list audit, illegal native YNXT rejection, governance request lookup/review/reject, Trust appeal lookup/resolution with Trust evidence correction summary, anti-unreasonable tracking review, Resource Market policy/quote evidence, validator metadata, validator peer-readiness evidence, expected/observed validator peer-discovery evidence, node identity/freshness evidence, expected release identity, release manifest checksum evidence, and synced validator peer records before `public-proof` can pass.
- Website repo state: `/Users/huangjiahao/Desktop/YNX-Chain-website`, branch `main`, remote `https://github.com/JiahaoAlbus/YNX-Chain-website.git`, latest observed commit `1ddc977 Harden website readiness and deployment`.

Completed modules in the chain repo:

- Local chain, faucet, indexer, explorer service code exists with Go tests.
- Validator records now persist peer-readiness metadata: `peerReady`, `peerStatus`, `latestHeight`, `lastSeenAt`, `updatedAt`, and optional `peerEvidence`. Produced blocks update the producing validator readiness, `POST /validators/{address}/heartbeat` records explicit validator readiness evidence, `GET /validators` exposes it, `GET /status` exposes `readyValidatorCount`, `validatorPeerReadiness`, `validatorPeerDiscovery`, and `validatorPeerSync`, and matching validator addresses preserve runtime readiness across snapshot reload and `YNX_VALIDATOR_SET` config refresh.
- Validator peer discovery now persists expected bootstrap peers and observed peer evidence. `YNX_BOOTSTRAP_PEERS` supports `address|peerId|host|p2pAddress|role` entries or JSON; absent explicit peers, the node derives expected peers from `YNX_VALIDATOR_SET`. `GET /validators/peers` exposes expected/observed peer records, and `POST /validators/{address}/peers/observe` records observed peer discovery evidence for restart-safe local proof.
- Validator peer sync now persists source/target validator sync evidence with source height, target height, lag, status, evidence, and timestamps. `POST /validators/{address}/peer-sync` records sync evidence for another configured validator, `GET /validators/peer-sync` exposes it, snapshot reload preserves it, and `/status` summarizes synced/lagging/total records. `ynx-chaind` also supports automatic polling with `YNX_LOCAL_VALIDATOR_ADDRESS`, `YNX_PEER_RPC_URLS`, and `YNX_PEER_SYNC_INTERVAL`, deriving target height from peer `/status` responses and recording `peer-rpc-poll:<url>/status` evidence through the same persistent state. This is local deployable sync evidence, not remote public proof.
- Node identity, release/build identity, and peer-sync freshness now expose non-secret runtime proof through `GET /node/identity`, `/status.nodeIdentity`, and `/status.build`: configured validator address, validator moniker/role/host/peer ID, expected validator count, peer sync target count and target validator addresses, peer sync interval, `build.commit`, `build.release`, `build.buildTime`, and per-target freshness records that classify targets as fresh, stale, missing, synced, or lagging. This lets remote proof identify a node's role, stale/lagging/missing sync state, and served source release without printing full env files or secrets.
- `ynx-chaind` now fails fast for unsafe multi-validator testnet startup configs: missing `YNX_LOCAL_VALIDATOR_ADDRESS`, local validator absent from `YNX_VALIDATOR_SET`, incomplete peer RPC target coverage, self-target peer RPC entries, duplicate target entries, or peer targets outside the configured validator set. Devnet and single-validator local testnet smoke remain allowed.
- `ynx-chaind --check-config` now validates the same testnet startup guard and prints non-secret build commit/release/buildTime without binding HTTP ports, writing chain state, producing blocks, or starting peer polling. Unit tests verify it accepts safe role env values, rejects unsafe self-target role env values, reports injected build identity, and does not create the configured data directory.
- Deploy, dry-run, verify, ops, backup, rollback, host-key audit, non-mutating host-key repair plan, blank host-key approval template generation, host-key approval request generation, host-key approval check, approval-gated known_hosts repair, legacy inventory, remote smoke, public proof, and package commands exist.
- `make validator-peer-readiness-check` starts two local validator node processes on dedicated ports, records a validator heartbeat and observed peer evidence, configures the primary node with `YNX_PEER_RPC_URLS`, waits for automatic peer sync polling to write derived source/target height evidence, verifies `/validators/peer-sync`, and verifies `/status` readiness/discovery/sync summary.
- `make deploy-dry-run` now builds a release bundle with role-specific `ynx-chaind-primary.env`, `ynx-chaind-singapore.env`, `ynx-chaind-silicon-valley.env`, and `ynx-chaind-seoul.env` files, verifies each role has the correct `YNX_LOCAL_VALIDATOR_ADDRESS`, peer poll targets, install command, shell-sourceable quoted env, release metadata file, release manifest with artifact SHA-256 checksums, `ynx-chaind`/`ynx-indexerd`/`ynx-explorerd`/`ynx-faucetd`/`ynx-ai-gatewayd` binary release strings, public ingress domains for REST and Indexer, Nginx server blocks/proxy targets, Caddy managed snippet site blocks/proxy targets, non-destructive Caddyfile managed-import install path with candidate validation, backup, reload command path, executable local installer fixture coverage for Caddy-backed primary hosts, packaged post-restart local service checker, checker self-test, generated checker commands for primary full-stack mode and validator-only mode, and `go run -ldflags ... ./cmd/ynx-chaind --check-config` build identity output so remote nodes cannot all silently use the primary validator identity, restart with incomplete peer targets, serve an unidentifiable or checksum-unbound binary, clobber unrelated existing Caddy routes, skip localhost release/chain/service verification after restart, leave stale primary auxiliary services running, or ship an ingress config that omits `rest.*` and `indexer.*` public proof domains.
- `deploy-testnet` now installs each role-specific env and runs `/usr/local/bin/ynx-chaind --check-config` through `/etc/ynx/ynx-chaind.env` on the target node before any `systemctl restart ynx-chaind` path. After restarts it runs the packaged local service checker on each target node, checking chain status and node identity against the expected commit/release/chain ID/YNXT and, for primary full-stack mode, checking indexer/explorer/faucet/AI Gateway health plus their expected build commit and release. In dry-run this is proven as a command path only; no remote host is mutated.
- `make verify-testnet-check` verifies the remote verifier remains strict-SSH, role-aware, checks only safe chain env fields, requires local validator identity/count, expected release commit/name, observed release manifest commit/release/path values, release manifest metadata, release manifest SHA-256, installed `ynx-chaind` SHA-256 matching the manifest, emits machine-readable release manifest evidence, plus `/status`, `/node/identity`, `/validators`, `/validators/peers`, and `/validators/peer-sync`, and does not print the full remote chain env. `make preflight` includes this local verifier guard, `make release-manifest-evidence-check`, `make validator-peer-readiness-check`, the local Chain Law / Appeal / Transparency smoke gates (`make anti-illegal-request-check`, `make request-validity-check`, `make transparency-report-check`, `make trust-appeal-check`, and `make anti-unreasonable-tracking-check`), `make native-ynxt-no-hidden-freeze-check`, and `make resource-market-check`.
- `remote-smoke-test` checks public RPC, EVM RPC, REST, gRPC, faucet, indexer, explorer, AI, Web4, release manifest evidence, Request Validity rules, transparency endpoints, validator metadata, validator peer readiness, validator node identity/freshness, release commit/name/buildTime on `/status` and `/node/identity`, validator peer discovery, and validator peer sync before mutable proof actions. `make remote-smoke-test` now writes the deploy gate evidence path `tmp/verify-testnet/remote-evidence.json`, and GET probes retry once on transient fetch failure before recording a failed check. After the public chain is verified as the new YNX Testnet, it also checks faucet, AI sensitive action proposal audit, AI active permission audit, AI permission-gated approval, AI action lookup/list audit, Pay intent/invoice idempotency, Pay webhook replay-safe audit metadata, Pay event audit hashes, Trust trace, Resource, IDE compile, Anti-Illegal Request rejection, governance review/reject, appeal resolution, Trust evidence false-positive correction summary, anti-unreasonable tracking, and final transparency counts.
- Legacy backup coverage is wired into deployment and ops checks.
- `remote-blocker-report` classifies node failures, public endpoint failures, freshness for underlying host-key-audit, remote-smoke, host-key approval-request evidence, external approval-packet evidence, and host-key approval-status evidence, plus consistency between current host-key mismatch fingerprints, approval-request JSON, approval-packet JSON, approval-status JSON, and remote-smoke evidence identity. It now marks remote-smoke evidence as a deploy-blocking source issue when the proof type, full git commit, expected release commit/name, chain ID, EVM ID, or native symbol do not match the current checkout. It rejects old approved host-key status JSON that lacks trusted approval metadata (`hostKeyAuditSha256`, `source`, `approvedAt`, `approvedBy`, `verificationChannel`, and `evidence`) even if the fingerprint rows match. It rejects approved status JSON whose approved audit SHA-256 or current status audit SHA-256 does not match the current host-key audit report. It rejects approval-packet JSON that does not match the current host-key audit, does not carry the current host-key audit SHA-256, or fills trusted-fingerprint draft slots from untrusted scan output. It surfaces approval-status skipped mismatch nodes so hosts with no valid scanned fingerprint remain visible as SSH/keyscan blockers, not silently treated as approved or approve-ready rows. Semantic public endpoint failures such as legacy `ynx_9102-1` evidence, wrong chain IDs, empty or incomplete validator metadata, missing release identity, missing release manifest checksum, missing validator node identity, missing validator peer-readiness/discovery/sync, missing block growth, and skipped mutable proof actions are deploy-blocking, even when HTTP responds successfully.
- `deploy-readiness-gate` reads `tmp/verify-testnet/remote-blockers.json` and blocks real `deploy-testnet` mutation when SSH/public ingress evidence is unsafe or when any required source evidence is missing, stale, invalid, inconsistent, or when current remote-smoke evidence status is not `passed`. Required sources always include host-key-audit and remote-smoke evidence; while host-key mismatches exist, the host-key approval-request markdown/JSON artifacts, external approval-packet markdown/JSON artifacts, and host-key approval-status JSON are also required to be fresh, current, and not awaiting trusted confirmation. `DEPLOY_DRY_RUN=1` skips the gate for local dry-run validation only.
- `make deploy-readiness-gate-check` verifies the gate and blocker report reject old-format blocker JSON, missing source evidence, stale source evidence, missing source files, remote-smoke evidence not bound to the current local git commit/release identity, remote-smoke evidence whose status is not `passed`, explicit endpoint blockers, semantic public endpoint blockers including missing release manifest/identity and validator peer evidence, missing dynamic required approval-request/approval-packet/status evidence, approval-request fingerprint mismatches, approval-packet fingerprint or blank-draft mismatches, old approval-status metadata, approval-status audit-hash mismatches, and approval-status awaiting-confirmation blockers while accepting a fresh no-blocker fixture.
- `make public-proof-evidence-check` verifies public proof evidence completeness with a required remote check list that now includes release/build identity, release manifest evidence, validator peer-readiness, peer-discovery, peer-sync evidence, AI permission/action audit evidence, Pay idempotency and webhook/event audit evidence, and Trust appeal correction evidence after appeal resolution. It now also rejects checks-only fixture evidence unless the evidence declares `proofType=remote-public-testnet-smoke`, carries a valid timestamp, full git SHA, expected YNX testnet identity (`ynx_6423-1`, EVM `6423` / `0x1917`, native `YNXT`, at least 3 validators), concrete release commit/name identity, non-local HTTPS public endpoints for RPC/EVM/REST/Faucet/Indexer/Explorer/AI/Web4, a non-local gRPC host, and a release-manifest evidence path. The evidence `gitCommit` and `expected.releaseCommit` must also match the current local HEAD, so stale old-release evidence cannot validate a current public proof package. `release-manifest-evidence.json` includes per-role observed release manifest commit, release name, `bin/ynx-chaind` path, manifest SHA-256, and binary SHA-256, and fails if any node's observed manifest identity differs from the expected release. This lets public proof tie `/status` release identity and public endpoint identity to the checked-out deployed artifacts instead of only text markers. `make public-proof-package-check` verifies the generated proof package manifest is internally consistent with remote evidence, validation JSON, release-manifest evidence, generated proof summary status, current HEAD, and per-file SHA-256s, and rejects diagnostic packages that are accidentally marked as valid public proof. `make public-proof` now runs the package checker against its generated `tmp/packages/public-proof/final` directory before returning success; it writes `public-proof-validation.json` and `release-manifest-evidence.json` into the package and only sets `validPublicProof=true` when remote-smoke passed, evidence metadata is valid and current-HEAD-bound, release-manifest evidence passed, evidence status is `passed`, every required AI / Pay / Chain Law / Appeal / Transparency / validator / release / release-manifest / endpoint / mutable proof check is present, no required check failed, mutable actions were not skipped, and the generated package manifest/hash validation passes.
- `make readme-positioning-check` verifies the README top positioning keeps YNX Chain framed as a Web4 L1 full-stack blockchain ecosystem built around YNXT while preserving honest current-scope boundaries and blocking unsupported claims about mainnet launch, exchange listing, stablecoin issuer support, wallet default support, or third-party partnerships.
- Deploy, ops, and verify SSH calls now use `StrictHostKeyChecking=yes`; `make objective-state-check` fails if source scripts reintroduce `StrictHostKeyChecking=accept-new`. Unknown or changed remote host keys must be handled only through host-key audit, approval request, trusted out-of-band fingerprint confirmation, approval check, and approved repair.
- `make host-key-approval-template` generates `tmp/host-key-audit/host-key-approvals.template.json` with the current host-key audit report path/SHA-256, blank fingerprint fields, plus required trusted-verification metadata fields (`approvedBy`, `verificationChannel`, top-level `evidence`, and per-node `evidence`) for strict SSH mismatch hosts only. It is not trusted approval and does not write `.host-key-approvals.json`.
- `make host-key-approval-request` generates `tmp/host-key-audit/HOST_KEY_APPROVAL_REQUEST.md` plus JSON with untrusted current-scan fingerprints and blank trusted-fingerprint fields for external cloud-console/provider comparison. It is not trusted approval and does not write `.host-key-approvals.json`; nodes with strict SSH failure but no valid scanned fingerprints are skipped as SSH/keyscan blockers instead of approval rows.
- `make host-key-approval-packet` generates `tmp/host-key-audit/HOST_KEY_EXTERNAL_APPROVAL_PACKET.md` plus JSON for external reviewers. The packet includes untrusted current-scan fingerprints, the host-key audit report path and SHA-256, a blank approval draft with empty trusted fingerprint slots, evidence requirements, and follow-up commands. It is non-mutating, not trusted approval, and does not write `.host-key-approvals.json`.
- `make host-key-approval-status` generates `tmp/host-key-audit/HOST_KEY_APPROVAL_STATUS.md` plus JSON without mutating `known_hosts`, records whether ignored `.host-key-approvals.json` exists/readable, records the current and approved host-key audit SHA-256 values, summarizes current mismatch fingerprints, and keeps the blocker machine-readable while external fingerprint confirmation is pending.
- `make host-key-approved-repair-dry-run` and `make host-key-approved-repair` call the same approval comparison first. Without a valid ignored `.host-key-approvals.json`, both fail closed before touching `known_hosts`; approval now requires the current `hostKeyAuditSha256`, source, approved timestamp, approver, verification channel, evidence summary, per-node evidence, and exact fingerprint matches. When approval exists, the apply target backs up `known_hosts`, replaces only approved host entries from current scan files, and verifies strict SSH.
- `make host-key-approval-check-test` verifies the host-key approval checker accepts exact matches and rejects mismatched approvals or stale audit-hash approvals without touching `known_hosts`.
- Anti-Illegal Request Engine now has persistent request intake, classification, rejection, and transparency entries for private-secret, signature-bypass, hidden-record, fake-risk, unsupported-Trust-conclusion, AI-auto-punishment, overbroad, and native YNXT direct-freeze/direct-transfer requests.
- Chain Law / Anti-Illegal / Request Validity / Appeal / Transparency local validation was refreshed on 2026-07-10 at `065fc43`: `go test ./...`, `make anti-illegal-request-check`, `make request-validity-check`, `make transparency-report-check`, `make trust-appeal-check`, `make native-ynxt-no-hidden-freeze-check`, `make test`, `make no-placeholder-check`, `make secret-scan`, `make env-check`, `make preflight`, and `make objective-state-check` all passed. `make preflight` now runs the Chain Law / Appeal / Transparency smoke checks and native YNXT hidden-freeze scan directly, so the local deploy-package gate cannot pass while those local API flows or native YNXT freeze protections are broken. This confirms local code, handlers, persistence, smoke commands, and package/preflight gates only; it is not remote deployment or public proof.
- Resource Market preflight coverage was added on 2026-07-10 after `c17c6d1`: `make preflight` now also runs `make resource-market-check`, covering policy identity/hash/version, YNXT currency enforcement, quote breakdown, delegation, rental settlement split, income records, and analytics policy evidence before deploy-package readiness can pass. This is local deploy-package proof only; it is not remote deployment or public proof.
- Validator peer-readiness preflight coverage was added on 2026-07-11 after `88d3ded`: `make preflight` now also runs `make validator-peer-readiness-check`, covering a two-node local validator process pair, heartbeat readiness, observed peer evidence, automatic peer-sync polling, `/validators/peer-sync`, and `/status` readiness/discovery/sync summaries before deploy-package readiness can pass. This is local deploy-package proof only; it is not remote deployment or public proof.
- Core public-service preflight coverage was added on 2026-07-11 after `3d3dbad`: `make preflight` now runs `make faucet-check`, `make indexer-check`, and `make explorer-check` sequentially. This verifies RPC-backed faucet funding, rate limits, persisted request logs and metrics; persistent index creation and resume against produced blocks and transactions; and RPC/indexer-backed Explorer health, chain identity, blocks, transactions, accounts, resources, token, validator, fee, search, wallet metadata, and metrics. The checks are deliberately sequential because they use shared local testnet ports. This is local deploy-package proof only; Faucet, Indexer, and Explorer remain not remotely deployed or publicly proven.
- Pay API now records merchant idempotency keys for intents, invoices, refunds, and webhook signatures; duplicate idempotency requests return the original object instead of creating conflicting records.
- Pay API now persists audit events with audit hashes and stores webhook signature metadata for lookup without storing or exposing signing secrets. Remote public proof now requires intent and invoice idempotency replay, webhook replay-safe audit fields, webhook lookup, webhook idempotency replay, and Pay event audit hashes after deployment.
- AI Gateway is now a standalone Go service (`ynx-ai-gatewayd`) rather than only chain-internal routes. It requires provider, client-access, and chain-upstream keys from env; calls an OpenAI-compatible provider with live chain status context; emits request-ID-bearing, session-isolated SSE; enforces per API-key/IP rate limits; exposes health/build identity and metrics; writes prompt-hash-only JSONL audit records; and proxies permission/action records to the persistent chain runtime. Deployed chain AI routes require the upstream secret, so the general REST domain cannot bypass gateway authentication. Unit tests include 16 concurrent isolated sessions and direct-bypass rejection. `make ai-gateway-check` starts the standalone daemon plus a local provider fixture and verifies auth, bypass protection, provider output, permission/action approval, metrics, and audit redaction. Docker, systemd, monitoring, backup/rollback, dedicated port `6429` ingress, release manifest, deploy dry-run, post-restart service checks, and public-proof requirements for authenticated SSE and gateway build identity are wired. This is local verified and deployable package evidence only; it is not remotely deployed or publicly proven.
- Pay is now a standalone merchant service (`ynx-payd`) instead of only public chain-internal routes. It requires merchant ID, client API key, chain-upstream key, and webhook signing key from a dedicated environment file; injects the authenticated merchant identity; requires idempotency keys on mutations; rejects client-supplied signing keys; enforces a 1 MiB body bound, per API-key/IP rate limits, and request IDs; exposes health/build identity and metrics; and writes body-hash-only JSONL access audit records before forwarding. Canonical payment intents, invoices, refunds, webhook metadata, Pay events, audit hashes, and idempotency records remain persistent in the chain runtime. Deployed chain Pay routes require the upstream secret, preventing general REST bypass. `make pay-api-check` verifies the real daemon and chain process, auth, direct-bypass rejection, merchant binding, idempotency, managed webhook signing, persistent events, metrics, and audit redaction. Docker, systemd, monitoring, backup/rollback, dedicated port `6430` and Pay-domain ingress, release manifest, deploy dry-run, post-restart checks, and public-proof requirements for Pay gateway health/build/auth are wired. This is local verified and deployable package evidence only; it is not remotely deployed or publicly proven.
- Trust and Chain Law now have a standalone public service (`ynx-trustd`) instead of only chain-internal routes. It requires client and chain-upstream keys from a dedicated environment file; enforces a 1 MiB body bound, 2 MiB evidence/export bound, per API-key/IP rate limits, and request IDs; exposes health/build identity and metrics; and writes body-hash-only fail-closed JSONL access audit records before forwarding. Canonical lot lineage, pro-rata traces, distinct address and known-transaction advisory labels, evidence packets, governance requests, Request Validity results, appeals, false-positive corrections, tracking reviews, and transparency entries remain persistent in the chain runtime. Deployed `/trust/*` and related `/governance/*` routes require the upstream secret, preventing general REST bypass. `make trust-api-check` verifies the real daemon and chain process, auth, direct-bypass rejection, lineage, address/transaction advisory labels, JSON/PDF evidence, native YNXT protection, Request Validity, appeal resolution, anti-unreasonable tracking, transparency, metrics, and audit redaction. Docker, systemd, monitoring, backup/rollback, dedicated port `6431` and Trust-domain ingress, release manifest, deploy dry-run, post-restart checks, and public-proof requirements for Trust gateway health/build/auth are wired. This is local verified and deployable package evidence only; it is not remotely deployed or publicly proven.
- EVM RPC now returns persisted transaction logs in receipts and filters logs by block range, address, and topics through `eth_getLogs`.
- Contract deploy and verify records now expose deterministic event metadata parsed from Solidity `event` declarations, and local contract deployment receipts emit contract-address logs filterable through `eth_getLogs`.
- IDE compile now exposes deterministic local artifact metadata with source hash, bytecode hash, deployed bytecode hash, artifact hash, artifact kind, pinned Solidity `0.8.24` compiler config hash, compiler execution status, deployed bytecode comparison status, ABI, events, functions, selector source, `bytecodeSelectorMatched`, local runtime storage seed, runtime storage-slot metadata, compiler mode, runtime mode, verifier mode, reproducibility status, and limitations. Repository sources that match built Hardhat artifacts upgrade to `pinned-solc-bytecode-artifact`; ad hoc IDE snippets remain `source-analyzer-artifact`. `POST /ide/deploy` accepts optional `constructorArgs`, stores them in contract metadata, and seeds canonical 32-byte hex local storage slots for simple public `uint` assignments such as `totalSupply = initialSupply`, `SampleEVMWriteCounter.count = initialCount`, plus `mapping(address=>uint)` assignments such as `balanceOf[msg.sender] = initialSupply` for local verifier/devnet evidence. `artifacts/ynx-selector-metadata.json` records `ethers` Keccak selectors, event topics, and deployed-bytecode selector presence for repository Hardhat artifacts, including `SampleEVMWriteCounter.count()`, `increment(uint256)`, and `CountChanged(address,uint256)`. `GET /ide/verifier/{address}` exposes explicit local verifier service evidence without claiming remote public proof. IDE/EVM calls distinguish source-analyzer literal returns from a bounded read-only EVM opcode interpreter staticcall subset that executes supported Hardhat getters such as ERC20 `decimals()`, constructor-seeded `totalSupply()`, mapping-backed `balanceOf(address)`, and `SampleEVMWriteCounter.count()` from solc deployed bytecode plus local storage seed. `POST /ide/execute` and local EVM `eth_sendTransaction` now support a generic pinned-artifact write-call subset for supported nonpayable/payable calldata that fits the bounded local state-transition interpreter; proof covers ERC20 `transfer(address,uint256)` and `SampleEVMWriteCounter.increment(uint256)`, records `storageWrites` and `executionLogs`, updates local runtime storage, decodes bounded ABI output, creates `contract_call` transactions, and materializes supported generic logs into local receipts/filterable `eth_getLogs`. It is still not a full EVM state-transition engine and does not support arbitrary opcode coverage, complex dynamic storage layouts, remote deployment, or remote public proof.
- EVM/IDE WIP is safely closed for this priority window: there are no further bounded opcode, Counter sample, Hardhat artifact, or IDE execution expansions in this update; existing local validation remains the acceptance boundary until the higher-priority Chain Law / Appeal / Transparency public proof gap is closed.
- Request Validity Standard now classifies scoped review, insufficient evidence, out-of-scope asset-boundary requests, overbroad tracking, illegal/abusive requests, governance review, user notice, and rejected states through named rule IDs exposed by `GET /governance/request-validity-rules`.
- Trust labels now include label ID, address, type, severity, risk weight, confidence, source, evidence hash, update time, expiry, review and appeal metadata, dispute status, legal status, rejected-request reference, and an advisory-only asset effect that rejects freeze/seize/confiscation behavior.
- Trust evidence packets now include reviewer-facing `riskSummary` with effective advisory risk weight, active/expired/low-confidence label counts, non-conclusive label IDs, active evidence hashes, appeal path, reviewer notes, and advisory-only asset effect. Expired labels and labels below 5000 confidence bps are excluded from active risk scoring.
- Appeal / Transparency APIs now persist Trust appeals, expose appeal lookup with transparency entry IDs, support manual governance rejection records, and expose transparency report counts and entries. Appeal intake now rejects orphaned appeals unless they reference an existing governance request or existing Trust label, preventing unlinked dispute records from entering transparency reports.
- `TestGovernanceRequestAndAppealAPIFlow` now exercises the HTTP handlers for `POST /governance/requests`, `GET /governance/requests/:id`, `POST /governance/requests/:id/review`, `POST /governance/requests/:id/reject`, `GET /governance/transparency`, `POST /trust/appeals`, and `GET /trust/appeals/:id` with rule IDs, evidence checks, native YNXT protection, asset-boundary rejection, overbroad rejection, manual rejection, appeal intake, and transparency entries.
- Appeals can now be resolved with reviewer, decision, updated status, resolution reason, transparency entries, and corrective labels for false-positive removal/reduction.
- Anti-Unreasonable Tracking now has a dedicated persistent tracking review API with purpose logging, evidence checks, minimum necessary data, sensitive/institutional review, confidence, expiry, appeal path, and transparency entries.
- Native YNXT no-hidden-freeze verification now has a dedicated local check target that rejects native YNXT freeze/direct-transfer/seize/confiscate/blacklist governance requests, rejects Trust labels that try to encode native-freeze asset effects, and statically scans runtime Go code for hidden native YNXT freeze hook shapes. This is local verification only, not remote public proof.
- Resource Market has an independent service (`ynx-resourced`) with authoritative rollback mode and process-local signer BFT mode. It requires client authentication; enforces a 1 MiB body bound, 2 MiB response bound, per API-key/IP rate limits, request IDs, health/build identity, metrics, and body-hash-only fail-closed JSONL audit. Canonical pricing policy, quote, delegation, rental settlement, income, analytics, signed BFT actions, idempotency, and supply conservation are locally verified; the complete BFT path also passed temporary four-validator candidate proof and rollback as recorded above. Docker, systemd, monitoring, backup/rollback, dedicated port `6432`, ingress, release manifest, deploy dry-run, post-restart checks, readiness packages, and public-proof requirements are wired. The public service remains on authoritative mode; no persistent or public BFT Resource deployment is claimed.
- `docs/acceptance/FEATURE_COMPLETION_TRACKER.md` now tracks all required modules with strict local/remote/proof status.

Incomplete modules or requirements:

- New remote `ynx_6423-1` public testnet is not proven live.
- Four-node remote validator set is proven in temporary candidate operation but is not persistently or publicly live.
- Validator peer-readiness, expected/observed peer discovery, persisted source/target peer sync evidence, automatic peer-sync polling, release/build identity, release manifest checksum binding, and public-proof release manifest evidence gating are implemented and locally verified, but no remote validator has provided public peer-readiness, peer-discovery, peer-sync, or expected release identity/manifest proof.
- Public endpoints are not proven to serve the new network.
- Faucet, Indexer, Explorer, AI, Pay, Trust/Chain Law, governance, appeal, transparency, Resource, and EVM receipt/log paths have temporary remote BFT candidate proof; IDE remains incomplete, and persistent public-service convergence is not complete.
- Appeal resolution / false-positive correction is implemented locally and now writes rich corrective Trust label metadata, but is not remotely deployed or publicly proven.
- Anti-unreasonable tracking policy is implemented locally but not remotely deployed or publicly proven.
- EVM/IDE bounded execution is implemented only as a local bounded subset; arbitrary EVM opcode coverage, complex dynamic storage layouts, arbitrary contract execution, remote verifier deployment, and public explorer/verifier proof are not complete.
- A real ignored `.env.deploy` is present locally and passes validation, but it is secret-bearing, not Git evidence, and does not prove current-HEAD full public deployment or public cutover readiness.

Remote deployment state:

- The host-key mismatch and approval bullets below describe historical 2026-07-10 full-deploy evidence. They are superseded for scoped candidate operations by repeated successful strict-SSH candidate deploy gates and remote candidate deploy/verify/rollback runs on 2026-07-12. This does not make the full public deploy gate pass: current-HEAD public release/manifest/service proof and an approved BFT cutover are still missing.

- `make host-key-audit` evidence refreshed on 2026-07-10 still fails safely: primary and Seoul strict SSH accepted current host keys; Singapore and Silicon Valley are host-key mismatches with current scanned ED25519/ECDSA/RSA fingerprints. No node may be mutated and no known_hosts repair may be applied until those presented fingerprints are independently verified through a trusted external source.
- `make host-key-repair-plan` now generates `tmp/host-key-audit/HOST_KEY_REPAIR_PLAN.md` with current known_hosts entries, presented fingerprints, and strict SSH output. It emits known_hosts repair commands only for confirmed host-key mismatch rows; SSH connection/auth/timeout/keyscan blockers are explicitly marked as not known_hosts repair rows. It points operators to `make host-key-approval-template`, `make host-key-approval-request`, `make host-key-approval-check`, `make host-key-approved-repair-dry-run`, and `make host-key-approved-repair`. It does not modify known_hosts and is not approval by itself.
- `make host-key-approval-template` / `make host-key-approval-request` now produce approve-ready rows for both Singapore and Silicon Valley because both mismatch hosts have valid scanned fingerprints. The request JSON currently has six untrusted rows: ECDSA, ED25519, and RSA for each mismatch host. The template/request are not trusted approval and do not write `.host-key-approvals.json`.
- `.host-key-approvals.json` is intentionally git-ignored. `make host-key-approval-check` will remain blocked until that local file contains the current `hostKeyAuditSha256`, exact fingerprints verified from a trusted external source, `approvedBy`, `verificationChannel`, top-level `evidence`, and per-node `evidence`.
- `make host-key-approval-check` remains expected to fail closed because ignored `.host-key-approvals.json` is absent; this is expected until fingerprints are confirmed through a trusted provider/cloud-console channel.
- `make host-key-approval-request`, `make host-key-approval-packet`, and `make host-key-approval-status` on 2026-07-10 can refresh `tmp/host-key-audit/HOST_KEY_APPROVAL_REQUEST.md`, `host-key-approval-request.json`, `HOST_KEY_EXTERNAL_APPROVAL_PACKET.md`, `host-key-external-approval-packet.json`, `HOST_KEY_APPROVAL_STATUS.md`, and `host-key-approval-status.json`. Status remains `awaiting-trusted-approval`: ignored approval file absent, approval request JSON readable, six approval rows for Singapore and Silicon Valley, current audit SHA-256 recorded, and no skipped mismatch nodes.
- `make verify-testnet` on 2026-07-09 at `tmp/verify-testnet/ssh-services.txt` mtime `2026-07-09T19:26:58.113Z` is hardened to require strict known_hosts checking plus per-node proof that `/etc/ynx/ynx-chaind.env` safely reports the expected `YNX_LOCAL_VALIDATOR_ADDRESS`, configured `YNX_PEER_RPC_URLS`, expected validator count `4`, active node services, local `/status` chain ID `6423`, `/status` peer readiness/discovery/sync summaries, `/status.build` expected release commit/name, `/node/identity.build` expected release commit/name, local `/validators` membership for that node, local `/validators/peers`, and local `/validators/peer-sync`. It failed honestly: primary and Seoul allowed strict SSH but had missing validator env identity, missing peer RPC config, missing validator count, inactive services, and unreachable local `127.0.0.1:6420` validator APIs; Singapore and Silicon Valley failed strict host-key verification; remote public smoke also failed.
- `make remote-smoke-test` on 2026-07-10 writes fresh deploy-gate evidence at `tmp/verify-testnet/remote-evidence.json` and fails correctly while carrying current local commit/release expectations at evidence time: release manifest evidence is missing at `tmp/verify-testnet/release-manifest-evidence.json`, public RPC still reports legacy `ynx_9102-1`, `/node/identity`, `/validators/peers`, and `/validators/peer-sync` are HTTP 404, EVM `eth_chainId` times out or returns the old `0x238e` instead of `0x1917`, REST/governance endpoints return HTTP 501, faucet/indexer/AI/Web4 still show legacy `ynx_9102-1`, faucet native symbol is `anyxt`, Explorer health/summary return HTTP 404, validator peer-readiness, peer-discovery, and peer-sync evidence are not remotely proven, and mutable remote actions were skipped.
- `node --check scripts/verify/public-proof-package-check.mjs`, `make public-proof-package-check`, `node --check scripts/verify/public-proof-evidence-check.mjs`, and `make public-proof-evidence-check` pass locally after public-proof current-HEAD and package-manifest binding were added. Earlier local gates after deploy-gate commit-binding hardening included `make deploy-readiness-gate-check`, `make test`, `make preflight`, `make objective-state-check`, `go test ./...`, `make anti-illegal-request-check`, `make request-validity-check`, `make transparency-report-check`, `make trust-appeal-check`, `make anti-unreasonable-tracking-check`, `make native-ynxt-no-hidden-freeze-check`, `make no-placeholder-check`, `make secret-scan`, and `make env-check`. The preflight dry-run bundle proves the local deployment command path for the checked-out commit at runtime and now includes local validator peer readiness/sync verification, local Chain Law smoke gates, native YNXT no-hidden-freeze verification, and Resource Market policy-flow verification; this is local deploy-package proof only, not remote public proof.
- `remote-blocker-report` generated fresh `tmp/verify-testnet/REMOTE_BLOCKERS.md` and `tmp/verify-testnet/remote-blockers.json` on 2026-07-10 with deploy gate status `blocked`. The JSON records fresh and current-release-bound `remoteEvidence`, fresh `hostKeyAudit`, `hostKeyApprovalRequest`, `hostKeyApprovalRequestJson`, `hostKeyApprovalPacket`, and `hostKeyApprovalPacketJson`; `hostKeyApprovalStatusJson` remains deploy-blocking as `approval-awaiting-trusted-confirmation`.
- `make deploy-readiness-gate` currently fails, as intended, after confirming required source evidence is fresh and remote-smoke evidence is bound to the local commit/release expectation at evidence time; it now explicitly rejects remote-smoke evidence whose `status` is not `passed`. Blockers remain host-key approval awaiting trusted confirmation, Singapore and Silicon Valley host-key mismatches awaiting trusted approval, and public ingress blockers including legacy-chain/wrong-chain-id, validator-set-empty, validator-metadata-missing, missing release identity for the expected current release, missing release manifest evidence, missing validator node identity/peer-readiness/discovery/sync proof, REST/governance HTTP 501, faucet native-symbol mismatch, Explorer HTTP 404, and skipped mutable proof actions.
- This is not public proof.

Current blockers:

- One public-cutover capability remains missing: deterministic IDE contract state transitions.
- Public services still run the authoritative rollback-compatible model; temporary BFT candidate proof is not persistent or public BFT deployment.
- Dedicated production signer custody, recovery/rotation, current-HEAD release-manifest evidence, public ingress transition, automated failure thresholds, and full public proof remain incomplete.
- Secret-bearing deploy values exist only in ignored local custody and must never be treated as committed evidence or printed into logs/docs.

Largest real gap that can still be advanced next:

- Implement signed deterministic BFT contract deploy/call state transitions for only the existing bounded execution subset, including artifact/state/hash/log/receipt persistence and Gateway evidence checks. Do not expand opcode coverage or public routing.
