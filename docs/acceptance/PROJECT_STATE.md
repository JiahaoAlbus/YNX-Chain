# Project State

## 2026-07-16 ecosystem candidate intake batch

- Total-control intake reviewed exact remote tips for Wallet Auth/Wallet, Social, Pay, Exchange, Trust/Resource, Explorer/Monitor, Developer, Mail/Calendar, Finance, and Video. Every branch stayed within its declared product paths plus one handoff and passed an isolated repository secret scan. These are queued candidates only; none was merged, deployed, or promoted to public proof.
- Focused independent reruns passed: Wallet protocol 13/13 and App 13/13 plus type/product checks; Social Go race plus 8/8 App tests and typecheck; Pay Go race, merchant test/build, consumer typecheck and 2/2 tests; Exchange Go race, 5/5 UI and smoke; Trust/Resource four race suites; Explorer 5/5 and Monitor 6/6 plus both builds; Developer 13/13 client, 5/5 Web and 2/2 sandbox; Mail/Calendar race, 3/3 UI and smoke for each; Finance race, 3/3 UI and smoke; Video race, viewer/studio checks and smoke.
- Integration conflicts remain explicit. Social uses `com.ynx.social` and a legacy query-field Wallet request while the reviewed Wallet Auth candidate requires exact registry binding and a canonical base64url request envelope. Pay, Exchange and other older candidates also retain temporary product-local Wallet adapters. No dependent product may merge until one central Wallet/Gateway verifier is implemented and exact cross-product vectors pass.
- Trust/Resource still sends AI prompts in a GET query and must adopt reviewed POST-body Gateway transport. Explorer/Monitor installs Vite 6.4.1, for which the current audit reports a high-severity path traversal/file-read advisory; it must be upgraded and rebuilt. Wallet, Pay and Social Expo dependency trees currently report ten moderate advisories. These dependency findings are integration-release blockers, not evidence that the implemented local workflows are fake.
- All product branches except Wallet/Social start from older baseline `51bed843c5aa8dc53b2dc32b29cb8ca349ff0e95`; Wallet/Social start from `b281376eac6fe3cf1ffa8c4b5a44e3546302791f`. Integration requires current-main replay/cherry-pick review and full central gates. Local screenshots, Hermes bundles, emulator runs, ad-hoc desktop archives and handoff-reported Playwright runs remain product evidence only.

## 2026-07-16 Seoul follower outage and executable replication alert rules

- A fresh bounded read-only cycle observed successful public RPC, EVM RPC, REST, gRPC, governance, Faucet, Indexer, Explorer, AI, Pay, Trust, Resource, and Web4 reads. A second RPC sample advanced from height `188136` to `188137`; the public chain remains release `0d31850f74b2`, not current source `145230ce73cc`.
- Strict SSH and current host keys passed for primary, Singapore, and Silicon Valley. Seoul's ports `22` and `6420` accepted TCP connections from the workstation, but direct and primary-origin probes received neither an SSH banner nor an HTTP response. Primary peer evidence for Seoul was stale by more than 20 minutes while the other two follower observations remained fresh. This is an unavailable follower/control-plane condition, not a host-key-change claim.
- Exact-release mismatch prevented the remote smoke from entering any Faucet, Pay, Trust, Resource, IDE, or governance mutation. `remote-blocker-report` recorded the Seoul node and peer-freshness failures, and `deploy-readiness-gate` rejected deployment. No backup, install, restart, cloud action, chain mutation, firewall/ingress change, or BFT phase ran.
- `infra/monitoring/replication-alerts.test.yml` now exercises the production Prometheus rules with synthetic follower timelines. Prometheus 2.55.1 `promtool` proves degraded, prolonged catch-up, lag-above-three, and consecutive-failure alerts remain pending for the configured `for` duration, fire, and clear after recovery. This is local rule behavior evidence only; no remote scrape target, Alertmanager notification, controlled remote interruption, or independent proof exists.
- Actual ordinary deployment now fails before the readiness gate or build when Git has tracked modifications or untracked files, preventing dirty source from being labeled as the clean `HEAD` release. Ignored runtime/secret evidence remains allowed, and dry-run remains non-mutating. An isolated check covers clean, ignored-only, tracked-dirty, and untracked repositories.

## 2026-07-16 Authoritative snapshot v2 corruption detection

- Current source upgrades the authoritative `devnet-state.json` from partial Resource Sponsor integrity to a versioned full-snapshot SHA-256 digest covering blocks, transactions, accounts, validators, operational peer records, Pay, Trust, governance, AI, Resource, contracts, and nested records. Startup and replication validate the digest before applying state.
- Persistent writes now fsync the mode-`0600` temporary file, rename it atomically, and fsync the parent directory. A separate `devnet-state.integrity-version` marker allows one valid marker-free v1 migration and rejects later local downgrade to v1. This detects corruption and unkeyed file changes; it is not a keyed signature or independent storage audit.
- Replication validates full integrity and Resource Sponsor integrity before mutation. If persistence fails after an incoming state is applied, memory is restored to the pre-apply snapshot and rollback persistence is attempted; failure evidence is returned rather than exposing a successful half-apply.
- Local tests cover v2 restart, account and block tamper rejection, authenticated replication tamper rejection, v1 migration, post-migration downgrade rejection, mode `0600`, temporary-file cleanup, write failure, rollback after failed persistence, and race safety. No remote state file was changed and no public v2 migration, remote corruption drill, or independent proof exists.
- The safe first rollout order is all three followers before the primary. New followers can consume authenticated v1 during the bounded mixed-version interval and reseal locally as v2; after the primary upgrade they must consume authenticated v2. Deploying the primary before old followers would make those old followers reject the unsupported snapshot version.

## 2026-07-16 Replication fault detection and bounded remote audit

- One fresh read-only remote cycle completed. Strict SSH and current host keys passed on the primary, Singapore, Silicon Valley, and Seoul roles. Public RPC identified chain `6423`, four active validators, four observed peer records, three peer-sync records, and deployed release `0d31850f74b2` at observed height `185750`.
- The bounded public smoke correctly failed against current `main`: release identity remained `0d31850f74b2`, no growth appeared during the one-second observation window, and EVM chain ID, REST, gRPC, Explorer summary, Resource health, and Web4 health intermittently timed out. Other RPC validator, Faucet, Indexer, AI, Pay, Trust, governance rules, and transparency reads succeeded. Mutable proof was skipped.
- `remote-blocker-report` refreshed the evidence and `deploy-readiness-gate` rejected deployment because the remote evidence status is failed. No backup, install, restart, firewall, ingress, chain state, or BFT transaction phase ran. This is a current operator diagnostic, not independent public proof.
- Current source now adds process-local total replication failures and Prometheus metrics for configured state, bounded lifecycle, catching up, freshness, lag, attempts, successes, failures, consecutive failures, and last-success time. Metrics omit source URLs, keys, and error text.
- Prometheus rules alert on non-fresh replication, prolonged catch-up, lag above three blocks, and new/consecutive failures. Grafana adds follower health, lag, and failure panels. Focused race tests, producer/follower metrics, exact local follower convergence, monitoring checks, JSON parsing, and alert YAML parsing pass locally.
- This monitoring source is not remotely deployed. No protected per-follower Prometheus scrape targets, fired-alert interruption drill, remote dashboard evidence, or independent monitoring proof exists yet.

## 2026-07-16 Authoritative follower runtime evidence

- Current source adds a thread-safe authoritative replication runtime status to `/status` and `/node/identity`: configured source, lifecycle state, `catchingUp`, freshness, exact source/local height and hash, lag, attempts, successes, failures, synchronization timestamps, bounded last error, and stage.
- Followers start and restart as `starting` with `catchingUp=true`; attempts move to `syncing`; authenticated exact snapshot application moves to `synced`; stale or failed polling remains catching up as `stale` or `degraded`; shutdown records `stopped`. Producers report `not_configured` and do not claim follower synchronization.
- The chain snapshot remains atomically persistent. The runtime record intentionally resets after process restart, preventing a pre-restart success from being reused as fresh convergence proof. Local tests verify persistence, revalidation, degraded recovery, exact height/hash equality, cancellation, and race safety.
- `validator-peer-readiness-check` and `verify-testnet` now require `status=synced`, `catchingUp=false`, `fresh=true`, and exact source/local height/hash equality for followers. Focused and full validation pass locally.
- This source is not remotely deployed. One operator diagnostic observed healthy primary Caddy, loopback chain/services, resource capacity, and Caddy configuration while external TLS intermittently stalled and later SSH connections closed. No firewall, server, or public route was changed; no fresh remote follower/restart or independent public proof is claimed.
- The subsequent non-mutating `deploy-readiness-gate` failed closed because `tmp/verify-testnet/remote-blockers.json` was about 678 minutes old, beyond its 120-minute maximum. No deployment ran. Fresh remote evidence remains deferred until SSH/ingress is stable enough for one bounded verification cycle.
- The public topology therefore remains the previously deployed authoritative producer plus three authenticated followers. It remains deterministic authoritative replication, not CometBFT validator voting or Byzantine fault tolerance.

## 2026-07-15 Independent Social and Wallet product boundary

- The previous `com.ynxweb4.mobile` Social/Wallet/Pay/Network binary is now classified as an internal integration and acceptance shell, not a final consumer product. `docs/ecosystem/PRODUCT_ARCHITECTURE.md` defines separate Social, Wallet, Pay, Exchange, Shop, Explorer, Developer, AI, Monitor, Trust, Resource, and Browser product boundaries, benchmark workflows, and honest incomplete status.
- Expo now generates separate `YNX Social` (`com.ynxweb4.social`, `ynxsocial`) and `YNX Wallet` (`com.ynxweb4.wallet`, `ynxwallet`) Android/iOS products. Social renders Feed/Messages/Alerts without Wallet/Pay/Network bottom destinations; Wallet renders its own custody workflow without Social. The integration package remains available only for cross-service engineering acceptance.
- Square profiles now persist a normalized unique Social `@handle`; reserved, malformed, and duplicate claims fail closed. Public `GET /square/handles/{handle}` resolves to the profile/signing account. Startup validates existing claimed handles for syntax and uniqueness while legacy profiles without handles remain readable.
- Chat creation now accepts a username or `ynxsocial://profile/{handle}` QR payload, resolves it through Square, and uses the account only below the UI boundary. Conversation headers and rows display profile name/`@handle`; address-based friend-search labels and `ynx1...` placeholders are removed.
- Gateway has exact product bindings for internal, Social, and Wallet packages. Social is limited to protected Chat/Square routes and cannot reach Pay; Wallet receives no protected product route from this Gateway. Go tests, 53 mobile tests, TypeScript, `make square-api-check`, `make app-gateway-check`, `make app-account-ownership-check`, `go test ./...`, `make mobile-check`, and `make mobile-product-split-check` pass. Both product variants export Android/iOS Hermes bundles.
- This product split and handle release is local only at this checkpoint. Remote Square/Gateway remain exact release `09598d2cbcd7`; no standalone Social/Wallet APK has yet been installed, owner production-signed, submitted to a store, accepted, independently audited, or independently verified.

## 2026-07-15 Persistent Social profiles and private alerts

- Square now persists bounded `ynx1...` profiles and member-scoped notifications in the same atomic mode-`0600` state as posts, comments, reactions, follows, reports, idempotency records, and the hash-chained audit. Existing schema-v1 state files are verified against their original field layout before in-memory normalization; a migration regression test prevents a legitimate pre-profile remote state from being rejected on upgrade.
- Signed profile update and public profile/count read handlers are implemented. Comments, newly active reactions, and newly active follows create recipient notifications atomically with the source mutation. Private notification pagination, unread counts, recipient-only access, exact-idempotent read acknowledgement, restart recovery, cross-account denial, malformed-input rejection, health counters, and Prometheus metrics are tested.
- App Gateway now distinguishes public profile reads from ownership-session-protected profile updates and notification routes. The native client strictly parses and signs profile/notification responses and exact query strings. One Social product now exposes Feed, Messages, and Alerts while the four-item bottom bar remains Social, Wallet, Pay, and Network. Alerts provides locked/private state, unread rows, read acknowledgement, and a native profile editor.
- Exact release `ynx-chain-09598d2cbcd7` is now remotely active for both Square and App Gateway. The installed Square binary SHA-256 is `ee003eeb21a1aecc094b67094be76d2e153218dd2506e1fe60a18d2f0e373719`; the Gateway SHA-256 is `184ae103fdb77489e5f15bebbcbf2ab3efbfe345a9bb1087ed9cf481d9ec243b`. The state-preserving scoped backup is `/var/backups/ynx-chain/social-profiles-alerts/20260715T115441Z-ynx-chain-09598d2cbcd7`; service env/state modes remain `0600`. A fresh read-only public TLS health check reports the exact builds, healthy Chat/Pay/Square upstreams, and `activeSessions: 0`. The public profile route is live and unauthenticated private notifications fail `401`; this is operator-controlled proof, not independent public proof.
- A disposable test-signed Release APK/AAB was built from clean source commit `09598d2cbcd716d03de9336e82bee0d640175c59`, with embedded Hermes `index.android.bundle`. APK SHA-256 is `f1219ea3b40f9f0a4dabc1f1565d36935cf068926cf033a9b0d0e9db4792f08b`; AAB SHA-256 is `7c6c6164886ca757515fbc9c65f858b014be877a1afc1774d26503e77b0163e2`. A clean Android 16 emulator cold-launched it without Metro or fatal/bundle errors and the accessibility-driven installed gate exercised Feed, Messages, Alerts, and Pay. The same APK was then non-destructively installed on a physical Pixel 9 / Android 16; foreground accessibility checks verified Logo/Testnet, Feed/Messages/Alerts, Social/Wallet/Pay/Network, a successful Alerts tap, live public zero-count profile rendering, and the private locked-alert state with no fatal/bundle log. This is real-device read/UI proof only. No installed signed Social mutation, public notification record, iOS proof, production signing, store acceptance, or independent proof is claimed.

## 2026-07-15 Native Social product foundation

- The native information architecture no longer exposes Square and Chat as separate bottom-level service buttons. One `Social` product now contains native `Feed` and `Messages` routes; Wallet, Pay, and Network remain separate complete workflows. This implements the portfolio boundary in `docs/ecosystem/PRODUCT_ARCHITECTURE.md` without adding fake Bank, Shop, Browser, IDE, or other placeholder windows.
- The mobile client now has strict response parsers and device-signed methods for existing persistent Square comments, `like`/`insight`/`support` reactions, follow/unfollow, and reports. Inputs are bounded before signing, account targets normalize to `ynx1...`, report evidence hashes are checked, malformed responses fail closed, authorization failures produce no mutation request, and each action requires the existing strong-biometric local-key boundary.
- A native post-detail route now reads real comments and following state and exposes signed reply, Support, follow/unfollow, and report-review workflows. Reports display the persistent report status and ID returned by Square and preserve the existing `/trust/appeals` route. No synthetic post, comment, reaction, follow, report, or notification is used.
- Strict TypeScript, 51 mobile tests, Android/iOS Hermes exports, and `make mobile-android-native-check` pass. A temporary `.dev` debug package was installed beside the preserved test Release package on Android 16 arm64; the installed first screen rendered the official Logo, Feed/Messages segment, four stable bottom destinations, and the real public empty-feed state without overlap or fatal logs. The temporary package and Metro process were removed after inspection.
- The public feed was empty, so installed post-detail rendering and installed signed mutations were not exercised against a real record. No owner-approved public post or mutable public Social proof was created. Persistent notifications, richer profiles, moments/media, contact discovery, moderation outcomes, Trust appeal completion, Pay/tips, groups, iOS/real devices, production signing, stores, audit, and independent proof remain incomplete; no remote service was redeployed for this client-only slice.

## 2026-07-15 Multi-device Chat and bounded device rotation

- Chat protocol v2 now stores one logical message with one authenticated encrypted envelope for every active device across both direct-conversation members, including sender devices for continuity. Each device envelope binds conversation/message/sender device/recipient account/recipient device/algorithm/ephemeral key/nonce/ciphertext; one message shares one ephemeral key but uses unique per-device nonces. The daemon rejects missing, duplicate, extra, revoked, mixed-key, malformed, changed-replay, incorrect-hash, and incorrect-signature inputs while persisting ciphertext only.
- Delivery/read acknowledgements are now keyed by device for v2. Revoked public keys remain available for historical authentication but are excluded from new envelope sets. Restart validation checks canonical ordering, recipient ownership, unique nonces, ciphertext hashes and bounds, common ephemeral keys, sender signatures, rotation records, and the existing audit/integrity chain.
- `POST /chat/devices/{id}/rotate` and `GET /chat/device-rotations` are implemented through `ynx-chatd` and allowlisted through the ownership-session Gateway. Rotation requires an authenticated active same-account authorizing device signature plus a separate new-device private-key proof, atomically revokes the replaced device, persists hash-only evidence/audit/idempotency, and survives restart. It is bounded active-device recovery, not social recovery, owner custody handover, or recovery when every device is lost.
- The native App fans out to all active self/peer devices, verifies sender signatures before per-device decryption, decrypts outgoing history through its own envelope, and exposes an App-native device manager with system confirmation and strong biometric authorization. A pending rotation record now preserves the new device secret and exact idempotency key in SecureStore before network submission so an uncertain result can be replayed after retry/restart. The exact network-loss replay is unit/service tested but was not fault-injected into the installed App.
- Two exact disposable-test-signed Android Release packages were installed on separate Android 16 arm64 emulators. The original package SHA-256 was `3d927acb5acbc88cbb2dbfe97af4deda1b07ba9508cb6ac018761743704a8012`; the rotation-recovery package SHA-256 was `a4772b3dfd87c4275e27dc449fd4e4be087ce1a3454023aa741994f82a3de333`. Both passed exact source/signature/tamper/false-claim/credential-cleanup release gates and are test-only artifacts, not owner production packages.
- Installed UI proof created retained conversation `conv_549b87d23cd10f00aad03dc9`. Message `message-752f0bf9793765f4b6d38797` was sent from the first App, stored as v2 ciphertext only, independently signature-verified and decrypted by both recipient devices, acknowledged by both, reloaded in the App, and displayed as read. The current-device rotation then produced one active replacement and one revoked old device while preserving the other sender device.
- The first post-rotation reconnect exposed a real server boundary bug: a rotation-created active device could prove its same account and exact signing/encryption keys but ordinary registration returned `device id already exists`. Commit `ea2c76be4860` now admits only an exact active-key match as a replay, persists the new registration idempotency binding, and still rejects revoked or mismatched devices. Exact Chat release `ynx-chain-ea2c76be4860` is remotely active with binary SHA-256 `181454e0998701e800a130532543ca69b9209d3a8855c0024aa85c4e72f7144f`; scoped backup is `/var/backups/ynx-chain/chat-rotation-reconnect/20260715T103618Z-ynx-chain-ea2c76be4860`. Env/state permissions remain `0600` under a mode-`0700` directory.
- After re-unlock, the replacement device retained the conversation and sent `message-e7bc7460debd28e062070d5e`. Both independent recipient devices authenticated/decrypted it, verified sender continuity/signature, and recorded read acknowledgements; the message has four envelopes and the server remains ciphertext-only. Recipient proof devices and their sessions were revoked. Current immutable remote state is eleven historical devices, one retained conversation, two retained messages, and one rotation.
- Rotation does not rewrap historical envelopes: the replacement correctly reports the pre-rotation message as unavailable. All-devices-lost/social recovery, historical-key backup/rewrap, groups, contacts, attachments, Pay/Trust/appeal integration, moderation, iOS/real-device proof, owner production signing, stores, audit, and independent proof remain incomplete.
- Failed Chat registration previously discarded the local Gateway token without server revocation. The client now performs best-effort `/app/session/revoke` while preserving the original error, with a regression test. Three already-lost operator test sessions remained active at the first final observation; a `2026-07-15T11:11:25Z` check reported two, and a later check after their bounded expiry reported `activeSessions: 0`. They expired through the configured 30-minute TTL and were not falsely claimed as explicit revocations. This client fix is locally tested but not rebuilt/installed in the two recorded Release packages.
- Full `GOMAXPROCS=2 make preflight` passed after this slice, including Square's persistent signed posts/comments/reactions/follows/reports, restart/tamper/replay/rate checks, Chat, Gateway, mobile, governance, Trust, Pay, chain, BFT, and static gates. Square's real next gap is native signed-client and Social UI coverage of its existing APIs, not another parallel comment/reaction/follow backend.

## 2026-07-15 App-native encrypted Chat window

- The Chat service now exposes a signed member-scoped `GET /chat/conversations` list and `GET /chat/accounts/{account}/devices`. The device route is denied before a shared conversation exists, returns public keys and active/revoked status only, and preserves revoked public keys for historical-envelope authentication. Conversation ordering, route authentication, directory privacy, revocation history, and a fixed mobile/Go encryption vector are covered by Go tests.
- The mobile client derives a domain-separated X25519 key from the SecureStore device seed, registers it with an Ed25519 proof, signs every Chat request, and performs Go-compatible HKDF-SHA256/XChaCha20-Poly1305 encryption with conversation/message AAD. It lists and creates direct conversations, queries member devices, rejects zero or multiple active recipient devices under the current single-envelope protocol, encrypts before upload, decrypts per message, preserves visible authentication failures, and records read acknowledgements. Square, Pay, and Chat now register only the service each window needs.
- The App now has five native tabs and a dedicated Chat flow rather than a website-style section: conversation list, native detail navigation, create-conversation sheet, message timeline, bubbles, keyboard-aware composer, pull refresh, bounded foreground polling, empty/error/loading states, and foreground lifecycle lock. Opening a conversation hides the global brand bar and bottom tabs so the detail behaves as a mobile message screen.
- `go test ./...`, forty-one mobile tests, strict TypeScript, Android/iOS Hermes exports, `make mobile-check`, disposable test-signed APK/AAB verification, and Android 16 arm64 installed Release navigation/rendering pass. Installed proof rendered the official Logo, five tabs, the locked Chat window and independent Pay window with no fatal log. The exact test-signed APK SHA-256 is `5372ed618c6b16afd9c4e4bcf635adb70c4c64cdff5288524630525812f01e36`; it is disposable test signing, not an owner production artifact.
- Chat release `ynx-chain-f81f3b6cabe2` is now remotely active on loopback with binary SHA-256 `59daa26c8ad2f899d9be8024b31e5b2dd4dc1ee745ced8178521c9d4b0418ba7`. App Gateway release `ynx-chain-376c95793d66` is remotely active with binary SHA-256 `e86157d6fd0143da3ad1615b67613c6371ad21fe079dbe45863c0387bd3a8f73`. Recoverable scoped backups are `/var/backups/ynx-chain/chat-app/20260715T083543Z-ynx-chain-f81f3b6cabe2` and `/var/backups/ynx-chain/chat-app/20260715T084133Z-ynx-chain-376c95793d66-gateway`; existing env, units, state, Square, Pay, and authoritative chain services were preserved.
- Public TLS health reports the exact Gateway and Chat releases. Unauthenticated conversation-list and member-device-directory requests return `401`. An operator-controlled disposable browser-bound ownership session reached `https://api.ynxweb4.com`, registered Square/Chat devices, completed a timestamped device-signed empty conversation-list read, revoked both devices, and revoked the session. A separate exact mobile client completed the native binding, Chat-only registration, empty signed read, device revocation, and session revocation through a strict SSH tunnel to the remote Gateway. Neither proof created a conversation, message, or Square post. Local direct TLS remained unavailable because the workstation DNS/proxy resolved the domain to `198.18.0.48`; these are operator-controlled proofs, not an independent public vantage.
- The installed check did not create an identity or send a message; no public conversation/message, installed end-to-end write, iOS installation, real device, production signing, store acceptance, independent audit, or independent proof is claimed. The current protocol supports one active recipient device, not multi-device fan-out, groups, contacts, attachments, recovery, Pay/Trust integration, moderation, or WeChat equivalence.

## 2026-07-15 Account-bound YNXT Pay and native App window

- The authoritative Pay model now binds a canonical merchant `ynx1...` payout address to intents and invoices and persists settlements. Settlement verifies a real committed native transaction against the session-bound payer, payout recipient, exact amount, fixed `1 YNXT` fee, invoice time/expiry, and global transaction uniqueness before atomically marking the invoice and intent paid. Restart persistence, exact replay, changed-input rejection, pending/mismatched/reused transaction rejection, paid event generation, and audit hashes are covered by focused tests.
- REST and `ynx-payd` expose `POST /pay/invoices/{id}/settle` and `GET /pay/invoices/{id}/settlement`. App Gateway has a third loopback Pay upstream, keeps the merchant key server-side, allows bounded reads, requires the existing account-bound session for settlement, rejects a client-supplied payer, and injects the verified session `ynx1...` account. Candidate BFT Pay settlement is not implemented and fails explicitly.
- `apps/mobile` now has separate Square, Wallet, Pay, and Network tabs. Pay uses invoice/link import, strict YNXT and payout validation, an App-native review sheet, exact fee/total display, ownership session, strong-biometric native transfer authorization, broadcast/finality tracking, unknown/pending recovery, and an audit-bound receipt. It does not reuse the website layout.
- `make pay-api-check` completed a local faucet, native transfer, block commitment, settlement, receipt lookup, event, and audit flow. `make app-gateway-check`, mobile strict types/tests, Android/iOS Hermes bundles, disposable externally signed APK/AAB integrity, and Android 16 arm64 installed Release navigation/Pay rendering pass. The installed APK SHA-256 is `8002651a5e7aebf9718ce0e0a3e5ce35018c8991adbe8f6261d910a792742124`.
- Exact pushed release `ynx-chain-0d31850f74b2` is deployed on all four authoritative roles. All four release manifests have SHA-256 `998d94ca3a5648c3958568899ce14613e664e1a2d5a2ff44631704af04363580`, and all four installed `ynx-chaind` binaries have SHA-256 `468443fa636e05ed473a04129f64b6d0b95c391ffd5403609124d1d13ceba842`. Predeploy backups are about `20 MB` on primary and `9.3 MB` on each follower; no BFT transition phase ran.
- App Gateway was then scoped-updated to the same exact release while preserving the existing loopback Chat/Square credentials and state. Its recoverable backup is `/var/backups/ynx-chain/app-gateway-pay/ynx-chain-0d31850f74b2-20260715T073903Z`; environment/state modes remain `0600`/`0700`. Public `/app/health` reports exact Gateway and Pay build `0d31850f74b2` with healthy Chat, Square, and Pay upstreams. An unknown invoice returned `404`, and unauthenticated settlement returned `401`; Square feed remained available and empty.
- Four-host release/service checks passed. The first fixed-height verifier attempt observed a transient empty Silicon Valley block response; a dedicated read-only retry proved the same primary height/hash on Singapore at attempt 1, Silicon Valley at attempt 37, and Seoul at attempt 1. The full `make verify-testnet` command was intentionally interrupted before its mutable Faucet/Pay/Trust smoke phase, so no public value transfer or complete fresh mutable public proof is claimed.
- The consumer settlement protocol and App ingress are now remotely deployed, but no payout-bound public invoice, owner-approved public transfer, public settlement receipt, installed iOS/real-device payment, production signing, store acceptance, compliance approval, audit, or independent proof is claimed. Direct public TLS and cross-region snapshot timing remain observed availability risks.

## 2026-07-15 Native YNXT Wallet workflow

- The mobile Wallet is no longer an identity-only page. It now uses App-native Assets, Activity, and Account routes with native back navigation, fixed bottom tabs, and Receive/Send bottom sheets. Assets reads the public testnet account balance and nonce, shows recent matching transactions from the latest 100 network transactions, and keeps a failed account query distinct from a verified unfunded account. `ynx1...` remains the default identity; `0x...` appears only as an EVM compatibility detail.
- Receive renders a QR and system Copy/Share actions containing only the public `ynx1...` address, chain ID `6423`, and asset `YNXT`. Send implements recipient normalization, positive whole-safe amount checks, self-transfer rejection, current balance/nonce validation, the exact `1 YNXT` native fee, explicit review, strong-biometric authorization, stale-state recheck, canonical signing, response binding, bounded finality polling, and a visible unknown-result state that warns against duplicate submission.
- The TypeScript signer and Go consensus verifier share `testdata/mobile-native-transfer-vector.json`; they agree on the `YNX_NATIVE_TX_V1` chain-bound payload, low-S secp256k1 DER signature, and deterministic transaction hash. Thirty-three mobile tests and the Go vector pass. Android 16 arm64 installed-app checks rendered Assets, Account, Activity, and Receive without overlap; the real public account query returned an unfunded disposable account and no matching activity.
- No real public transfer was executed because no owner-approved recipient/amount was provided and the disposable account was unfunded. Installed Send review, biometric transfer confirmation, broadcast, and receipt evidence therefore remain incomplete. No chain service or website was redeployed for this client-only slice. Cross-chain is visibly `Not active`: the repository still has no external-chain adapter, mint/burn authority, deployed public Bridge, or real cross-chain transaction. iOS installed proof, Android/iOS real-device proof, owner production signing/recovery/handover, stores, audit, and independent proof remain absent.
- Exact pushed commit `f570a0a` passes the disposable externally signed Release APK/AAB integrity gate, including shared signer fingerprint, exact source provenance, tamper rejection, false-claim rejection, and credential cleanup. A subsequent installed Release cold launch stayed active and emitted no fatal Android log, but the required live Square text did not load because both the host and emulator experienced an `api.ynxweb4.com:443` TLS handshake timeout. The App displayed its visible retry state. This is recorded as an incomplete live-data installed check, not a successful online proof.
- The earlier Pay audit gap is closed in code and the protocol/App ingress are remotely deployed. Public payment execution and receipt proof remain open.

## 2026-07-15 Android installed-app, release, biometric, and official-brand slice

- `apps/mobile` is now a real Expo SDK 57 / React Native 0.86 native-control project for iOS and Android, not a WebView. Its first screen is the usable Square feed with live `api.ynxweb4.com` data; Wallet provides YNX-native create/import, one-time recovery-key acknowledgement, `ynx1...` default identity, secondary EVM compatibility identity, and platform SecureStore custody; Network reads live Gateway status.
- The mobile signer matches the existing Go/browser `ynx1...`, compressed secp256k1 low-S ownership, Ed25519 device, Square registration, and Square request vectors. The mobile session client supports ownership challenge/verify, device registration, signed post publication by explicit user action, signed device revocation, and session revocation without sending account/device secrets.
- App Gateway adds a separate exact `ynx-mobile-v1` request selector and binds signed challenges/sessions to `ynx-mobile://com.ynxweb4.mobile`. Browser sessions still require an exact configured HTTPS Origin, mixed browser/native bindings fail closed, and the native selector is not accepted as authentication without both account and device proofs.
- Lifecycle hardening commit `1437771` immediately locks local sessions on background/inactive AppState, Square unmount, or identity change and attempts bounded remote session revocation. It also clears expired and `401/403` sessions, closes and clears compose state, rejects corrupted SecureStore records, and exposes a confirmed local cleanup path before recovery-key import.
- Thirteen mobile tests, strict TypeScript, prohibited-storage scanning, timeout abort, iOS Hermes export, Android Hermes export, `go test ./...`, and full preflight pass. Expo Doctor passes 20/20. Owner source `/Users/huangjiahao/Downloads/ynx logo 扣过.png` is now the canonical official Logo; commit `c01874a` preserves its logo pixels, trims transparent whitespace, and applies it to mobile launcher/splash/favicon surfaces and Explorer.
- Scoped Gateway release `ynx-chain-ae3e0457c082` is now remotely active after an exact-SHA upload, old binary/env/unit/state backup at `/var/backups/ynx-chain/app-gateway/ynx-chain-ae3e0457c082-20260714T182951Z`, config check, bounded restart, and health polling. Public health reports `nativeBoundary=ynx-mobile-v1`, healthy Chat/Square, and zero active sessions. Disposable operator-controlled native-protocol account `ynx1x5yl8yfkg7tf2d5epe0nz26krqvzqlffan26wz` completed ownership, registration, signed device revocation, and session revocation without posting; the zero-post feed SHA-256 stayed `01a3b8bdd0e07df72859f79dbb56815b04e7e41ca3eea9f7823340910480ac34`, and the authoritative chain continued from height `141802` to `141834` on unchanged release `fb6f1726719b`.
- The Android SDK/Emulator toolchain now builds and installs package `com.ynxweb4.mobile` on Android 16 arm64 emulator `YNX_API_36`. Installed-app proof observed a disposable SecureStore identity, public Gateway sessions `0 -> 1`, Home/background lock and remote revocation `1 -> 0`, the explicit foreground-lock message, and safe identity removal; no post was made and the feed stayed empty. An immediate recovery capture initially exposed a newly generated disposable key before the React effect applied; generation now awaits native capture protection before producing the secret. A 50 ms capture and a settled capture were both black, and the Android windows reported `SECURE`. The rebuilt debug APK SHA-256 is `c889263e0026c233ca443ef0fed913f5df77384b0210d622b5f5c2046ac5428d`, and `make mobile-android-native-check` reproduces the native build/package checks.
- The identity/session lifecycle remains Android emulator/debug-APK proof; the separate Release-integrity check below does not add a real Android device, owner production-signed AAB, Google Play, iOS simulator/device, IPA, biometric unlock, owner handover, audit, or independent proof. The 64-hex recovery key is a bounded first implementation, not a mnemonic, social recovery, hardware wallet, or completed custody product. Expo's transitive build chain currently has 10 moderate npm advisories and zero high/critical advisories; the suggested incompatible Expo 46 downgrade was not applied.
- Android Release no longer inherits Expo's generated debug signing. A config plugin admits only complete external JKS/PKCS12 credentials; the package command requires repository-external mode-`0600` files, exact alias/type validation, explicit owner approval and a clean worktree for owner mode. The canonical manifest binds commit, tracked mobile source digest, package/version, official Logo, APK/AAB hashes and one signer certificate while keeping real-device/store/iOS status false.
- `make mobile-android-release-check` built and verified a disposable-test-signed Release APK and AAB, rejected modified artifacts and a false production claim, and deleted the disposable credentials/package. The generated Release APK was then installed on `YNX_API_36`; cold launch kept `com.ynxweb4.mobile/.MainActivity` resumed, embedded Hermes ran, the official Logo and live empty feed rendered, and no fatal Android log appeared. APK SHA-256 for that local disposable run was `9728fa0a1c09741a64a1520f6ce466fc18dac18494e107b791ca9a2d94f35340`; it is not a stable or production artifact.
- `expo-local-authentication` 57.0.0 is pinned and autolinked. One strong-biometric, no-device-fallback authorizer now sits inside ownership proof, signed post, and explicit signed device-revocation methods, while recovery-key create/reveal/import and both identity-removal paths call the same boundary. Unavailable hardware, no enrollment, cancellation, failure, and lockout are distinct fail-closed states; biometric results and secrets are not logged or sent to a server.
- Twenty-two mobile tests, strict TypeScript, dual Hermes export, `make mobile-check`, and native debug compilation pass. Android 16 arm64 installed-app evidence first rejected recovery-key generation with no enrolled fingerprint, then used real system PIN/Pixel Imprint enrollment. The reproducible emulator-only target proves native cancellation, unregistered-fingerprint rejection, registered-fingerprint success, `SECURE` recovery UI, no fatal log, and test-data cleanup. APK SHA-256 was `5c429f2d3a6eb17641342d71ec71ec2128080099c79c35253af2ef0f76d43f24`.
- This earlier identity/safety slice is superseded by the bounded native YNXT Wallet workflow above. It is still not Binance-class or a complete payment/custody product: contacts, token portfolios, merchant Pay, owner recovery/handover, and live Bridge remain absent.

## 2026-07-14 First-party application boundary

- `ynx-app-gatewayd` now runs a remotely active ownership-bound session release while preserving sessionless public Square reads. `POST /app/session/challenges` binds a five-minute canonical sign document to chain `6423`, a claimed `ynx1...` account, Ed25519 device key, exact HTTPS origin, random nonce, and expiry. Verification requires the matching compressed secp256k1 key and canonical low-S DER account signature plus the bound Ed25519 device signature.
- Successful verification returns a 30-minute exact-origin/account/device session. Only its SHA-256 token hash is persisted in bounded atomic mode-`0600` state under a mode-`0700` directory. Replay, expiry, wrong account/key/device/origin, high-S signatures, changed registration identity, and revoked sessions fail closed. Protected Chat/Square routes still require the upstream timestamped device signature, and service keys remain server-only.
- Focused race/unit/HTTP checks, real Chat/Square daemon smoke, restart persistence, tamper rejection, token non-disclosure, file modes, exact CORS/routes, release env/systemd/backup wiring, complete deployment dry-run, and full `make preflight` pass for exact commit `132b711450f6`.
- Exact App Gateway release `ynx-chain-132b711450f6` is remotely installed after scoped backup. The exact binary SHA-256 is `6bf47edae649431fc7f82d957ffe2f89fb9c5cef869f95662d5cd3ed1b16dd95`; the service runs on loopback behind Caddy `/app/*`, and its mode-`0600` state remains under a mode-`0700` `ynx:ynx` directory. A failed pre-install permission probe triggered rollback to `428068b` without touching chain, Chat, or Square; a corrected stop/install/start deployment then succeeded.
- Operator-controlled source-bound TLS smoke through `https://api.ynxweb4.com` returned exact Gateway release `132b711450f6`, completed disposable `ynx1...` ownership proof, Ed25519 device binding, Square and Chat registration, both signed device revocations, and session revocation. It created no Square post. State restart digest remained identical, all four services stayed active, and chain height advanced from `128717` to `128759`. This is real remote protected-route proof, not independent public-vantage proof.
- Website commit `9967633` is pushed and production deployment `dpl_3BBSTkc6Q5eb6nLaacfnMLmo9oxG` is `Ready` at `www.ynxweb4.com`. `/square` now combines live persisted reads with local create/import, encrypted backup, lock/unlock, account ownership verification, device registration, signed post composition, signed device revocation, local-vault cleanup, strict CSP, and an installable PWA shell that never caches APIs or cross-origin Gateway requests. Desktop and 390px checks found no horizontal overflow. A production browser disposable account completed the signed session flow, enabled composition, created no post, revoked the device, returned active sessions to zero, and left the public feed empty.
- Private package `sdk/browser` commit `5bab0e0` implements the YNX-native signing boundary without changing the dependency-free read SDK. It derives shared `ynx1...` vectors, emits canonical low-S secp256k1 DER ownership proof, binds Ed25519 device and Square signatures, encrypts account/device secrets with PBKDF2-SHA256 plus AES-256-GCM, rejects wrong-password/tampered vaults, keeps session tokens private to the client, and performs registration, signed Square actions, device revocation, and session revocation. Go verifies the JavaScript-generated vectors; website provenance pins the exact chain commit and file SHA-256 values. Production QA found and fixed the browser `fetch` binding bug before acceptance. It remains unaudited and is not a complete recovery/custody product.
- IDE, Pay, Bank, Shop, and other ecosystem products must be tracked separately as real workflows; existing APIs or readiness packages do not by themselves constitute finished applications.

- Last implementation and deployed Social source commit before this evidence closeout is `09598d2cbcd716d03de9336e82bee0d640175c59` (`feat: add persistent Social profiles and alerts`). Last pushed commit used by all four authoritative chain roles remains `0d31850f74b239872ea417d064a0af903fe4598e`; release `ynx-chain-0d31850f74b2` remains active on those roles. Scoped service releases now use Chat `ynx-chain-ea2c76be4860`, App Gateway `ynx-chain-09598d2cbcd7`, Square `ynx-chain-09598d2cbcd7`, and Pay `ynx-chain-0d31850f74b2`; these service updates do not imply a new chain release, public Social mutation, or public settlement.
- State snapshot baseline commit: `09598d2cbcd716d03de9336e82bee0d640175c59` (`feat: add persistent Social profiles and alerts`). This identifies the implementation and deployed artifact baseline; the evidence-only documentation closeout may have a later Git commit.
- Chain repo state: `/Users/huangjiahao/Desktop/YNX Chain`, branch `main`, remote `https://github.com/JiahaoAlbus/YNX-Chain.git`; the native-first Explorer and public Exchange candidate status are pushed and ordinarily deployed.
- Website repo state: `/Users/huangjiahao/Desktop/YNX-Chain-website`, branch `main`, remote `https://github.com/JiahaoAlbus/YNX-Chain-website.git`, pushed production commit `6ab009c`.
- Vercel project `ynx-web4-website-new` (`prj_tPB0KDTFohQ9FXZAzq25mYFWkbNa`) is now Git-connected to `JiahaoAlbus/YNX-Chain-website`, production branch `main`; the previous `ynx-web4-website-new` repository is no longer the automatic source.
- Vercel production deployment for commit `6ab009c` was observed `Ready` and aliased to `www.ynxweb4.com`; the production official Logo SHA-256 is `df071f540f21d54e92286fd709df5293187c269058850820adb11e7c5087c12d`. Apps/Square/Docs routes, strict CSP/PWA assets, three read-only App API functions, and the direct account-bound Square browser client remain present.

Remote deployment state:

- Ordinary authoritative chain release `0d31850f74b2` is live on the primary, Singapore, Silicon Valley, and Seoul roles. All roles matched release-manifest SHA-256 `998d94ca3a5648c3958568899ce14613e664e1a2d5a2ff44631704af04363580` and `ynx-chaind` SHA-256 `468443fa636e05ed473a04129f64b6d0b95c391ffd5403609124d1d13ceba842`; a bounded retry proved fixed-height convergence after transient Silicon Valley snapshot delay.
- Scoped predeploy backups were created for release `0d31850f74b2`: about `20 MB` on primary and `9.3 MB` on each follower. No BFT freeze, pause, final snapshot, signer installation, candidate start, or ingress cutover phase ran.
- Active topology remains one authoritative producer plus three authenticated followers. It is not public CometBFT validator voting or Byzantine fault tolerance.
- `www.ynxweb4.com` now serves the intended full-stack website from Vercel. `ynxweb4.com` redirects to `www`.
- The production homepage uses a zero-image CSS/DOM chain scene with draggable depth layers, live block height, four role labels, block propagation, EVM/RPC/Index/Trust/State layers, responsive mobile behavior, and real public API data.
- Production website now makes `ynx1...` the first/default identity, confines `0x...` to the EVM compatibility label, and exposes the verified Exchange integration candidate with an explicit no-listing boundary.
- App Gateway and Square are scoped-updated to exact release `ynx-chain-09598d2cbcd7` with preserved server-only service credentials and healthy Chat/Square/Pay loopback upstreams. Public route evidence proves profile reads reach the upgraded Square service and private notifications remain ownership-session protected; existing Pay and Chat boundaries remain healthy. No installed Social mutation, public payment, production package, store acceptance, or independent proof is inferred.
- Explorer is now part of exact ordinary release `ynx-chain-0d31850f74b2`; public health reports that build and the native-first real-data UI remains active. The earlier scoped Explorer backup at `/var/backups/ynx-chain/explorer/ynx-chain-c01874aef1a6-20260715T042216Z` and the current full-release predeploy backup remain available. The public `/assets/ynx-logo.png` still uses the canonical Logo.

Public proof state:

- Production browser proof observed chain ID `6423`, native `YNXT`, EVM `0x1917`, four validator-role records around height `114449`, and exact website title `YNX Chain | Full-Stack Web4 L1 Ecosystem`.
- Desktop and 390px mobile checks verified no horizontal overflow, no hero `<img>`, interactive layer expansion, mobile menu behavior, live height growth, current-state/mainnet boundary language, and direct route rendering.
- Public TLS connection attempts were intermittently slow or timed out during repeated verification; retries succeeded for production routes. This is an observed availability risk, not a claim of continuous outage or guaranteed availability.
- Existing Singapore-routed chain smoke remains operator-controlled evidence. Independent public-vantage proof, successful provider-backed AI generation, and public BFT proof remain absent.
- Post-deploy Singapore-routed smoke verified exact release identity, chain growth, EVM `0x1917`, REST/gRPC, Faucet-to-Explorer indexing, Pay, AI action governance, Trust/Chain Law requests, appeal/correction, transparency growth `180` to `192`, Resource flows, and IDE compile. Provider-backed AI SSE alone returned YNX HTTP `502`; it remains an external provider/quota blocker.
- Public address proof used the testnet account `0x7e5f...5bdf` and alias `ynx10e...hn80`: RPC alias and hex queries returned the same canonical account, Explorer returned both address formats, and Explorer search normalized the alias to the canonical address. The route was operator-controlled Singapore SSH with original TLS hostnames, not independent third-party proof.
- Production website proof converted `0x7e5f...5bdf` to `ynx10e...hn80`, rendered `YNX native (default)` first and `EVM compatibility / MetaMask` second, exposed Exchange candidate/no-listing language, and passed desktop plus 390px mobile width checks (`scrollWidth=390`).
- Direct public verification intermittently hit SSL/faucet connection timeouts; the controlled Singapore route succeeded. Public ingress stability remains a real operational risk.
- Exact public Exchange proof on release `c4df905ebc54` verified signed deposit `0x62bf...9336`, withdrawal `0x5469...a5ac`, exact replay, two confirmations, balances/nonces, historical blocks, receipts/logs, and dual addresses. The unchanged Exchange runtime is in `fb6f1726719b`; a later direct `exchange-live-check` timed out at Explorer health, so independent proof remains false.
- Exact-release `verify-testnet` for `fb6f1726719b` passed all host release/checksum and fixed-height convergence checks. Direct public smoke then passed release identity, block growth, EVM/REST/gRPC, governance, Pay and Trust flows, but had intermittent Faucet/AI/Resource failures. Singapore-routed read-only proof confirmed exact release and core services but observed temporarily stale peer polling immediately after restart. These failures are recorded, not hidden.
- Current release `0d31850f74b2` has exact four-host manifest/binary evidence, chain growth, and bounded public App Pay route evidence. A fresh full mutable public proof was not run because it would create Faucet and protocol writes without separate approval; this deployment is not public settlement proof.

Completed modules:

- Chain runtime, authoritative replication, RPC/EVM RPC, Faucet, Indexer, Explorer, monitoring, deployment, backup, rollback, AI action governance, Pay, Trust/Chain Law, Anti-Illegal Request, Request Validity, native YNXT protection, Appeal/Correction, Transparency, Resource Market, bounded IDE, and JS/Python SDK slices have real code and local tests. The authoritative public release provides operator-controlled public proof for the documented deployed subset.
- Website repository selection, Vercel Git binding, production deployment, real API aggregation, responsive CSS chain interaction, truthful disclosure routes, SPA deep links, native-first account converter, and Exchange candidate boundary are implemented and production verified.
- Native-first account addressing is implemented and remotely deployed: first-party website and Explorer surfaces default to checksummed Bech32 `ynx1...`; the equivalent lowercase `0x...` is secondary and explicitly EVM-compatible. Go/JS/Python shared vectors, signed-transfer canonicalization, REST boundary normalization, account-key output, Explorer alias search/detail, dedicated checks, and operator-controlled public proof pass. Standard EVM JSON-RPC and MetaMask remain on `0x...`.
- The native mobile slice is implemented and Android-emulator verified: React Native iOS/Android UI, SecureStore-backed account/device keys, create/import and backup acknowledgement, native-first identity, live Square/Network reads, separate native Gateway binding, ownership/device proofs, signed compose, explicit revoke, fail-closed locking, auth/expiry invalidation, corruption recovery, 13 focused tests, both Hermes exports, an installed debug APK, and an installed disposable-test-signed Release APK pass. The Release pipeline produces verified APK/AAB artifacts without debug signing, but it is not owner production-signed, iOS-installed, real-device verified, or store distributed.
- Bridge coordinator code is implemented and locally verified at `44870de94b1b`: persistent source-event/idempotency state, bounded route/finality/amount policy, Ed25519 relayer threshold, local-only finalization, audit integrity, authenticated handlers, health/metrics, restart checks, mutation freeze, and deployment-package wiring pass. The real deploy gate is false and external submission is disabled.
- Stablecoin Issuer Control code is implemented and locally verified at `23a5702ec979`: standalone authenticated daemon, mode-`0600` atomic persistence, issuer/asset approval and revocation, governance/evidence references, legal-review status, supply ceilings and reservations, exact-idempotent non-executing mint/burn intents, native/protocol asset rejection, audit/transparency, truthful health/metrics, mutation freeze, and release/systemd/backup/health-check wiring pass. The real deploy gate is false and no issuer support or token execution is claimed.
- Sponsored resource pools are implemented in the authoritative runtime at `6571cf34afac` and promoted into deterministic CometBFT/AppHash state at `563fe1f36918`: canonical pool create/fund/policy/status and sponsorship actions, direct owner/beneficiary secp256k1 signatures, shared account nonces, public/allowlisted policies, scope/type/per-action/cumulative/expiry bounds, owner resource reservations, deterministic pool selection, atomic resource-only consumption, exact replay snapshots, action-reference uniqueness, pause/resume/revoke with unused-resource release, hash-chained audit, BFT queries, unchanged client-signed Resource Gateway relay, committed transaction mapping, and Explorer source rendering. Four independent applications produced identical state/AppHash and the restart, tamper, accounting, proposal-ordering, BFT Gateway, Resource Gateway signer-separation, Indexer, and Explorer suites pass. Full `make preflight` also passes. No YNXT movement, Gateway impersonation, arbitrary signing, account abstraction, remote deployment, persistent public BFT routing, or public proof is claimed.
- SDK release integrity is implemented and locally verified at `6c7e2f37a6c8`: deterministic npm-compatible JavaScript and offline-installable Python wheel artifacts, canonical chain/source/package manifest, exact Git commit and address-vector binding, artifact/file SHA-256 values, strict bounded tar/ZIP parsing, optional external Ed25519 detached-signature verification, isolated artifact-only npm/virtual-environment consumers, and ecosystem-package embedding. Two builds are byte-identical and tampered artifacts, metadata, vectors, signatures, extra/traversal/symlink entries, unsafe modes, and source mismatches fail closed. `make sdk-release-integrity-check`, `make sdk-check`, `make address-codec-check`, `go test ./...`, `make test`, static gates, and full `make preflight` pass on the pushed commit. No owner production key was generated or read and no registry publication is claimed.
- Chainlist and bounded wallet integration are implemented and locally verified at `56cd3d1a1a23`: canonical YNX Testnet metadata generates the SDK network constants and exact EIP-3085 payload; YNX Mainnet remains endpoint-free and excluded; package output is deterministic and binds exact source commit/file digests; collision evidence is source/digest/time bound; strict tests reject metadata errors, stale/conflicting evidence, tampering, unsafe entries, and mainnet leakage. The EIP-1193 helper covers add/switch/recheck/error paths without account, transaction, seed, or key requests. The live read-only check observed `0x1917` / `6423`, block growth `109029` to `109030`, release `ynx-chain-97ed0c645bd2`, healthy Faucet/Explorer/indexer, account search, REST identity, and website HTML. Full `make preflight` passes on the pushed implementation commit.
- Exchange integration is implemented and publicly deployed as a testnet integration candidate. Signed vector mutation/query proof passed, `candidatePublicRuntimeDeployed=true`, and every external submission/listing/partnership/independent/mainnet field remains false. Standard Ethereum RLP and production custody are unsupported/unapproved.
- Owner-handover tooling now classifies four validator identities, five future BFT service signers, the authoritative Faucet runtime account, the funded public proof account, and ephemeral smoke identities without reading secret values. `make owner-handover-check` is part of `make preflight` and fail-closes on tampering, stale commits, self-review, duplicate signers, unknown funded ownership, or incomplete recovery/handover assertions.
- A real mode-`0600` unreviewed packet was generated at `/Volumes/Data/Users/huangjiahao/.ynx-chain-custody/owner-handover-packets/owner-handover-4c3af99a39c2-20260713T184628Z`, bound to inventory digest `sha256:1674f8d80b6a8e4f09150a1f004486e33890dbd3cceb0e0fad062ddfbd9adb30`. It contains 12 public identity/status records, 9 handover-required identities, zero unknown ownership records, and no secret material.
- Production custody review now requires and revalidates packet-local owner inventory/receipt evidence, requires exact service-signer/evidence/hash equality, enforces four distinct roles (owner, handover reviewer, custody reviewer, transaction approver), and propagates exact owner hashes through freeze/cutover approval evidence and candidate binding.
- A current-commit replacement packet exists at `/Volumes/Data/Users/huangjiahao/.ynx-chain-custody/owner-handover-packets/owner-handover-d4bebbc22fe6-20260713T190221Z`, inventory digest `sha256:4e11b68953415234325d4e4873a7beae429e321969e0fd97391e52d8218ff558`. Its three files are mode `0600`; its receipt remains default-false and is correctly rejected.

Incomplete modules or requirements:

- Public CometBFT voting and BFT service routing are not active. No production BFT mutation phase has run.
- The owner packet is deliberately unacknowledged. Offline recovery, actual owner handover, signer rotation evidence, remote signer installation, and independent custody review remain incomplete; validation correctly rejects its default-false receipt.
- Provider-backed AI generation proof is incomplete because upstream quota returns HTTP `429`.
- Independent public-vantage evidence is absent.
- SDK owner production signing, npm/PyPI namespace approval and publication, independent registry-consumer proof, mainnet audit/legal/custody, exchange listing, stablecoin issuer support, wallet default support, production bridge readiness, and third-party partnerships remain incomplete and are not claimed.
- Bridge external-chain execution remains incomplete: there is no production relayer custody, approved asset route, external adapter, mint/burn authority, liquidity/rate controls, remote daemon, public endpoint, external transaction, rollback drill, independent audit, or public proof.
- Stablecoin issuer support remains absent. The persistent local control plane and deployment package exist, but there is no external issuer approval, reserve/redemption proof, legal/custody approval, signer, token contract integration, mint/burn execution, remote daemon, public endpoint, rollback drill, independent audit, or public proof.
- Sponsored resource pools now have locally verified authoritative and CometBFT/AppHash implementations, but commit `563fe1f36918` is not exposed by the current public release. There is no approved remote candidate transaction, persistent public BFT routing, remote pool/sponsorship transaction, rollback drill, or independent public Explorer proof.
- The production website exposes address conversion and the truthful MetaMask `0x...` boundary. Independent public-vantage proof is absent, and no wallet-default support is claimed.
- A production YNX-native native-mobile wallet application is not yet delivered. React Native source and Android 16 arm64 emulator now prove SecureStore custody, create/import, recovery acknowledgement, capture protection, native Assets/Activity/Account routes, real public balance/nonce/history reads, receive QR, canonical fee-aware transfer construction, centralized strong-biometric authorization, deterministic Go-compatible signing, bounded broadcast/finality handling, and test-only Release APK/AAB packaging. Installed transfer execution, owner production signing/recovery/handover, iOS installed-app, both-platform real-device lifecycle, signed production IPA/AAB, store review, independent audit, and independent proof remain incomplete.
- YNX Chat has an App-native direct-message window, signed member-scoped conversation/device reads, and Go-compatible on-device E2EE. Exact daemon release `ynx-chain-f81f3b6cabe2` and Gateway release `ynx-chain-376c95793d66` run remotely; public TLS and native-bound operator smokes completed signed empty-list reads and cleanup without a conversation or message write. The App itself remains local/test-signed and no installed authenticated message was sent. Multi-device fan-out/recovery, groups, contacts, attachments, Pay/Trust/appeal, moderation, multi-region storage, independent audit, and independent public proof remain incomplete.
- YNX Square bounded social core and the signed Web/PWA post window are implemented. Exact daemon release `ynx-chain-0d34755e5196` runs remotely on loopback; Gateway release `0d31850f74b2` exposes sessionless reads and browser/native ownership-session-protected mutations. Disposable browser, native-protocol, and installed Android-emulator lifecycles completed without posting and returned active sessions to zero. Public post creation still lacks owner-approved content proof. Comment/reaction/follow/report controls, moderation decisions, Trust/Pay execution, installed iOS and real-device evidence, independent audit, and independent public proof remain incomplete.
- The Chainlist package remains candidate-only. No pull request was submitted, no registry accepted it, no wallet vendor granted default support, and no independent wallet or public-vantage proof exists. Collision and live evidence must be refreshed immediately before any owner-authorized submission.
- The Exchange runtime candidate is public, but production confirmation policy, custody integration, independent exchange validation, submission, and listing remain absent.

Current blockers:

- Public BFT remains externally gated by recovery/handover/rotation evidence and independent custody approval.
- Successful AI generation remains blocked by external provider capacity/quota.
- Secret-bearing deployment and signer material must remain ignored and must never appear in commits or public proof.
- Direct public TLS and cross-region snapshot transfer remain intermittent; retries recover, but availability hardening and monitoring are still required.

Largest real gap that can still be advanced next:

- Build the first real native Chat window over the remotely protected direct-message core: conversation list/create, encrypted message send/read, delivery/read state, device/session lifecycle, retry-safe errors, and honest empty/unavailable states. Keep the App native rather than website-shaped. Groups, attachments, Bank, Shop, live Bridge execution, iOS/real devices, owner production signing, stores, audit, and independent proof remain later gaps. Bounded EVM/IDE remains frozen.
