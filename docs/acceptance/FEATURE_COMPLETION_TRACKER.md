# Feature Completion Tracker

Statuses are intentionally strict: `complete` requires code, tests, remote deployment, and public proof.

Current remote-state override (2026-07-11): the current verifier release is deployed on four validator-role nodes, and the primary serves public RPC, EVM, Faucet, Indexer, Explorer, AI, Pay, Trust, and Resource endpoints. Strict SSH and release-manifest evidence pass. The public mutable run verified bounded Explorer index-lag polling, Anti-Illegal Request rejection, native YNXT protection, governance review/reject, appeal resolution, false-positive correction evidence, transparency reports, Pay idempotency/webhook audit, AI permission/action approval audit, Resource policy/delegation/rental/income, and IDE compile. Authenticated authoritative replication is remotely deployed and fixed-height/hash convergence passed on all three followers. Full public proof still fails because the AI provider account returns `429 insufficient_quota` and Web4 remains on legacy chain `ynx_9102-1`. This is deterministic producer/follower state convergence, not BFT consensus or validator voting. Therefore no module is marked `complete`, and older row-level `not remote deployed` text below should be read as superseded by this override until each row is rewritten.

Current chain-convergence status (2026-07-12): authenticated authoritative replication remains the public network. Separately, owner-controlled host-local production validator keys, the four-role public manifest, private WireGuard P2P overlay, fresh height-`16165` migration, remote 4-of-4 candidate quorum, 3-of-4 fault progress and recovery, owner-signed YNXT transaction convergence, CometBFT Gateway broadcast/lookup/history, and approval-gated rollback were remotely verified. The candidate is now intentionally absent after rollback and `publicCutoverAuthorized=false`; authoritative replication must still not be labeled public BFT consensus.

| Module | Required capability | Current status | Code path | Test command | Remote deployed? | Public proof? | Blocker | Next implementation gap |
|---|---|---|---|---|---|---|---|---|
| Chain runtime | Produce blocks, process native transfers, expose status, expose validator readiness, peer discovery, and peer sync summaries | local verified / preflight covered / not remote deployed | `internal/chain`, `cmd/ynx-chaind` | `make test`, `make smoke-test`, `make validator-peer-readiness-check`, `make preflight` | no | no | remote SSH/public ingress blockers | Remote deploy and verify block growth plus validator readiness/discovery/sync |
| Genesis / chain config | New `ynx_6423-1`, EVM `6423`, native `YNXT` | local verified / not remote deployed | `internal/chain`, `scripts/deploy` | `make env-check`, `make wallet-integration-check` | no | no | public endpoints still show old/broken state | Deploy new config remotely |
| Validator set | Configurable validator metadata plus deterministic binding of four active YNX identities to exact CometBFT ed25519 public keys/addresses in shared genesis | owner-controlled host-local production keys and exact public role binding remotely verified; candidate rolled back after proof | `internal/chain/consensus_migration.go`, `internal/consensus/genesis.go`, `internal/consensus/production.go`, `cmd/ynx-consensus-keycheck` | `go test ./internal/chain ./internal/consensus`, `make consensus-quorum-check`, `make consensus-production-package-check`, remote keycheck/deploy evidence | candidate proof only; not currently active | no | public network still uses authoritative replication | Build approval-gated public cutover/rollback path and revalidate key custody before cutover |
| Multi-validator peer sync | Real CometBFT P2P quorum plus legacy authoritative follower sync evidence during migration | encrypted four-server overlay and remote parallel 4-of-4 quorum verified; 3-of-4 progress and stopped-node catch-up passed; candidate rolled back | `internal/consensus/network.go`, `internal/consensus/production.go`, `scripts/deploy/deploy-consensus-candidate.sh`, `scripts/verify/verify-consensus-candidate.sh` | `make consensus-quorum-check`, `make consensus-production-package-check`, remote candidate/fault evidence | candidate proof only; not currently active | no | no approved public cutover path or public BFT endpoint proof | Implement cutover health gates, public RPC/service rebinding, and automatic rollback criteria |
| Persistent chain state | JSON devnet snapshot restore plus atomic mode-`0600` ABCI state, migration-anchor validation, InitChain handshake, tamper rejection, restart restore, candidate state path, backup, and rollback | fresh remote height-`16165` migration, persistent candidate execution, fault recovery, and clean rollback verified | `internal/chain/devnet.go`, `internal/consensus/state.go`, `internal/consensus/production.go`, `scripts/ops/rollback-consensus-candidate.sh` | `go test -race ./internal/consensus`, `make consensus-quorum-check`, remote candidate/fault/rollback evidence | candidate proof only; rolled back | no | authoritative-to-BFT cutover backup and rollback point are not yet automated as one operation | Implement cutover snapshot binding, service transition, failure thresholds, and rollback verification |
| BFT state migration / ABCI | Deterministic YNXT migration anchor; CometBFT v0.38.23 ABCI 2.0; signed execution; validator key binding; four homes/genesis; production candidate package; quorum/fault/restart verification | fresh 14-account migration and four-role package remotely exercised through quorum, signed transfer, fault recovery, and rollback | `internal/chain/consensus_migration.go`, `internal/consensus`, `cmd/ynx-abci`, `cmd/ynx-consensus-package`, `scripts/deploy/deploy-consensus-candidate.sh`, `scripts/verify` | local BFT suite plus remote candidate, signed-tx, fault, and rollback evidence | candidate proof only; rolled back | no | no separately reviewed public cutover implementation | Build and test a deterministic fresh-anchor cutover with explicit public rollback evidence |
| Node restart recovery | Restart devnet, durable ABCI application, and a stopped CometBFT validator without changing the common committed result | local and remote candidate 3-of-4 progress/recovery verified; Seoul recovered to height `16325` | `internal/consensus/state.go`, `scripts/verify/consensus-quorum-check.sh`, `scripts/verify/consensus-candidate-fault-drill.sh` | `make consensus-quorum-check`, remote `make consensus-candidate-fault-drill` evidence | candidate proof only; rolled back | no | public service behavior during a BFT validator restart is not proven | Include public RPC/indexer/explorer continuity in cutover and restart acceptance |
| RPC | Health/status/node identity/blocks/accounts/txs/validators plus validator heartbeat, peer observation, and peer sync APIs | local verified / preflight covered / not remote deployed | `internal/api` | `make smoke-test`, `make validator-peer-readiness-check`, `make preflight` | no | no | public RPC not new chain | Public RPC proof |
| BFT compatibility Gateway | Translate CometBFT/ABCI committed state into existing YNX RPC shapes and refuse cutover until every public capability is BFT-backed | status, block/tx decoding, account, four-validator, node identity, native signed broadcast/lookup/bounded history, `eth_chainId`, and `eth_blockNumber` locally tested and verified against the remote candidate; eight implemented and seven missing capabilities; cutover gate fails closed | `internal/bftgateway`, `cmd/ynx-bft-gatewayd`, `scripts/verify/consensus-public-cutover-gate.mjs` | `make bft-gateway-check`, `make consensus-public-cutover-check`, `go test ./...`, remote loopback broadcast/lookup/history and four-signer evidence | temporary candidate proof only; removed | no | EVM receipts/logs, Faucet, AI, Pay, Trust/Chain Law, Resource, and IDE transitions are not BFT-backed | Route Faucet funding through canonical signed Gateway transactions, then continue capability-by-capability until the cutover gate passes |
| EVM RPC | chainId, block, balance, tx, receipt, persisted logs, filtered log query subset, contract deployment event logs, source-analyzer `eth_call`, Hardhat ABI Keccak selector/event metadata, deployed-bytecode selector presence checks, bounded read-only EVM opcode interpreter for supported staticcall getters including constructor-seeded `totalSupply()`, mapping/SHA3-backed `balanceOf(address)`, and `SampleEVMWriteCounter.count()`, generic pinned-artifact `eth_sendTransaction` write-call subset for supported nonpayable/payable calldata including ERC20 `transfer(address,uint256)` and `SampleEVMWriteCounter.increment(uint256)` with `SSTORE` write evidence, `executionLogs`, bounded ABI return decoding, canonical local storage updates, filterable bounded ERC20 Transfer logs, filterable generic CountChanged logs, Hardhat bytecode hash metadata, and local verifier evidence endpoint | WIP safely closed: local verified / not remote deployed | `internal/chain`, `internal/api`, `internal/chain/evm_static.go`, `internal/chain/keccak.go`, `contracts/devtools/SampleEVMWriteCounter.sol`, `artifacts/ynx-selector-metadata.json` | `go test ./internal/api`, `make smoke-test`, `make exchange-integration-check`, `make contract-tooling-check` | no | no | public EVM chainId wrong; not a full arbitrary EVM execution engine | Do not expand bounded opcode/Counter/Hardhat coverage now; next real gap is public EVM RPC/verifier proof after higher-priority Chain Law proof |
| Transaction broadcast | Legacy devnet transfer, bounded calls, canonical secp256k1-signed YNXT transfers through CometBFT RPC, and Gateway HTTP broadcast/lookup/history with four-node convergence evidence | direct candidate transfer `E3A6D8...5548` committed at height `16277`; Gateway transfer `0x33be8038...68ec0` committed at height `16356` and matched lookup/history; signing key remained local | `internal/chain`, `internal/api`, `internal/consensus`, `internal/bftgateway`, `cmd/ynx-consensus-tx`, `scripts/verify/consensus-candidate-signed-tx-drill.sh` | local signed-transfer/Gateway tests plus remote signed-tx and Gateway evidence | candidate proof only; rolled back | no | public CometBFT Gateway is not active and dependent service transitions are not migrated | Reprove with a fresh nonce after public cutover while preserving key custody |
| Transaction receipt | EVM receipt for known tx | local verified / not remote deployed | `internal/api/server.go` | `go test ./internal/api` | no | no | remote deploy blocked | Receipt proof on public EVM RPC |
| Logs / events | Persist deterministic EVM-style tx logs, parse contract event metadata, emit contract deployment event logs, emit bounded ERC20 Transfer logs for local contract calls, capture supported generic bytecode `LOGn` output as `executionLogs`, materialize supported generic logs into local receipts, and filter persisted logs by block range, address, and topics | local verified / not remote deployed | `internal/chain`, `internal/api` | `go test ./internal/chain ./internal/api`, `make smoke-test` | no | no | remote deploy blocked | Production compiler fidelity, broader EVM log execution, complex ABI/event coverage, and remote proof |
| Balance / account query | Native and EVM balance/account query | local verified / not remote deployed | `internal/chain`, `internal/api` | `make smoke-test` | no | no | remote deploy blocked | Public account proof |
| Faucet | Backend daemon, rate limits, request log, RPC funding | authoritative RPC mode locally verified and publicly deployed under the current replication model; not BFT-backed | `internal/faucet`, `cmd/ynx-faucetd` | `make faucet-check`, `make smoke-test`, `make preflight` | authoritative public service only | no BFT proof | current Faucet calls the legacy privileged `/faucet` mutation instead of locally signing canonical transactions for CometBFT | Add explicit BFT upstream mode that queries nonce/balance, signs locally with a dedicated Faucet key, broadcasts through the Gateway, preserves idempotency/rate limits, and fails closed on custody or nonce errors |
| Indexer | Persist block/tx index and resume | local verified / preflight covered / not remote deployed | `internal/indexer`, `cmd/ynx-indexerd` | `make indexer-check`, `make smoke-test`, `make preflight` | no | no | public indexer old chain evidence | Remote new-chain index proof |
| Explorer | Real RPC/indexer-backed explorer API with a shared two-second SSE sampler instead of per-client upstream polling; parallel core snapshots; optional validator/resource failure isolation; no-store HTML shell; responsive Apple-inspired operational UI; animated finalized-block ribbon; live height, indexed-window TPS, block time, index lag, activity, validator and resource metrics; live block/transaction feeds; transaction filtering; structured search; block/transaction/account detail drawer and copy actions; stale/reconnecting fallback; wallet integration; and explicit testnet boundary | locally verified and remotely deployed as scoped Explorer release `4ec8a807344c`; cross-region HTTP/build/truth checks and live SSE height growth passed on 2026-07-12 | `internal/explorer`, `cmd/ynx-explorerd` | `go test ./...`, `make explorer-check`, desktop/mobile browser render, public SSE height growth, cross-region HTTP/build/truth checks, public HTML cache/build verification | no | no | the real public testnet currently has only 13 indexed transactions, so transaction rows correctly remain unchanged until new transactions occur; broader TRON-class token/contract/history analytics and public BFT cutover are not complete | Preserve rollback, add paginated historical/token/contract/analytics surfaces only from real indexed data, and retain public SSE/cache/build/cross-region checks |
| AI Gateway | Independent provider-backed daemon with public health/metrics, API-key auth, per-key/IP rate limits, request IDs, concurrent session-isolated SSE, prompt-hash-only JSONL audit, authenticated chain upstream, persisted permission/action proxy, sensitive-action approval gate, release/build identity, dedicated ingress, systemd/Docker/monitoring, and public-proof requirements for gateway health/build, authenticated streaming, permission, approval, lookup, and list evidence | local verified / preflight, deploy package, and public-proof gate wired / not remote deployed | `internal/aigateway`, `cmd/ynx-ai-gatewayd`, `internal/chain`, `internal/api`, `scripts/deploy`, `scripts/verify/ai-gateway-check.sh`, `scripts/verify/remote-smoke-test.mjs` | `go test ./internal/aigateway ./internal/api`, `make ai-gateway-check`, `make deploy-dry-run`, `make public-proof-evidence-check`, `make preflight` | no | no | trusted host-key approval and real provider/deploy secrets unavailable; public AI endpoint is not new service | Deploy `ynx-ai-gatewayd` with real provider/access/upstream keys, then prove health/build identity, authenticated SSE, redacted audit operation, and chain-backed permission/action flow publicly |
| Pay API | Independent merchant daemon with public health/metrics and build identity; API-key auth; request IDs; per-key/IP limits; 1 MiB body bound; authenticated merchant binding; required mutation idempotency; environment-only webhook signing; redacted fail-closed access audit; authenticated chain upstream; canonical persistent intent/invoice/refund/webhook/event records; dedicated ingress, systemd, Docker, monitoring, release/ops wiring; and public-proof health/build/auth/idempotency/webhook/event gates | local verified / preflight, deploy package, and public-proof gate wired / not remote deployed | `internal/paygateway`, `cmd/ynx-payd`, `internal/chain`, `internal/api`, `scripts/verify/pay-api-check.sh`, `scripts/deploy`, `scripts/verify/remote-smoke-test.mjs` | `go test ./internal/paygateway ./internal/api`, `make pay-api-check`, `make deploy-dry-run`, `make public-proof-evidence-check`, `make preflight` | no | no | trusted host-key approval and real merchant/deploy secrets unavailable; public Pay endpoint is not new service | Deploy `ynx-payd` with real merchant/API/upstream/webhook keys, then prove health/build identity, authenticated request IDs, idempotency, managed signing, audit operation, and persistent Pay records publicly |
| Trust API | Independent Trust/Chain Law daemon with public health/metrics and build identity; API-key auth; request IDs; per-key/IP limits; 1 MiB request and 2 MiB evidence-export bounds; body-hash-only fail-closed audit; authenticated chain upstream; persistent lot lineage, pro-rata trace, distinct address and known-transaction advisory labels, JSON/PDF evidence, Request Validity, Anti-Illegal Request, appeal/correction, anti-unreasonable tracking, and transparency routes; dedicated ingress, systemd, Docker, monitoring, release/ops wiring; and public-proof health/build/auth gates | local verified / preflight, deploy package, and public-proof gate wired / not remote deployed | `internal/trustgateway`, `cmd/ynx-trustd`, `internal/chain`, `internal/api`, `scripts/verify/trust-api-check.sh`, `scripts/deploy`, `scripts/verify/remote-smoke-test.mjs` | `go test ./internal/trustgateway ./internal/api`, `make trust-api-check`, `make request-validity-check`, `make trust-appeal-check`, `make anti-unreasonable-tracking-check`, `make deploy-dry-run`, `make public-proof-evidence-check`, `make preflight` | no | no | trusted host-key approval and real Trust/deploy secrets unavailable; public Trust endpoint is not new service | Deploy `ynx-trustd` with real client/upstream keys, then prove health/build identity, authenticated request IDs, bounded evidence, redacted audit, lineage, address/transaction advisory labels, Chain Law, appeal/correction, tracking, and transparency publicly |
| Lot lineage | Pro-rata lot flow tracing | local verified / not remote deployed | `internal/chain/devnet.go` | `go test ./internal/chain` | no | no | remote deploy blocked | Remote Trust proof |
| Pro-rata taint tracking | Lot-based risk lineage without freezing funds and with non-conclusive low-confidence handling | local verified / not remote deployed | `internal/chain/devnet.go` | `go test ./internal/chain` | no | no | remote deploy blocked | Remote Trust proof |
| Anti-Illegal Request Engine | Intake and reject private-secret, signature-bypass, hidden-record, fake-risk, unsupported-Trust-conclusion, AI-auto-punishment, overbroad, and native YNXT direct-freeze/direct-transfer requests with audit transparency | local verified on 2026-07-10 / not remote deployed | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make anti-illegal-request-check`, `make preflight`, `make remote-smoke-test` | no | no | remote deploy blocked | Prove the same rejection flow on public endpoints after deploy gate clears |
| Request Validity Standard | Classify valid/evidence-missing/out-of-scope asset-boundary/overbroad/review/user-notice/rejected with inspectable rule IDs and persisted request lookup/review state | local verified on 2026-07-10 / not remote deployed | `internal/chain/devnet.go`, `internal/api/server.go`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make request-validity-check`, `make preflight`, `make remote-smoke-test` | no | no | remote deploy blocked | Prove request-validity rules and request lookup/review/reject on public endpoints |
| Appeal / Dispute | Open, read, resolve appeals with reviewer, decision, status, transparency entry IDs, Trust false-positive correction records, required linkage to an existing governance request or Trust label so orphaned appeals cannot create transparency entries, and public-proof gate coverage that requires post-resolution Trust evidence correction summary | local verified on 2026-07-10 / not remote deployed | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/public-proof-evidence-check.mjs` | `go test ./...`, `make trust-appeal-check`, `make public-proof-evidence-check`, `make preflight`, `make remote-smoke-test` | no | no | remote deploy blocked | Remote appeal proof, including correction evidence summary on public endpoints |
| False Positive correction | Appeal decisions can remove/reduce labels with corrective Trust labels, and remote public proof must show `riskSummary.correctionLabelCount` plus advisory-only appeal metadata after resolution | local verified / public-proof gate wired / not remote deployed | `internal/chain/devnet.go`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/public-proof-evidence-check.mjs` | `make trust-appeal-check`, `make public-proof-evidence-check` | no | no | remote deploy blocked | Remote false-positive correction proof |
| Transparency Report | Generate persisted report entries and counts for request intake, review, manual rejection, appeal open, and appeal resolution | local verified on 2026-07-10 / not remote deployed | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make transparency-report-check`, `make preflight`, `make remote-smoke-test` | no | no | remote deploy blocked | Remote transparency proof |
| Native YNXT no-hidden-freeze checks | Reject native YNXT direct freeze/transfer/seize/confiscate/blacklist requests, reject Trust labels with native-freeze asset effects, and statically scan runtime code for hidden native YNXT freeze hooks | local behavior + static scan verified / not remote deployed | `internal/chain/devnet.go`, `scripts/verify/native-ynxt-no-hidden-freeze-check.sh` | `make native-ynxt-no-hidden-freeze-check`, `make preflight` | no | no | remote deploy blocked | Remote public proof after deploy |
| Anti-Unreasonable Tracking Policy | Purpose logging, evidence checks, minimum necessary data, sensitive review, confidence/expiry/appeal path | local verified / remote proof gate wired | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `make anti-unreasonable-tracking-check`, `make preflight`, `make remote-smoke-test` | no | no | remote deploy blocked | Remote anti-unreasonable tracking proof |
| Resource Market | Independent Resource Market daemon with public health/metrics and build identity; API-key auth; request IDs; per-key/IP limits; 1 MiB request and 2 MiB response bounds; body-hash-only fail-closed audit; authenticated chain upstream; persistent policy, quote, delegation, rental, income, and analytics routes; dedicated ingress, systemd, Docker, monitoring, release/ops wiring; and public-proof health/build/auth plus mutation gates | local policy-driven implementation and standalone gateway verified / preflight, deploy package, and public-proof gate wired / not remote deployed | `internal/resourcegateway`, `cmd/ynx-resourced`, `internal/chain`, `internal/api`, `scripts/verify/resource-api-check.sh`, `scripts/verify/resource-market-check.sh`, `scripts/deploy`, `scripts/verify/remote-smoke-test.mjs` | `go test ./internal/resourcegateway ./internal/api`, `make resource-api-check`, `make resource-market-check`, `make deploy-dry-run`, `make public-proof-evidence-check`, `make preflight` | no | no | trusted host-key approval and real Resource/deploy secrets unavailable; public Resource endpoint is not new service | Deploy `ynx-resourced` with real client/upstream keys, then prove health/build identity, authenticated request IDs, policy/quote, delegation/rental/income/analytics persistence, and redacted audit publicly |
| Resource rental / delegation | Authenticated standalone delegation and rental flow with policy-bound capacity multipliers and settlement split persisted by the chain runtime | local verified through `ynx-resourced` / preflight covered / not remote deployed | `internal/resourcegateway`, `internal/chain/devnet.go` | `make resource-api-check`, `make resource-market-check`, `make preflight` | no | no | remote deploy blocked | Remote authenticated delegation and rental proof |
| Resource income records | Authenticated provider/protocol income records and analytics with policy identity/hash persisted by the chain runtime | local verified through `ynx-resourced` / preflight covered / not remote deployed | `internal/resourcegateway`, `internal/chain/devnet.go` | `make resource-api-check`, `make resource-market-check`, `make preflight` | no | no | remote deploy blocked | Remote authenticated income and analytics proof |
| IDE compile / deploy / verify | Solidity preflight, pinned Solidity `0.8.24` compiler config metadata, deterministic source-analyzer artifacts for ad hoc snippets, Hardhat `pinned-solc-bytecode-artifact` metadata for matched repository sources, deploy with optional `constructorArgs`, local deployed bytecode hash comparison status, `ethers` selector/event metadata, deployed-bytecode selector match evidence, explicit local verifier evidence endpoint, contract lookup, source-analyzer pure/view calls, canonical local runtime storage seed, runtime storage-slot metadata, simple constructor-seeded public `uint` storage, constructor-seeded `mapping(address=>uint)` storage, bounded EVM opcode interpreter staticcall subset for supported Hardhat getters, and generic bounded `POST /ide/execute` write-call subset for supported pinned-artifact nonpayable/payable calldata including ERC20 `transfer(address,uint256)` and `SampleEVMWriteCounter.increment(uint256)` with `SSTORE` write evidence, `executionLogs`, bounded return decoding, and local receipt/log filtering | WIP safely closed: local verified / not remote deployed | `internal/api`, `internal/chain`, `internal/chain/evm_static.go`, `internal/chain/keccak.go`, `contracts/devtools/SampleEVMWriteCounter.sol`, `scripts/contracts/generate-selector-metadata.mjs`, `artifacts/ynx-selector-metadata.json` | `go test ./internal/api`, `make smoke-test`, `make developer-quickstart-check`, `make contract-tooling-check` | no | no | remote deploy blocked; not a full arbitrary EVM execution engine | Do not expand bounded opcode/Counter/Hardhat/IDE execution now; next real gap is remote public verifier/explorer proof after higher-priority Chain Law proof |
| SDK | JS/Python entrypoints and metadata checks | local verified / not remote deployed | `sdk`, `scripts/verify/developer-quickstart-check.sh` | `make developer-quickstart-check` | no | no | remote deploy blocked | Remote SDK quickstart proof |
| Wallet / MetaMask integration | Add-chain metadata and EVM chainId checks; EVM-compatible signed native accounts; owner-controlled treasury/foundation/team/deployer/faucet/validator key handover | add-chain and signed-account enforcement verified; two owner-controlled local testnet accounts and four host-local validator/node key pairs exist; remote candidate signed transfer passed; full role handover not complete | `chain-metadata`, `internal/consensus`, `cmd/ynx-consensus-tx`, `cmd/ynx-consensus-account-key`, `scripts/package` | `make wallet-integration-check`, `make consensus-signed-transfer-check`, remote signed-tx evidence | candidate proof only | no | treasury/foundation/team/deployer/faucet separation, backup/recovery, rotation, and formal handover are incomplete | Perform the remaining offline role-separated custody ceremony and owner recovery/handover verification without exposing secrets |
| Chainlist package | Chain metadata package | local verified / not remote deployed | `chain-metadata` | `make chainlist-package` | no | no | public endpoints not new chain | Submit after public proof |
| Website remote API integration | Website should read real public API | not remote verified | `/Users/huangjiahao/Desktop/YNX-Chain-website` | website repo checks | no | no | public chain endpoints not ready | Reverify website after chain deploy |
| Remote deployment | Safe deploy with backup, rollback, gate, host-key audit, non-mutating host-key repair plan that emits known_hosts repair commands only for confirmed host-key mismatch rows, blank host-key approval template generation, external approval-request generation, external reviewer approval-packet generation with blank trusted-fingerprint draft and host-key audit SHA-256 binding, non-mutating approval status report with skipped mismatch nodes and current audit SHA binding, ignored host-key approval check requiring current `hostKeyAuditSha256`, approval-gated known_hosts repair, strict SSH known_hosts enforcement with no auto-accept-new fallback, freshness/consistency checks for required source evidence including conditional approval-request/approval-packet/status freshness and current-fingerprint/current-audit-hash matching during host-key mismatch, current-HEAD/release/status binding for remote-smoke evidence in blocker report, deploy gate, and public-proof evidence validation, role-specific validator env files, automated peer-sync poller env, binary-level `ynx-chaind --check-config` validation for generated role envs and installed remote envs before restart, post-restart localhost service checks for expected commit/release/chain ID/YNXT plus primary indexer/explorer/faucet/AI Gateway health and auxiliary service build identity, deploy-injected `ynx-chaind`/`ynx-indexerd`/`ynx-explorerd`/`ynx-faucetd`/`ynx-ai-gatewayd` release commit/name/build time, release manifest artifact checksums, per-node observed release manifest commit/release/path evidence, per-node release manifest SHA-256 and `ynx-chaind` SHA-256 evidence, explicit `REST_DOMAIN` and `INDEXER_DOMAIN` deploy inputs, Nginx server blocks and managed Caddy snippet site blocks for Explorer/Faucet/Indexer/AI Gateway/RPC/EVM/REST API public proof domains, non-destructive Caddyfile managed import with candidate validation, backup, reload path, executable local installer fixture coverage for Caddy-backed primary hosts, packaged local service checker self-test and generated post-restart checker command coverage for primary plus validators, strict `verify-testnet` checks for safe role env fields plus local `/status`, `/node/identity`, `/validators`, `/validators/peers`, and `/validators/peer-sync` evidence, plus semantic public-endpoint blockers for old-chain, wrong-chain-id, empty validator set, incomplete validator metadata, missing release identity, missing release manifest evidence, missing public-proof metadata, missing validator node identity/freshness, missing validator peer-readiness/discovery/sync, missing block growth, and skipped mutable proof actions | implemented / blocked by trusted approval and fresh passed remote evidence | `scripts/deploy`, `scripts/ops`, `scripts/verify`, `.env.deploy.example`, `ENV_INTAKE_FORM.md` | `make deploy-dry-run`, `scripts/deploy/check-local-services.sh --self-test`, `make caddy-ingress-check`, `make release-manifest-evidence-check`, `make verify-testnet-check`, `make public-proof-evidence-check`, `make host-key-audit`, `make host-key-repair-plan`, `make host-key-approval-template`, `make host-key-approval-request`, `make host-key-approval-packet`, `make host-key-approval-status`, `make host-key-approval-check-test`, `make host-key-approved-repair-dry-run`, `make deploy-readiness-gate-check`, `make deploy-readiness-gate`, `make objective-state-check` | no | no | Singapore and Silicon Valley host-key mismatches awaiting trusted audit-bound approval, approval status awaiting trusted confirmation, plus public ingress blockers | Verify Singapore and Silicon Valley fingerprints out-of-band with the approval packet, fill ignored approval file only with trusted fingerprints plus current `hostKeyAuditSha256`, make approval check pass, dry-run approved repair, correct known_hosts only after approval, refresh host-key and remote-smoke evidence for the current HEAD with status `passed`, rerun gate, then deploy |
| Backup / rollback | Backup and rollback scripts | local verified / not remote deployed | `scripts/ops` | `make ops-check` | no | no | remote SSH blockers | Remote backup drill |
| Public proof | Generate remote public proof only from real endpoints, including proof metadata, expected YNX chain identity, release/build identity, current-HEAD/release binding, non-local public endpoint identity, non-local gRPC host, per-node observed release manifest commit/release/path evidence, release manifest checksum evidence, validator peer-readiness, peer-discovery, peer-sync, and AI / Pay / Trust / Resource / Chain Law / Anti-Illegal / Appeal / Transparency checks from remote smoke evidence plus required-check completeness and package-manifest validators that must keep `validPublicProof=false` when metadata, any endpoint, validator, release identity, release manifest identity/hash evidence, service auth/build health, AI permission/action audit evidence, Pay idempotency or webhook audit evidence, Chain Law, Appeal, Trust evidence correction summary, Transparency, anti-unreasonable tracking, Resource Market policy/quote/delegation/rental/income/analytics evidence, IDE, package file hash, generated proof summary, or mutable proof check is missing, failed, stale, inconsistent, local-only, old-chain, skipped, or package-manifest-invalid | local validator/package gate + generated package self-check + metadata/current-HEAD gate + package manifest/hash gate + deploy ingress contract + manifest identity gate + AI/Pay/Trust/Resource authenticated service gates verified / blocked by remote evidence | `scripts/package/public-proof.sh`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/public-proof-evidence-check.mjs`, `scripts/verify/public-proof-package-check.mjs`, `scripts/verify/release-manifest-evidence.mjs`, `scripts/deploy/deploy-testnet.sh` | `make public-proof`, `make remote-smoke-test`, `make release-manifest-evidence-check`, `make public-proof-evidence-check`, `make public-proof-package-check`, `make verify-testnet-check`, `make deploy-dry-run` | no | no | endpoints not new chain; release manifest evidence not remotely generated | Run after verified deploy and require current-HEAD-bound authenticated Resource health/build/policy/quote/delegation/rental/income/analytics evidence plus the existing chain, validator, AI, Pay, Trust, Chain Law, release-manifest, and package gates |
| Grant package | Grant docs/package generation | local verified / not remote deployed | `docs/grants`, `scripts/package` | `make grant-package` | no | no | public proof missing | Regenerate after remote proof |
| Ecosystem package | Ecosystem submission package | local verified / not remote deployed | `docs/ecosystem`, `scripts/package` | `make ecosystem-package` | no | no | public proof missing | Regenerate after remote proof |
| Exchange readiness | Exchange listing readiness package | local verified / not remote deployed | `docs/exchange-listing`, `scripts/package` | `make exchange-package` | no | no | public proof missing | Regenerate after remote proof |
| Mainnet readiness | Mainnet checklist and gates | readiness only / not mainnet ready | `docs/mainnet-readiness`, `scripts/package` | `make mainnet-readiness` | no | no | audit/legal/validator/public proof missing | Keep mainnet as not ready |
