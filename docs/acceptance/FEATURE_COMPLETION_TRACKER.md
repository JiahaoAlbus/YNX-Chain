# Feature Completion Tracker

Statuses are intentionally strict: `complete` requires code, tests, remote deployment, and public proof.

Current remote-state override (2026-07-13): the current authoritative release is deployed on four validator-role nodes, and the primary serves public RPC, EVM, Faucet, Indexer, Explorer, AI, Pay, Trust, and Resource endpoints. Strict SSH and release-manifest evidence pass. The public mutable run verified bounded Explorer index-lag polling, Anti-Illegal Request rejection, native YNXT protection, governance review/reject, appeal resolution, false-positive correction evidence, transparency reports, Pay idempotency/webhook audit, AI permission/action approval audit, Resource policy/delegation/rental/income, and IDE compile. Authenticated authoritative replication is remotely deployed and fixed-height/hash convergence passed on all three followers. The separately hosted Web4 Hub is now fail-closed bound to live `ynx_6423-1` status and publicly reports observed chain `6423`, native `YNXT`, a growing height, `publicNetwork=true`, and release `ynx-chain-0ee044bd7d78`. Full public proof still fails because the AI provider account returns `429 insufficient_quota`, and independent public vantage plus public BFT proof remain absent. This is deterministic producer/follower state convergence, not BFT consensus or validator voting. Therefore no module is marked `complete`, and older row-level `not remote deployed` text below should be read as superseded by this override until each row is rewritten.

Current chain-convergence status (2026-07-12): authenticated authoritative replication remains the public network. Separately, owner-controlled host-local validator keys, the four-role public manifest, private WireGuard P2P overlay, repeated fresh migrations, remote 4-of-4 quorum and 3-of-4 recovery, native/Faucet/AI/Pay/Trust/Resource state transitions, Indexer/Explorer compatibility, EVM receipts/logs, and deterministic bounded IDE deploy/call state have private candidate proof and rollback. The IDE proof includes pinned identity, constructor/write storage, real LOG/bloom/filter evidence, replay/conflict/unsupported rejection, supply conservation, direct Comet matching, four-application equality, four-signer evidence, cleanup, and public authoritative post-rollback growth. Gateway now reports fifteen implemented and zero missing capabilities while `publicCutoverReady=false`. The candidate is intentionally absent; capability completeness is not public cutover authorization and authoritative replication must not be labeled public BFT consensus.

Current parallel-dependency status (2026-07-13): exact-transaction `start_dependencies`, `verify_continuity`, and `rollback_dependencies` are implemented and locally fixture/dry-run verified. Candidate deployment builds exact-current-commit BFT Gateway, Indexer, Explorer, Faucet, AI, Pay, Trust, and Resource binaries under the candidate root; no global binary is replaced. The primary candidate stack is constrained to loopback ports `27620` and `27626-27632`, copies and verifies the authoritative Indexer tip at the approved migration boundary, maps only the first candidate block to that parent anchor, clears inherited raw signer variables, requires five mode-`0600` file-backed signers, and isolates candidate state/logs. Continuity rejects migration-anchor mismatch, Indexer below-boundary/over-lag state, stale Explorer, wrong build identity, or unhealthy signer-bound BFT services. Rollback removes only transaction-owned candidate dependencies and verifies authoritative services remain active. None of this code is remotely deployed or public; signer custody/recovery and live transaction evidence remain incomplete. Bounded EVM/IDE remains intentionally frozen at its already verified subset.

Current ingress/public-verification status (2026-07-13): exact-transaction `switch_ingress`, `verify_public`, and `rollback_ingress` are implemented and locally fixture/dry-run failure-tested. Managed YNX Caddy/Nginx files receive mode-`0600` backups and checksum manifests; only eight YNX upstreams change; Gateway runtime authorization and config reload are restored on failure; repeated rollback rechecks authoritative checksums. Frozen public verification rejects wrong chain/EVM/four-validator/build identity, height stall/regression, stale Indexer/Explorer, signer drift, unhealthy services, or a mutation probe other than HTTP `503`. Root-path EVM compatibility is implemented for existing wallet/domain clients. All named transaction phases now exist in code, but the completed control plane is not deployed, no live transaction phase or ingress switch occurred, and there is no public BFT or independent public proof. Remaining work is full local gate, commit/push, ordinary control-plane deployment, rebound non-mutating rehearsal, custody/recovery review, exact independent approval, live rollback evidence, and post-unfreeze public proof.

Current exact-release override (verified 2026-07-13): ordinary authoritative release `0ee044bd7d78` is deployed on all four roles with manifest SHA-256 `ead0efab4cc1def179c3347be55e9ea6df16afb7883cb81458cd52383d7673b4`, daemon SHA-256 `e020a42ee4e0f043572699ec47abc734644bc75000750eb1afdbf12b67120822`, and fixed-height convergence at `80608` / `efabca1c02d2bce411b4603232b2a527fc0d2ff77b38942446ccde85a0f8bf56`. The complete transaction control-plane scripts are shipped, and the non-mutating rehearsal passed at `tmp/public-bft-production-rehearsal/rehearsal-0ee044bd7d78-20260713T083256Z`. The new packet at `tmp/public-bft-freeze-rehearsal-approval/freeze-rehearsal-0ee044bd7d78-20260713T083400Z` is deliberately unapproved. Web4 source commits `06b8f57` and `b6da2ef` are pushed; its chain-binding unit tests pass, its scoped deployment backup is retained at `/var/backups/ynx-web4-hub/web4-current-chain-b6da2ef-20260713T085652Z`, and public health plus Singapore-routed smoke verify the current chain binding. No Caddy ingress or BFT transaction phase changed. AI provider HTTP `429`, signer custody/recovery review, independent approval, live rollback evidence, independent public vantage, and public BFT remain incomplete.

Current custody-gate status (verified 2026-07-13): both the bounded freeze-rehearsal approval validator and the full public BFT cutover approval/evidence/candidate-binding chain now require an identified custody reviewer distinct from the transaction approver, an exact `sha256:<digest>` custody-review reference, and explicit verification of validator-key recovery, all five service-signer recovery paths, owner handover, and rotation procedure. A public-only review-packet generator reads only the mode-restricted signer manifest and ceremony status, emits a mode-`0600` default-false template, refuses overwrite, and never reads or installs keys. Its validator rejects unknown fields, manifest mismatch, free-form evidence, incomplete assertions, invalid references, and expired reviews before emitting the exact packet hash. Generated freeze templates default every new assertion to false; full-cutover fixtures and transaction tests reject missing recovery assertions, self-review, and free-form custody evidence. `make production-custody-review-check`, `make public-bft-cutover-transaction-check`, and `make public-bft-production-driver-check` pass locally, including ten failure injections, exact baseline restoration, candidate/dependency/ingress dry-run, and propagation of the custody assertions into downstream approval evidence. The role-specific ceremony tool creates five distinct mode-`0600` owner-local keys, verifies a recovery staging copy, emits only public identities, rejects overwrite/tamper, and performs no remote install. The first unencrypted-volume ceremony was never uploaded or funded, was abandoned, and its files were removed. The active identities were regenerated directly on a separate FileVault-protected volume at `/Volumes/Data/Users/huangjiahao/.ynx-chain-custody/production-service-signers/service-signers-20260713T101647Z`; public manifest SHA-256 is `89ea82399b1c9907d5ed4a61132dceb068d0fbe136c1e7780b2e88b654d69ae3`. A read-only primary-host inventory found the validator key/state/node files present as `ynx:ynx` mode `0600`, but remote `/etc/ynx/consensus-signers/{faucet,ai,pay,trust,resource}.key` remain absent. The same-volume encrypted recovery staging copy is not offline proof; no real independent review packet has been completed, while remote install, offline recovery, owner handover, rotation, independent review, live cutover, and public BFT remain false. The existing unapproved `0ee044bd7d78` packet does not satisfy this newer gate.

Current custody-review transaction binding (verified 2026-07-13): freeze and full-cutover execution now require `PUBLIC_BFT_CUSTODY_REVIEW_FILE` and revalidate its commit, service-signer manifest, reviewer, recovery/handover/rotation assertions, expiry, and exact file hash before writing approval evidence. Local fixtures reject missing, free-form, mismatched, and tampered review evidence. This is local code only; no reviewed production file or transaction authorization exists.

| Module | Required capability | Current status | Code path | Test command | Remote deployed? | Public proof? | Blocker | Next implementation gap |
|---|---|---|---|---|---|---|---|---|
| Chain runtime | Produce blocks, process native transfers, expose status, expose validator readiness, peer discovery, and peer sync summaries | authoritative producer/follower runtime deployed; public block growth, native transfer, four-role metadata, peer evidence, and fixed-height convergence verified | `internal/chain`, `cmd/ynx-chaind` | `make test`, `make smoke-test`, `make validator-peer-readiness-check`, `make verify-testnet`, `make preflight` | authoritative `0ee044bd7d78` yes; BFT runtime no | partial operator-controlled proof | public BFT and independent public proof absent | Preserve authoritative service while approval-gated BFT transition evidence is reviewed |
| Genesis / chain config | New `ynx_6423-1`, EVM `6423`, native `YNXT` | authoritative public RPC/EVM/Faucet/Explorer and fail-closed live-chain-bound Web4 report current chain `6423` / `ynx_6423-1` and native `YNXT`; BFT candidate genesis remains nonpersistent | `internal/chain`, `scripts/deploy`, sibling YNX `infra/web4-hub` | `make env-check`, `make wallet-integration-check`, `make verify-testnet`, Web4 unit tests, Singapore-routed remote smoke | authoritative and Web4 identity yes; BFT genesis no | operator-controlled public endpoints yes; independent proof no | public BFT and independent public proof are absent | Preserve exact chain metadata and fail-closed Web4 binding through any approved BFT transition |
| Validator set | Configurable validator metadata plus deterministic binding of four active YNX identities to exact CometBFT ed25519 public keys/addresses in shared genesis | owner-controlled host-local production keys and exact public role binding remotely verified; final-snapshot-to-manifest/package binding and candidate deploy/verify/rollback phases now pass isolated four-role dry-run; candidate remains absent | `internal/chain/consensus_migration.go`, `internal/consensus/genesis.go`, `internal/consensus/production.go`, `cmd/ynx-consensus-keycheck`, `scripts/ops/public-bft-production-driver.sh`, `scripts/verify/public-bft-candidate-binding.mjs` | `go test ./internal/chain ./internal/consensus`, `make consensus-quorum-check`, `make consensus-production-package-check`, `make public-bft-production-driver-check`, remote keycheck/deploy evidence | private candidate proof only; new production phases not deployed | no | public network still uses authoritative replication; custody must be revalidated for an exact approved transaction | Revalidate signer custody/recovery against a new exact-commit unapproved packet after control-plane deployment and rehearsal |
| Multi-validator peer sync | Real CometBFT P2P quorum plus legacy authoritative follower sync evidence during migration | encrypted four-server overlay and remote parallel 4-of-4 quorum verified; 3-of-4 progress and stopped-node catch-up passed; current authoritative four-role fixed-height convergence and exact-release rehearsal pass; candidate remains rolled back | `internal/consensus/network.go`, `internal/consensus/production.go`, `scripts/deploy/deploy-consensus-candidate.sh`, `scripts/verify/verify-consensus-candidate.sh`, `scripts/ops/public-bft-production-driver.sh` | `make consensus-quorum-check`, `make consensus-production-package-check`, `make public-bft-production-driver-check`, `make verify-testnet`, remote candidate/fault evidence | authoritative convergence and control plane deployed; candidate proof only and rolled back | no public BFT proof | no approved persistent BFT service/dependency or public endpoint transition | Obtain independent review before any persistent candidate or dependency transition |
| Persistent chain state | JSON devnet snapshot restore plus atomic mode-`0600` ABCI state, migration-anchor validation, InitChain handshake, tamper rejection, restart restore, candidate state path, scoped backup, read-preserving authoritative pause, final snapshot export, transaction-owned Indexer continuation, and rollback tooling | fresh remote candidate migration/fault/rollback proof exists; all transaction-bound forward/reverse phases reject tampered or mismatched evidence and pass fixture/four-role command dry-run; not remotely executed as one operation | `internal/chain/devnet.go`, `internal/consensus/state.go`, `internal/consensus/production.go`, `cmd/ynx-chaind`, `cmd/ynx-consensus-package`, `scripts/ops`, `scripts/verify/public-bft-candidate-binding.mjs`, `scripts/verify/validate-public-bft-dependency-continuity.mjs`, `scripts/verify/validate-public-bft-public-continuity.mjs` | `go test ./cmd/ynx-chaind ./cmd/ynx-consensus-package ./internal/chain`, `make public-bft-production-driver-check`, `make consensus-production-package-check` | private candidate proof only; completed production phases local only | no | a current-control-plane deployment, rebound rehearsal, approval, and one live rollback-proven transaction remain absent | Deploy only the ordinary control plane and rerun the non-mutating rehearsal before generating a fresh unapproved packet |
| BFT state migration / ABCI | Deterministic YNXT migration anchor; CometBFT v0.38.23 ABCI 2.0; signed execution; validator key binding; four homes/genesis; production candidate package; quorum/fault/restart verification | fresh 14-account migration and four-role package remotely exercised through quorum, signed transfer, fault recovery, and rollback; exact-release read-only production rehearsal passed at `b638de55c4cd`; every final-snapshot-bound production forward/reverse phase is fixture/four-role-dry-run verified but not remotely executed | `internal/chain/consensus_migration.go`, `internal/consensus`, `cmd/ynx-abci`, `cmd/ynx-consensus-package`, `scripts/deploy/deploy-consensus-candidate.sh`, `scripts/ops/public-bft-cutover-transaction.sh`, `scripts/ops/public-bft-freeze-rehearsal-transaction.sh`, `scripts/ops/public-bft-production-driver.sh`, `scripts/ops/remote`, `scripts/verify` | local BFT suite, remote private-candidate evidence, `make public-bft-cutover-transaction-check`, `make public-bft-freeze-rehearsal-transaction-check`, `make public-bft-production-recovery-check`, `make public-bft-production-driver-check` | private candidate rolled back; read-only production rehearsal passed; completed control plane local only | no | deploy the complete control plane, rerun read-only rehearsal, obtain explicit transaction approval, then gather remote backup/freeze/pause/snapshot/candidate/dependency/ingress/rollback evidence | Deploy the ordinary control plane and rerun the exact-release read-only rehearsal; do not execute a live cutover without independent approval |
| Node restart and follower convergence | Restart devnet, durable ABCI application, and a stopped CometBFT validator without changing the common committed result; keep authoritative followers converged | local and remote candidate 3-of-4 progress/recovery verified; gzip authenticated replication and bounded timeout are deployed; all three authoritative followers matched height `80608` and hash `efabca1c...bf56` after current release deployment | `internal/consensus/state.go`, `internal/api/server.go`, `cmd/ynx-chaind/replication.go`, `scripts/verify/consensus-quorum-check.sh`, `scripts/verify/replication-compression-check.sh` | quorum/fault tests, authenticated gzip/HMAC/decompressed-bound tests, `make verify-testnet` | authoritative convergence deployed and verified; candidate remains nonpersistent | operator-controlled authoritative convergence only | persistent/public BFT consensus is not approved | Preserve bounded lag and common fixed-height checks before every transaction phase |
| RPC | Health/status/node identity/blocks/accounts/txs/validators plus validator heartbeat, peer observation, and peer sync APIs | authoritative public RPC deployed with exact build, chain `6423`, height growth, four-role peer evidence, blocks/accounts/txs/validators | `internal/api` | `make smoke-test`, `make validator-peer-readiness-check`, `make verify-testnet`, `make preflight` | authoritative yes; BFT Gateway no | operator-controlled public proof yes; independent proof no | public BFT Gateway route absent | Revalidate the same RPC contract during any independently approved transition |
| BFT compatibility Gateway and cutover transaction | Translate CometBFT/ABCI committed state into existing YNX RPC shapes and refuse public cutover until separately authorized | all fifteen capabilities are private-candidate proven; readiness remains default-false; full-cutover and freeze-only transactions exist; every production forward/reverse phase fails closed with exact approval/input/build/migration/signer/ingress binding and automatic rollback thresholds | `internal/bftgateway`, `internal/consensus`, `internal/chain/bounded_contract.go`, `internal/mutationfreeze`, `cmd/ynx-bft-gatewayd`, `scripts/ops/public-bft-cutover-transaction.sh`, `scripts/ops/public-bft-freeze-rehearsal-transaction.sh`, `scripts/ops/public-bft-production-driver.sh`, `scripts/ops/remote/public-bft-dependencies.sh`, `scripts/ops/remote/public-bft-ingress.sh`, `scripts/verify/public-bft-candidate-binding.mjs`, `scripts/verify/validate-public-bft-dependency-continuity.mjs`, `scripts/verify/validate-public-bft-public-continuity.mjs` | focused BFT checks, private candidate evidence, ten transaction failure injections, `make public-bft-production-driver-check`, live read-only rehearsal at `0ee044bd7d78` | completed control plane deployed in authoritative release; candidate absent; no transaction mutation phase executed | no | custody/recovery, independent approval, remote rollback evidence, and public BFT proof remain | Keep the exact-commit packet unapproved until independent review; do not execute ingress/cutover without its separate approval |
| EVM RPC | chainId, block, balance, tx, receipt, persisted logs, filtered log query subset, contract deployment event logs, source-analyzer `eth_call`, and the already bounded verified execution subset | WIP safely closed: authoritative public EVM reports `0x1917` and current build; bounded execution remains a limited subset, with private BFT receipt/log proof only | `internal/chain`, `internal/api`, `internal/chain/evm_static.go`, `internal/chain/keccak.go`, `contracts/devtools/SampleEVMWriteCounter.sol`, `artifacts/ynx-selector-metadata.json` | `go test ./internal/api`, `make smoke-test`, `make exchange-integration-check`, `make contract-tooling-check`, remote smoke | authoritative EVM yes; BFT EVM no | chain ID/current build operator proof; no public BFT verifier proof | not a full arbitrary EVM execution engine; BFT route not public | Do not expand bounded opcode/Counter/Hardhat coverage now; reverify existing behavior only during an approved transition |
| Transaction broadcast | Legacy devnet transfer, bounded calls, canonical secp256k1-signed YNXT transfers through CometBFT RPC, and Gateway HTTP broadcast/lookup/history with four-node convergence evidence | direct candidate transfer `E3A6D8...5548` committed at height `16277`; Gateway transfer `0x33be8038...68ec0` committed at height `16356` and matched lookup/history; signing key remained local | `internal/chain`, `internal/api`, `internal/consensus`, `internal/bftgateway`, `cmd/ynx-consensus-tx`, `scripts/verify/consensus-candidate-signed-tx-drill.sh` | local signed-transfer/Gateway tests plus remote signed-tx and Gateway evidence | candidate proof only; rolled back | no | public CometBFT Gateway is not active and dependent service transitions are not migrated | Reprove with a fresh nonce after public cutover while preserving key custody |
| Transaction receipt | Local devnet plus BFT receipts derived from Comet `/tx`, block membership/index, gas and cumulative `/block_results`, with AppHash-persisted bounded IDE execution evidence | remote bounded IDE deploy/call receipt matched direct Comet height `41098`, index `0`, block hash, gas/cumulative gas `1`, address boundaries, encoded result, write and log evidence on four applications | `internal/api/server.go`, `internal/consensus/ide_application.go`, `internal/bftgateway/evm.go` | `make bft-evm-receipt-check`, `make bft-ide-contract-check`, remote direct-evidence assertions | complete temporary candidate proof; removed | no | persistent/public BFT route not active | Include receipt continuity and current-HEAD identity in the approval-gated public transition drill |
| Logs / events | Local logs plus AppHash-persisted bounded BFT IDE LOG evidence, real receipt bloom, and retained block/address/topic filtering | remote candidate committed one real `CountChanged` log; four apps agreed byte-for-byte, nonzero bloom and filtered result matched direct Comet block/tx evidence, and invalid calls added no logs | `internal/chain`, `internal/consensus/ide_application.go`, `internal/bftgateway/evm.go` | `make bft-evm-receipt-check`, `make bft-ide-contract-check`, remote filter/four-app assertions | complete temporary candidate proof; removed | no | persistent/public BFT route not active | Recheck receipt/log indexing and Explorer continuity during the approval-gated public transition drill |
| Balance / account query | Native and EVM balance/account query | local verified / not remote deployed | `internal/chain`, `internal/api` | `make smoke-test` | no | no | remote deploy blocked | Public account proof |
| Faucet | Backend daemon, rate limits, request log, authoritative rollback mode, process-local canonical BFT signing, serialized nonce selection, and committed response verification | authoritative mode publicly deployed under the current replication model; BFT mode race-tested locally and verified through a remote candidate at height `16234`; temporary components removed | `internal/faucet`, `cmd/ynx-faucetd`, `internal/bftgateway` | `go test -race ./internal/faucet`, `make faucet-check`, `make bft-gateway-check`, remote candidate Faucet/lookup/history/four-signer evidence | authoritative public service plus temporary BFT candidate proof; BFT mode not active | no public BFT proof | a dedicated production Faucet account/key ceremony, migration funding, persistent BFT deployment, recovery/rotation, and public proof remain incomplete | Create and fund the dedicated Faucet custody role during the final fresh migration, then deploy BFT mode with recovery/rotation evidence and public rate-limit/funding proof |
| Indexer | Persist block/tx index, migration-height bootstrap, restart resume, parent continuity, and pruned-history protection | authoritative service publicly active; schema-v2 earliest-boundary bootstrap and BFT Faucet transaction indexing/restart remotely candidate-verified from height `16166` through live height `16295`; temporary stack removed | `internal/indexer`, `cmd/ynx-indexerd` | `go test -race ./internal/indexer`, `make indexer-check`, `make smoke-test`, remote empty-store/resume/transaction/lag evidence | authoritative public service plus temporary BFT candidate proof; BFT source not active | no public BFT proof | public cutover and long-duration pruning/rebuild operations are not yet proven | Include Indexer backup/rebuild and cross-region lag thresholds in the final approval-gated BFT cutover drill |
| Explorer | Real RPC/indexer-backed explorer API and responsive operational UI; live stream, search/detail, validators/resources, wallet metadata, and dual-format account display/search | release `97ed0c645bd2` is public; operator-controlled Singapore-routed proof resolved `ynx10e...hn80` and `0x7e5f...5bdf` to the same account and returned both `addressFormats`; local Explorer tests/smoke pass | `internal/explorer`, `cmd/ynx-explorerd`, `internal/accountaddress` | `go test ./...`, `make explorer-check`, `make address-codec-check`, `make verify-testnet`, public alias/account/search checks | authoritative public release yes | operator-controlled dual-address proof; no independent public or BFT proof | broader TRON-class analytics, independent public proof, and public BFT cutover are incomplete | Preserve real-data-only behavior; add website address tooling now that the chain feature is public, then re-run Explorer at any future release |
| AI Gateway | Independent provider-backed daemon; authenticated SSE; redacted access audit; authoritative rollback mode; process-local BFT signer; canonical permission/proposal/approval/rejection actions; query/list; block-time append-only audit; signer/subject/scope binding; serialized nonce and response verification | authoritative public daemon and AI permission/proposal/approval/lookup/list proof pass on the current release; private BFT candidate transitions passed at heights `16324-16326`; authenticated SSE currently fails because the configured provider returns HTTP 429 | `internal/aigateway`, `cmd/ynx-ai-gatewayd`, `internal/consensus`, `internal/bftgateway`, `internal/chain`, `internal/api` | `go test -race ./internal/consensus ./internal/bftgateway ./internal/aigateway`, `make ai-gateway-check`, `make bft-ai-action-check`, `make remote-smoke-test` | authoritative mode deployed; BFT mode not active | partial: action governance yes, generation no | provider quota/capacity, production AI signer/access custody, persistent BFT routing, recovery/rotation, and public SSE proof remain incomplete | Restore provider capacity without changing the truthful 429/502 boundary, then include dedicated signer custody and repeat SSE/action proof during final cutover |
| Pay API | Independent merchant daemon with authoritative rollback mode and process-local signer BFT mode; API-key auth; merchant binding; canonical intent/invoice/refund/webhook actions; deterministic ABCI persistence/AppHash; append-only events; global idempotency; cumulative refund bounds; environment-only webhook HMAC; explicit Pay resource accounting; committed evidence verification; and existing ingress/ops/public-proof wiring | the current authoritative release passed authenticated public intent/invoice exact replay, changed-input HTTP 400 rejection, webhook replay/audit lookup, and event audit-hash proof; BFT mode remains private-candidate verified only | `internal/paygateway`, `cmd/ynx-payd`, `internal/consensus`, `internal/bftgateway`, `internal/chain`, `internal/api`, `scripts/verify/pay-api-check.sh`, `scripts/verify/bft-pay-action-check.sh`, `scripts/verify/remote-smoke-test.mjs` | `go test -race ./internal/consensus ./internal/bftgateway ./internal/paygateway`, `make pay-api-check`, `make bft-pay-action-check`, `make remote-smoke-test` | authoritative mode deployed; BFT mode not active | yes for authoritative Pay surface | persistent BFT deployment and production merchant/signer/webhook custody/recovery remain incomplete | Preserve exact-replay/conflict semantics and repeat authenticated Pay proof after any BFT cutover |
| Trust API | Independent Trust/Chain Law daemon with authoritative full-surface mode and process-local signer BFT mode; authenticated governance request/review/reject, advisory labels, deterministic evidence JSON/PDF, purpose-limited tracking review, label/request/tracking-linked appeal/resolve, Request Validity rules, native YNXT protection, advisory false-positive correction, append-only transparency, deterministic ABCI/AppHash, Trust resource charging, and committed evidence verification | complete BFT surface passed local race/restart/hash/export tests and fresh remote proof: nine actions, rejected no-state evidence without fee, identical four-application digest/AppHash, four-signer evidence at height `28911`, promoted Gateway capability, and successful rollback; authoritative public Trust remains active | `internal/trustgateway`, `cmd/ynx-trustd`, `internal/consensus`, `internal/bftgateway`, `internal/chain`, `internal/api`, `scripts/verify/trust-api-check.sh`, `scripts/verify/bft-trust-action-check.sh` | `go test -race ./internal/consensus ./internal/bftgateway ./internal/trustgateway`, `make trust-api-check`, `make bft-trust-action-check`, `make request-validity-check`, `make trust-appeal-check`, `make transparency-report-check`, `make preflight`, remote four-application/four-signer/rollback evidence | temporary complete BFT candidate proof only; not currently active | no | persistent BFT routing, production Trust signer custody/recovery, and public authenticated BFT proof remain incomplete | Include dedicated Trust signer custody/recovery and authenticated public proof in the final cutover |
| Lot lineage | Pro-rata lot flow tracing | local verified / not remote deployed | `internal/chain/devnet.go` | `go test ./internal/chain` | no | no | remote deploy blocked | Remote Trust proof |
| Pro-rata taint tracking | Lot-based risk lineage without freezing funds and with non-conclusive low-confidence handling | local verified / not remote deployed | `internal/chain/devnet.go` | `go test ./internal/chain` | no | no | remote deploy blocked | Remote Trust proof |
| Anti-Illegal Request Engine | Intake and reject private-secret, signature-bypass, hidden-record, fake-risk, unsupported-Trust-conclusion, AI-auto-punishment, overbroad, and native YNXT direct-freeze/direct-transfer requests with audit transparency | the current authoritative release passed public illegal-request rejection, native YNXT protection, lookup/review/manual-reject, and transparency recording | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make anti-illegal-request-check`, `make remote-smoke-test` | yes, authoritative mode | yes, authoritative mode | BFT route is not publicly active | Repeat the same fail-closed classifications after any approved BFT transition |
| Request Validity Standard | Classify valid/evidence-missing/out-of-scope asset-boundary/overbroad/review/user-notice/rejected with inspectable rule IDs and persisted request lookup/review state | the current authoritative release publicly exposed all 11 required rules and passed illegal, review, notice, manual-reject, and overbroad tracking classifications | `internal/chain/devnet.go`, `internal/api/server.go`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make request-validity-check`, `make remote-smoke-test` | yes, authoritative mode | yes, authoritative mode | BFT route is not publicly active | Repeat rules and transition evidence after any approved BFT transition |
| Appeal / Dispute | Open, read, resolve appeals with reviewer, decision, status, transparency entry IDs, Trust false-positive correction records, required linkage to an existing governance request or Trust label so orphaned appeals cannot create transparency entries, and public-proof gate coverage that requires post-resolution Trust evidence correction summary | the current authoritative release passed public appeal open/lookup/resolve and correction-evidence risk summary proof | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/public-proof-evidence-check.mjs` | `go test ./...`, `make trust-appeal-check`, `make public-proof-evidence-check`, `make remote-smoke-test` | yes, authoritative mode | yes, authoritative mode | BFT route is not publicly active | Repeat appeal and correction evidence after any approved BFT transition |
| False Positive correction | Appeal decisions can remove/reduce labels with corrective Trust labels, and remote public proof must show `riskSummary.correctionLabelCount` plus advisory-only appeal metadata after resolution | deployed authoritative behavior; exact-release public appeal/correction proof passed | `internal/chain/devnet.go`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/public-proof-evidence-check.mjs` | `make trust-appeal-check`, `make public-proof-evidence-check`, `make verify-testnet` | yes, authoritative mode | yes, operator-controlled route | BFT route is not publicly active | Repeat correction proof after any approved BFT transition |
| Transparency Report | Generate persisted report entries and counts for request intake, review, manual rejection, appeal open, and appeal resolution | release `c9324fbfc464` passed public initial/final report checks; the latest smoke grew persisted entries from `168` to `180` with rejected, appeal, and review counts updated | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `go test ./...`, `make transparency-report-check`, `make verify-testnet` | yes, authoritative mode | yes, operator-controlled route | BFT route is not publicly active | Repeat count and audit-link integrity after any approved BFT transition |
| Native YNXT no-hidden-freeze checks | Reject native YNXT direct freeze/transfer/seize/confiscate/blacklist requests, reject Trust labels with native-freeze asset effects, and statically scan runtime code for hidden native YNXT freeze hooks | local static/behavior checks and exact-release remote rejection proof pass | `internal/chain/devnet.go`, `scripts/verify/native-ynxt-no-hidden-freeze-check.sh`, `scripts/verify/remote-smoke-test.mjs` | `make native-ynxt-no-hidden-freeze-check`, `make preflight`, `make verify-testnet` | yes, authoritative mode | yes, operator-controlled route | BFT route is not publicly active | Repeat native-protection proof after any approved BFT transition |
| Anti-Unreasonable Tracking Policy | Purpose logging, evidence checks, minimum necessary data, sensitive review, confidence/expiry/appeal path | local checks and exact-release remote tracking review proof pass | `internal/chain`, `internal/api`, `scripts/verify/remote-smoke-test.mjs` | `make anti-unreasonable-tracking-check`, `make preflight`, `make verify-testnet` | yes, authoritative mode | yes, operator-controlled route | BFT route is not publicly active | Repeat tracking review after any approved BFT transition |
| Resource Market | Independent daemon with authoritative rollback mode and process-local signer BFT mode; API-key auth; bounded/redacted audit; signed delegation/rental; policy-bound quote commitments; deterministic AppHash; liquid-plus-staked conservation; provider/protocol settlement; Resource events/idempotency; committed-response verification; existing ops/public-proof wiring | full local race/restart/overflow suite plus fresh remote candidate proof: valid delegation/rental, invalid no-state provider, exact replay, changed-input conflict, four-app digest/AppHash equality, four-signer evidence, capability promotion, cleanup, rollback, and public authoritative health | `internal/resourcegateway`, `cmd/ynx-resourced`, `internal/consensus`, `internal/bftgateway`, `internal/chain`, `scripts/verify/bft-resource-action-check.sh` | `go test ./...`, `make bft-resource-action-check`, `make preflight`, remote four-application/four-signer/rollback evidence | temporary complete BFT candidate proof only; not currently active | no | persistent BFT routing, production signer custody/recovery, public authenticated BFT proof, and full cutover remain incomplete | Include Resource signer custody/recovery and authenticated public proof in the final cutover |
| Resource rental / delegation | Signer-bound delegation converts liquid YNXT to beneficiary stake; rental binds policy/quote/max price/expiry/provider, charges fixed fee, settles quote value, reduces only used counters, and persists signed records | remote candidate committed delegation `7aa8a41ceef2cad269cecbb4` and rental `1fa8fe971d0fe8dc6cde5a68`; invalid provider consumed no nonce/fee; exact replay and conflicts behaved correctly on all four apps | `internal/consensus/resource_action.go`, `internal/consensus/resource_application.go`, `internal/consensus/resource_state.go`, `internal/bftgateway/resource.go`, `internal/resourcegateway` | `make bft-resource-action-check`, remote four-app state digest/AppHash and four-signer evidence | temporary candidate proof only | no | persistent/public deployment and production custody remain incomplete | Re-run with final cutover signer and public authenticated evidence after custody approval |
| Resource income records | Provider and protocol treasury income persist with policy/rental/transaction linkage, audit hashes, and exact settlement/analytics reconciliation | remote candidate proved `6 = 4 provider + 2 protocol`, two income records, treasury delta, analytics, identical digest `f885cb50...cefde4`, and AppHash `400d460c...b14f5e` on four apps | `internal/consensus/resource_state.go`, `internal/consensus/resource_application.go`, `internal/bftgateway/resource.go` | `make bft-resource-action-check`, remote four-app equality and post-promotion evidence | temporary candidate proof only | no | persistent/public income and analytics evidence pending final cutover | Re-prove balances/income/analytics on authenticated public endpoints after cutover approval |
| IDE compile / deploy / verify | Existing local compiler/verifier plus canonical signed BFT deploy/call restricted to two already-proven pinned bounded artifacts; deterministic identity/address/storage/SSTORE/LOG/receipt/audit/idempotency in AppHash; ABCI/Gateway contract/call/verifier/receipt/log queries | local race/full tests plus fresh remote candidate proof passed: deploy `0x3c41...6f69`, call `0xfdba...8532`, storage `7->12`, replay/conflict/unsupported `422`, owner `998/0/2`, supply conserved, four-app AppHash/digest equality, four-signer height `41389`, cleanup and rollback | `internal/api`, `internal/chain/bounded_contract.go`, `internal/consensus/ide_action.go`, `internal/consensus/ide_application.go`, `internal/bftgateway/ide.go`, `internal/bftgateway/evm.go` | `go test ./...`, `make bft-ide-contract-check`, `make bft-evm-receipt-check`, remote direct-Comet/four-app/four-signer evidence | complete temporary candidate proof; removed | no | not full arbitrary EVM; persistent/public route and production IDE signer/client custody remain | Keep bounded scope fixed; include production custody, API client packaging, and public route evidence in the cutover plan |
| SDK | Package-ready dependency-free JS/Python clients with REST status, EVM JSON-RPC, timeout/error handling, chain snapshot validation, and reversible address conversion | local JS/Python tests pass against shared Go-compatible fixtures; `toYNXAddress`/`toEVMAddress` and Python equivalents reject malformed checksum/HRP/case/payload; prior public read-only chain compatibility remains valid but package registries are not published | `sdk`, `testdata/address-vectors.json`, `scripts/verify/sdk-check.sh`, `scripts/verify/sdk-remote-check.sh` | `make sdk-check`, `make address-codec-check`, `make sdk-remote-check` | chain compatibility proven; address helpers local/package-ready | no registry or independent consumer proof | registry publication, signed versioning, and independent consumer proof remain | Keep fixtures synchronized and publish only after owner review and signed versioning |
| Wallet / MetaMask integration | Add-chain metadata and EVM chainId checks; EVM-compatible signed accounts; reversible checksummed YNX address alias; owner-controlled key handover | release `97ed0c645bd2` deploys codec, CLI, REST normalization, account-key output, Explorer, and SDK helpers; four roles match manifest/binary checksums; operator-controlled public proof confirms one `ynx1...`/`0x...` account identity; MetaMask remains canonical `0x...` | `internal/accountaddress`, `internal/consensus`, `internal/api`, `cmd/ynx-consensus-tx`, `cmd/ynx-consensus-account-key`, `sdk`, `internal/explorer`, `docs/ecosystem/ADDRESS_FORMATS.md` | `make address-codec-check`, `make wallet-integration-check`, `make consensus-signed-transfer-check`, `make explorer-check`, `make verify-testnet`, public dual-lookup proof | yes, authoritative release `97ed0c645bd2` | operator-controlled public dual-address proof | offline recovery, owner handover, rotation, role separation, remote signer install, independent review, and wallet-default support remain incomplete | Add the now-proven converter to the website without claiming MetaMask accepts `ynx1...`; continue custody work separately |
| Chainlist package | Chain metadata package | local verified / not remote deployed | `chain-metadata` | `make chainlist-package` | no | no | public endpoints not new chain | Submit after public proof |
| Website remote API integration | Website should read real public API and preserve honest current/target boundaries | intended repo `JiahaoAlbus/YNX-Chain-website` is bound to Vercel project `ynx-web4-website-new`; Git deployment `dpl_22cxGGiFypkzebGXhibDk7eaWG6q` from `main@7a638b0` is `READY`; production renders a zero-image CSS chain scene and live chain `6423` / YNXT / EVM `0x1917` / four-role data; deep links pass | `/Users/huangjiahao/Desktop/YNX-Chain-website`, `api/network/status.js`, `server/network-status.mjs` | `npm test`, `npm run build`, `vercel build --prod`, desktop/mobile browser QA, production route/API checks | yes | operator-controlled production proof yes | intermittent public TLS connection latency/timeouts; independent public vantage absent | Preserve real-data/error boundaries and add ecosystem modules only after their code/deployment proof exists |
| Remote deployment | Strict-SSH four-host deployment, release/checksum proof, scoped backup/rollback, authoritative-service preservation, compressed replication, bounded connection retry, per-transaction SSH/SCP multiplexing, mode/checksum/cleanup-hardened release transport, operator-controlled cross-region proof routing, and approval-gated BFT transition tooling | runtime `c9324fbfc464` deployed on all four roles; manifest SHA `3751ce0a...a3da` and daemon SHA `f6709e58...93a1` match; fixed height `85622`/hash `9c022089...b702` converged; exact-release read-only rehearsal passed; custody-review validators and complete transaction control plane are shipped but unexecuted | `scripts/deploy`, `scripts/ops`, `scripts/verify`, `internal/replication`, `internal/mutationfreeze` | `make verify-testnet`, `make remote-smoke-transport-check`, `ENV_FILE=.env.deploy make remote-smoke-test-via-sg`, `make deploy-connection-retry-check`, `make consensus-production-package-check`, `make public-bft-production-rehearsal` | authoritative `c9324fbfc464` live; BFT candidate absent; no transaction mutation phase executed | operator-controlled cross-region exact-live-release proof only; no independent third-party or public BFT proof | custody/recovery, independent approval, live transaction rollback evidence, and public BFT proof remain | Keep review packets unapproved until independent custody/recovery and bounded-freeze review complete |
| Backup / rollback | Scoped release predeploy backup plus separately approval-gated transaction backup/freeze/pause/final-snapshot/recovery, candidate/dependency/ingress rollback, and public continuity thresholds | scoped predeploy backups completed on all four roles before `c9324fbfc464` (about `9.0 MB` primary, `4.9 MB` followers); transaction phases remain unexecuted; every rollback path is implemented and fixture/dry-run failure-tested | `scripts/deploy`, `scripts/ops` | `make deploy-dry-run`, `make ops-check`, `make public-bft-production-driver-check`, `make consensus-production-package-check` | predeploy backup yes; BFT transaction phases no | no production transaction rollback proof | remote transaction rollback evidence and independent public proof remain absent | Execute no transaction phase without exact independent approval; preserve automatic rollback permission once approved |
| Public proof | Generate remote public proof only from real endpoints, including proof metadata and route provenance, current release/checksum identity, four-role evidence, chain/validator/EVM/services, Chain Law, Pay, Trust, Resource, IDE, AI, Web4, and mutable proof completeness; fail closed on missing, stale, inconsistent, old-chain, skipped, or failed checks | Singapore-routed exact-`c9324fbfc464` proof passed release/checksum, RPC/EVM/REST/gRPC, validators, Faucet/Explorer, all Pay mutations, AI action governance, Trust/Chain Law/appeal/transparency mutations (`168` to `180`), Resource flows, bounded IDE compile, and current-chain Web4 binding; strict status failed only on provider-backed AI SSE upstream HTTP `429`/YNX `502` | `scripts/package/public-proof.sh`, `scripts/verify/remote-smoke-test.mjs`, `scripts/verify/lib/remote-smoke-transport.mjs`, `scripts/verify/remote-smoke-via-sg.sh`, `scripts/verify/public-proof-evidence-check.mjs` | `make remote-smoke-transport-check`, `ENV_FILE=.env.deploy make remote-smoke-test-via-sg`, `make verify-testnet`, `make public-proof-evidence-check`, `make public-proof-package-check` | authoritative exact release and Web4 binding proven; BFT route not public | partial: operator-controlled route only; strict proof failed | successful provider-backed AI generation, independent public vantage, and public BFT remain | Preserve the strict gate; restore provider capacity, then repeat exact-release proof from an independent public vantage |
| Grant package | Grant docs/package generation | local verified / not remote deployed | `docs/grants`, `scripts/package` | `make grant-package` | no | no | public proof missing | Regenerate after remote proof |
| Ecosystem package | Ecosystem submission package | local verified / not remote deployed | `docs/ecosystem`, `scripts/package` | `make ecosystem-package` | no | no | public proof missing | Regenerate after remote proof |
| Exchange readiness | Exchange listing readiness package | local verified / not remote deployed | `docs/exchange-listing`, `scripts/package` | `make exchange-package` | no | no | public proof missing | Regenerate after remote proof |
| Mainnet readiness | Mainnet checklist and gates | readiness only / not mainnet ready | `docs/mainnet-readiness`, `scripts/package` | `make mainnet-readiness` | no | no | audit/legal/validator/public proof missing | Keep mainnet as not ready |
