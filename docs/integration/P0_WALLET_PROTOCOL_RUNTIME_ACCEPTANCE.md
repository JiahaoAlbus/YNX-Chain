# P0 Wallet Protocol runtime acceptance and execution lease

Central Integration accepts the Wallet/Auth Origin Binding candidate for one controlled runtime transaction. This is not `integratedCentral`, public deployment, public CORS, installed-client, signing or store acceptance.

The immutable behavior contract remains `66003e76e804da16d472255efde50cb879055b96` (PR #104). Origin Binding provenance remains `5231e7509d6218bbbf25029cf73d456992cc37bd`; P0-003 accepts runtime compatibility commit `49e30d999e9a9cbdd2c565021009f2cab0dc125c` and verifier head `30ba2a0c41d7b070dec56f37e39653e21a780ead`. The exact runtime archive passed 119/119, focused 4/4 and diff-check. The compatibility fix accepts both historical wallet-auth-v1 state shapes, validates `accountPublicKey` against `account` when present, and grants no authority: every v1 session remains `SESSION_RETIRED`.

The candidate branch diverges historically from the current control plane. It must not be merged as a whole tree. The deployment owner must materialize only the exact `packages/wallet-auth` subtree at `49e30d99…`, preserve `66003e76…` and `5231e750…` provenance plus `30ba2a0c…` verifier tooling, and leave every other repository and product path unchanged.

The original acceptance commit `401eee474ac188fed1fc1e6089d6a0f9e1250651` remains superseded. P0-003 is based on authoritative commit `15d5ae2bc5278088fd2168cdbcb3746066bc851c` and preserves all later Finance, Explorer/Monitor and Data Fabric control-plane additions.

The deployment registry is frozen at `release/integration/p0-wallet-protocol-deployment-registry-v4.json`, Git blob `f11f73bb10943e6ae4e3ecd9f4e5bcedfb7b4701`, SHA-256 `ae156b317b9a97bfd42397cca634021deefe10ffb009102899e24276d8721e31`, 20,037 bytes. It preserves Calendar, Developer, Exchange, Finance, Quant and Shop as enabled, but gives each `webOrigins: []` because no direct Origin approval exists. Only Social is newly approved/enabled for browser CORS, with exact callback `ynx-social://com.ynx.social` and exact Origin `https://social.ynxweb4.com`. No Origin is inferred from a callback.

The execution lease is `release/integration/p0-wallet-protocol-execution-lease.json`. Lease `P0-WALLET-CONNECTIVITY-2026-08-runtime-lease-20260820T095840Z` supersedes the 092717Z lease, expires at `2026-08-20T11:58:40Z`, and remains restricted to one rollback-protected Wallet/Auth Gateway runtime transaction. Before any mutation, exact `49e30d99…` must boot read-only against a copy of actual 18445 state and prove both historical v1 shapes load only as `SESSION_RETIRED`. Wallet UI/platform, other products, Chain Core, website changes and unrelated restarts remain excluded.

P0-003 runtime execution is complete and verified by `b30775951dcea09bf6b72aeb4806f47ff81b3bef` (blob `6e426777…`). Rollback restored source `6ed04310…` and state `e05306a6…` exactly before final activation. Public `/health`, `/ready`, `/version`, registered Social OPTIONS, negative zero-mutation checks, lifecycle, controlled restart, replay rejection, revoke and post-revoke rejection all pass against source `49e30d99…`; therefore runtime `deployedPublic`, public CORS and public lifecycle are true.

This does not prove an installed client, standard injected wallet, account/sign/send/transaction, central integration, Website publication, production signing, store release or aggregate readiness. Installed-client lease `…T101002Z` closed fail-closed at preflight with `NO_ELIGIBLE_SIGNED_INSTALLED_CLIENT`: the only Wallet-named macOS bundle is an ad-hoc Edge shortcut to `127.0.0.1:4173`, ADB exposes only an emulator, and no physical iOS target exists. No App was launched and no approve/reject/callback/session action or cleanup was started. Exact Owner evidence `2d6c9aa354a459b224b79c11c69aab670336a41d` / blob `8974960c…` is frozen byte-for-byte at `release/integration/p0-wallet-protocol-installed-client-preflight-evidence.json`; the exact-head suite passes 122/122. Public runtime verification remains intact and no rollback occurred. Website handoff remains ready for Owner review but unpublished.
