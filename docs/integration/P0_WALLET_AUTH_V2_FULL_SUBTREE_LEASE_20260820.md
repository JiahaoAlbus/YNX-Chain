# P0 Wallet/Auth v2 full-subtree replacement lease

The seven-file v2 lease failed closed before deployment because public source `49e30d999` lacks its imported Product Session dependency closure. It is superseded and cannot be reused. Production stayed active on `wallet-auth-49e30d99`; no backup or mutation started.

Integration independently reproduced the exact replacement closure: full `packages/wallet-auth` tree `4c544d2e2ddb63caef536ea67c8f27b45044fd89` at source `6cf3ef845202bd879ed94515a71b323dd2fc9e14`, 160 Git entries. The frozen inventory is 18,241 bytes with SHA-256 `a96ba130a236459e6a3352d6c14be91c7c6bd0945b2eb019b5e65811ef3137b0`; the deterministic prefixed tar.gz is 462,902 bytes with SHA-256 `fac046697f8cc3902976764f959d69f83e13067c37abfe2b37f9b8adf7ba2da0`.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-full-subtree-lease-20260820T121054Z` was revoked before start. Its required copied-`49e` no-network cold start ran under Node `v22.23.1` and exited nonzero with `ERR_MODULE_NOT_FOUND`: `@noble/hashes` was not present in the copied production `node_modules`, and the import originated from `packages/wallet-auth/src/canonical.js`. The exact source subtree therefore was not a complete reproducible runtime artifact. No backup, production activation, service restart, Caddy change or production mutation occurred. `/opt/ynx/wallet-auth` still targets `wallet-auth-49e30d99`, the service remains active, and the exact v1 state SHA-256 remains `93b18583abac95d040e5feca6c1db778f9193ac3471c5415d38e411b60ac63cb`.

No replacement lease may be issued until an immutable owner handoff freezes a reproducible dependency closure or complete deterministic runtime artifact. It must include exact `@noble/hashes@2.2.0`, `@noble/curves@2.2.0`, every transitive dependency, file inventory and archive SHA-256/bytes, an SBOM, and a copied-`49e` no-network cold-start pass. Integration will not run `npm install` or copy unreviewed local `node_modules` to fill the gap.

Owner follow-up narrowed the lock graph to exactly `@noble/curves@2.2.0` plus its sole runtime dependency `@noble/hashes@2.2.0`; hashes has no runtime dependencies. An isolated offline install contained 2 packages, 212 files and 3,207,168 unpacked bytes with sorted manifest SHA-256 `a3ac48aac90383bac3e172528956f75ec404b35965c915bd684d6ab9bb640453`. Its isolated daemon cold-start passed `/version`, Finance-origin OPTIONS, and invalid-v2 fail-closed probes. This is useful direct engineering evidence, but it is not yet a deployable immutable artifact: no dependency archive path/bytes/SHA or SBOM SHA was handed off, and the copied-`49e` runtime plus artifact cold-start has not passed. The lease therefore remains revoked.

Integration subsequently recovered and reverified the exact original npm cache tarball bytes, then froze a deterministic runtime artifact without running an install. The ready-to-extract archive is 665,983 bytes, SHA-256 `2a2f29218914580bce70dcc31852186223965b8cf1c5cd34c3cb66c7edf69a54`; it contains 211 package files only. CycloneDX SBOM SHA-256 is `b3588ecf29e24242c621509bb5ee34b9d4bca6ea8583fead01d14c5c3efbcfad`. The original curves and hashes tarballs retain exact package-lock SHA-512 integrity and SHA-256 `6daf47e557e070b0657eda25549466bae3d07df555de4f702363355e8fb92ec8` and `018b38bd7af36645fa0ece8f89eba21c828f3e4d219da5aacadd78bd0e654606`.

New single-use lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-20260820T122441Z` accepts only the exact source tree plus that exact runtime archive. It forbids `npm install`, network dependency resolution and unreviewed `node_modules`. Before any backup or activation, `p0-wallet-auth-v2-copied-runtime-cold-start.mjs` must pass against a marked copy of public `49e`, the exact v1 state SHA, exact registries and independent v2 state path. The prior failed lease remains revoked and cannot be reused.

That lease was superseded before start after a verifier-only schema defect was found: exact daemon `/version` returns the source at `build.sourceCommit`, and canonical failures return their code at `error.code`. No upload, install, switch, restart or production mutation occurred. Replacement lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v2-20260820T123316Z` binds the corrected paths and a committed schema self-test. All artifact identities and activation prerequisites are otherwise unchanged.

The schema-corrected lease was later consumed by a production rollback drill and failed transaction isolation. Candidate `6cf` temporarily wrote a newer v1 snapshot to the shared legacy state path, changing its SHA-256 from `ba386fb9e474ea0886c2e41184db7fac3fcf6aea6dd02f5fe47122a62d3a8c9e` to `d2a7dae9d66f0df7f6d231d3da91e26c0c5a91071494f2388bb7d4e29a12e599`. Rolled-back `49e` rejected that snapshot with `UNKNOWN_OR_MISSING_FIELD`. The candidate-mutated state was preserved, the exact pre-drill backup was atomically restored, and direct checks recovered exact `49e`, health/ready 200 and Social OPTIONS 204. Caddy, environment and other services were unchanged. No final activation is claimed.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v3-20260820T124935Z` replaces that consumed lease. With the service stopped, the operator must copy the exact shared v1 snapshot to a distinct candidate-v1 path, fsync the file and directory, and bind candidate `YNX_WALLET_GATEWAY_STATE_PATH` only to that copy. Candidate v2 state uses a third independent path. The shared legacy file must remain byte-exact at SHA-256 `ba386fb9e474ea0886c2e41184db7fac3fcf6aea6dd02f5fe47122a62d3a8c9e` before candidate start, after rollback drill and after any later activation. The new read-only state-isolation verifier rejects path or inode aliasing, symlinks, hardlinks, non-0600 modes and shared-state mutation.

Current public source, v1 state, Caddy, installed clients, product migrations and aggregate truth are unchanged. Public v2 lifecycle, deployment, integratedCentral, signing and store gates remain false.
