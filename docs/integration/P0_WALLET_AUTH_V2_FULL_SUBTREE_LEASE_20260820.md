# P0 Wallet/Auth v2 full-subtree replacement lease

The seven-file v2 lease failed closed before deployment because public source `49e30d999` lacks its imported Product Session dependency closure. It is superseded and cannot be reused. Production stayed active on `wallet-auth-49e30d99`; no backup or mutation started.

Integration independently reproduced the exact replacement closure: full `packages/wallet-auth` tree `4c544d2e2ddb63caef536ea67c8f27b45044fd89` at source `6cf3ef845202bd879ed94515a71b323dd2fc9e14`, 160 Git entries. The frozen inventory is 18,241 bytes with SHA-256 `a96ba130a236459e6a3352d6c14be91c7c6bd0945b2eb019b5e65811ef3137b0`; the deterministic prefixed tar.gz is 462,902 bytes with SHA-256 `fac046697f8cc3902976764f959d69f83e13067c37abfe2b37f9b8adf7ba2da0`.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-full-subtree-lease-20260820T121054Z` was revoked before start. Its required copied-`49e` no-network cold start ran under Node `v22.23.1` and exited nonzero with `ERR_MODULE_NOT_FOUND`: `@noble/hashes` was not present in the copied production `node_modules`, and the import originated from `packages/wallet-auth/src/canonical.js`. The exact source subtree therefore was not a complete reproducible runtime artifact. No backup, production activation, service restart, Caddy change or production mutation occurred. `/opt/ynx/wallet-auth` still targets `wallet-auth-49e30d99`, the service remains active, and the exact v1 state SHA-256 remains `93b18583abac95d040e5feca6c1db778f9193ac3471c5415d38e411b60ac63cb`.

No replacement lease may be issued until an immutable owner handoff freezes a reproducible dependency closure or complete deterministic runtime artifact. It must include exact `@noble/hashes@2.2.0`, `@noble/curves@2.2.0`, every transitive dependency, file inventory and archive SHA-256/bytes, an SBOM, and a copied-`49e` no-network cold-start pass. Integration will not run `npm install` or copy unreviewed local `node_modules` to fill the gap.

Owner follow-up narrowed the lock graph to exactly `@noble/curves@2.2.0` plus its sole runtime dependency `@noble/hashes@2.2.0`; hashes has no runtime dependencies. An isolated offline install contained 2 packages, 212 files and 3,207,168 unpacked bytes with sorted manifest SHA-256 `a3ac48aac90383bac3e172528956f75ec404b35965c915bd684d6ab9bb640453`. Its isolated daemon cold-start passed `/version`, Finance-origin OPTIONS, and invalid-v2 fail-closed probes. This is useful direct engineering evidence, but it is not yet a deployable immutable artifact: no dependency archive path/bytes/SHA or SBOM SHA was handed off, and the copied-`49e` runtime plus artifact cold-start has not passed. The lease therefore remains revoked.

Integration subsequently recovered and reverified the exact original npm cache tarball bytes, then froze a deterministic runtime artifact without running an install. The ready-to-extract archive is 665,983 bytes, SHA-256 `2a2f29218914580bce70dcc31852186223965b8cf1c5cd34c3cb66c7edf69a54`; it contains 211 package files only. CycloneDX SBOM SHA-256 is `b3588ecf29e24242c621509bb5ee34b9d4bca6ea8583fead01d14c5c3efbcfad`. The original curves and hashes tarballs retain exact package-lock SHA-512 integrity and SHA-256 `6daf47e557e070b0657eda25549466bae3d07df555de4f702363355e8fb92ec8` and `018b38bd7af36645fa0ece8f89eba21c828f3e4d219da5aacadd78bd0e654606`.

New single-use lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-20260820T122441Z` accepts only the exact source tree plus that exact runtime archive. It forbids `npm install`, network dependency resolution and unreviewed `node_modules`. Before any backup or activation, `p0-wallet-auth-v2-copied-runtime-cold-start.mjs` must pass against a marked copy of public `49e`, the exact v1 state SHA, exact registries and independent v2 state path. The prior failed lease remains revoked and cannot be reused.

That lease was superseded before start after a verifier-only schema defect was found: exact daemon `/version` returns the source at `build.sourceCommit`, and canonical failures return their code at `error.code`. No upload, install, switch, restart or production mutation occurred. Replacement lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v2-20260820T123316Z` binds the corrected paths and a committed schema self-test. All artifact identities and activation prerequisites are otherwise unchanged.

The schema-corrected lease was later consumed by a production rollback drill and failed transaction isolation. Candidate `6cf` temporarily wrote a newer v1 snapshot to the shared legacy state path, changing its SHA-256 from `ba386fb9e474ea0886c2e41184db7fac3fcf6aea6dd02f5fe47122a62d3a8c9e` to `d2a7dae9d66f0df7f6d231d3da91e26c0c5a91071494f2388bb7d4e29a12e599`. Rolled-back `49e` rejected that snapshot with `UNKNOWN_OR_MISSING_FIELD`. The candidate-mutated state was preserved, the exact pre-drill backup was atomically restored, and direct checks recovered exact `49e`, health/ready 200 and Social OPTIONS 204. Caddy, environment and other services were unchanged. No final activation is claimed.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v3-20260820T124935Z` replaces that consumed lease. With the service stopped, the operator must copy the exact shared v1 snapshot to a distinct candidate-v1 path, fsync the file and directory, and bind candidate `YNX_WALLET_GATEWAY_STATE_PATH` only to that copy. Candidate v2 state uses a third independent path. The shared legacy file must remain byte-exact at SHA-256 `ba386fb9e474ea0886c2e41184db7fac3fcf6aea6dd02f5fe47122a62d3a8c9e` before candidate start, after rollback drill and after any later activation. The new read-only state-isolation verifier rejects path or inode aliasing, symlinks, hardlinks, non-0600 modes and shared-state mutation.

The v3 drill was consumed and failed because systemd's main `EnvironmentFile=/etc/ynx/wallet-gateway.env` overrode same-name `Environment=` values in the candidate drop-in. Candidate code ran from the `6cf` symlink, but its effective v1 state and source/release/build identity remained old; only the new v2 variable applied. Shared v1 again changed to `d2a7…`, candidate-v1 correctly remained `ba386…`, and the exact-source wait timed out. The operator preserved candidate states, atomically restored shared v1 to `ba386…`, and directly recovered exact `49e`, health/ready 200 and Social OPTIONS 204. No final activation is claimed.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v4-20260820T130028Z` adds a later mode-0600 candidate EnvironmentFile. The systemd drop-in may only append that file after the base file. Before the production service may start, the effective-environment verifier runs a separate transient systemd probe and requires six exact candidate values: candidate-v1, candidate-v2, v2 registry, source commit, release and build time. It rejects an effective shared v1 path. Only after this gate may the existing state-isolation pre-switch and rollback transaction proceed.

V4 was superseded before start because those six values omitted `YNX_WALLET_GATEWAY_REGISTRY_PATH`. Exact candidate package `central-registry.json` is 13,319 bytes with SHA-256 `294119abb2f3e31dd6e1834440e2d0c7cd20dd11a2ba2a3849cee4b30ca7301c`, while the authoritative public v1 registry is 20,037 bytes with SHA-256 `ae156b317b9a97bfd42397cca634021deefe10ffb009102899e24276d8721e31`. Without an override, candidate v1 would silently use the package default and violate the accepted registry identity. No v4 EnvironmentFile, drop-in or candidate state was created, no service was restarted, and no production mutation occurred.

Lease `P0-WALLET-CONNECTIVITY-2026-08-wallet-auth-v2-runtime-artifact-lease-v5-20260820T130757Z` adds the seventh exact override: `YNX_WALLET_GATEWAY_REGISTRY_PATH=/etc/ynx/wallet/central-registry-ae156b31.json`. Before any production service start, the verifier requires that immutable copy to be a root-owned mode-0644 regular non-symlink with nlink 1, exactly 20,037 bytes and SHA-256 `ae156b317b9a97bfd42397cca634021deefe10ffb009102899e24276d8721e31`. The candidate EnvironmentFile must contain exactly seven unique overrides, and the transient systemd probe must read back the same seven effective values. The candidate package registry is explicitly forbidden as the effective production v1 registry.

V5 was consumed and failed closed during its pre-start verifier. The verifier ran as `ubuntu` and could not traverse `/etc/ynx` to `lstat` the root-owned registry, returning `EACCES`. The candidate process never started. The trap left no live candidate EnvironmentFile or drop-in, restored exact public `49e`, retained shared and candidate-v1 SHA-256 `ba386…`, and reverified health/ready 200 plus Social OPTIONS 204. V5 is non-reusable and no production mutation is claimed.

V6 moves neither permissions nor ACLs on `/etc/ynx`. It binds v1 to `/opt/ynx/wallet-auth-6cf3ef84/runtime-config/v1-central-registry.json` and v2 to `/opt/ynx/wallet-auth-6cf3ef84/packages/wallet-auth/product-session-registry.json`. Both are versioned immutable root-owned mode-0644 files beneath root-owned mode-0755 candidate parents. The signed verifier must be invoked exactly through `sudo -n node`, validates both registry hashes and all parent traversal facts, then runs `sudo -n -u ubuntu test -r` plus a no-start JSON parse/SHA read probe for both files. Only after that gate may the candidate service start; exact source plus registered Social OPTIONS 204/ACAO and attacker 403 must then prove the actual systemd service read the registry. Any read failure requires rollback.

All v6 pre-start gates passed, but its actual candidate start failed with `EROFS` before the service became active. `ProtectSystem=strict` exposes only `/var/lib/ynx-wallet-gateway` through the unchanged unit `ReadWritePaths`; v6 placed candidate states in the sibling `/var/lib/ynx-wallet-gateway-candidate-6cf3ef84`, so the runtime could not create its v1 temporary file. The rollback trap restored exact active `49e`; shared v1 stayed byte-exact at `ba386…` throughout, candidate-v1 remained `ba386…`, candidate-v2 was never created, and live candidate env/drop-in were removed. V6 is failed, consumed and non-reusable.

V7 retains the environment-only drop-in and does not change Caddy, the main unit, `ProtectSystem` or `ReadWritePaths`. Candidate state moves to the ubuntu-owned mode-0700 real directory `/var/lib/ynx-wallet-gateway/candidate-6cf3ef84`, with exact v1/v2 paths beneath it. The state verifier requires the systemd `ReadWritePaths` value to remain exactly `/var/lib/ynx-wallet-gateway`, rejects sibling paths, `..` escape, symlink traversal or symlink parents, verifies the candidate parent owner/mode, and preserves three distinct paths and inodes plus the exact shared SHA. Candidate activation and all public/client truth remain unproved.

The v7 rollback drill passed within the same single-use transaction. Pre-switch state containment, root registry/systemd inspection and all seven overrides passed. Candidate source `6cf3ef84…` became active for the drill; Social v1 and Finance v2 OPTIONS returned 204 while an attacker origin returned 403. Controlled rollback restored exact active `49e30d99…`, health/ready and Social 204, with shared v1 remaining `ba386…` before, during and after. Drill candidate-v1 and the v2-absent marker were preserved, and the failed v6 sibling directory was moved recoverably into backup. Caddy, unit permissions and services 6437/6439/6441 were unchanged. The current v7 transaction may proceed to a fresh stopped-service copy and final activation, but the lease may not be restarted from the beginning; final public lifecycle/restart/replay/revoke acceptance remains false.

The v7 single transaction then completed and is consumed. Owner evidence commit `83a0a4f09a61d84a667d88a49708ffbe7643adc8` (tree `e0f163a399407899a3f1ea799e59e961f42c117d`) proves source `6cf3ef845202bd879ed94515a71b323dd2fc9e14`, release `p0-v2-state-isolated-6cf3ef84`, active at `https://wallet-auth.ynxweb4.com`. The exact evidence blob is `276e17c88f78e11942bb52ee9de7a084d2ca4863` (7,581 bytes, SHA-256 `19eed89441966ec440366eb6c56e3452e499c1af8556eed96aa9f3195b078134`); the handoff blob is `3193f5a1ddc1df2e9ec948f1c510dc692591d274` (2,126 bytes, SHA-256 `9113c83d151e7f3dce4e146c74e85eaf8fa6d395b94e4a7ac4e30e22f5750458`). Public challenge, complete, introspection, real service restart and re-introspection, replay rejection, revoke and post-revoke, device revoke and post-device-revoke all passed. Registered CORS passed and wrong origin/header/method/route/query failed closed with zero business mutation. Caddy and the base unit remained byte-identical; shared legacy v1 remained exact at `ba386fb9…`.

Central therefore promotes only `walletAuthGatewayDeployedPublic=true` and `productSessionV2PublicLifecycleVerified=true`. Computer-Control visible flow, installed Wallet approval, account/sign/transaction, Chain-disconnect Retry, public expiry, Website publication, production signing, store release, all 12 product migrations, `integratedCentral` and aggregate completion remain false. The consumed v7 lease is not reusable and authorizes no further production action.

Website follow-up commit `c6f14070170de8859ea5613c697db0056def31f6` binds authoritative Website commit `a7313313014bb8792f38e649e9f556dbee983c8c`. Official apex and www `/docs` each returned HTTP 200, 4,748 bytes, SHA-256 `cdfb1d0921619b8dfa92d585ab8e6688405ddd9f4fe4309a68dd5fee96fc284b`; both runtime records returned HTTP 200, 1,900 bytes, SHA-256 `635fefdcc1cd5675a1787a9bf17c87102ccadd1c8232450be7b847333fe75941` and bind Central `3c6288e1…`, Owner `83a0a4f0…` and Gateway `6cf3ef84…`. The frontend asset is 585,453 bytes with SHA-256 `fb9a264b58f8ea5c3f9c1649cf10fc78040bd0a6ef63efc04eb02d099b7018bd`. The observed Vercel production deployment is READY, but its access-protected direct URL was not content-verified. Central therefore adds only `websiteUpdated=true`; every client, migration, authority and aggregate boundary above remains unchanged.
