# YNX Resource Market integration handoff

## Identity

- Product owner: `16-resource-market`
- Contract: `release/integration/resource-market-contract.json`
- Contract version: `resource-market-integration-v1`
- Implementation source: `a940d2efa824bd9f43522ed792c9a563b55e1e11`
- Current phase: `FREEZE → INTEGRATE`
- Current product status: local candidate; not centrally integrated, staged, public, production-signed or store-released.

## Authority split

Resource Market owns provider registration, verified capacity, offers, matching, auctions, reservation, service lifecycle, signed usage metering and local dispute evidence. It does not own Wallet identity, asset finality, billing-ledger authority, public Explorer proof, central monitoring, public Website entry or protocol freeze.

A quote, accepted intent, reservation, service start, meter, service completion, HTTP success or provider statement is never asset settlement. Reservations are bound to the exact Offer referenced by the accepted Quote; capacity from a sibling Offer cannot satisfy or release that reservation. Settlement is accepted only when an authorized settlement identity supplies a non-empty asset, transaction hash, evidence and source; amounts exactly reconcile to signed meters; the order is `settlement_pending`; and the normalized transaction hash has not already been consumed by another receipt.

## Canonical integration inputs

- Wallet registry: `apps/resource-market/integration/canonical-wallet-registry.json`
- Wallet vectors: `apps/resource-market/integration/canonical-wallet-v1-test-vector.json`
- Existing central manifest: `apps/resource-market/integration/central-integration-manifest.json`
- Frozen product contract: `release/integration/resource-market-contract.json`
- Cross-product vectors: `docs/integration/CROSS_PRODUCT_TEST_VECTORS.json`
- Dependency acceptance: `docs/integration/DEPENDENCY_ACCEPTANCE.md`

## Required central behavior

1. Product 02 registers the exact client, bundle, callback, ordered scopes and P-256 product-device algorithm.
2. Product 29 freezes the exact method/path/body product-session proof semantics and one-to-one proxy route mapping.
3. Product 01 provides authoritative transaction finality and settlement evidence; product 16 does not infer finality.
4. Product 26 accepts only signed-meter and confirmed-settlement events, preserving idempotency and lineage.
5. Product 12 exposes public receipt evidence only after authoritative settlement.
6. Product 13 alerts on stale providers, metering failures, settlement reconciliation failure and receipt replay rejection.
7. Product 15 links provider failure and dispute/appeal evidence without gaining asset authority.
8. Product 28 publishes only release states that have direct evidence.

## Stable errors

The product returns a stable `code` with `errorId`, `requestId` and `traceId`. Settlement integrations must preserve at least:

- `RESOURCE_SELF_DEALING_REJECTED`
- `RESOURCE_AMOUNT_OUT_OF_RANGE`
- `RESOURCE_CAPACITY_UNAVAILABLE`
- `RESOURCE_METER_WINDOW_INVALID`
- `RESOURCE_METER_LIMIT`
- `RESOURCE_SETTLEMENT_STATE_INVALID`
- `RESOURCE_SETTLEMENT_EVIDENCE_REQUIRED`
- `RESOURCE_SETTLEMENT_RECONCILIATION`
- `RESOURCE_SETTLEMENT_REPLAY`

No consumer may translate these failures into success, paid, settled or refunded.

## Acceptance gate

Central integration remains false until every applicable dependency row in `DEPENDENCY_ACCEPTANCE.md` has direct evidence and the vectors in `CROSS_PRODUCT_TEST_VECTORS.json` pass against deployed Testnet services. Local tests are not public or central proof.

## Wallet/Auth owner addendum — multi-user Gateway recovery

Wallet/Auth source `c8eca508d4575af188b2a23750e88ecadf98055f` adds an acknowledged local-process crash-durability boundary. Mutations now write an exclusive `O_NOFOLLOW` mode-0600 temporary state, fsync it before atomic rename, and fsync the already-open parent directory before emitting success. If directory sync fails after rename, 503 `STATE_COMMIT_UNCERTAIN` keeps the renamed winner in memory instead of rolling back behind disk. Independent child processes SIGKILLed immediately after acknowledged completion and revoke cold-restarted with the exact session/nonce/audit or revoke/proof/audit state and no leftover lock. This does not prove power-loss, network-filesystem, pre-ack crash/stale-lock recovery, central, staging or public behavior.

Wallet/Auth source `838ced040abe13fed2f75e13724380190b0a3626` adds an explicit stale-owner recovery boundary without automatic lock deletion. The canonical mode-0600 lock binds PID, acquisition time and a random token and is atomically published by hard link. Normal traffic always fails 503 `STATE_LOCKED` while it remains. Operator recovery requires a configured age floor, a dead PID, an unchanged safe owner record, valid current state digest and the exact Registry SHA-256; it serializes recoveries and rejects unsafe dead-owner temporary-state substitutions. A real pre-ack SIGKILL test proves the unknown completion outcome converges after recovery and exact retry to exactly one session/audit winner. This is manual single-host POSIX evidence only; PID-namespace policy, automatic recovery, network filesystems, power loss, central, staging, public and multi-region states remain false.

Wallet/Auth source `8e91c58e3f997b35da1b3d0298f174a2e8533fa2` uses the same canonical locked state path without adding a competing protocol. Concurrent Approval Revoke and Product Device Revoke both return success, persist both exact cutoffs and exactly one audit event per control, and consume exactly their two successful proofs. A fresh later introspection returns 403 `REVOKED` without proof consumption, and cold restart is byte-semantically identical. This is local shared-filesystem ordering evidence only; central, staging, public and multi-region booleans remain false.

The next Core control matrix races Strategy Action authorization against the Kill Switch in independent Gateway processes. Kill persists exactly once; the action is either the sole pre-kill winner or receives 409 `MANDATE_KILLED`. Every new post-kill action is rejected without consuming its proof, and cold restart retains the unique killed digest and audit. This reuses the canonical state lock and introduces no second protocol owner; local results do not change central, staging, public or multi-region state.

Wallet/Auth source `f3574cc17c53dfbe937933836ccdadf24f9e191e` hardens the online Gateway state reader to the same private-path boundary as backup/restore: the immediate directory must be non-symlink mode 0700; the state is opened with `O_NOFOLLOW`; it must be one regular-file link, mode 0600, at most 64 MiB and exact canonical JSON. Startup rejects symlink, hard-link, directory, broad-mode and noncanonical substitutions without rewriting any target. Runtime symlink replacement returns 503 `STATE_UNAVAILABLE` and leaves the target byte-identical. This excludes an owner-level malicious writer and does not prove network-filesystem, multi-region, central, staging or public behavior.

Wallet/Auth source `7974924bdf8f7096612ae9de01c69e91e750a274` adds exact boundary evidence to the preceding state-file slice without replacing its source proof: startup rejects mode 0644 and a sparse 67,108,865-byte state against the 64 MiB maximum before reading, preserves the rejected file size/content, and continues to reject noncanonical JSON and runtime link replacement. Central, staging, public and network-filesystem claims remain false.

Wallet/Auth source `9330045bfd1153d1129f74bc1a7a1e7866f70cf0` introduces Gateway Node state envelope v2 with required `registrySha256`, computed from the exact parsed canonical Registry. Startup and every locked dispatch reject a different same-version Registry with `REGISTRY_STATE_MISMATCH`; replacing state with legacy v1 while migration is disabled returns `LEGACY_STATE_MIGRATION_REQUIRED`, and neither case rewrites state. A reviewed copied-state migration may explicitly enable v1→v2 for one cold start, verify the resulting digest, then remove the flag. Backup/restore and the historical account-key migration script accept the new envelope. This is local operator-misconfiguration protection, not malicious-writer cryptographic integrity, central acceptance, staging or public deployment; those booleans remain false.

Wallet/Auth source `394a2728a8c0737dff025dd459e91d376a5c44ff` closes the shared-local-state lost-update gap. Every canonical protocol dispatch now acquires an atomic mode-0700 lock beside the state file, reloads and revalidates the latest persisted snapshot inside that lock, then dispatches and persists before release. Direct loopback tests with independent Node processes preserve two concurrent completions, linearize introspection/revoke, and produce exactly one winner for distinct and exact duplicate revoke. A lock held beyond the bounded two-second wait returns 503 `STATE_LOCKED` with zero mutation; exact retry succeeds after release. This is not network-filesystem, automatic stale-lock recovery, multi-region, central, staging or public proof; those booleans remain false.

Wallet/Auth source `acc906c4c3a86e6557685e1959b78a4505a961c3` closes the single-process Node Host persistence split-brain gap. If atomic snapshot persistence fails after Kernel completion or revoke, the host reconstructs its in-memory Kernel from the exact pre-request snapshot before returning bounded 500 `INTERNAL`; the disk envelope remains unchanged, and the authorization nonce or Product Session proof can be retried exactly after storage recovery. The final state file inherits mode 0600 before atomic rename, leaving no fallible post-rename mutation step. This is not shared-state multi-process or multi-region proof, and all central/staging/public booleans remain false.

Wallet/Auth source `a3dc6d52bfba48a3d272084828764192d4c7ac34` freezes `YNX_SESSION_LINEARIZATION_RACE_V1`. Within one canonical Node Host process, request bodies may arrive concurrently but mutation linearizes at synchronous Kernel dispatch and the winning snapshot is persisted by atomic rename. Concurrent introspection/revoke admits only the valid serialized outcomes; distinct or exact duplicate revoke has exactly one success and one `ALREADY_REVOKED` or `REPLAY`; an expiry-boundary race has one pre-expiry winner and one `EXPIRED`; failed proofs are not consumed; restart preserves the winner; no session resurrects. This does not prove shared-state multi-process or multi-region linearization, and all central/staging/public booleans remain false.

Wallet/Auth source `806f342723bdc7911367b9db9a72c4f33cd0a3db` freezes `YNX_SESSION_INVENTORY_HTTP_PRIVACY_MATRIX_V1`. The `/v1/wallet/sessions` response is derived only from the authenticated canonical Wallet Product Session account; the body cannot select an account, and a non-Wallet product cannot gain inventory authority by receiving `wallet:sessions`. Ten fail-closed cases disclose no inventory and preserve exact state, proof replay causes no additional mutation, and a two-account test proves each Wallet sees only its own session binding. This remains local-only and absent from deployed `c6ac582c`; central, staging, public and public-route booleans remain false.

Wallet/Auth source `979b791a87320718e66832cf690755792e998ab5` freezes `YNX_REVOCATION_CONTROL_HTTP_NEGATIVE_MATRIX_V1`. Approval and device revocation remain self-scoped to the authenticated Product Session; request bodies cannot select another approval, device or account. Account-wide logout requires both the exact `wallet:sessions` scope and the canonical `ynx-wallet-v1`/`com.ynxweb4.wallet` product identity, so granting that scope to a non-Wallet registration still fails with `WALLET_CONTROL_REQUIRED`. The 26-case local matrix covers binding, time, replay, repeat and control-boundary failures with unchanged state digests and zero extra audit, revocation, logout or proof consumption. The deployed `c6ac582c` Gateway predates this slice; central, staging, public and public-route booleans remain false.

Wallet/Auth source `0a3c048d1baf0cca51f78630dc8a26ec14fd43fd` freezes `YNX_PRODUCT_SESSION_HTTP_NEGATIVE_MATRIX_V1` for the canonical introspection and self-revoke routes. Integration consumers must preserve exact POST method, canonical path/body digest, P-256 device binding, granted-scope subset, proof/session time window and one-time proof semantics. The shared 16-case vector proves wrong method/path/body/device/scope, pre-session, expiry and replay fail closed; fourteen pre-success failures and two post-success replays cause zero additional state mutation. This slice is local-only and is absent from deployed Gateway source `c6ac582c8d490ef980a8cadfd9172f133d1b1a49`, so `integratedCentral`, `deployedStaging`, `deployedPublic` and `publicRouteVerified` remain false.

Wallet/Auth source `dcca919068c18daef58a10bbfdb38368f1b0db1c` freezes `WA-AUTHORIZATION-REJECTION-001`. `YNX_WALLET_AUTH_REJECTION_V1` binds rejection to the exact request digest, nonce, network, product, bundle, callback and request lifetime while requiring `authorityGranted=false`, an empty scope list, no account and no Wallet signature. The local canonical HTTP route returns 403 `AUTHORIZATION_REJECTED` with an unchanged state digest and zero created sessions/consumed nonces/proofs. Request, product, callback and time substitution, authority/scope injection, unknown fields and proof-header injection fail closed. The shared Gateway manifest now lists all fourteen runtime routes, recovering the previously omitted Wallet-only session inventory route. Central Integration must consume the vector and deploy the route before changing any integration/public boolean; the currently deployed `c6ac582c` Gateway does not contain this slice, so central, staging, public and public-route verification remain false.

Wallet/Auth source commit `7678c5764010e9d6ff31445687555ae848070e5c` adds a real loopback HTTP acceptance slice for 32 distinct Wallet accounts and 32 distinct P-256 product devices. It concurrently completes and introspects canonical Product Sessions, then proves exact-proof replay rejection, session revocation, mode-0600 state persistence and the same `REPLAY`/`REVOKED` failures after reconstructing the Gateway host.

Consumers must use `WA-MULTIUSER-ISOLATION-001`, `WA-REPLAY-RESTART-001` and `WA-REVOKE-RESTART-001` from `CROSS_PRODUCT_TEST_VECTORS.json`. This is local HTTP, filesystem and process-reconstruction evidence only. It does not prove public concurrency, staging capacity, a production load balancer, multi-region recovery, Monitor acceptance or a Testnet asset transaction; all corresponding publication booleans remain false absent direct evidence.

Source `d13170c0697462ed821207ca8b720d48114e82d6` adds the opt-in `WA-PUBLIC-GATEWAY-MULTIUSER-BOUNDED-001` verifier. A source-bound public Testnet run used four fresh random Wallet and P-256 identities: 4/4 completions and active introspections preserved unique accounts/session bindings; cross-session proof substitution returned 403 `SESSION_BINDING_MISMATCH`; 4/4 exact replays returned 409 `REPLAY`; 4/4 revokes completed and 4/4 fresh post-revoke proofs returned 403 `REVOKED`. Cleanup was complete, no assets moved and no existing user/provider, secret or identifier value was recorded. This proves only one four-user bounded sample. Integration and Monitor must keep production capacity, external load-balancer capacity, multi-region recovery and request/trace/error identifier completeness false.

The opt-in public lifecycle probe introduced at `652b9da67a47e13f1826db07d423d464100939c1` initially failed with `UND_ERR_CONNECT_TIMEOUT`; that record remains as outage history. A later run from source `803d595b9ebee149b7823f59d165b65ff97d816b` now satisfies `WA-PUBLIC-GATEWAY-LIFECYCLE-001`: fresh random Wallet and P-256 keys completed an isolated Bridge Product Session, introspected it active, received 409 `REPLAY` for exact proof reuse, revoked it and received 403 `REVOKED` for a fresh post-revoke proof. No asset moved and no existing user/provider or secret was recorded. This proves current public Auth/Gateway lifecycle behavior only. Because the probe reported that not all five responses carried expected UUID-format request IDs, public request/trace/error identifier completeness remains false and Monitor must treat it as a separate gap.

Source `7bf0df85eafe7c2f6470a28f1784f96edfd4f048` precisely isolates that gap through `WA-PUBLIC-GATEWAY-IDENTIFIERS-001` without recording header values. A second successful public lifecycle found zero of five responses with valid `x-request-id`, zero of five with valid `x-trace-id`, and zero of the two error responses with valid `x-error-id`. `Cache-Control: no-store`, replay rejection, revocation and post-revocation rejection still passed. Integration must deploy the canonical Node Host identifier behavior or an exact equivalent and rerun the probe; Monitor must keep public identifier completeness false until all required phase booleans are true.

Wallet Gateway release `ynx-wallet-gateway-c6ac582c8d49` (source `c6ac582c8d490ef980a8cadfd9172f133d1b1a49`) was atomically installed after exact archive/registry verification and a copied-state migration preflight. Unit, environment and mode-0600 state were backed up under a mode-0700 release-specific directory; the installer armed automatic rollback for every post-switch failure and then verified both Wallet Gateway and App Gateway before disarming it. The deployed 6439 runtime reports the exact build and emits valid request/trace IDs. Active Caddy topology routes public `/v1/wallet/*` to App Gateway 6437 and exposes only `/wallet-gateway/health` directly from Wallet Gateway 6439. Therefore the public identifier gap is an ingress boundary, not a failed Wallet runtime deployment.

The only recommended Integration diff is `WA-PUBLIC-GATEWAY-INGRESS-IDENTIFIERS-001`: at the existing 6437 `/v1/wallet/*` boundary, preserve upstream identifiers or generate canonical `x-request-id`/`x-trace-id` on every response and `x-error-id` on errors. Do not change route ownership, protocol bodies, HTTP statuses, state, replay or revoke behavior. Acceptance requires the complete public lifecycle to remain 200 → 200 → 409 `REPLAY` → 200 → 403 `REVOKED`, with all identifier booleans true and no identifier values stored in evidence. Deployed identifier completeness remains false until that direct rerun passes.

Source `6b1f1f21a79861178ee7fc168ad21c2869296fd5` moves concurrency/rate admission into the canonical Node Host. Monitor and Integration consumers must accept `WA-ADMISSION-OBSERVABILITY-001`: 429 `RATE_LIMIT` and 503 `CONCURRENCY_LIMIT` are fail-closed canonical errors carrying request/trace/error IDs and state digest, with bounded metrics and redacted events. This candidate passed local HTTP and real CLI loopback verification but is not merged into the central/public Gateway; its integrated and deployed booleans remain false.

Wallet/Auth source `80291d53893aa6735d401c692a786fbcdbca8424` freezes the Sponsored UserOperation evidence boundary. Chain, Explorer, Monitor and Integration consumers must use `WA-UO-EVIDENCE-SUBSTITUTION-001`: a Bundler response is not final merely because one provider says success. The exact submitted PackedUserOperation, userOpHash, EntryPoint, sender/nonce, transaction hash/block, transaction recipient, Paymaster prefix and success status must agree across lookup, UserOperation receipt and inclusion transaction. Substitution, missing finality and failed execution return stable fail-closed errors. This passed an isolated local Hardhat EntryPoint/Paymaster flow, 115/115 Wallet-owned tests and the 117/117 full package audit. The additional 2/2 SampleEVMWriteCounter assertions are Developer-owned and passed without Wallet modifying or claiming ownership of their artifact. Fresh public chain reads reached chain 6423/block 997284, but `eth_getCode` remained unavailable with `-32601`; public EntryPoint, Bundler, Paymaster, receipt and Explorer states remain false.

Wallet/Auth source `2a19cb153952c7e3e1e253fea39186e2ebff194b` provides `WA-ERC4337-PUBLIC-READINESS-001`. The explicit public run verified EVM RPC chain 6423, while the only repository-discovered Bundler candidate returned Vercel HTTP 404 `DEPLOYMENT_NOT_FOUND`; no authoritative public EntryPoint address/runtime hash exists, so the probe refused to guess a fixture address and exited 2. Integration must treat exit 2 as not ready, never success. Acceptance requires chain 6423 on both endpoints, the exact frozen EntryPoint runtime hash and Bundler support for that same address before any public boolean can change. The current suite is 120/120 Wallet-owned and 122/122 full-package; the two additional Developer-owned assertions passed without Wallet modifying their artifact.

Wallet/Auth source `ddd7ce3397d40d7a49c3511a452d32d78f0450b8` freezes `WA-SPONSORSHIP-SESSION-BINDING-001`. Sponsorship request v2 is not eligible unless its product, Product Session binding, Wallet account, P-256 product-device key, exact ordered scopes, anti-Sybil subject and UserOperation digest all match the canonical context. A one-time request nonce is consumed only after an eligible decision; binding failures and bounded-ledger exhaustion grant zero sponsorship. The slice passed 9/9 focused and 176/176 Wallet/Auth tests. A bounded public probe on 2026-08-14 still returned unavailable RPC and Bundler HTTP 404 `DEPLOYMENT_NOT_FOUND`, with no authoritative EntryPoint; public EntryPoint, Bundler, Paymaster and sponsored receipt states remain false. The in-memory ledger does not prove crash or multi-process replay atomicity; those claims remain false pending a durable follow-up slice.

Wallet/Auth source `69aff0cad57737fbdb30fda9dbca348356e96f7a` freezes `WA-SPONSORSHIP-PREACK-CRASH-001` for the Node-only durable replay ledger. The child process was killed after the canonical mode-0600 state was fsynced, atomically renamed and its mode-0700 parent directory fsynced, but before `authorize` returned. A cold reconstruction recovered exactly one consumed nonce and rejected the exact retry with `SPONSORSHIP_REPLAY`. Broad-mode, symlink, hard-link, noncanonical and 32 MiB + 1 byte state files fail closed. This passed 3/3 focused and 179/179 full Wallet/Auth tests. It does not prove two-process linearization, power-loss behavior, network filesystems or multi-region recovery; those claims remain false.

Wallet/Auth source `a044e0d46349283979509d5e7c4e9d8c82970698` freezes `WA-SPONSORSHIP-MULTIPROCESS-LINEARIZATION-001`. The fail-first run proved the unlocked implementation lost seven of eight distinct nonces. The corrected ledger uses a mode-0600 exclusive lock and performs reload, replay decision and durable persistence inside the same critical section: eight independent processes preserved 8/8 distinct nonces, while eight processes using one nonce produced exactly one accepted winner and seven replay failures. The pre-ack SIGKILL regression still passes. Focused tests are 5/5 and the full Wallet/Auth suite is 181/181. This proves one private local POSIX filesystem only; network filesystems, a distributed database and multi-region linearization remain false.

Wallet/Auth source `c952d795764d25a32f62e12a1d0011fc8afa7708` freezes `WA-SPONSORSHIP-STALE-LOCK-RECOVERY-001`. Normal startup never guesses that a lock is stale and returns `SPONSORSHIP_STATE_LOCKED` without mutation. Explicit recovery requires a bounded minimum age, a dead owner PID, a canonical mode-0600 PID/token record, a valid canonical replay state and unchanged lock inode/token immediately before unlink. Too-young and live/PID-reused owners fail closed. A real killed child left the owner lock; recovery preserved the state byte-for-byte and cold restart still rejected the consumed nonce. Focused tests are 7/7 and the full Wallet/Auth suite is 183/183. This is manual local POSIX recovery, not automatic, power-loss, PID-namespace, network-filesystem, distributed-database or multi-region evidence.

Wallet/Auth source `644e490044a52a0e4f36d4e6a866579b02d20560` freezes `WA-SPONSORSHIP-STALE-LOCK-CLI-001`. The npm package exposes mode-0755 `ynx-wallet-sponsorship-state-lock recover`, requiring explicit state path and minimum-age environment values. Missing input and too-young recovery exit 2 with canonical JSON; success exits 0 and exposes only age, owner PID and the recovered boolean. Paths, owner token, stack and source locations are absent, and state bytes remain unchanged. Focused tests are 8/8. The first full run had one unrelated existing acknowledged-revoke SIGKILL timing failure (183/184); that file then passed 2/2 focused and the clean full rerun passed 184/184. This CLI does not make recovery automatic or public.

Wallet/Auth source `04aad00c775943040da7fb75a024dcd884c8608d` converts that timing failure into `WA-GATEWAY-ACK-LOCK-RELEASE-001`. The fail-first full run observed the state lock still present after a successful revoke response. The Node Host now releases the held lock before emitting either the business response or a canonical error; a lock-release failure overrides the business response instead of acknowledging an uncertain terminal state. Completion/revoke crash durability passed 2/2, then ten additional repeated runs passed 20/20 assertions, and the full suite passed 184/184. This is local process/filesystem evidence only; central, staging, public, power-loss, network-filesystem and multi-region states remain false.

Wallet/Auth source `c7031a845864b0086d36c7e48cce8cc1e7c31cad` freezes `WA-GUARDIAN-RECOVERY-SESSION-CUTOFF-001`. Fail-first proved that requesting delayed Guardian Recovery left the existing Session Key signature-valid at epoch 1. `requestRecovery` now performs the already-authorized Guardian all-session cutoff before recording the delayed recovery: the old key fails through the real local EntryPoint before the delay, owner cancellation does not resurrect it, and a replacement request creates a fresh cutoff. Recovery execution no longer increments the epoch a second time for the same request. Focused Hardhat, 184/184 Wallet/Auth, package and release gates passed. This changes no public or central claim: no authoritative public EntryPoint/Bundler/Paymaster/receipt exists, and `integratedCentral`, staging, public and route-verification states remain false for this slice.

Wallet/Auth source `f455e60be05eef04a9d3a544ee478c692c95188d` freezes `WA-SESSION-EXACT-CALLDATA-001`. Fail-first proved that exact target/selector/value/time policy still allowed a signed UserOperation to append a replacement address parameter after the allowed selector. `Session` and `SessionConfigured` now bind an exact calldata `keccak256`; same-selector argument substitution fails with `SessionPolicyViolation`, while the configured call, per-call/daily value limits and 50-call local soak remain valid. This is an intentional pre-deployment ABI/event change: consumers must include `dataHash` when configuring sessions and must not treat selector matching as parameter authorization. No authoritative public Smart Account, EntryPoint, Bundler, Paymaster or receipt is proven, so all new central/staging/public booleans remain false.

Wallet/Auth source `b1f6376bd609369c0faf4e3cb84e50c8f7cb9a50` freezes `WA-SESSION-POLICY-PREVALIDATION-001`. Fail-first showed that invalid Session target/calldata/value policy passed account signature validation and failed only during execution, allowing nonce and gas consumption without asset-policy eligibility. `validateUserOp` now requires canonical `executeSession` ABI and checks the exact key, target, selector, calldata digest, per-call limit and current-day budget before accepting the signature. Four invalid operations leave EntryPoint nonce 2 unchanged; a valid Passkey operation then succeeds at nonce 2. The execution-stage daily-limit check remains authoritative for multiple operations validated before execution, and that same-bundle race is not overstated as prevalidation proof. Public EntryPoint, Bundler, Paymaster, receipt, staging and central states remain false.

Wallet/Auth source `42978bc6d1fe33473d60b0cd2bc5e36e83cf5479` freezes `WA-SESSION-DAILY-LIMIT-BUNDLE-001`. With 0.20 remaining under a 0.30 daily cap, two 0.15 Session UserOperations are submitted in one EntryPoint `handleOps` bundle and both prevalidate before execution. The execution backstop linearizes to exactly one success and one `SessionPolicyViolation`; target balance and `spentToday` increase only by 0.15, so no overspend or partial failed-call mutation occurs. Both nonces are consumed because EntryPoint validated both operations before execution; consumers must not misreport the failed operation as zero-nonce mutation. This is local EDR evidence, not external/multi-Bundler, public receipt or deployment proof.

Wallet/Auth source `3517440411735082870b1b65752f9d2096e0daab` freezes `WA-SMART-ACCOUNT-SESSION-TIME-001`. A dedicated Session UserOperation is rejected by the local EntryPoint with `AA22 expired or not due` before `validAfter`, succeeds at the same nonce inside the window, and is rejected after `validUntil` without consuming the next nonce. Target and session spending remain unchanged on both time failures. This is Smart Account/EntryPoint timing evidence and does not inherit Gateway HTTP deployment claims; external Bundler, public receipt, central, staging and public states remain false.

Wallet/Auth source `a592f2d80956cdd9df4f2db2a3592dde38a73833` freezes `WA-PAYMASTER-PRODUCT-POLICY-001`. Fail-first proved that `SponsorAuthorization.policyId` was signed but not matched to any on-chain ProductBudget policy, so a signer-authorized retired policy still sponsored an operation. Product configuration, storage and `ProductBudgetConfigured` now bind an exact nonzero policy ID; mismatch fails before reservation with no product/subject budget, replay marker or sponsored-account nonce mutation. This intentionally changes an undeployed local `configureProduct` ABI/event. No authoritative public Paymaster, Bundler or receipt exists, so central/staging/public states remain false.

Wallet/Auth source `a296526f57d2e466813cbf86b8079f534476f9cf` freezes `WA-PAYMASTER-LOWERED-LIMIT-001`. When a same-day per-subject limit was lowered below existing reserved usage, the next authorization previously underflowed and surfaced EntryPoint-wrapped panic selector `0x4e487b71` code `0x11`. Explicit pre-subtraction guards now return Paymaster `BudgetExceeded` selector `0x50b2c4e1`; product/subject budget, authorization replay marker and sponsored-account nonce remain unchanged. This is local error-contract evidence, not public Paymaster/Bundler/receipt or central deployment proof.

Wallet/Auth source `d5bb0a036827878b291b2c26b1f7e09ca31a93b1` freezes `WA-SMART-ACCOUNT-BATCH-BOUNDARY-001`. The inherited ERC-4337 `executeBatch` path is now directly exercised through the local EntryPoint: owner and WebAuthn UV UserOperations execute ordered subcalls, while a Session Key cannot use batch to bypass its exact `executeSession` calldata policy and is rejected with `AA24 signature error` before nonce or target mutation. If a later subcall reverts, all earlier subcall state rolls back; because the owner operation already validated, EntryPoint consumes that failed operation's nonce. This is local EDR evidence only; no external Bundler, public receipt, central, staging or public state is claimed.

Wallet/Auth source `05f73a6ee595bb399e03c908f7ecdef7044dfa99` freezes `WA-PAYMASTER-BATCH-POLICY-001`. The policy signer signs the exact batch operation, but the current Paymaster authorization model binds one destination and therefore rejects `executeBatch` with EntryPoint-wrapped `PolicyViolation` before reservation. Product/subject budgets, authorization replay marker, sponsored-account nonce and every batch target remain unchanged, while existing single-destination first-action, merchant, developer and product sponsorship regressions pass. Batch sponsorship remains unsupported until a versioned per-call policy exists; this is local evidence, not an external Bundler, public Paymaster/receipt, central, staging or public claim.

Wallet/Auth source `5e4c3b8a23581c5e48edd4156b32b70b30c51a9f` freezes `WA-GUARDIAN-RECOVERY-CREDENTIAL-ROTATION-001`. After delayed recovery executes, UserOperations signed by the old owner and old WebAuthn credential both fail at AA24 without consuming EntryPoint nonce 59. The replacement owner then succeeds at nonce 59 and the replacement UV passkey succeeds at nonce 60; all pre-recovery Session Keys remain revoked. This is local EntryPoint credential-rotation evidence only and does not prove an external Bundler, public receipt, central, staging or public deployment.

Wallet/Auth source `d0c321f455b2038bb6689a83caae94d7337c8c3d` freezes `WA-PAYMASTER-SPONSORSHIP-STATUS-001`. Fail-first proved there was no single protocol read for Sponsor/Policy/Cost status. `getSponsorshipStatus` now returns policy ID and signer, global/product enablement, allowed types, required target, every configured limit, current-day reserved/observed product and subject cost, first-action state, merchant approval, UTC day and EntryPoint deposit. Stale daily counters normalize to zero in the view after rollover without rewriting storage or changing sponsorship authority. This is a local ABI/evidence change; no external Bundler, public Paymaster/receipt, central, staging or public deployment is claimed.

Wallet/Auth source `58938b8d4a61d8c3d1ccc7c02322e5d95b271f59` freezes `WA-PAYMASTER-POLICY-SIGNER-ROTATION-001`. A complete authorization signed by S1 before rotation is submitted only after the owner rotates to S2; EntryPoint rejects it with AA34 and leaves product/subject budgets, replay marker and sponsored-account nonce zero. S2 is immediately visible through the status view and its first-action authorization succeeds at the same nonce 0. This is local authority-rotation evidence only; no external Bundler, public Paymaster/receipt, central, staging or public state is claimed.

The 2026-08-14 Wallet contract deployment audit is frozen in `apps/wallet/proof/smart-account-testnet-deployment-blocked-2026-08-14.json`. The repository contains a chain-6423-only local Hardhat deployment command, but no Wallet deployment workflow or remote signer, no populated authoritative address registry, no Wallet-contract rollback runbook and no Wallet-contract monitor definition. The current process has none of the named RPC/deployer/EntryPoint/Paymaster/Bundler variables. A fresh secret-free bounded probe observed unavailable RPC and Bundler HTTP 404 `DEPLOYMENT_NOT_FOUND`; no transaction was broadcast. Integration must keep every Smart Account/Paymaster/Bundler/public receipt and website-address claim false until source-bound receipts, runtime code, events, Bundler support and monitoring evidence exist.

Wallet/Auth source `28b76ad504f71b282439e39b407174b585d28540` adds the explicit, secret-free deployment verifier. `npm --prefix packages/wallet-auth run verify:testnet-deployment` accepts a source-bound manifest and uses only `eth_chainId`, `eth_supportedEntryPoints`, `eth_getTransactionReceipt`, `eth_getCode` and `eth_call`. Public readiness is all-or-nothing across three exact receipts/contract addresses, runtime SHA-256 values, Factory/Paymaster EntryPoint relationships and Bundler support. It never accepts a deployer or signer secret and never broadcasts. The local mocked-provider suite passed 3/3 and the full suite 187/187; this improves acceptance rigor but changes no deployed/public boolean.

Wallet/Auth source `ae24bf3357b0036f6f3b8d167955dc4f91ba80ff` closes the producer/consumer format gap: the chain-6423 Hardhat deployment command emits exactly the verifier manifest only after three successful mined creation receipts, exact receipt contract addresses, non-empty runtime SHA-256 values and Factory/Paymaster EntryPoint getters pass. Existing-EntryPoint mode additionally requires the authoritative creation transaction hash. The builder's fail-first and substitution suite passed 4/4; full Wallet/Auth passed 188/188. No deployer channel was available, so no transaction or public metadata was produced.

Wallet/Auth source `492ba0e53cf5497e3c5e3c3ff6dcb58b9793f187` freezes `smart-account-testnet-operations.json`. EntryPoint, Factory and Paymaster code is immutable: rollback never means deletion or replacement. An incident response disables global sponsorship, disables the affected product, stops affected Bundler admission, freezes evidence, then rotates authority only through an Owner ceremony. Risk Officer cannot enable sponsorship. Partial deployments and website metadata may not precede verification. The contract passed 2/2 focused and 190/190 full tests; Monitor integration, public drill and deployed states remain false.

Wallet/Auth source `26d9932f0a2f14078f6e58e6b13778e540c16ff9` additionally binds the Paymaster creation event. Both manifest production and public verification require an exact receipt log emitted by the Paymaster with `OwnershipTransferred(address,address)`, zero previous Owner, nonzero new Owner and empty data. Receipt status and bytecode alone are insufficient. A missing or substituted log fails the all-or-nothing deployment gate; no public claim changes.

Wallet/Auth source `ab19d19ff83ab985010c67603939d2abf46810d2` adds `monitor:testnet-deployment`. It is explicit-opt-in, accepts no secrets and revalidates the exact deployment plus block freshness, expected Paymaster enablement and minimum EntryPoint deposit. Stale/future/malformed blocks, unexpected enablement, malformed ABI words and insufficient deposit are unhealthy. Even a healthy single sample reports only `monitoringSampleHealthy`; `monitoringPublic` stays false until Integration/Monitor actually schedules and proves continuous execution.

Wallet/Auth source `1acd74a27b2cae2d4624a2896309ef8d6a24410d` extends that sample with bounded event continuity from the verified Paymaster creation block through the verified fresh latest block. Only exact self-emitted signer rotation, risk-officer rotation, global sponsorship change and product-disable logs with canonical order and ABI shape are accepted. Removed, unknown, wrong-emitter, out-of-range, out-of-order or oversized results fail the health sample. This remains local verifier evidence, not a deployed Monitor claim.

Wallet/Auth source `53f5ac97f4efbb29eca489be72a665821f654a64` adds a manual-only, environment-protected Testnet deployment workflow. It checks out an exact reviewed SHA with persisted credentials disabled, runs complete gates before broadcast, deploys a default-disabled Paymaster, verifies public receipts/code/events/relationships/Bundler and uploads the manifest without pushing code or metadata. Direct GitHub API evidence returned 404 for `ynx-testnet-wallet-contracts`; secret-name enumeration timed out twice without requesting values. Therefore the workflow is source-ready but not an authorized active deployment channel and was not dispatched.

Wallet/Auth source `fb97a149250475e5df0f8265387acd9f8a2099ea` adds `WA-PUBLIC-ERC4337-METADATA-PROMOTION-001`. A public metadata candidate is generated only when the exact source-bound deployment verifier, fresh healthy Paymaster monitor sample and authoritative successful ERC-7769 sponsored receipt agree on chain, contracts, runtime hashes and block interval. Local fixtures, partial readiness, source/address substitution, unhealthy monitoring, failed receipts and credential-bearing Explorer URLs fail closed. The command emits a candidate only: Website/public publication and continuous-monitoring flags remain false until the Website Owner accepts and publishes independently. No live evidence triplet was available, so no public metadata was changed.

Wallet/Auth source `efcd580dee079812a438549a4370c31325ca484f` adds `WA-DEPLOYMENT-ARTIFACT-INTEGRITY-001`. The protected deployment job creates a canonical SHA-256 sidecar for the exact manifest bytes, binds it to the reviewed 40-character source and chain 6423, immediately rereads and verifies both files, and uploads both with missing artifacts treated as failure. Manifest-byte, source, digest and noncanonical-sidecar substitutions fail closed. This hardens the handoff but does not activate the absent protected environment or prove any public artifact/deployment.

Wallet/Auth source `7d0fa0072d38890f43b7d1cfc198f8116df0aaa4` fixes the deployment artifact capture path: direct local Hardhat and Node entrypoints now write pure JSON, avoiding npm lifecycle banners in the manifest and integrity sidecar. The regression gate rejects future `npm run` redirection for those files. No workflow dispatch or public deployment is inferred.

Wallet/Auth/App Gateway source `abb2f6ceebe2942f20af45a32583d4b73341da24` consumes frozen Product Session router contract `739fca33d230d390f0c001467c39efb14450b9ec` and mounts its five exact `/v2/product-sessions/*` POST routes before any external Chain fallback. The proxy preserves only the explicit v2 proof and request-ID boundary; unknown paths, query variants, methods, origins and preflight headers fail closed. This is a local routing candidate only: v2 authority persistence, restart/rollback and the full public lifecycle are still pending, therefore `deployedV2=false`.

Wallet/Auth source `2d72165b6fd281dde3e8c2d41f50ec8bffad6a6a` adds the persistent v2 host and deployment gate. The separate Product Session state is registry-SHA-bound, canonical, private, no-follow, single-link and 64-MiB-bounded; each audit/session/proof/idempotency mutation is file-fsynced, atomically renamed and directory-fsynced before response. A lost challenge response returns byte-identically after restart. Candidate preflight uses a copy of existing production v2 state, and rollback restores that state or removes the first-deploy state. These are local source/tests only; the authorized public deploy and complete lifecycle are still absent, so `deployedV2=false`.
