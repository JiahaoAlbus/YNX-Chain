# Demo Script

Run `make smoke-test`, connect MetaMask to YNX Testnet after real RPC is configured, request faucet YNXT, send a transfer, view explorer tx, stream AI, create Pay intent, inspect Trust trace, and compile a contract.

