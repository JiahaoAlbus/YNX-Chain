# Grant Reviewer Checklist

Run `make preflight`, `make smoke-test`, `make grant-package`, `make ecosystem-package`, `make exchange-package`, and inspect `ENV_INTAKE_FORM.md` for deployment blockers.

