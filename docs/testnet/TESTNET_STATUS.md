# Testnet Status

Local devnet is verifiable with `make smoke-test`.

Public testnet deployment requires real server, domain, validator, wallet, database, Redis, object storage, AI, Pay, Trust, monitoring, and TLS values.

