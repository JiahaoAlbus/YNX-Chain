# Next action

Commit this checkpoint and push the resulting exact head to `codex/pay-native-ci-recovery`. Require successful `test`, Pay native and Card native checks for that head. Then advance `origin/codex/final-pay`, apply and verify strict branch protection, update the owner PRs, and hand the frozen Pay source to `29-integration`.

Do not classify the expiring unsigned Simulator artifact as a public download or production signature. Do not claim shared Testnet or public deployment until direct source-bound evidence exists.
