# Next Action — YNX 21 Bridge

Build an immutable unsigned Testnet candidate package from checkpoint source `8c96a3fd22b7f32dbdb5b99f5048b1f527db63ef` containing the Linux AMD64 Bridge binary, read-only SDK package, SPDX SBOM, provenance statement, SHA-256 manifest and installation instructions. Verify reproducibility, downloaded-byte equality, configuration validation and cold start before publishing a GitHub pre-release. Preserve `productionSigned=false`, `externalSubmissionEnabled=false`, `userAssetMovementEnabled=false` and `testnetVerified=false` unless direct evidence changes them.
