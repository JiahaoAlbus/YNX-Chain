# YNX Calendar next action

1. Submit `release/integration/calendar-contract.json` and vector `CAL-X-013` to `29-integration` and `30-security-platform`.
2. Require accepted encrypted offsite retention, independent backup-key escrow, representative restore data, measured verification/promotion/rollback time and immutable recovery evidence before any production RTO/RPO claim.
3. Preserve accepted canonical Wallet evidence and execute the remaining shared Testnet vectors after AI, Mail and Data Fabric versions are accepted and deployed.
4. Produce current-source install/cold-start, SBOM, provenance, immutable hashes and hosted artifact evidence before creating a Calendar release.
5. Keep `websitePublished=true` and `deployedPublic=true` only while `/dapp/calendar`, the release registry, exact-build runtime health and served assets agree; add independent support/privacy/security/status probes without implying production readiness.
