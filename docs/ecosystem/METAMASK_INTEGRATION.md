# MetaMask Integration

Use chainId `0x1917` for YNX Testnet, native currency YNXT, decimals 18.

RPC URL, explorer URL, faucet URL, icon URL, support URL, and docs URL must come from real environment values before public sharing.

