import {readFile,readdir} from 'node:fs/promises';import {join} from 'node:path';import {fileURLToPath} from 'node:url';
const root=fileURLToPath(new URL('../src/',import.meta.url)),appRoot=fileURLToPath(new URL('..',import.meta.url)),files=[];async function collect(dir){for(const entry of await readdir(dir,{withFileTypes:true})){const file=join(dir,entry.name);if(entry.isDirectory())await collect(file);else if(/\.(ts|tsx)$/.test(entry.name)&&!entry.name.endsWith('.test.ts')&&!entry.name.endsWith('.test.tsx'))files.push(file)}}await collect(root);const sources=await Promise.all(files.map(async file=>[file,await readFile(file,'utf8')])),joined=sources.map(([,v])=>v).join('\n'),packageJSON=JSON.parse(await readFile(join(appRoot,'package.json'),'utf8'));for(const marker of ['launchWebAuthorization','connectStandardWallet','connectMetaMask'])if(!joined.includes(marker))throw new Error(`DEX v2 Wallet path lacks ${marker}`);if(packageJSON.dependencies?.['@ynx-chain/wallet-auth']!=='file:vendor/ynx-chain-wallet-auth-1.1.0-safe-launcher-v2.tgz')throw new Error('DEX is not pinned to the accepted safeWalletAuthorizeLauncher v2 package');for(const [file,value] of sources)if(/ynxwallet:\/\/authorize|(?:window\.open|<iframe|document\.location\s*=|location\.href\s*=)\s*\(?\s*['"`]ynxwallet/.test(value))throw new Error(`Forbidden Web Wallet authorization route found in ${file}`);console.log(`canonical-authorize: scanned ${files.length} DEX source files; v2 provider-only Web route verified`);
