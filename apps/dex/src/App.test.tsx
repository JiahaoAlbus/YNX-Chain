import { cleanup, fireEvent, render, screen, waitFor, within } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import App from "./App";

const json=(value:unknown)=>Promise.resolve({ok:true,json:()=>Promise.resolve(value)}) as Promise<Response>;
describe("YNX DEX product shell",()=>{
 beforeEach(()=>{location.hash="";localStorage.clear();Object.defineProperty(navigator,"onLine",{configurable:true,value:true});vi.stubGlobal("fetch",vi.fn((input:RequestInfo|URL)=>{const path=String(input);if(path.includes("/v1/pools"))return json({items:[],source:"indexed YNX Testnet EVM events"});if(path.includes("/v1/tokens"))return json({items:[],source:"owner-reviewed Testnet token list",chainId:6423,mainnet:false});if(path.includes("/v1/transactions"))return json({items:[],source:"indexed YNX Testnet EVM events"});if(path.includes("/v1/prices")||path.includes("/v1/twap")||path.includes("/v1/fees"))return json({items:[],source:"indexed raw amounts"});return json({source:"YNX Testnet EVM events",indexedEvents:0,pools:0,swaps:0,liquidityEvents:0,latestBlock:0})}))});
 afterEach(()=>{cleanup();vi.unstubAllGlobals();document.documentElement.dir="ltr";document.documentElement.lang="en"});
 it("renders truthful empty swap and pool states without fabricated metrics",async()=>{render(<App/>);expect(screen.getByRole("heading",{name:"Swap"})).toBeInTheDocument();await waitFor(()=>expect(screen.getByText("No executable route")).toBeInTheDocument());expect(screen.queryByText(/APY [1-9]/)).not.toBeInTheDocument();const primary=screen.getByRole("complementary",{name:"Primary navigation"});fireEvent.click(within(primary).getByRole("button",{name:"Pools"}));await waitFor(()=>expect(screen.getByText("No verified pools indexed yet")).toBeInTheDocument())});
 it("fails closed when canonical Wallet registry is unavailable",async()=>{render(<App/>);fireEvent.click(screen.getByRole("button",{name:"Connect Wallet"}));expect(await screen.findByRole("dialog",{name:"Connect Wallet"})).toBeInTheDocument();expect(screen.getByText(/registry is not integrated/)).toBeInTheDocument();expect(screen.getByRole("button",{name:"Continue in YNX Wallet"})).toBeDisabled()});
 it("persists Arabic RTL and dark appearance",async()=>{render(<App/>);fireEvent.click(screen.getByRole("button",{name:"Settings"}));fireEvent.change(screen.getByLabelText("Language"),{target:{value:"ar"}});expect(document.documentElement.dir).toBe("rtl");fireEvent.click(screen.getByLabelText("داكن"));expect(document.documentElement.dataset.theme).toBe("dark")});
 it("quotes reviewed Testnet tokens from indexed reserves and opens a fail-closed review",async()=>{const token0="0x0000000000000000000000000000000000000001";const token1="0x0000000000000000000000000000000000000002";vi.stubGlobal("fetch",vi.fn((input:RequestInfo|URL)=>{const path=String(input);if(path.includes("/v1/tokens"))return json({items:[{chainId:6423,address:token0,symbol:"T0",name:"Test Zero",decimals:18,standard:"ERC-20",reviewStatus:"owner-reviewed-testnet"},{chainId:6423,address:token1,symbol:"T1",name:"Test One",decimals:18,standard:"ERC-20",reviewStatus:"owner-reviewed-testnet"}],source:"owner-reviewed Testnet token list",chainId:6423,mainnet:false});if(path.includes("/v1/pools"))return json({items:[{address:"0x0000000000000000000000000000000000000011",token0,token1,reserve0:"1000000000000000000000",reserve1:"2000000000000000000000",contractVersion:"ynx-dex-cpmm-v1",updatedBlock:12,updatedAt:"2026-07-18T00:00:00Z"}],source:"indexed YNX Testnet EVM events"});if(path.includes("/v1/transactions"))return json({items:[],source:"indexed YNX Testnet EVM events"});if(path.includes("/v1/prices")||path.includes("/v1/twap")||path.includes("/v1/fees"))return json({items:[],source:"indexed raw amounts"});return json({source:"YNX Testnet EVM events",indexedEvents:1,pools:1,swaps:0,liquidityEvents:1,latestBlock:12})}));render(<App/>);await waitFor(()=>expect(screen.getByLabelText("You pay token")).toHaveValue(token0));fireEvent.change(screen.getByLabelText("You pay amount"),{target:{value:"1"}});await waitFor(()=>expect(screen.getByText("Deterministic quote from current indexed reserves.")).toBeInTheDocument());fireEvent.click(screen.getByRole("button",{name:"Review swap"}));const dialog=screen.getByRole("dialog",{name:"Review swap"});expect(within(dialog).getByText(/1\.99/)).toBeInTheDocument();expect(within(dialog).getByRole("button",{name:"Continue in YNX Wallet"})).toBeDisabled()});
});
