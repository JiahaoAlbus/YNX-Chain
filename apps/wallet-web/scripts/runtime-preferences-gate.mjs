import {createHash} from "node:crypto";
import {createServer} from "node:http";
import {mkdir,mkdtemp,readFile,rm,writeFile} from "node:fs/promises";
import {tmpdir} from "node:os";
import {dirname,extname,join,resolve} from "node:path";
import {fileURLToPath} from "node:url";
import {chromium} from "playwright";

const root=resolve(dirname(fileURLToPath(import.meta.url)),".."),dist=join(root,"dist","pwa"),evidencePath=join(root,"evidence","runtime","pwa-preferences-second-launch-20260814.json"),profile=await mkdtemp(join(tmpdir(),"ynx-pwa-preferences-"));
const bounded=(promise,ms,label)=>Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(Object.assign(new Error(`${label} timed out after ${ms}ms`),{code:"GATE_TIMEOUT"})),ms))]);
const server=createServer(async(request,response)=>{const pathname=new URL(request.url||"/","http://fixture").pathname,path=resolve(dist,pathname==="/"?"index.html":pathname.slice(1));if(!path.startsWith(dist)){response.writeHead(403).end();return}try{const body=await readFile(path);response.setHeader("content-type",extname(path)===".html"?"text/html":extname(path)===".js"?"text/javascript":extname(path)===".css"?"text/css":"application/octet-stream");response.end(body)}catch{response.writeHead(404).end()}});
await bounded(new Promise((resolveListen,reject)=>{server.once("error",reject);server.listen(0,"127.0.0.1",resolveListen)}),3000,"server start");
const url=`http://127.0.0.1:${server.address().port}/`,result={schemaVersion:1,sourceCommit:process.env.YNX_WALLET_WEB_SOURCE_COMMIT||"uncommitted-source-tree",generatedAt:new Date().toISOString(),runtimeClass:"Playwright bundled Chromium-compatible PWA; not branded Chrome/Edge and not an installed PWA",viewport:{width:390,height:844},passed:false,installedLocal:false,downloadHosted:false,productionSigned:false,storeReleased:false};
let context;
async function launch(){return bounded(chromium.launchPersistentContext(profile,{headless:true,viewport:{width:390,height:844},timeout:10000}),12000,"Chromium-compatible launch")}
async function close(){if(context)await bounded(context.close(),3000,"browser close").catch(()=>{});context=null}
try{
  context=await launch();let page=context.pages()[0]||await context.newPage();await bounded(page.goto(url,{waitUntil:"domcontentloaded",timeout:5000}),6000,"first launch");await page.selectOption("#locale","ar");await page.click("#theme");await page.waitForFunction(()=>document.documentElement.lang==="ar"&&document.documentElement.dir==="rtl"&&document.documentElement.dataset.theme==="dark");result.firstLaunch=await page.evaluate(()=>({lang:document.documentElement.lang,dir:document.documentElement.dir,theme:document.documentElement.dataset.theme,preferences:JSON.parse(localStorage.getItem("ynx.wallet.web.preferences.v1"))}));await close();
  context=await launch();page=context.pages()[0]||await context.newPage();await bounded(page.goto(url,{waitUntil:"domcontentloaded",timeout:5000}),6000,"second launch");await page.waitForFunction(()=>document.documentElement.lang==="ar"&&document.documentElement.dir==="rtl"&&document.documentElement.dataset.theme==="dark");result.secondLaunch=await page.evaluate(()=>({lang:document.documentElement.lang,dir:document.documentElement.dir,theme:document.documentElement.dataset.theme,horizontalOverflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,download:{visible:!document.querySelector("#download").classList.contains("hidden"),href:document.querySelector("#download").href},metamask:{visible:!document.querySelector("#metamask").classList.contains("hidden"),href:document.querySelector("#metamask").href}}));await page.evaluate(()=>localStorage.setItem("ynx.wallet.web.preferences.v1","{"));await close();
  context=await launch();page=context.pages()[0]||await context.newPage();await bounded(page.goto(url,{waitUntil:"domcontentloaded",timeout:5000}),6000,"tamper relaunch");await page.waitForFunction(()=>document.querySelector("#status")?.dataset.kind==="error");result.tamperRelaunch=await page.evaluate(()=>({lang:document.documentElement.lang,dir:document.documentElement.dir,theme:document.documentElement.dataset.theme||"system",statusKind:document.querySelector("#status").dataset.kind,statusText:document.querySelector("#status").textContent,stored:localStorage.getItem("ynx.wallet.web.preferences.v1")}));
  result.passed=result.firstLaunch.lang==="ar"&&result.firstLaunch.dir==="rtl"&&result.firstLaunch.theme==="dark"&&result.secondLaunch.lang==="ar"&&result.secondLaunch.dir==="rtl"&&result.secondLaunch.theme==="dark"&&!result.secondLaunch.horizontalOverflow&&result.secondLaunch.download.visible&&result.secondLaunch.download.href.startsWith("https://")&&result.secondLaunch.metamask.visible&&result.secondLaunch.metamask.href==="https://metamask.io/download"&&result.tamperRelaunch.lang==="en"&&result.tamperRelaunch.dir==="ltr"&&result.tamperRelaunch.theme==="system"&&result.tamperRelaunch.statusKind==="error"&&result.tamperRelaunch.stored===null;
}catch(error){result.error={name:error?.name||"Error",code:error?.code||null,message:error?.message||String(error)}}finally{await close();server.close();await rm(profile,{recursive:true,force:true}).catch(()=>{})}
const artifact=await readFile(join(root,"artifacts","ynx-wallet-web-pwa-0.1.0.zip"));result.artifact={name:"ynx-wallet-web-pwa-0.1.0.zip",bytes:artifact.length,sha256:createHash("sha256").update(artifact).digest("hex"),minimumOS:"modern browser with Service Worker support",signingClass:"unsigned-web-bundle"};await mkdir(dirname(evidencePath),{recursive:true});await writeFile(evidencePath,`${JSON.stringify(result,null,2)}\n`);console.log(JSON.stringify(result,null,2));process.exit(result.passed?0:1);
