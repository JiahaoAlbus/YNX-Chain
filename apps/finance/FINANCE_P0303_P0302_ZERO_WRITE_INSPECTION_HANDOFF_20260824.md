# Finance P0-303 read-only terminal-state inspection handoff

P0-302 ended after one authorized attempt with `phase=RELEASE_MATERIALIZE`, `failureClass=POSTWRITE_RELEASE_MATERIALIZE`, and exit status 1. Its terminal transport receipt was not validated. There is no retry or manual-rollback authority, and the remote state is unknown.

This successor is inspection-only. The reviewed script inventories only the exact P0-302 controls, stage container and leaf, backup container and leaf, release container and release, current link plus `.next` and `.rollback`, runtime metadata, service state, and six loopback/public HTTP bodies. Files expose only tuple, byte count, and SHA-256 metadata. Environment content and values are never printed. It performs no production-path write, mkdir, cleanup, deletion, service lifecycle, deployment, rollback, or SSH construction.

The corrected HTTP helper never stores a response body in a shell variable, command substitution, or file. A single `curl` request emits its final status marker through `%{stderr}` while the complete body remains on anonymous pipes and is streamed through `tee` to `wc -c` and `sha256sum`. Only the three bounded metric lines enter a shell variable. The positive fixture uses one request returning status 200 plus bytes `0x78 0x0a 0x0a` and proves all three body bytes and their raw hash are retained. A transport-failure fixture fails closed without a receipt. Static gates prohibit response-body variables, `mktemp`, temp cleanup, and remote writes.

The source hypothesis is deliberately not promoted as a fix. `RELEASE_MATERIALIZE` spans release-container creation, ownership changes, candidate move, and stage cleanup. The highest-probability defect is the pre/post-ownership identity comparison: it currently includes uid/gid even though the signed operation deliberately changes release-container group ownership. The local fixture used an unchanged process gid, so this comparison passed locally. P0-303 direct state will distinguish this from a later move or stage-cleanup failure.

Central `1979e248b` atomically released P0-302 as failed closed and nonreusable; Heavy is idle. Earlier P0-303 reviews rejected a lossy shell-body implementation and then a temporary-file implementation whose writes contradicted `remoteWrites=0`; both are retained as superseded history. The only executable blocker is a wholly-new Central P0-303 read-only single-use lease. Central must independently verify the exact streaming script, freeze one literal protected-host SSH argv with this script as stdin, execute it once with zero remote files and zero production mutation, return normalized evidence, and release the lease. No deployment successor may be prepared until that evidence is terminal.
