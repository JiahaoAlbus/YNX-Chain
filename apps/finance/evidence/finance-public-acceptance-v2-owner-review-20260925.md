# Finance Endpoint Authority v2 owner review — contract acceptance only

The exact public Finance `/version` response at `2026-09-25T08:57:14Z` named source commit `94e6997f4d817233d93f9b4a96fc51b5f31e98bf`, tree `f7f8a5effe5c5ea9983ca42079ab99fa30c96ab9`, and release `finance-weekly-v3-94e6997f4d81-linux-amd64`. The independent release-owner readback matched `/`, `/app.js` and `/wallet-auth.js` byte-for-byte with the candidate. This owner accepts that public source identity; the acceptance is not an account-approval claim.

The Finance source at that exact commit consumes `createBrowserProductSessionClient` and `ProductSessionGatewayFetchAdapter` through the accepted Wallet/Auth browser bundle, verifies the authority before private begin, passes the complete callback to the shared `handleReturn`, and gates private API requests on a fresh v2 proof. The packaged registry exactly equals `packages/wallet-auth/product-session-registry.json` (SHA-256 `e74e1668e631dd623a4364cc9580951a9fe96fcef0c66c5910d84c774f4fdc08`). The Finance-vendored callback SDK bundle is `apps/finance/web/vendor/product-session-browser-a7dad7ec.mjs` (SHA-256 `16b0d677ec21e84b5ce425138f175c37e1ac6d319db7fe5a85926b276dccd336`). The local cross-service test executed a real browser callback, durable Gateway NodeHost and Finance Go v2 proof verifier (1/1 pass on 2026-09-25). This owner accepts the **source and Product Session contract**, not an installed or publicly completed session.

Limits remain explicit: public `/api/endpoint-authority/v2/config` returned HTTP 503 because the six protected v2 authority inputs are absent. No protected root, signed manifest or checkpoint was fabricated. Installed Wallet approval, live public Product Session, callback recovery, private API completion, real account login, official Broker Sandbox, trading, signing, and production approval remain unverified/false. The pending Hosted Wallet consumer code is outside the accepted public source and needs a separate release. The acceptance JSON is an owner attestation for an offline authority issuer, not a manifest signature or deployment instruction.
