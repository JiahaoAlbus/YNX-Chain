# Finance Wallet asset-cache correction — source candidate only

- Owner branch: `codex/finance-cache-versioned-assets-20260925`, based on integrated `8d6a011607f120a1cc66fdd94a961a6a822685c0`.
- Implementation: `b8da86754f9b926f2b685b6cfaf8e59df9f19184`, tree `0209b403f7c0d514567495800da1aaa55d6aabd7`.
- Scope: `apps/finance/**` and `internal/finance/{server.go,finance_test.go}` only. No Wallet owner, Exchange, Quant, website, production or shared SDK edits.

Observed public failure (independent audit): the new Finance HTML was served while an old 182014-byte `/wallet-auth.js` with SHA-256 `c6419bddc26227bf884583a79fa508b33e9321ee45201812263f38c9b455b63c` stayed in the browser cache. It lacked Hosted Wallet. Disabling the browser cache exposed the new Hosted entry. This is a cache mismatch, not proof of a provider failure or a successful Wallet approval.

Correction: the HTML now names each of its 12 JS/CSS/manifest/icon dependencies with its exact 64-hex content SHA-256 query. The Finance release builder and pinned Wallet verifier independently reject an omitted, duplicate, unversioned or hash-mismatched reference. The verifier manifest is `0427154b6ec50482965c7ef04689958e9577638e0a68b97ab27d6fd6d0f800e4`; `index.html` is `da7688866e4c7db64998f286a5a2fde7d1845ea69a58d6b2fc3612eb13f04c89`. The Go server serves `/` and both auth callback HTML routes with `Cache-Control: no-store`; query-versioned asset URLs resolve to unchanged static bytes.

Local gates: Finance Node tests 163 total / 162 passed / 1 skipped (external Hosted dist absent); Go `go test ./internal/finance/...` passed; Wallet bundle double rebuild/pin passed (`204110` bytes, SHA-256 `30bbe997959706e287228588446aa34f68d8c75c998f6df9d0e82314fb9d21b6`); Finance security gate passed across 389 text files; `git diff --check` passed. Local Chrome test #1 cached an immutable old unversioned script, then normal-reloaded to the versioned script and confirmed Hosted UI and one tab. Local Chrome test #2 ran the exact old `94e6997f4d817233d93f9b4a96fc51b5f31e98bf` Finance HTML/scripts through an exact-origin route, observed no-provider unavailable on its old YNX button, switched to the new source, and observed the Hosted popup request at `/hosted/#connect=...`. Playwright routing disables HTTP cache, so test #2 proves old/new product behavior but not cache semantics; test #1 covers caching separately. Neither test uses a real installed Wallet, account approval, signing or transaction.

Integration/public gate: review and merge this candidate into the authoritative source, build a new Finance release with the existing source-bound pipeline, obtain a separate Finance deployment lease, and verify the public `index.html`, 12 asset URLs, `/version`, `/health`, exact bytes/headers and an ordinary previously cached browser refresh. Do not require users to clear cache. Then independently test Hosted approval/rejection and wallet-domain availability; current public `ERR_CONNECTION_RESET` at the Wallet origin remains another owner's issue. `deployedPublic`, `installed`, `realWalletApproval`, `signed`, `transacted`, and `migratedV2` remain false for this candidate.

Rollback: retain the current production release and its exact env/unit/Caddy/state binding; on an authorized deployment failure restore that release via the existing rollback-first Finance procedure. This source commit has not switched any public symlink or service and cannot be treated as a rollback authorization.
