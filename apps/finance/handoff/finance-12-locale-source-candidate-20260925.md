# Finance 12-locale source candidate — 2026-09-25

Status: `SOURCE_CANDIDATE_REVIEW_REQUIRED`. This is not a public release, an approved translation, a Wallet approval, or a testnet trade.

The Finance selector now offers English (default), Simplified Chinese (`zh-CN`, with `zh-Hans` saved-setting migration), Traditional Chinese, Japanese, Korean, Spanish, French, German, Portuguese, Russian, Arabic, and Indonesian. All 304 current message keys are present in each locale. The new ten-locale draft was machine-assisted from public UI strings, then checked for key/marker integrity and a focused financial-terminology review. No runtime translation service or secret text transfer is used. Arabic sets `dir=rtl`; dates and general amounts use the chosen locale. The EVM-only identity view now consumes this single Finance locale catalog, while Wallet signing requests, protocol payloads, addresses and diagnostic codes remain unchanged.

Focused terminology corrections cover order partial/full execution, cancellation reconciliation, token allowance, test-only local preview, Hardhat identity, simulated cash/shares and the Traditional Chinese evidence boundary. This is a focused review, not native-speaker certification. Before public activation, review the complete financial copy and private planning, statement, AI and provider-supplied dynamic text; these legacy surfaces still contain English strings or untranslated source data. Browser tests only prove rendered local copy and layout, not linguistic correctness.

Verification: `node --test apps/finance/tests/finance-12-locales.test.mjs apps/finance/tests/broker-execution-browser.test.mjs apps/finance/tests/evm-subject-browser.test.mjs apps/finance/tests/product-catalog.test.mjs` passed 33/33; `go test ./internal/finance/... ./apps/finance/cmd/...` passed; `git diff --check` and changed JavaScript syntax checks passed. The 390px Chromium test covered all 12 guest locales without document overflow, Arabic RTL, and no runtime translation request. EVM-only Chromium covered all 12 states, refresh, revoke, reject and separation from Standard Wallet.

The full Node suite is expected to fail `FINANCE_WALLET_FILE_INTEGRITY_MISMATCH` until the final source bytes and bundles receive an independent manifest review. Do **not** relax the verifier, modify its hardcoded pin here, or treat the earlier test-market manifest candidate as current. Freeze a new exact manifest candidate only after this language source is finalized, then request independent activation review. Public source/runtime binding, installed app flow, genuine Wallet approval, Product Session v2, Broker execution, and chain trading remain unverified/false.
