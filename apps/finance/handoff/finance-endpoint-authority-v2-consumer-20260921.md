# Finance Endpoint Authority v2 consumer handoff

Implementation checkpoint: `f949e7afb1e407780a8c93f9fac42d1aef775c6a` / tree `859175ce6ed09892e1e43f62fe0e877fae490506`, based on `42e0abaa46bcaa6a14158a78e70d1fcbde13e06e`.

The Finance consumer now uses the shared `sdk/js/endpoint-authority-v2.js` verifier. It does not copy its Ed25519, canonicalization, policy or progression logic. Server requests pass a fixed absolute Node executable and script with no shell and no request-derived arguments. If v2 is absent, partial, expired, revoked, equivocated, clock-rolled-back, CAS-conflicted or storage-lost, Product Session fails before Wallet Gateway network access; guest and Standard Wallet remain independent.

Web now obtains its verified public authority metadata from the shipped same-origin `GET /api/endpoint-authority/v2/config` path; no external bootstrapping global is required. The server verifies the signed root/manifest before returning them and includes its durable checkpoint and trusted-time anchor. The browser uses that server checkpoint as its recovery floor, so a complete IndexedDB plus localStorage clear cannot reset progression to the trust-root anchor. It still uses one IndexedDB read-write transaction for CAS, a marker for partial loss detection, a monotonic trusted clock, cross-tab invalidation and re-verification at begin, restore/callback, proof and order-persistence boundaries.

Malformed or missing v2 proof headers are rejected before the external Node verifier can start, and verifier subprocess concurrency is bounded. Node uses no-follow file-handle reads plus an append-only CAS: it fsyncs a complete transition, then atomically hard-links it to a fixed name derived from the previous checkpoint. It never reopens the checkpoint pathname for mutation, so a pre-existing symlink or competing transition is rejected without writing through to another target. Reaccepting the exact current manifest is an idempotent success with no self-transition; every non-idempotent next checkpoint is fully validated before publication, so an invalid next value cannot poison later reads. Per-transition commit markers detect deletion, a published transition with a missing marker repairs safely, and a marker without its transition fails closed. SIGKILL before publish leaves current state unchanged; SIGKILL after publish is recovered from the complete hard-linked transition. Twenty rounds of eight simultaneous OS processes each produced exactly one successful CAS and seven stable false results.

The checkpoint prefix must be in a service-owned directory with exact mode 0700. Every ancestor to the filesystem root must be a real directory, owned by root or the service uid, and not group/other writable; the complete chain identity is checked around publication. A persisted genesis remains historical across an accepted trust-root rotation, while rotation back to an older root and any transition root/sequence/trusted-clock rollback fail closed. Mobile 1.2.0 exposes capability rejection only: Expo digest/SecureStore are not presented as Ed25519/durable CAS.

The release builder carries the exact helper source graph at `authority-runtime/apps/finance/...` plus the unchanged shared SDK v2 verifier. The reproducible Linux amd64 candidate is `/tmp/ynx-finance-f949e7afb1e4-linux-amd64.tar.gz`, 30,506,088 bytes, SHA-256 `d06c61f46226b6006932a848b1a4d6dbf394ee2c08e3966e3ed569ed3042fe83`. It is local and unpublished.

Tests passed: Finance product Node 67/67 and authority adapter 15/15 (82/82 in the combined runner), two consecutive Go authority-gate invocations, Mobile 18/18 plus typecheck and Android/iOS Expo export, wallet reproducibility verifier, Finance security/smoke across 254 text files, and Go race/vet/build for all Finance packages. Chromium directly exercised the shipped same-origin config path, valid and invalid signatures, two-tab same-sequence equivocation, partial checkpoint loss, complete site-data reset anchored by the server checkpoint, clock rollback, expiry and key revocation with no cross-origin private network.

Truth remains false for public deployment, protected manifest activation, installed Wallet callback, provider verification, official Sandbox, production approval, account approval, signing and transactions. Activation requires Central-provided protected trust root and signed manifest, trusted-clock feed, a checkpoint directory satisfying the full protected ancestor-chain contract, and a separate deployment lease. Rollback is the prior deployed Finance release; no runtime was changed by this work.

## 2026-09-25 activation handoff (read-only)

The later Finance Web/runtime source `3f9b9dacc21c627fb4de1d475dafd3eee1ec8676` is publicly version-readable, but that does **not** activate private Endpoint Authority v2. A same-day read of `/api/endpoint-authority/v2/config` returned HTTP 503 once; later bounded reads timed out. Neither result identifies the protected host files. No production signer or protected public-key origin was verified in the available owner records: **UNKNOWN**. `chain-metadata/endpoint-authority/v2/trust-root.example.json` has no keys and is an example, not a production root. `chain-metadata/endpoint-authority/current.json` remains a v1.1 pin with `walletGateway` and `products.finance` pending. Do not mint a fixture signature or treat a Broker API key as a login prerequisite.

`apps/finance/cmd/server/main.go` passes exactly these six environment inputs into `internal/finance/endpoint_authority.go`; all six must be absent or all six must be fixed absolute paths:

| Input | Existing implementation / remaining production evidence |
| --- | --- |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_NODE_BINARY` | Node subprocess integration exists; deployment-host executable path, version and byte identity require a fresh release binding. |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_SCRIPT` | `apps/finance/scripts/finance-endpoint-authority-v2.mjs` is packaged; its installed absolute path and hash require a fresh release binding. |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_TRUST_ROOT_FILE` | Protected, reviewed public root and signer-key provenance are **UNKNOWN**; the repository example is unusable. Central owns establishment. |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_MANIFEST_FILE` | A reviewed and genuinely signed v2 manifest is not evidenced. Central must bind `walletGateway` and `products.finance` as `VERIFIED` with exact observation/acceptance receipts. |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_CHECKPOINT_FILE` | Append-only checkpoint code exists; the deployed service-owned 0700 directory, complete protected ancestor chain, path and tuple are unverified. |
| `YNX_FINANCE_ENDPOINT_AUTHORITY_V2_TRUSTED_TIME_FILE` | Monotonic trusted-time/high-water logic exists; a deployed protected file and accepted live time-source receipt are unverified. Central owns the trusted-time source. |

The issuer/doctor/verify implementation already exists in `scripts/ops/endpoint-authority-v2.mjs`; issuance requires a reviewed payload digest, exact receipts and an external 0600 Ed25519 signer matching the protected root. Only the authority/signing owner should run `issue`. After Central supplies genuine root, signed manifest and receipts, the next single Finance owner can run `doctor`/`verify` against the protected paths and fixed consumer `ynx-finance-v1` / `https://finance.ynxweb4.com` / `1.0.0`, inspect six path identities and checkpoint/clock permissions without printing contents, then request a bounded deployment lease. Run `node --test apps/finance/authority/*.test.mjs` and `go test ./internal/finance/... ./apps/finance/...`; after deployment, bind `/version`, `/health`, and the same-origin authority-config response to the exact release before attempting real Wallet approval/callback. Rollback restores the previously signed Finance runtime and environment only under its own lease, while preserving (never deleting or downgrading) the append-only checkpoint and trusted-time high-water mark. A missing authority must leave private Finance degraded while Standard Wallet and public Finance remain usable.
