# Finance Linux preflight lease request — port-bound successor

**State:** `PENDING_CENTRAL_SINGLE_USE_LEASE`; **scope:** `apps/finance/**` only.

This P0-219 successor binds Finance source `c5ff310e6373c474cc52d6b6bca106602e77bc63` / tree `a40a70f797ddd72abe93a896aae425ecbc45e100` and the frozen Linux-amd64 archive `d8dcd45174dd50c93ef45af7d10d36dc078d6f4982da08dc92b9470e8290a59d`. P0-217, P0-218 and P0-219 are consumed/nonreusable. P0-219 was a Central NO_GO without a lease because carrier-root nlink was incorrectly requested as `1`; target nested layout is carrier `0:0:755:3`, digest directory `0:0:755:2`, archive `0:0:644:1`. This request authorizes only a wholly new isolated preflight, never a production release.

Central must sign one unused `127.0.0.1` port in `1024..65535`, immediate lease carrier root `/opt/ynx/preflight/finance/artifacts/p0<lease-number>`, exact archive basename `ynx-finance-7824af677dd0-linux-amd64-p0146.tar.gz`, three distinct tuples—carrier `0:0:755:3`, digest directory `0:0:755:2`, archive `0:0:644:1`—run directory, command bytes `17e84f676ec645307890940bb04498c05beb5ffff34abd9b01b999bd471beab0` (8,878 bytes), carrier regression `b8317ff4100ec0516e4d8276c267466f64d392107382b501efd64d780a7a6dca` (1,469 bytes), manifest verifier `a48ad5279656775a87d73533512cfc09cbc684cbbf0dc157ed9fa3caadee4cab`, and inode-rollback command `753f322303cbb264b7cd3f07d7b434ae4d2e2d0f95919768224fb2f5be891f9e`. The script requires realpath containment, no symlinks, all three exact stat tuples and archive SHA before creating its run directory.

Required receipts are cold start from absence; pre/post absence; created state device/inode/owner/mode/nlink/bytes/SHA; stopped exact-inode deletion; final absence; cleanup; result; and retained SHA256SUMS. `SHA256SUMS` is written last, excludes itself and temporary manifests, then verifies both every listed digest and exact equality with retained receipt files. No systemd, symlink, env, Caddy, public route, account request, signature, or transaction is permitted. P0-141/P0-174 remain nonreusable. A later production lease must freshly bind release, binary, env, unit, Caddy, state, service and public rollback values.
