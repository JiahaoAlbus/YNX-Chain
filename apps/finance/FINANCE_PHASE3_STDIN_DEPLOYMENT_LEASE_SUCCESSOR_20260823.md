# Finance Phase3 stdin deployment lease successor

Central's latest Phase3 review found one further production-layout mismatch: the existing host has `/opt/ynx/releases/finance`, but not the new lease-owned directory beneath it. The prior fixture accidentally hid this by pre-creating the candidate release directory.

This successor retains the corrected identity split: the exact carrier remains `p0228-finance-phase1-20260822T234100Z` with the accepted P0-236 tuples and hashes, while a wholly new Phase3 lease ID owns only mutable paths. The executor now requires the existing release parent tuple and an absent lease-owned release container, creates that container once without parent creation or replacement, verifies its signed path/UID/GID/mode, and emits its full tuple. It then moves the candidate and emits the candidate-release full tuple plus deterministic tree-inventory SHA-256.

Before deploy begins, any partial placement is removed only when the exact created file identity still matches. On a post-move failure, the executor first restores the old current/env/state/service, then deletes the candidate release only when its exact full tuple and inventory still match; it removes the release container only when its captured tuple still matches and it is empty. A foreign file, symlink, or post-move substitution is preserved and forces failure. The actual-shell suite covers the positive path, missing parent, foreign content, symlink, pre-move cleanup, normal post-move cleanup and post-move substitution, and still invokes the exact stdin bootstrap plus real executor across the P0-236-to-Phase3 boundary.

This remains a request only. Fresh public `/version` and `/health` were read without SSH and still report the old `3b2383f5...` runtime. Central must independently rerun all fixtures, fresh-read every production tuple, construct the exact signed lease, and freeze every literal command/argument before issuing a wholly new single-use lease. No production SSH, deployment, restart, current/unit/Caddy write, account request, signing or transaction occurred.
