# Finance Phase3 stdin deployment lease successor

Central's latest Phase3 review found that the prior executor incorrectly derived the already-prepared archive and environment paths from a future deployment lease ID. Those files actually belong to the immutable P0-228/P0-236 carrier, so a new Phase3 lease could never satisfy both identities.

This successor separates those identities. The exact carrier remains `p0228-finance-phase1-20260822T234100Z` at `/opt/ynx/stage/finance/p0228-finance-phase1-20260822T234100Z`, with the accepted P0-236 directory/file tuples and hashes. A wholly new Phase3 lease ID owns only its new stage, backup and release children. The bootstrap accepts raw bytes for one exact Central-signed, non-secret production lease on stdin, verifies the fixed carrier and prepared objects, refuses foreign content or a pre-existing deployment lease directory, atomically places the exact reviewed production executor and signed lease, and invokes deploy once. It never retries.

Before deploy begins, any partial placement is removed only when the exact created file identity still matches. Once deploy begins, the exact executor/lease pair is deliberately retained on success or failure. The production executor owns its first-failure rollback trap, while the retained pair freezes the only possible separately authorized manual recovery command: the same executor with `rollback` and the same lease. The actual-shell fixture now also runs the exact stdin bootstrap and reviewed executor together against a P0-236-style fixed carrier with a distinct new Phase3 ID, proving the corrected cross-stage path contract.

This remains a request only. Fresh public `/version` and `/health` were read without SSH and still report the old `3b2383f5...` runtime. Central must independently rerun all fixtures, fresh-read every production tuple, construct the exact signed lease, and freeze every literal command/argument before issuing a wholly new single-use lease. No production SSH, deployment, restart, current/unit/Caddy write, account request, signing or transaction occurred.
