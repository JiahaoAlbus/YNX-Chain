import test from "node:test"; import assert from "node:assert/strict"; import { mkdtemp, readFile } from "node:fs/promises"; import { join } from "node:path"; import { tmpdir } from "node:os"; import { BrowserState, PhishingPolicy, updateBoundary } from "../src/state.js";
test("tabs, history, bookmarks, downloads and recovery persist",async()=>{const dir=await mkdtemp(join(tmpdir(),"ynx-browser-"));const path=join(dir,"state.json");const state=new BrowserState(path);let tab=await state.openTab("https://a.example");tab=await state.updateTab(tab.id,{title:"A"});await state.recordVisit(tab,{title:"A",url:tab.url});await state.addBookmark({title:"A",url:tab.url});await state.recordDownload({filename:"a.pdf",url:"https://a.example/a.pdf",state:"completed"});const snap=await state.snapshot();assert.equal(snap.history.length,1);assert.equal(snap.bookmarks.length,1);assert.equal(snap.downloads.length,1);assert.equal((await state.recoveryPlan()).length,1)});
test("private state leaves no persistent trace",async()=>{const dir=await mkdtemp(join(tmpdir(),"ynx-browser-private-"));const path=join(dir,"state.json");const state=new BrowserState(path);const tab=await state.openTab("https://private.example",{privateMode:true});await state.recordVisit(tab,{title:"Private",url:tab.url});await state.recordDownload({filename:"private.txt",url:tab.url,state:"completed"},{privateMode:true});state.closePrivateWindow();await assert.rejects(readFile(path),{code:"ENOENT"})});
test("crash recovery excludes private tabs",async()=>{const dir=await mkdtemp(join(tmpdir(),"ynx-crash-"));const state=new BrowserState(join(dir,"state.json"));const tab=await state.openTab("https://a.example");await state.updateTab(tab.id,{crashed:true});await state.openTab("https://private.example",{privateMode:true});const plan=await state.recoveryPlan();assert.deepEqual(plan.map(x=>[x.url,x.crashed]),[["https://a.example",true]])});
test("phishing boundary states evidence limits",()=>{const policy=new PhishingPolicy({blockedOrigins:["https://bad.example"]});assert.equal(policy.check("https://bad.example/x").action,"warn");assert.equal(policy.check("https://unknown.example").claim,"not-a-safety-guarantee")});
test("updates require a valid signature and newer semver",()=>{assert.equal(updateBoundary({currentVersion:"1.0.0",offeredVersion:"1.1.0",signatureValid:true,channel:"stable"}).allowed,true);assert.equal(updateBoundary({currentVersion:"1.0.0",offeredVersion:"2.0.0",signatureValid:false,channel:"stable"}).allowed,false)});
