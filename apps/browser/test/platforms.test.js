import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";
const read=path=>readFile(new URL(path,import.meta.url),"utf8");
test("Android is an installable WebView app with isolated identity and exact callback",async()=>{const manifest=await read("../android/app/src/main/AndroidManifest.xml"),activity=await read("../android/app/src/main/java/com/ynxweb4/browser/MainActivity.java"),locales=await read("../android/app/src/main/java/com/ynxweb4/browser/LocaleCatalog.java");assert.match(manifest,/com\.ynxweb4\.browser/);assert.match(manifest,/ynxbrowser/);assert.match(manifest,/allowBackup="false"/);assert.match(activity,/new WebView\(this\)/);assert.match(activity,/AndroidKeyStore/);assert.match(activity,/putBoolean\("used:"/);assert.match(activity,/replayed response/);for(const locale of ["en","zh-Hans","zh-Hant","ja","ko","es","fr","de","pt","ru","ar","id"])assert.match(locales,new RegExp(`"${locale}"`));});
test("iOS is a native WKWebView project with Keychain P-256 identity and private store",async()=>{const project=await read("../ios/YNXBrowser.xcodeproj/project.pbxproj"),model=await read("../ios/YNXBrowser/BrowserModel.swift"),web=await read("../ios/YNXBrowser/BrowserWebView.swift");assert.match(project,/com\.ynxweb4\.browser\.ios/);assert.match(web,/WKWebView/);assert.match(web,/nonPersistent/);assert.match(model,/P256\.Signing\.PrivateKey/);assert.match(model,/kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly/);assert.doesNotMatch(model,/String\(repeating:"A"/);});
test("desktop apps expose mature engine and desktop-only recovery, files, permissions and update boundaries",async()=>{const mac=await read("../native/Sources/YNXBrowserNative/main.swift"),windows=await read("../windows/YNXBrowser.Windows/MainWindow.xaml.cs"),readme=await read("../windows/README.md");for(const token of ["WKWebView","setFrameAutosaveName","runOpenPanelWith","WKDownloadDelegate","requestMediaCapturePermissionFor","Signed update boundary"])assert.match(mac,new RegExp(token));for(const token of ["WebView2","PermissionRequested","DownloadStarting","ClearBrowsingDataAsync","ProcessFailed","OnKeyDown"])assert.match(windows,new RegExp(token));assert.match(readme,/does not claim a built or signed Windows package/);});
