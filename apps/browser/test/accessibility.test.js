import test from "node:test"; import assert from "node:assert/strict"; import { readFile } from "node:fs/promises";
test("browser chrome has language, labels, live status, focus and reduced-motion handling",async()=>{const html=await readFile(new URL("../src/renderer/index.html",import.meta.url),"utf8");const css=await readFile(new URL("../src/renderer/styles.css",import.meta.url),"utf8");for(const token of ['lang="en"','aria-label=','aria-live="polite"','<label'])assert.match(html,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(css,/:focus-visible/);assert.match(css,/prefers-reduced-motion/)});
