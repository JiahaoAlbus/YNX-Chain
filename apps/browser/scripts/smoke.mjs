import { mkdtemp } from "node:fs/promises"; import { join } from "node:path"; import { tmpdir } from "node:os"; import { BrowserState } from "../src/state.js"; import { PermissionStore, originPartition } from "../../../packages/web4-permissions/src/index.js";
const dir=await mkdtemp(join(tmpdir(),"ynx-browser-smoke-"));const state=new BrowserState(join(dir,"state.json"));const tab=await state.openTab("https://example.com");await state.recordVisit(tab,{title:"Example",url:tab.url});const permissions=new PermissionStore(join(dir,"permissions.json"));await permissions.decide(tab.url,"notifications","deny");if((await permissions.get(tab.url,"notifications"))!=="deny"||originPartition("https://example.com")===originPartition("https://ynx.org"))throw new Error("browser smoke failed");console.log("browser smoke ok: persistent state and origin-scoped permission boundary");
