# Dependency, license and SBOM review

Review date: 2026-07-29.

- After the lock update, `npm audit --offline --omit=dev --audit-level=high` reports zero Wallet runtime vulnerabilities. The online audit request timed out and is not counted as online evidence. A previous full development-tree audit reported ten moderate transitive findings in Expo build tooling through `xcode`/`uuid`; the proposed automatic fix downgraded Expo across a breaking boundary, so it was not applied. These packages do not handle Wallet keys at runtime, but the development tree must be rechecked on every Expo upgrade.
- `packages/wallet-auth` has three runtime packages and `npm audit --offline --omit=dev` reports zero vulnerabilities.
- Direct runtime inventory is fixed by `package-lock.json`: Noble curves/hashes, Expo SDK 57 modules, React Native 0.86, React 19, SVG/QR and the local canonical auth package.
- `npm query '*' --omit=dev` license inventory found MIT, ISC, BSD-2/3, Apache-2.0, MPL-2.0, CC0/CC-BY, Python-2.0, BlueOak, 0BSD and declared dual-license packages. The only entries without upstream license metadata were the two YNX packages; they are now explicitly `UNLICENSED`, so no outbound license grant is implied.
- `sbom.cdx.json` is a release-grade CycloneDX 1.6 runtime-dependency graph with 431 components and 504 dependency nodes. `@cyclonedx/cyclonedx-npm` is pinned to version 6.0.0 and package-lock integrity `sha512-kpWjjV0j5y0mMHUB5dSx1hxweH8K2blSqkgdQ6eHgU7aClB4CcXGhbHtGY6WVHSo3A01Rt7WOLas/wQ1E+tBDg==`. Exact transitive overrides keep npm 11's production-tree validation deterministic across the shared Expo dependency graph. Generation succeeds without `--ignore-npm-errors`; the production npm tree exits cleanly, all 431 components carry license metadata, and duplicate component references are rejected by `npm run sbom:check`.
- Deterministic `--output-reproducible` generation was repeated against source `a5c99e4e26e150aa6cf4138f4ecf8ac6d1ea8b2f` and produced the same SBOM SHA-256 `7d1afd4f176776840a45c131843cde1550154f738a1e3da67f1163cf88dab633`. The machine evidence is `proof/wallet-sbom-release-grade-2026-07-29.json`. Root contract-tooling `npm audit --omit=dev` reports zero runtime vulnerabilities; the root development dependency audit reports three high findings and remains a central build-toolchain gate outside the Wallet runtime package.
- Smart Account contract tooling pins `@account-abstraction/contracts` 0.8.0 (MIT, eth-infinitism) and resolves OpenZeppelin Contracts 5.6.1 (MIT). The official account-abstraction package also declares Uniswap v3-periphery, whose dependency closure includes GPL-2.0-or-later, GPL-3.0-or-later and BUSL-1.1 packages. YNX imports only EntryPoint/BaseAccount/interfaces and does not import Uniswap source into the Smart Account bytecode, but the complete build dependency closure remains subject to legal/license review before public distribution. The npm tarball integrity for account-abstraction 0.8.0 was independently checked as `sha512-8krPx/gpnoT+5xAroagVCbeA7FbUigMZWXFKKPm+oghyr29Dksssdx5sI7xGv9212i4JPaDDUGFk58dpuwVgHA==`.
