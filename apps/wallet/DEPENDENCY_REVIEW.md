# Dependency, license and SBOM review

Review date: 2026-07-22.

- `npm audit --omit=dev --audit-level=high` passes for Wallet: no high or critical finding. npm reports ten moderate transitive findings in Expo build tooling through `xcode`/`uuid`; the proposed automatic fix downgrades Expo across a breaking boundary, so it is not applied. These packages do not handle Wallet keys at runtime, but the finding must be rechecked on every Expo upgrade.
- `packages/wallet-auth` has three installed packages and `npm audit --omit=dev` reports zero vulnerabilities.
- Direct runtime inventory is fixed by `package-lock.json`: Noble curves/hashes, Expo SDK 57 modules, React Native 0.86, React 19, SVG/QR and the local canonical auth package.
- `npm query '*' --omit=dev` license inventory found MIT, ISC, BSD-2/3, Apache-2.0, MPL-2.0, CC0/CC-BY, Python-2.0, BlueOak, 0BSD and declared dual-license packages. The only entries without upstream license metadata were the two YNX packages; they are now explicitly `UNLICENSED`, so no outbound license grant is implied.
- `sbom.cdx.json` is a CycloneDX 1.6 production-dependency candidate with 431 components and 518 dependency nodes, generated by `@cyclonedx/cyclonedx-npm`. The generator still reports `ELSPROBLEMS` for four Expo override nodes (`@expo/json-file`, `@expo/spawn-async`, `chalk`, and `@babel/code-frame`) and required `--ignore-npm-errors` to emit the graph. The SBOM is therefore useful for review but is not yet release-grade completeness evidence. `package-lock.json` remains the authoritative full dependency graph; normalization plus error-free regeneration and schema/license validation remain release gates.
- Reproducible SBOM SHA-256: `dc2950c4bc999d4aaf01f38afca5628dd0f7f6ed483da928ad8d880d199353ee`. Root contract-tooling `npm audit --omit=dev` reports zero runtime vulnerabilities; the root development dependency audit reports three high findings and remains a central build-toolchain gate outside the Wallet runtime package.
- Smart Account contract tooling pins `@account-abstraction/contracts` 0.8.0 (MIT, eth-infinitism) and resolves OpenZeppelin Contracts 5.6.1 (MIT). The official account-abstraction package also declares Uniswap v3-periphery, whose dependency closure includes GPL-2.0-or-later, GPL-3.0-or-later and BUSL-1.1 packages. YNX imports only EntryPoint/BaseAccount/interfaces and does not import Uniswap source into the Smart Account bytecode, but the complete build dependency closure remains subject to legal/license review before public distribution. The npm tarball integrity for account-abstraction 0.8.0 was independently checked as `sha512-8krPx/gpnoT+5xAroagVCbeA7FbUigMZWXFKKPm+oghyr29Dksssdx5sI7xGv9212i4JPaDDUGFk58dpuwVgHA==`.
