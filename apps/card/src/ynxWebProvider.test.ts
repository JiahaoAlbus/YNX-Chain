import assert from "node:assert/strict";
import test from "node:test";
import {readFileSync} from "node:fs";
import {resolveYNXEip1193Provider} from "./wallet";

test("Card discovers only an explicit YNX EIP-6963 or injected provider",async()=>{const provider={request:async()=>"0x1917"},fallback={isYNXWallet:true,request:async()=>"0x1917"},listeners=new Map<string,(event:unknown)=>void>();const target={addEventListener:(name:string,listener:(event:unknown)=>void)=>void listeners.set(name,listener),removeEventListener:()=>{},dispatchEvent:()=>listeners.get("eip6963:announceProvider")?.({detail:{info:{rdns:"com.ynx.wallet"},provider}})};assert.equal(await resolveYNXEip1193Provider(target,0),provider);assert.equal(await resolveYNXEip1193Provider({ethereum:fallback} as never,0),fallback);assert.equal(await resolveYNXEip1193Provider({ethereum:{request:provider.request}} as never,0),null);assert.equal(await resolveYNXEip1193Provider({ethereum:{providers:[fallback,{isYNXWallet:true,request:provider.request}]}} as never,0),null);});
test("Web YNX button cannot construct a custom-scheme request without a provider",()=>{const app=readFileSync(new URL("../App.tsx",import.meta.url),"utf8"),webBranch=app.slice(app.indexOf('if(Platform.OS==="web")'),app.indexOf('const reusable='));assert.match(webBranch,/resolveYNXEip1193Provider/);assert.doesNotMatch(webBranch,/ynxwallet:|walletDeepLink|createAuthorization|Linking\.openURL/);});
