// Web custom-scheme launching is intentionally disabled. Chrome can create an
// inaccessible blank tab even when the Card page itself remains visible.
export function webCustomSchemeLauncherDisabled():"unavailable"{return"unavailable"}
