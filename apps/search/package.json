{
  "name": "@ynx/search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm test && npm run smoke"
  },
  "engines": { "node": ">=20" }
}
