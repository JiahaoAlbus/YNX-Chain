{
  "name": "@ynx/search",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run build && YNX_SEARCH_STATIC_DIR=dist node src/server.js",
    "build": "node build.mjs",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.mjs",
    "test:e2e": "playwright test",
    "check": "npm test && npm run smoke"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ynx-chain/wallet-auth": "file:vendor/ynx-chain-wallet-auth-1.0.0-safe-authorize-p0.0.tgz"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "esbuild": "0.25.12"
  }
}
