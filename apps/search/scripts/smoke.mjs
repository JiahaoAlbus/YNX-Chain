import{mkdtemp}from"node:fs/promises";import{join}from"node:path";import{tmpdir}from"node:os";import{spawn}from"node:child_process";import{fileURLToPath}from"node:url";import{SearchStore}from"../src/store.js";
const dir=await mkdtemp(join(tmpdir(),"ynx-search-smoke-")),dataPath=join(dir,"index.json"),store=new SearchStore(dataPath),port=44000+(process.pid%1000),origin=`http://127.0.0.1:${port}`;
const source=await store.registerSource({url:"https://docs.example/",label:"Authorized docs",authorizationEvidence:"operator ticket 2026-07",robotsPolicy:"respect"});await store.indexDocument(source.id,{url:"https://docs.example/permission",title:"Origin permission",text:"An origin permission is scoped to the exact source origin and reviewed by the user."});
const child=spawn(process.execPath,[fileURLToPath(new URL("../src/server.js",import.meta.url))],{env:{...process.env,PORT:String(port),YNX_SEARCH_DATA_PATH:dataPath},stdio:["ignore","pipe","inherit"]});
try{let health;for(let i=0;i<50;i++){try{const response=await fetch(`${origin}/api/health`);if(response.ok){health=await response.json();break}}catch{}await new Promise(resolve=>setTimeout(resolve,100))}if(!health)throw new Error("search service did not become healthy");if(health.provider.globalCoverage!==false||health.provider.coverage!=="registered-authorized-sources-only")throw new Error("provider truth contract missing");const result=await(await fetch(`${origin}/api/search?q=origin%20permission`)).json();if(result.total!==1||result.results[0].sourceUrl!=="https://docs.example/permission"||result.inference!==false||!result.results[0].indexReceiptDigest)throw new Error("search citation/index receipt smoke failed");const cross=await fetch(`${origin}/api/privacy/clear`,{method:"POST",headers:{"content-type":"application/json","origin":"https://evil.example"},body:"{}"});if(cross.status!==403)throw new Error("cross-origin privacy mutation was not rejected");const cleared=await fetch(`${origin}/api/privacy/clear`,{method:"POST",headers:{"content-type":"application/json","origin":origin},body:JSON.stringify({walletChallenges:true,aiAudit:true})});if(!cleared.ok)throw new Error("privacy clear smoke failed");console.log("search smoke ok: service health, bounded provider, cited receipt, CSRF and privacy clear");}finally{child.kill("SIGTERM")}
