import{mkdtemp}from"node:fs/promises";import{join}from"node:path";import{tmpdir}from"node:os";import{SearchStore}from"../src/store.js";
const dir=await mkdtemp(join(tmpdir(),"ynx-search-smoke-"));const store=new SearchStore(join(dir,"index.json"));const source=await store.registerSource({url:"https://docs.example/",label:"Authorized docs",authorizationEvidence:"operator ticket 2026-07",robotsPolicy:"respect"});await store.indexDocument(source.id,{url:"https://docs.example/permission",title:"Origin permission",text:"An origin permission is scoped to the exact source origin and reviewed by the user."});const result=await store.search("origin permission");if(result.total!==1||result.results[0].sourceUrl!=="https://docs.example/permission"||result.inference!==false)throw new Error("search smoke failed");console.log("search smoke ok: authorized index, source citation, freshness and retrieval boundary");
