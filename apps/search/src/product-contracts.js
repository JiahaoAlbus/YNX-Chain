import {createHash} from "node:crypto";

const AI_FIELDS=["version","requestId","product","workflow","provider","model","context","locale","costEstimate","consent","issuedAt","expiresAt"];
const INDEX_FIELDS=["version","sourceId","sourceUrl","authorizationRef","robotsDecision","contentDigest","fetchedAt","indexedAt","revision","status"];
export function createConsentReceipt({id,product,workflow,contextClasses,sourceUrls=[],provider,model,locale,expiresAt},now=new Date()){if(!/^[A-Za-z0-9_-]{16,128}$/.test(id??""))throw new Error("invalid consent id");if(product!=="search")throw new Error("invalid consent product");if(!Array.isArray(contextClasses)||contextClasses.length<1||contextClasses.some(value=>value!=="indexed-public-source-snippet"))throw new Error("invalid consent context");for(const url of sourceUrls)strictHttpUrl(url);const expiry=Date.parse(expiresAt);if(!Number.isFinite(expiry)||expiry<=now.getTime()||expiry-now.getTime()>300000)throw new Error("invalid consent expiry");const payload={version:"1",id,product,workflow,contextClasses:[...contextClasses],sourceUrls:[...sourceUrls],provider:String(provider),model:String(model),locale:String(locale),approvedAt:now.toISOString(),expiresAt:new Date(expiry).toISOString()};return Object.freeze({...payload,digest:digest("YNX_AI_CONSENT_V1",payload)})}
export function validateAiGatewayRequest(input,{now=Date.now()}={}){exact(input,AI_FIELDS,"AI Gateway request");bounded(input.version,/^1$/,"version");bounded(input.requestId,/^[A-Za-z0-9_-]{16,128}$/,"requestId");if(input.product!=="search")throw new Error("invalid AI product");if(typeof input.provider!=="string"||!input.provider||typeof input.model!=="string"||!input.model)throw new Error("provider and model required");const unsigned=Object.fromEntries(Object.entries(input.consent??{}).filter(([key])=>key!=="digest"));if(!input.consent||input.consent.digest!==digest("YNX_AI_CONSENT_V1",unsigned))throw new Error("AI consent tampered");if(input.consent.product!==input.product||input.consent.workflow!==input.workflow||input.consent.provider!==input.provider||input.consent.model!==input.model||input.consent.locale!==input.locale)throw new Error("AI consent binding mismatch");if(!input.context||!Array.isArray(input.context.items)||input.context.items.length<1||input.context.items.length>8)throw new Error("bounded AI context required");for(const item of input.context.items){if(typeof item.text!=="string"||item.text.length>50000||item.private===true)throw new Error("private or oversized AI context");if(item.sourceUrl&&!input.consent.sourceUrls.includes(item.sourceUrl))throw new Error("AI context source not consented")}if(!input.costEstimate||!Number.isSafeInteger(input.costEstimate.inputTokens)||!Number.isSafeInteger(input.costEstimate.outputTokens)||input.costEstimate.inputTokens<0||input.costEstimate.outputTokens<0)throw new Error("invalid AI cost estimate");const issued=Date.parse(input.issuedAt),expires=Date.parse(input.expiresAt);if(!Number.isFinite(issued)||!Number.isFinite(expires)||issued>now+30000||expires<=now||expires-issued>300000||expires>Date.parse(input.consent.expiresAt))throw new Error("invalid AI request time");return Object.freeze(structuredClone(input))}
export function validateAiGatewayResponse(response,request){if(!response||response.version!=="1"||response.requestId!==request.requestId||response.provider!==request.provider||response.model!==request.model)throw new Error("AI response binding mismatch");if(!["complete","cancelled","failed","unavailable"].includes(response.status))throw new Error("invalid AI response status");const allowed=new Set(request.consent.sourceUrls);if(!Array.isArray(response.citations)||response.citations.some(url=>!allowed.has(url)))throw new Error("AI response cites unconsented source");if(!Array.isArray(response.retrieval)||!Array.isArray(response.inference))throw new Error("AI retrieval and inference must be separate");return Object.freeze(structuredClone(response))}
export function createTrustReferral({product,kind,objectId,sourceUrl,reason,evidenceUrls=[]}){if(product!=="search"||!["abuse-removal","correction","appeal","ai-correction"].includes(kind))throw new Error("invalid Trust referral");if(!/^[A-Za-z0-9_-]{8,128}$/.test(objectId??"")||typeof reason!=="string"||reason.length<8||reason.length>2000)throw new Error("invalid Trust referral detail");const payload={version:"1",product,kind,objectId,sourceUrl:strictHttpUrl(sourceUrl),reason,evidenceUrls:evidenceUrls.slice(0,10).map(strictHttpUrl),createdAt:new Date().toISOString()};return Object.freeze({...payload,digest:digest("YNX_TRUST_REFERRAL_V1",payload)})}
export function validateIndexReceipt(input){exact(input,INDEX_FIELDS,"index receipt");if(input.version!=="1"||!input.sourceId||!input.authorizationRef)throw new Error("invalid index identity");strictHttpUrl(input.sourceUrl);if(!["allowed","blocked","override-with-evidence"].includes(input.robotsDecision))throw new Error("invalid robots decision");if(!/^[0-9a-f]{64}$/.test(input.contentDigest??"")||!Number.isSafeInteger(input.revision)||input.revision<1||!["ready","removed","failed"].includes(input.status))throw new Error("invalid index receipt");if(Date.parse(input.indexedAt)<Date.parse(input.fetchedAt))throw new Error("index time precedes fetch");return Object.freeze(structuredClone(input))}
export function canonicalDigest(domain,value){return digest(domain,value)}
function digest(domain,value){return createHash("sha256").update(`${domain}\n${canonical(value)}`).digest("hex")}
function canonical(value){if(value===null||typeof value!=="object")return JSON.stringify(value);if(Array.isArray(value))return`[${value.map(canonical).join(",")}]`;return`{${Object.keys(value).sort().map(key=>`${JSON.stringify(key)}:${canonical(value[key])}`).join(",")}}`}
function strictHttpUrl(value){const url=new URL(value);if(!["https:","http:"].includes(url.protocol)||url.username||url.password||url.hash)throw new Error("invalid source URL");return url.href}
function exact(value,fields,label){if(!value||typeof value!=="object"||Array.isArray(value)||Object.keys(value).sort().join("\n")!==[...fields].sort().join("\n"))throw new Error(`${label} fields mismatch`)}
function bounded(value,pattern,label){if(typeof value!=="string"||value.length>256||!pattern.test(value))throw new Error(`invalid ${label}`)}
