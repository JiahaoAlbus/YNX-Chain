import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";
test("search UI has semantic search, labels, live status, focus and responsive boundaries",async()=>{const html=await readFile(new URL("../src/public/index.html",import.meta.url),"utf8");const css=await readFile(new URL("../src/public/styles.css",import.meta.url),"utf8");for(const token of ['lang="en"','role="search"','aria-live="polite"','aria-label=','<label'])assert.ok(html.includes(token),`missing ${token}`);assert.match(css,/:focus-visible/);assert.match(css,/@media\(max-width/);assert.match(css,/prefers-reduced-motion/)})
