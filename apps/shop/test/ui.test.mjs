import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('buyer UI exposes complete truthful workflow',async()=>{const h=await readFile(new URL('../index.html',import.meta.url),'utf8');for(const text of['Sign in with YNX Wallet','Search products','Shipping address','Trust evidence'])assert.ok(h.includes(text));const js=await readFile(new URL('../app.js',import.meta.url),'utf8');assert.ok(js.includes('Continue to YNX Pay'));assert.match(js,/TaxStatus/);assert.match(js,/pay-handoff/)});
