# Third-party notices

Production frontend dependencies currently include `@noble/curves`, `@noble/hashes` and the YNX canonical Wallet Auth package. Build tooling includes esbuild. Exact versions and integrity hashes are in `package-lock.json`.

Before public distribution, generate notices from the complete resolved dependency graph, verify each license against its authoritative package source, and include Go dependency notices. This file is a release gate ledger, not a completed legal determination.
