import{spawn}from"node:child_process";import{fileURLToPath}from"node:url";const port=4274,child=spawn(process.execPath,[fileURLToPath(new URL("server.mjs",import.meta.url))],{env:{...process.env,PORT:String(port)}});try{await new Promise(r=>setTimeout(r,350));const r=await fetch(`http://127.0.0.1:${port}/`);if(!r.ok||!(await r.text()).includes("Creator Studio"))throw new Error("studio smoke failed");console.log("studio smoke ok")}finally{child.kill()}
