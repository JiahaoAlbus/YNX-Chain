import{spawn}from"node:child_process";import{fileURLToPath}from"node:url";const port=4274,child=spawn(process.execPath,[fileURLToPath(new URL("server.mjs",import.meta.url))],{env:{...process.env,PORT:String(port)}});try{let response;for(let attempt=0;attempt<30;attempt++){try{response=await fetch(`http://127.0.0.1:${port}/`);if(response.ok)break}catch{}await new Promise(resolve=>setTimeout(resolve,100))}if(!response?.ok||!(await response.text()).includes("Creator Studio"))throw new Error("studio smoke failed");console.log("studio smoke ok")}finally{child.kill()}
