import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";test("studio states human and evidence boundaries",async()=>{const h=await readFile(new URL("index.html",import.meta.url),"utf8");for(const term of ["owned media","malware","payout intent","Revenue dispute","AI production workspace"])assert.match(h,new RegExp(term,"i"));assert.match(h,/never enables monetization automatically/i);const js=await readFile(new URL("app.js",import.meta.url),"utf8");assert.doesNotMatch(js,/Math\.random|fake revenue/i)});
