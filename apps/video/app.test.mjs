import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("viewer exposes truthful and accessible states",async()=>{const h=await readFile(new URL("index.html",import.meta.url),"utf8");assert.match(h,/Sign in with YNX Wallet/);assert.match(h,/aria-live/);assert.match(h,/playlists/i);const js=await readFile(new URL("app.js",import.meta.url),"utf8");assert.match(js,/does not generate placeholder activity/);assert.doesNotMatch(js,/Math\.random|fake views/i)});
