import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";
test("viewer exposes complete truthful interaction paths",async()=>{const html=await readFile(new URL("index.html",import.meta.url),"utf8"),js=await readFile(new URL("app.js",import.meta.url),"utf8");for(const term of ["Sign in with YNX Wallet","Subscriptions","Playlists","History","Comments"])assert.match(html,new RegExp(term,"i"));for(const path of ["/watch","/comments","/reports","/subscription","/playlists","/history"])assert.match(js,new RegExp(path));assert.match(js,/original-fallback/);assert.match(js,/human_approved/);assert.match(js,/No placeholder records/);assert.doesNotMatch(js,/Math\.random|fake views/i)});
