import{completeWalletCallback,connectWallet,connectedSession,productSessionProof}from"./wallet-auth.js";
import{ready as i18nReady,t}from"./i18n.js";
const publicAPI=`${location.origin}/video/api`,localAPI="http://127.0.0.1:8423";
const API=localStorage.getItem("ynx.video.api")||(location.hostname==="127.0.0.1"||location.hostname==="localhost"?localAPI:publicAPI);
const $=selector=>document.querySelector(selector);
let current=null,lastWatchPosition=0,activeSession=null,replyTo="";

function requiredScope(path,method){if(path.includes("/comments"))return"video.comment";if(path.includes("/reports"))return"video.report";if(path.includes("subscription"))return"video.subscribe";if(path.includes("history")||path.includes("playlists")||path.includes("/watch"))return"video.history";return method==="GET"?"video.read":"video.history"}
export async function api(path,options={}){
  const headers={...(options.headers||{})},method=(options.method||"GET").toUpperCase(),isPublic=method==="GET"&&(/^\/v1\/videos(?:\/[^/]+)?$/.test(path.split("?")[0])||/\/comments$/.test(path)||/^\/v1\/channels\/[^/]+$/.test(path));if(!isPublic)headers["X-YNX-Product-Session-Proof"]=await productSessionProof(requiredScope(path,method));if(!["GET","HEAD"].includes(method))headers["Idempotency-Key"]||=crypto.randomUUID();
  let response;for(let attempt=0;attempt<2;attempt++){try{response=await fetch(API+path,{...options,headers});break}catch(error){if(attempt===1)throw error}}const data=await response.json().catch(()=>({error:"Invalid service response"}));
  if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);return data;
}
const json=(body)=>({method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});
function notice(message,bad=false){$("#notice").textContent=message;$("#notice").style.color=bad?"#8b1e2d":"#344054"}
function esc(value){return String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]))}
function empty(title,detail="No placeholder records are shown.",retry=false){$("#content").innerHTML=`<div class="empty"><h2>${esc(title)}</h2><p>${esc(detail)}</p>${retry?`<button id="retry-load">${esc(t("retry"))}</button>`:""}</div>`;$("#retry-load")?.addEventListener("click",()=>loadVideos($("#query").value))}
function activate(button){document.querySelectorAll("nav button").forEach(x=>x.classList.toggle("active",x===button))}

function videoCard(video){
  const article=document.createElement("article");article.className="card";
  const thumb=video.thumbnail_key?`<img class="thumb image" src="${API}/media/${encodeURI(video.thumbnail_key)}" alt="">`:"";
  const state=video.takedown?.state?`Takedown: ${video.takedown.state}`:`${video.status} · ${video.visibility}`;
  article.innerHTML=`<button class="card-open" aria-label="Play ${esc(video.title)}">${thumb}</button><h2>${esc(video.title)}</h2><p class="meta">${esc(state)}</p><p>${esc(video.description||"No description")}</p>`;
  article.querySelector("button").onclick=()=>openVideo(video);return article;
}
function renderVideos(videos,title){const box=$("#content");box.replaceChildren();if(!videos.length){empty(title);return}box.append(...videos.map(videoCard))}
async function loadVideos(query=""){$("#content").setAttribute("aria-busy","true");try{const videos=await api(`/v1/videos?q=${encodeURIComponent(query)}`);renderVideos(videos,query?"No matching published videos":t("empty"));notice("")}catch(error){empty(t("unavailable"),navigator.onLine?t("unavailable"):t("offline"),true);notice(t("unavailable"),true)}finally{$("#content").setAttribute("aria-busy","false")}}

async function openVideo(video){
  current=video;lastWatchPosition=0;$("#player-title").textContent=video.title;$("#player-state").textContent=video.takedown?`Unavailable: ${video.takedown.reason}`:`${video.captions?.length||0} caption track(s) · ${video.status}`;
  const media=$("#video");media.replaceChildren();const hls=video.variants?.find(v=>v.mime==="application/vnd.apple.mpegurl"),fallback=video.variants?.find(v=>v.name==="original-fallback");
  const chosen=hls&&media.canPlayType(hls.mime)?hls:fallback||hls;if(chosen)media.src=`${API}/media/${chosen.object_key}`;else media.removeAttribute("src");
  for(const caption of video.captions||[]){if(!caption.human_approved)continue;const track=document.createElement("track");track.kind="captions";track.label=caption.label;track.srclang=caption.language;track.src=`${API}/media/${caption.object_key}`;media.append(track)}
  await loadComments();$("#player").showModal();
}
async function loadComments(){if(!current)return;try{const comments=await api(`/v1/videos/${current.id}/comments`);$("#comments").innerHTML=comments.length?comments.map(c=>`<article class="${c.parent_id?"reply":""}"><b>${esc(c.Author||c.author)}</b><p>${esc(c.Body||c.body)}</p>${activeSession?`<button class="comment-reply outline" data-comment="${esc(c.ID||c.id)}">Reply</button>`:""}</article>`).join(""):"<p class=\"meta\">No comments yet.</p>";document.querySelectorAll(".comment-reply").forEach(button=>button.onclick=()=>{replyTo=button.dataset.comment;$("#comment textarea").focus();notice("Your next comment will be posted as a reply.")})}catch(error){$("#comments").textContent=error.message}}
async function flushWatch(completed=false){if(!current||!activeSession)return;const position=Math.floor($("#video").currentTime||0),seconds=Math.max(0,position-lastWatchPosition);if(seconds<1)return;lastWatchPosition=position;try{await api(`/v1/videos/${current.id}/watch`,json({seconds,completed}))}catch(error){notice(`Watch history not saved: ${error.message}`,true)}}

async function showSubscriptions(button){activate(button);try{const channels=await api("/v1/subscriptions");const box=$("#content");box.replaceChildren();if(!channels.length){empty("Subscriptions are empty");return}for(const channel of channels){const article=document.createElement("article");article.className="card";article.innerHTML=`<div class="thumb thumb-fallback">Channel</div><h2>${esc(channel.Name||channel.name)}</h2><p class="meta">@${esc(channel.Handle||channel.handle)}</p><button>Open channel</button>`;article.querySelector("button").onclick=()=>showChannel(channel.ID||channel.id);box.append(article)}}catch(error){notice(error.message,true)}}
async function showChannel(channelID){try{const view=await api(`/v1/channels/${channelID}`),channel=view.channel;renderVideos(view.videos||[],`@${channel.Handle||channel.handle} has no published videos`);notice(`${channel.Name||channel.name} · ${view.subscribers} persisted subscriber(s)`);$("#player").open&&$("#player").close()}catch(error){notice(error.message,true)}}
async function showPlaylists(button){activate(button);try{const lists=await api("/v1/playlists");const box=$("#content");box.replaceChildren();if(!lists.length){empty("Playlists are empty");return}for(const list of lists){const ids=list.VideoIDs||list.video_ids||[];const article=document.createElement("article");article.className="card";article.innerHTML=`<div class="thumb thumb-fallback">Playlist</div><h2>${esc(list.Name||list.name)}</h2><p class="meta">${ids.length} saved video(s)</p>`;box.append(article)}}catch(error){notice(error.message,true)}}
async function showHistory(button){activate(button);try{const events=await api("/v1/history");const box=$("#content");box.replaceChildren();if(!events.length){empty("History is empty");return}for(const event of events){const article=document.createElement("article");article.className="card";article.innerHTML=`<h2>${esc(event.VideoID||event.video_id)}</h2><p>${event.Seconds||event.seconds} watched second(s)</p><p class="meta">${esc(event.CreatedAt||event.created_at)}</p>`;box.append(article)}}catch(error){notice(error.message,true)}}

async function connectMetaMask(){const ethereum=globalThis.ethereum;if(!ethereum?.request)throw new Error("MetaMask was not found. Install it or use YNX Wallet.");const chainId="0x1917";try{await ethereum.request({method:"wallet_switchEthereumChain",params:[{chainId}]})}catch(error){if(error?.code!==4902)throw error;await ethereum.request({method:"wallet_addEthereumChain",params:[{chainId,chainName:"YNX Testnet",nativeCurrency:{name:"YNXT",symbol:"YNXT",decimals:18},rpcUrls:["https://rpc.ynxweb4.com"],blockExplorerUrls:["https://explorer.ynxweb4.com"]}]})}const accounts=await ethereum.request({method:"eth_requestAccounts"});if(!accounts?.[0])throw new Error("MetaMask did not return an account.");$("#wallet-state").textContent=`MetaMask connected: ${accounts[0].slice(0,8)}…${accounts[0].slice(-6)}. Use YNX Wallet for private Video actions.`}
$("#signin").onclick=()=>$("#wallet-choice").showModal();
$("#wallet-close").onclick=()=>$("#wallet-choice").close();
$("#wallet-guest").onclick=()=>{$("#wallet-choice").close();notice("Guest mode active. Public videos remain available without a Wallet session.")};
$("#wallet-ynx").onclick=async()=>{try{$("#wallet-state").textContent="Opening YNX Wallet for explicit approval…";await connectWallet()}catch(error){$("#wallet-state").textContent=error.message;notice(error.message,true)}};
$("#wallet-metamask").onclick=async()=>{try{await connectMetaMask()}catch(error){$("#wallet-state").textContent=error.message}};
try{activeSession=await completeWalletCallback()||await connectedSession()}catch(error){notice(error.message,true)}if(activeSession){$("#session").textContent="Wallet-authorized Product Session active. A fresh device proof protects every private action.";$("#signin").textContent="Wallet connected"}
$("#search").onsubmit=event=>{event.preventDefault();loadVideos($("#query").value)};
$("#close").onclick=async()=>{await flushWatch(false);$("#video").pause();$("#player").close()};
$("#video").addEventListener("pause",()=>flushWatch(false));$("#video").addEventListener("ended",()=>flushWatch(true));
$("#channel").onclick=()=>current&&showChannel(current.channel_id);$("#subscribe").onclick=async()=>{try{await api(`/v1/channels/${current.channel_id}/subscription`,{method:"POST"});notice("Subscription state persisted.")}catch(error){notice(error.message,true)}};
$("#playlist").onclick=async()=>{if(!current)return;try{let lists=await api("/v1/playlists");let selected;if(!lists.length){const name=prompt("Create a playlist");if(!name)return;selected=await api("/v1/playlists",json({name}))}else{const choice=prompt(lists.map((p,i)=>`${i+1}. ${p.Name||p.name}`).join("\n"),"1");if(!choice)return;selected=lists[Number(choice)-1]}const id=selected.ID||selected.id;if(!id)throw new Error("Invalid playlist selection");await api(`/v1/playlists/${id}/videos`,json({video_id:current.id}));notice("Video added to the persisted playlist.")}catch(error){notice(error.message,true)}};
$("#report").onclick=async()=>{const reason=prompt("Reason for human review");if(!reason)return;try{await api(`/v1/videos/${current.id}/reports`,json({reason,details:"Submitted from YNX Video viewer"}));notice("Report submitted for human review. No automatic takedown occurred.")}catch(error){notice(error.message,true)}};
$("#comment").onsubmit=async event=>{event.preventDefault();try{await api(`/v1/videos/${current.id}/comments`,json({body:event.target.elements[0].value,parent_id:replyTo}));event.target.reset();replyTo="";await loadComments();notice("Comment persisted.")}catch(error){notice(error.message,true)}};
const nav=[...document.querySelectorAll("nav button")];nav.find(b=>b.dataset.view==="discover").onclick=event=>{activate(event.currentTarget);loadVideos()};nav.find(b=>b.dataset.view==="subscriptions").onclick=event=>showSubscriptions(event.currentTarget);nav.find(b=>b.dataset.view==="playlists").onclick=event=>showPlaylists(event.currentTarget);nav.find(b=>b.dataset.view==="history").onclick=event=>showHistory(event.currentTarget);
$("#privacy").onclick=async()=>{if(!confirm("Delete your persisted watch history, subscriptions, playlists, and comment text? Minimal deletion audit evidence remains."))return;try{const result=await api("/v1/privacy/account-data",{method:"DELETE"});notice(`Viewer data deleted: ${Object.entries(result).map(([key,value])=>`${key} ${value}`).join(", ")}`);loadVideos()}catch(error){notice(error.message,true)}};
await i18nReady.catch(()=>null);const linkedVideo=new URLSearchParams(location.search).get("video");if(linkedVideo){api(`/v1/videos/${encodeURIComponent(linkedVideo)}`).then(openVideo).catch(error=>{notice(error.message,true);loadVideos()})}else loadVideos();
