import {spawn} from "node:child_process";import{fileURLToPath}from"node:url";const port=4273;const child=spawn(process.execPath,[fileURLToPath(new URL("server.mjs",import.meta.url))],{env:{...process.env,PORT:String(port)}});try{await new Promise(r=>setTimeout(r,350));const r=await fetch(`http://127.0.0.1:${port}/`);if(!r.ok||!(await r.text()).includes("YNX Video"))throw new Error("viewer smoke failed");console.log("viewer smoke ok")}finally{child.kill()}
