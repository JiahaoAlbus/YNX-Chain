import {discoverEIP6963} from "../../packages/dapp-connect-sdk/src/discovery.js";
import {DAppConnectError, productSessionStateFromError} from "../../packages/dapp-connect-sdk/src/errors.js";
import {StandardWalletConnection} from "../../packages/dapp-connect-sdk/src/provider.js";

export const WALLET_INSTALL_OPTIONS=Object.freeze([{id:"ynx",label:"Download YNX Wallet",url:"https://ynxweb4.com/dapp/download"},{id:"metamask",label:"Use MetaMask",url:"https://metamask.io/download/"}]);
export async function connectStandardWallet({windowLike=globalThis,timeoutMs=250,network=globalThis.YNX_STANDARD_WALLET_NETWORK}={}){const announced=await discoverEIP6963(windowLike,{timeoutMs});const preferred=[...announced].sort((left,right)=>walletRank(left)-walletRank(right))[0];const provider=preferred?.provider??windowLike.ethereum;if(!provider?.request)throw new DAppConnectError("WALLET_NOT_FOUND","No standard EVM wallet was found.",{details:{installOptions:WALLET_INSTALL_OPTIONS}});const connection=new StandardWalletConnection(provider);const connected=await connection.connect();const addChain=network?.rpcUrl?{chainId:"0x1917",chainName:"YNX Testnet",nativeCurrency:{name:"YNX Testnet",symbol:"YNXT",decimals:18},rpcUrls:[network.rpcUrl],blockExplorerUrls:network.explorerUrl?[network.explorerUrl]:[]}:undefined;const chain=await connection.ensureYNXTestnet({addChain});return Object.freeze({connection,providerInfo:preferred?.info??null,providerName:preferred?.info?.name??"EVM Wallet",account:connected.account,chainId:chain.chainId,state:"STANDARD_CONNECTED"})}
export function privateServiceDegraded({error=new DAppConnectError("PRODUCT_SESSION_GATEWAY_UNREACHABLE","Private Seller service is unavailable."),account=null,chainId=null}={}){return Object.freeze({standardConnection:Object.freeze({state:account?"STANDARD_CONNECTED":"NOT_CONNECTED",account,chainId}),privateService:productSessionStateFromError(error)})}
function walletRank(detail){const identity=`${detail?.info?.name??""} ${detail?.info?.rdns??""}`.toLowerCase();return identity.includes("ynx")?0:identity.includes("metamask")?1:2}
