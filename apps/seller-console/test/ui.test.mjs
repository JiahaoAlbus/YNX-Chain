import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('seller UI exposes operations and truthful boundaries',async()=>{const h=await readFile(new URL('../index.html',import.meta.url),'utf8');for(const text of['Catalog','Orders','Returns & refunds','Settlements','Policy','Roles','Audit','authoritative Pay settlement evidence'])assert.ok(h.includes(text));const js=await readFile(new URL('../app.js',import.meta.url),'utf8');assert.match(js,/not published/)});
