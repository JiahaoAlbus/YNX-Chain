import test from 'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('seller bearer is memory-only and central product-bound',async()=>{const js=await readFile(new URL('../wallet-auth.js',import.meta.url),'utf8');assert.ok(!js.includes('localStorage'));for(const v of['ynx-seller-v1','com.ynxweb4.seller-console','ynxseller://wallet-auth/callback','YNX_PRODUCT_SESSION_CHALLENGE_V1'])assert.ok(js.includes(v),v)});
test('seller console persists only locale preferences and supports RTL',async()=>{const js=await readFile(new URL('../i18n.js',import.meta.url),'utf8');for(const lang of['en','zh-CN','zh-TW','ja','ko','es','fr','de','pt','ru','ar','id'])assert.ok(js.includes(lang),lang);assert.ok(js.includes("language==='ar'?'rtl':'ltr'"));assert.ok(js.includes('ynx-ai-language'));assert.ok(!/localStorage\.setItem\([^)]*(token|bearer|session)/i.test(js))});
