const state={token:localStorage.getItem('ynx.calendar.session')||'',user:JSON.parse(localStorage.getItem('ynx.calendar.user')||'null'),weekStart:startOfWeek(new Date()),occurrences:[],selected:null,selectedEvent:null,pendingChange:null,editing:null,aiJob:null};
const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];const escapeHTML=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function toast(m){const el=$('#toast');el.textContent=m;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2600)}
function startOfWeek(d){const x=new Date(d);x.setHours(0,0,0,0);x.setDate(x.getDate()-((x.getDay()+6)%7));return x}function plusDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}function localInput(d){const x=new Date(d.getTime()-d.getTimezoneOffset()*60000);return x.toISOString().slice(0,16)}function mutationID(){return crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`}
async function api(path,options={}){const headers={'Content-Type':'application/json',...(options.headers||{})};if(state.token)headers.Authorization=`Bearer ${state.token}`;const r=await fetch(path,{...options,headers});const body=await r.json().catch(()=>({detail:'服务响应无效'}));if(!r.ok){if(r.status===401)signOut(false);throw new Error(body.detail||body.error||`请求失败 ${r.status}`)}return body}
async function beginSignIn(recovery=false){try{$('#signin-state').textContent='正在创建一次性 Wallet 授权请求…';const c=await api('/v1/auth/challenges',{method:'POST',body:'{}'});localStorage.setItem('ynx.calendar.challenge',c.id);localStorage.setItem('ynx.calendar.recovery',recovery?'1':'0');const intent=new URL('ynxwallet://authorize');intent.searchParams.set('product','com.ynx.calendar');intent.searchParams.set('scope',recovery?'calendar:recover':'calendar:account');intent.searchParams.set('challenge',c.id);intent.searchParams.set('callback',`${location.origin}/?calendar_callback=1`);intent.searchParams.set('expires_at',c.expires_at);$('#signin-state').textContent='已打开 Wallet，请核对 Calendar 范围和设备绑定。';location.href=intent.toString()}catch(e){$('#signin-state').textContent=e.message}}
async function completeCallback(){const encoded=new URLSearchParams(location.search).get('wallet_proof');if(!encoded)return;try{const proof=JSON.parse(new TextDecoder().decode(Uint8Array.from(atob(encoded.replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0))));if(proof.challenge!==localStorage.getItem('ynx.calendar.challenge'))throw new Error('Wallet 回调 challenge 不匹配');const endpoint=localStorage.getItem('ynx.calendar.recovery')==='1'?'/v1/auth/recovery':'/v1/auth/sessions';const result=await api(endpoint,{method:'POST',body:JSON.stringify(proof)});state.token=result.token;state.user=result.user;localStorage.setItem('ynx.calendar.session',state.token);localStorage.setItem('ynx.calendar.user',JSON.stringify(state.user));localStorage.removeItem('ynx.calendar.challenge');localStorage.removeItem('ynx.calendar.recovery');history.replaceState({},'',location.pathname)}catch(e){$('#signin-state').textContent=e.message}}
function signOut(show=true){state.token='';state.user=null;localStorage.removeItem('ynx.calendar.session');localStorage.removeItem('ynx.calendar.user');$('#signin').hidden=false;if(show)toast('Calendar 会话已撤销')}
function renderFrame(){const days=$('#days');days.replaceChildren();const weekdays=['周一','周二','周三','周四','周五','周六','周日'];for(let i=0;i<7;i++){const d=plusDays(state.weekStart,i),el=document.createElement('div');el.className=`day-head${d.toDateString()===new Date().toDateString()?' today':''}`;el.innerHTML=`<span>${weekdays[i]}</span><b>${d.getDate()}</b>`;days.append(el)}$('#range').textContent=`${state.weekStart.toLocaleDateString(undefined,{month:'short',day:'numeric'})} — ${plusDays(state.weekStart,6).toLocaleDateString(undefined,{month:'short',day:'numeric'})}`;const hours=$('#hours');hours.replaceChildren();for(let h=0;h<24;h++){const e=document.createElement('span');e.className='hour-label';e.style.top=`${h*60}px`;e.textContent=`${String(h).padStart(2,'0')}:00`;hours.append(e)}}
async function loadEvents(){if(!state.token)return;renderFrame();$('#app').setAttribute('aria-busy','true');const from=state.weekStart.toISOString(),to=plusDays(state.weekStart,7).toISOString();try{state.occurrences=await api(`/v1/events?from=${encodeURIComponent(from)}&to=${encodeURIComponent(to)}`);renderEvents()}catch(e){toast(e.message)}finally{$('#app').setAttribute('aria-busy','false')}}
function renderEvents(){const week=$('#week');week.replaceChildren();$('#empty').hidden=state.occurrences.length>0;const overlap=new Set();for(let i=0;i<state.occurrences.length;i++)for(let j=i+1;j<state.occurrences.length;j++){const a=state.occurrences[i],b=state.occurrences[j];if(new Date(a.start_utc)<new Date(b.end_utc)&&new Date(b.start_utc)<new Date(a.end_utc)){overlap.add(`${a.event_id}:${a.start_utc}`);overlap.add(`${b.event_id}:${b.start_utc}`)}}for(const o of state.occurrences){const start=new Date(o.start_utc),end=new Date(o.end_utc),day=(start.getDay()+6)%7,minutes=start.getHours()*60+start.getMinutes(),duration=Math.max(30,(end-start)/60000),el=document.createElement('button');el.className=`event${overlap.has(`${o.event_id}:${o.start_utc}`)?' conflict':''}`;el.style.left=`calc(${day} * 14.2857% + 3px)`;el.style.width='calc(14.2857% - 6px)';el.style.top=`${minutes}px`;el.style.height=`${duration}px`;el.innerHTML=`<b>${escapeHTML(o.title)}</b><span>${start.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}–${end.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</span>`;el.setAttribute('aria-label',`${o.title}，${start.toLocaleString()} 到 ${end.toLocaleTimeString()}`);el.onclick=()=>openEvent(o);week.append(el)}}
function populateTimeZones(){const zones=Intl.supportedValuesOf?.('timeZone')||['Asia/Shanghai','UTC','America/Los_Angeles','Europe/London'];const local=Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';$('#timezone').innerHTML=zones.map(z=>`<option${z===local?' selected':''}>${escapeHTML(z)}</option>`).join('');$('#zone-label').textContent=local}
function openForm(event=null){state.editing=event;const start=event?new Date(event.start_utc):new Date(Date.now()+3600000);start.setMinutes(Math.ceil(start.getMinutes()/30)*30,0,0);const end=event?new Date(event.end_utc):new Date(start.getTime()+3600000);$('#title').value=event?.title||'';$('#description').value=event?.description||'';$('#start').value=localInput(start);$('#end').value=localInput(end);$('#timezone').value=event?.time_zone||Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';$('#recurrence').value=event?.recurrence?.frequency||'';$('#count').value=event?.recurrence?.count||1;$('#invitees').value=(event?.invites||[]).map(i=>i.handle).join(', ');$('#meeting-link').value=event?.meeting_link||'';$('#event-title').textContent=event?'修改事件':'安排时间';$('#event-dialog').showModal();$('#title').focus()}
function eventInput(){const freq=$('#recurrence').value;return{title:$('#title').value,description:$('#description').value,local_start:$('#start').value,local_end:$('#end').value,time_zone:$('#timezone').value,recurrence:freq?{frequency:freq,interval:1,count:Number($('#count').value)}:{frequency:'',interval:0,count:0},invitees:$('#invitees').value.split(',').map(v=>v.trim()).filter(Boolean),reminders:[{minutes_before:Number($('#reminder').value),channel:'local'}],meeting_link:$('#meeting-link').value,client_mutation_id:mutationID(),base_version:state.editing?.version||0}}
async function submitEvent(e){e.preventDefault();const input=eventInput();if(!navigator.onLine){const queue=JSON.parse(localStorage.getItem('ynx.calendar.offlineQueue')||'[]');queue.push({kind:state.editing?'update':'create',eventID:state.editing?.id,input});localStorage.setItem('ynx.calendar.offlineQueue',JSON.stringify(queue));$('#event-dialog').close();toast('已加入离线队列；联网后仍需逐项预览批准');return}try{const path=state.editing?`/v1/events/${state.editing.id}/preview`:'/v1/events/preview';state.pendingChange=await api(path,{method:'POST',body:JSON.stringify(input)});showChange()}catch(e2){toast(e2.message)}}
function showChange(){const c=state.pendingChange,a=c.after;$('#change-preview').innerHTML=`<div class="preview-block"><span>变更</span><b>${escapeHTML(c.kind)}</b><span>事件</span><b>${escapeHTML(a.title)}</b><span>时间</span><b>${new Date(a.start_utc).toLocaleString()} — ${new Date(a.end_utc).toLocaleTimeString()}</b><span>时区</span><b>${escapeHTML(a.time_zone)}</b><span>邀请</span><b>${a.invites.length?a.invites.map(i=>escapeHTML(i.handle)).join('、'):'无'}</b><span>重复</span><b>${a.recurrence.frequency?`${escapeHTML(a.recurrence.frequency)} × ${a.recurrence.count}`:'不重复'}</b></div>${c.conflicts?.length?`<div class="conflicts"><b>${c.conflicts.length} 个冲突</b>${c.conflicts.map(x=>`<p>${escapeHTML(x.title)} · ${new Date(x.start_utc).toLocaleString()}</p>`).join('')}</div>`:''}`;$('#conflict-override').hidden=!(c.conflicts?.length);$('#accept-conflicts').checked=false;$('#event-dialog').close();$('#change-dialog').showModal()}
async function approveChange(){if(!state.pendingChange)return;try{const changeID=state.pendingChange.id;const event=await api(`/v1/changes/${changeID}/approve`,{method:'POST',body:JSON.stringify({accept_conflicts:$('#accept-conflicts').checked})});state.pendingChange=null;state.editing=null;$('#change-dialog').close();toast('变更已批准，可从事件详情撤销');await loadEvents();await openEvent({event_id:event.id});const actions=$('#event-content .detail-actions');if(actions){const undo=document.createElement('button');undo.className='quiet';undo.textContent='撤销刚才变更';undo.onclick=()=>revert(changeID);actions.prepend(undo)}}catch(e){toast(e.message)}}
async function openEvent(occurrence){try{const event=await api(`/v1/events/${occurrence.event_id}`);state.selected=occurrence;state.selectedEvent=event;$('#ai-begin').disabled=false;$('#ai-preview').textContent=`只会选择：${event.title}。点击后再查看 provider、模型和成本。`;const mine=event.owner_handle===state.user.handle;$('#event-content').innerHTML=`<span class="eyebrow">${escapeHTML(event.state)} · v${event.version}</span><h1>${escapeHTML(event.title)}</h1><p>${escapeHTML(event.description||'无说明')}</p><div class="detail-row"><span>时间</span><b>${new Date(event.start_utc).toLocaleString()} — ${new Date(event.end_utc).toLocaleString()}</b><span>时区</span><b>${escapeHTML(event.time_zone)}</b><span>组织者</span><b>${escapeHTML(event.owner_handle)}</b><span>重复</span><b>${event.recurrence?.frequency?`${escapeHTML(event.recurrence.frequency)} × ${event.recurrence.count}`:'不重复'}</b><span>提醒</span><b>${event.reminders?.map(r=>`提前 ${r.minutes_before} 分钟`).join('、')||'无'}</b><span>邀请</span><b>${event.invites?.map(i=>`${escapeHTML(i.handle)} · ${escapeHTML(i.state)}`).join('<br>')||'无'}</b><span>共享</span><b>${event.shares?.map(s=>`${escapeHTML(s.handle)} · ${escapeHTML(s.role)}`).join('<br>')||'无'}</b><span>会议链接</span><b>${event.meeting_link?`<a href="${escapeHTML(event.meeting_link)}" target="_blank" rel="noopener noreferrer">打开边界链接</a>`:'无'}</b></div><div class="detail-actions">${mine?'<button class="primary" id="edit-event">预览修改</button><button class="quiet" id="cancel-event">预览取消</button><button class="quiet" id="share-event">共享</button>':'<button class="primary" data-rsvp="accepted">接受</button><button class="quiet" data-rsvp="tentative">暂定</button><button class="quiet" data-rsvp="declined">拒绝</button>'}${event._lastChange?'<button class="quiet" id="revert-event">撤销上次变更</button>':''}<button class="quiet" id="close-detail">关闭</button></div>`;$('#event-detail').showModal();$('#close-detail').onclick=()=>$('#event-detail').close();if(mine){$('#edit-event').onclick=()=>{$('#event-detail').close();openForm(event)};$('#cancel-event').onclick=()=>previewCancel(event);$('#share-event').onclick=()=>shareEvent(event)};$$('[data-rsvp]').forEach(b=>b.onclick=()=>rsvp(event,b.dataset.rsvp));if($('#revert-event'))$('#revert-event').onclick=()=>revert(event._lastChange)}catch(e){toast(e.message)}}
async function previewCancel(event){if(!confirm('先创建取消预览；此时不会通知联系人。继续？'))return;try{state.pendingChange=await api(`/v1/events/${event.id}/cancel-preview`,{method:'POST',body:JSON.stringify({client_mutation_id:mutationID(),base_version:event.version})});$('#event-detail').close();showChange()}catch(e){toast(e.message)}}
async function shareEvent(event){const handle=prompt('共享给哪个 @handle？');if(!handle)return;const role=prompt('角色：viewer 或 editor','viewer');if(!role)return;if(!confirm(`确认将 ${event.title} 以 ${role} 权限共享给 ${handle}？共享可通过后续变更移除。`))return;try{await api(`/v1/events/${event.id}/share`,{method:'POST',body:JSON.stringify({handle,role})});toast('已共享并写入审计');$('#event-detail').close();loadEvents()}catch(e){toast(e.message)}}
async function rsvp(event,response){if(!confirm(`确认 RSVP：${response}？`))return;try{await api(`/v1/events/${event.id}/rsvp`,{method:'POST',body:JSON.stringify({response})});toast('RSVP 已更新');$('#event-detail').close();loadEvents()}catch(e){toast(e.message)}}
async function revert(changeID){if(!confirm('撤销上次变更？若事件已有更新将安全失败。'))return;try{await api(`/v1/changes/${changeID}/revert`,{method:'POST',body:'{}'});toast('变更已撤销并写入审计');$('#event-detail').close();loadEvents()}catch(e){toast(e.message)}}
async function syncOffline(){if(!navigator.onLine||!state.token)return;const queue=JSON.parse(localStorage.getItem('ynx.calendar.offlineQueue')||'[]');if(!queue.length)return;const item=queue[0];try{const path=item.kind==='update'?`/v1/events/${item.eventID}/preview`:'/v1/events/preview';state.pendingChange=await api(path,{method:'POST',body:JSON.stringify(item.input)});queue.shift();localStorage.setItem('ynx.calendar.offlineQueue',JSON.stringify(queue));toast('离线变更已恢复；仍需你的批准');showChange()}catch(e){toast(`离线变更需要处理：${e.message}`)}}
async function beginAI(){if(!state.selectedEvent)return;try{state.aiJob=await api('/v1/ai/jobs',{method:'POST',body:JSON.stringify({kind:$('#ai-kind').value,event_ids:[state.selectedEvent.id]})});$('#ai-preview').innerHTML=`<b>数据范围</b><br>${escapeHTML(state.aiJob.context_preview)}<br><br><b>Provider / 模型</b><br>${escapeHTML(state.aiJob.provider)} · ${escapeHTML(state.aiJob.model)}<br><br><b>成本估算</b><br>${escapeHTML(state.aiJob.cost_estimate||'由 Gateway 结算')}<br><small>批准后才会发送这 1 个事件的时间元数据。</small>`;$('#ai-begin').textContent='批准并开始（可取消）';$('#ai-begin').onclick=approveAI}catch(e){toast(e.message)}}
async function approveAI(){const jobID=state.aiJob.id;$('#ai-result').innerHTML='<div class="permission">AI 正在运行并流式记录状态。<button class="quiet" id="ai-cancel">取消生成</button></div>';$('#ai-cancel').onclick=()=>reviewAI('cancel');try{state.aiJob=await api(`/v1/ai/jobs/${jobID}/approve`,{method:'POST',body:'{}'});if(state.aiJob.state==='cancelled'){toast('AI 生成已取消，晚到结果不会应用');return}$('#ai-result').innerHTML=`<div class="ai-output">${escapeHTML(state.aiJob.result)}</div><p>结果尚未更改事件、邀请或自动化。</p><div class="detail-actions"><button class="primary" id="ai-apply">保留为建议</button><button class="quiet" id="ai-reject">拒绝</button></div>`;$('#ai-apply').onclick=()=>reviewAI('apply');$('#ai-reject').onclick=()=>reviewAI('reject')}catch(e){if(!String(e.message).includes('canceled'))toast(e.message)}}
async function reviewAI(decision){try{await api(`/v1/ai/jobs/${state.aiJob.id}/review`,{method:'POST',body:JSON.stringify({decision})});toast(decision==='apply'?'建议已保留，但未修改日程':'AI 建议已拒绝');state.aiJob=null;$('#ai-result').replaceChildren()}catch(e){toast(e.message)}}
function updateNetwork(){const offline=!navigator.onLine;$('#offline').hidden=!offline;if(!offline)syncOffline()}
function init(){populateTimeZones();renderFrame();completeCallback().finally(()=>{if(state.token&&state.user){$('#signin').hidden=true;$('#account').textContent=state.user.handle.replace('@','').slice(0,2).toUpperCase();loadEvents();syncOffline()}else $('#signin').hidden=false;$('#app').setAttribute('aria-busy','false')});$('#wallet-signin').onclick=beginSignIn;$('#account').onclick=()=>{if(confirm('撤销此 Calendar 设备会话并退出？'))api('/v1/auth/session',{method:'DELETE',body:'{}'}).finally(()=>signOut())};$('#new-event').onclick=()=>openForm();$('#event-form').onsubmit=submitEvent;$('#approve-change').onclick=approveChange;$('#edit-change').onclick=()=>{$('#change-dialog').close();$('#event-dialog').showModal()};$('#today').onclick=()=>{state.weekStart=startOfWeek(new Date());loadEvents()};$('#prev').onclick=()=>{state.weekStart=plusDays(state.weekStart,-7);loadEvents()};$('#next').onclick=()=>{state.weekStart=plusDays(state.weekStart,7);loadEvents()};$('#ai-open').onclick=()=>{$('#ai-panel').hidden=false;$('#ai-open').setAttribute('aria-expanded','true')};$('#ai-close').onclick=()=>{$('#ai-panel').hidden=true;$('#ai-open').setAttribute('aria-expanded','false')};$('#ai-begin').onclick=beginAI;addEventListener('online',()=>{updateNetwork();toast('网络已恢复，准备离线变更预览')});addEventListener('offline',updateNetwork);updateNetwork();if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js')}
const renderEventsBase=renderEvents;renderEvents=()=>{renderEventsBase();$('#empty').style.display=state.occurrences.length?'none':'grid'};
init();
$('#wallet-signin').onclick=()=>beginSignIn(false);
$('#wallet-recover').onclick=()=>beginSignIn(true);
const auditButton=document.createElement('button');auditButton.className='avatar';auditButton.textContent='审';auditButton.setAttribute('aria-label','打开 Calendar 审计');auditButton.onclick=showAudit;$('#ai-open').before(auditButton);
async function showAudit(){try{const audit=await api('/v1/audit');const dialog=document.createElement('dialog');dialog.className='change-dialog';const card=document.createElement('div');card.className='change-card';card.innerHTML='<span class="eyebrow">账户证据</span><h2>Calendar 审计</h2><p>预览、批准、撤销、RSVP、共享、提醒和 AI 审批均在此留痕。</p>';for(const entry of audit.slice(-25).reverse()){const row=document.createElement('p');row.textContent=`${new Date(entry.created_at).toLocaleString()} · ${entry.action}`;card.append(row)}const close=document.createElement('button');close.className='primary';close.textContent='关闭';close.onclick=()=>dialog.close();card.append(close);dialog.append(card);document.body.append(dialog);dialog.addEventListener('close',()=>dialog.remove());dialog.showModal()}catch(e){toast(e.message)}}
let lastReminder=localStorage.getItem('ynx.calendar.lastReminder')||'';async function loadReminders(){if(!state.token)return;try{const reminders=await api('/v1/reminders');if(reminders[0]&&reminders[0].id!==lastReminder){lastReminder=reminders[0].id;localStorage.setItem('ynx.calendar.lastReminder',lastReminder);toast(`提醒：${reminders[0].title}${reminders[0].state.includes('late')?'（重启后恢复）':''}`)}}catch{}}setInterval(loadReminders,30000);setTimeout(loadReminders,1200);
