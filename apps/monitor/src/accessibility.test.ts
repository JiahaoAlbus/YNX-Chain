import{readFileSync}from'node:fs';import test from'node:test';import assert from'node:assert/strict';const source=readFileSync('src/App.tsx','utf8');test('includes labelled navigation, login controls, errors and approval inputs',()=>{for(const marker of ['aria-label="Monitor views"','autoComplete="username"','autoComplete="current-password"','role="alert"','aria-label={`Approval phrase','aria-label="Close AI incident summary"'])assert.ok(source.includes(marker),marker)});
