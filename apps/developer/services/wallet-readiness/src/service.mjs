const PROTOCOL="ynx-code-wallet-readiness/v1",PRODUCT_CLIENT_ID="ynx-developer-v1",BUNDLE_ID="com.ynxweb4.developer.testnetpreview",CALLBACK="ynxdeveloper://wallet-auth/callback",SCOPES=["account:read","developer:deploy"],MAX_RESPONSE=64*1024;

export function createWalletReadinessService({ownerForRequest,gatewayURL=process.env.YNX_CODE_WALLET_GATEWAY_URL||"http://127.0.0.1:18445",fetcher=fetch,maxOwnerRequests=2,maxGlobalRequests=32}={}){
  const upstream=approvedUpstream(gatewayURL),owners=new Map();let active=0;
  async function handler(request,response){const url=new URL(request.url,`http://${request.headers.host||"127.0.0.1"}`);if(url.pathname!=="/runtime/wallet/readiness")return false;if(request.method!=="GET"){json(response,405,{code:"wallet_route_not_allowed",error:"Wallet readiness accepts GET only."});return true}const owner=ownerForRequest?.(request);if(!owner){json(response,401,{code:"workspace_session_required",error:"A signed workspace session is required."});return true}const ownerActive=owners.get(owner)||0;if(ownerActive>=maxOwnerRequests||active>=maxGlobalRequests){json(response,429,{code:"wallet_capacity_exhausted",error:"Wallet readiness capacity is currently full."});return true}owners.set(owner,ownerActive+1);active++;try{const [health,ready,version]=await Promise.all([read(fetcher,new URL("health",upstream)),read(fetcher,new URL("ready",upstream)),read(fetcher,new URL("version",upstream))]),attestation=developerAttestation(version);json(response,200,{protocolVersion:PROTOCOL,gateway:{reachable:true,remoteDeployed:health.remoteDeployed===true,runtimeReady:ready.runtimeReady===true,publicDeploymentReady:ready.publicDeploymentReady===true,build:version.build??null},developerBinding:attestation});return true}catch(error){json(response,error.status||503,{protocolVersion:PROTOCOL,gateway:{reachable:false,remoteDeployed:false,runtimeReady:false,publicDeploymentReady:false,build:null},developerBinding:developerAttestation(null),code:error.code||"wallet_gateway_unreachable",error:error.message||"Wallet Gateway is unreachable."});return true}finally{active--;const next=(owners.get(owner)||1)-1;if(next)owners.set(owner,next);else owners.delete(owner)}}
  return{handler,status:()=>({protocolVersion:PROTOCOL,upstream:upstream.origin,maxOwnerRequests,maxGlobalRequests,active})};
}

function developerAttestation(version){const ids=Array.isArray(version?.enabledProductClientIds)?version.enabledProductClientIds:[],registrySha256=version?.registrySha256,exact=ids.includes(PRODUCT_CLIENT_ID)&&typeof registrySha256==="string"&&/^[0-9a-f]{64}$/.test(registrySha256);return{productClientId:PRODUCT_CLIENT_ID,bundleId:BUNDLE_ID,callback:CALLBACK,scopes:SCOPES,attested:exact,registrySha256:exact?registrySha256:null,reason:exact?"exact_gateway_registry_attested":"gateway_version_does_not_attest_developer_registry"}}
function approvedUpstream(value){let url;try{url=new URL(value.endsWith("/")?value:`${value}/`)}catch{throw new Error("YNX_CODE_WALLET_GATEWAY_URL must be an absolute URL.")}if(url.username||url.password||url.search||url.hash||url.protocol!=="http:"||!["127.0.0.1","localhost","::1"].includes(url.hostname))throw new Error("YNX_CODE_WALLET_GATEWAY_URL must be credential-free loopback HTTP.");return url}
async function read(fetcher,url){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),3000);let response;try{response=await fetcher(url,{headers:{accept:"application/json"},redirect:"error",signal:controller.signal})}catch{throw fault("Wallet Gateway is unreachable.","wallet_gateway_unreachable",503)}finally{clearTimeout(timer)}const declared=Number(response.headers?.get?.("content-length")||0);if(declared>MAX_RESPONSE)throw fault("Wallet Gateway response is too large.","wallet_response_too_large",502);const text=await response.text();if(Buffer.byteLength(text)>MAX_RESPONSE)throw fault("Wallet Gateway response is too large.","wallet_response_too_large",502);let value;try{value=JSON.parse(text)}catch{throw fault("Wallet Gateway returned invalid JSON.","wallet_invalid_response",502)}if(!response.ok||value?.ok!==true)throw fault("Wallet Gateway rejected its readiness probe.","wallet_gateway_rejected",502);return value}
function json(response,status,value){response.writeHead(status,{"content-type":"application/json; charset=utf-8","cache-control":"no-store"});response.end(JSON.stringify(value))}
function fault(message,code,status){return Object.assign(new Error(message),{code,status})}
