# Developer 59508494 release candidate

Source `595084945e635daf0efdfbc5a43e984629fc87e4`, tree `4b64c14a8b9b4af189fcc912066e918910da1df7`, replaces 5ad as the release candidate. It prevents incompatible legacy browser recovery data from being replaced by the starter project. The recovery screen preserves original bytes, offers download/retry, and verifies a separate backup before an explicit new workspace. The existing native snapshot remains usable if WebKit storage cannot be read.

The Mac arm64 DMG is 298980748 bytes, SHA-256 `2ac2174b52db53fed9c87d66527145dd6a72555d8524837b601379327913f158`. Minimum OS remains 13.5. Exact build, checksum, ad-hoc signature and all 13 Mach-O / 14 architecture compatibility records passed; 14 recovery/native regression tests passed. Installed 595 upgrade, clipboard, split/diff, original Desktop profile recovery and macOS 13.5 runtime acceptance are pending. Older 048 Select All/Undo/Redo proof is historical.

The Linux x64 Web r2 archive is 137676746 bytes, SHA-256 `c229b056dbc9b2641b5b18b9081b888d328d645e0221eda5b12fadf540434973`. It contains every workspace dependency directory, including the language service's TypeScript 5.9.3. The first Linux archive omitted that nested dependency and is rejected. The r2 candidate started on an isolated loopback port with separate temporary state, returned exact source/tree health identity, and served all 203 frontend files with matching hashes. Twenty Linux tests passed. This is not public deployment or user-interface acceptance.

Actual public topology was verified through the listening process and 21,505 file hashes. Caddy routes the Developer domain to `ynx-code-candidate.service`, port 18113, `/opt/ynx-developer/candidates/current` → `d4052228a2261c5ced6a8e8cfcbf763edabf2103`. All 350 tracked source files matched that Git source. The old `ynx-developer-web.service` on 18111 and `/opt/ynx-developer/current` → `17ee9ae5` remain its AI upstream. The public health response lacks source/tree fields; the version string alone is not source identity.

All server persistence modules and the dependency lock are unchanged from public d405. `/var/lib/ynx-code-candidate` and its session/environment keys remain in place. The proposed deployment uses an exact source override for only the Gateway command, preserves existing environment files, backs up stopped state including SQLite WAL, and repeats quiet/owner/peer checks immediately before stopping. Code rollback preserves current user state. Explicit disaster restoration first retains the entire post-deploy state directory. Root controls deployment; no current link, service unit, production state or environment has been changed by this preparation.

Developer retains its existing complete vendored Wallet SDK tree `46edc44187b76a694c0bf3dccb62dc435ad68b76`, identical to d405. It is not the Creator/Viewer ff68 SDK and has not consumed the later 529 handoff. Installed Wallet return/signing and full consumer migration remain unverified.

Current Mac machine record: `apps/developer/evidence/desktop/macos-current-59508494.json`. The proposed immutable installer URL is `https://developer.ynxweb4.com/downloads/ynx-developer-0.2.0-testnet-preview-595084945e63-macos-arm64-unsigned.dmg`; it is not published. Root must still complete official download publication, full HTTPS byte verification, installation and visible use/save/restart recovery. Production signing/notarization remain false.
