import assert from "node:assert/strict";import{mkdtemp}from"node:fs/promises";import{join}from"node:path";import{tmpdir}from"node:os";import test from"node:test";import{createWorkspaceStore}from"../../workspace-manager/src/store.mjs";import{createProjectMemory}from"../src/service.mjs";
const vector=text=>{const value=Array(64).fill(0);for(const token of text.toLowerCase().match(/[a-z]+/g)||[])value[token.charCodeAt(0)%64]+=1;return value};
test("project memory indexes current revisions, performs vector ranking and isolates owners",async t=>{const root=await mkdtemp(join(tmpdir(),"ynx-memory-")),store=createWorkspaceStore({filename:join(root,"workspaces.sqlite")}),memory=createProjectMemory({filename:join(root,"memory.sqlite"),ownerForRequest:()=>"a",workspaceStore:store,embed:async inputs=>inputs.map(vector)});t.after(()=>{memory.close();store.close()});store.put("a","p",{expectedRevision:0,idempotencyKey:"memory-initial-a",payload:{name:"P",files:{"src/auth.ts":"export function authenticateWallet() { return verifySignature(); }","src/math.ts":"export function sum(a,b) { return a + b; }"},folders:["src"],open:[],active:"src/auth.ts"}});store.put("b","p",{expectedRevision:0,idempotencyKey:"memory-initial-b",payload:{name:"Other",files:{"private.ts":"secret customer ledger"},folders:[],open:[],active:"private.ts"}});const indexed=await memory.index("a","p",1);assert.equal(indexed.chunks,2);assert.equal(indexed.dimensions,64);const result=await memory.search("a","p","wallet authentication",1);assert.equal(result.results.length,1);assert.equal(result.results[0].path,"src/auth.ts");assert.equal((await memory.search("b","p","secret",5)).results.length,0);await assert.rejects(memory.index("a","p",0),error=>error.code==="revision_conflict")});
