# YNX Cloud 1.0.0 Testnet Preview

- Canonical, device-bound Sign in with YNX Wallet for native and Web clients; legacy/query/local-session paths removed.
- Files, folders, search, recent/starred, upload/download/verified preview, immutable versions, restore, sharing/grants/links/revoke/access requests, permissions, quota, audit, trash/restore, explicit permanent delete, offline queue, retry, conflict, and recovery.
- Content-addressed storage with verified reads, strict schemas and bounds, scanner/quota failure closure, atomic state persistence, and verified backup/restore.
- Fail-closed Cloud/Docs data separation across objects, listings, uploads, AI jobs, quota, audit, portability exports, access decisions, and physical-deletion records, including schema-v3 migration and legacy rollback output.
- Dependency-free JavaScript/TypeScript SDK with short-lived Wallet session callbacks, typed Cloud/Docs operations, bounded idempotent retries, and correlated request/error failures.
- Schema-v5 product usage ledger for accepted ingress, delivered egress, scan bytes, AI estimates, exact current storage, exact whole-second storage integration with explicit coverage start, and zero-charge economics while pricing is unconfigured.
- Enforced artifact retention windows and indefinite legal holds, plus an operator user-exit mode that keeps authentication, export, revoke, cancellation, and deletion available while blocking new writes.
- Schema-v6 dedicated-scope product-account erasure with exact confirmation, atomic retention preflight, collaboration/session/job/upload cleanup, hashed receipts, and crash-recoverable provider deletion truth; external backup/replica sanitization remains unclaimed.
- Schema-v7 versioned hot/cold/archive lifecycle with exact Account/Product binding, provider-result evidence, retry, same-owner/product dedup copy-on-write, archive restore-required reads, migration validation, portable transition history, and pending/failed deletion and product-erasure barriers. Provider-native lifecycle, CDN, replication/erasure coding and cross-region proof remain unclaimed.
- Twelve-locale Web and native export-first erasure controls keep routine sessions least-privileged, request `data.delete` separately, require exact confirmation, distinguish provider-pending receipts, and support receipt recovery after session revocation.
- Integrity-checked persistent RED telemetry, normalized route templates, correlated request traces, authenticated readiness, evaluated local alerts, and versioned dashboard/alert definitions.
- Explicit threat/security boundaries, deterministic CycloneDX dependency inventory, reviewed build-script allowlist, artifact verification provenance, and exact-source GitHub Actions run `30279314603` covering least-privilege image cold-start, Trivy 0.70.0 Critical/High OS/library scanning with zero configured-scope findings, retained JSON report Artifact `8658138768`, and recovery DAST. Immutable image publication, continuous monitoring, reproducible production provenance and signing remain unclaimed.
- Explicit-context AI with provider status, consent, cancel, review, accept/reject, citations, and audit; AI cannot share, delete, alter permissions, or overwrite source files.
- Twelve Web/native locales, Arabic RTL, light/dark, keyboard/focus, screen-reader labels, reduced motion, high contrast, and responsive desktop/mobile layouts.
- The Android Testnet Preview pinned to source `db9bc224` is debug-certificate signed and emulator installed; it has not been rebuilt from the current release source, so current `installedLocal` remains false. It is not production signed or store released. iOS is CI-ready but lacks current simulator/install evidence.
