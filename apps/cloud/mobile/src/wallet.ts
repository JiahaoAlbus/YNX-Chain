export const binding={requestingProduct:"cloud",productClientId:"ynx-cloud-mobile-v1",bundleId:"com.ynxweb4.cloud",callback:"ynxcloud://wallet-auth/callback",scopes:["ai.use","audit.read","files.read","files.write","permissions.manage"]};
export function requestURL(deviceKey:string,nonce:string,now=new Date()){const request={version:"1",nonce,chainId:"ynx_6423-1",...binding,productDeviceAlgorithm:"p256-sha256",productDeviceKey:deviceKey,purpose:"Access only files explicitly authorized to YNX Cloud",issuedAt:now.toISOString(),expiresAt:new Date(now.getTime()+300000).toISOString()};return `ynxwallet://authorize?request=${encodeURIComponent(JSON.stringify(request))}`}
export function approvalFromURL(url:string){const u=new URL(url);if(u.protocol!=="ynxcloud:"||u.host!=="wallet-auth"||u.pathname!=="/callback")throw Error("Wallet callback mismatch");const raw=u.searchParams.get("approval");if(!raw)throw Error("Wallet approval missing");const a=JSON.parse(raw);for(const key of["account","walletSignature","nonce","expiresAt","productClientId","bundleId","callback","productDeviceKey","requestingProduct","chainId"]){if(typeof a[key]!=="string")throw Error(`Wallet approval ${key} invalid`)}if(a.requestingProduct!==binding.requestingProduct||a.chainId!=="ynx_6423-1"||a.productClientId!==binding.productClientId||a.bundleId!==binding.bundleId||a.callback!==binding.callback)throw Error("Wallet approval binding mismatch");if(!Array.isArray(a.grantedScopes)||a.grantedScopes.length===0||new Set(a.grantedScopes).size!==a.grantedScopes.length||a.grantedScopes.some((x:unknown)=>typeof x!=="string"||!binding.scopes.includes(x as string)))throw Error("Wallet approval scopes invalid");return a}
