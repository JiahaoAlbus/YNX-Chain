import{readFileSync}from'node:fs';import test from'node:test';import assert from'node:assert/strict';const source=readFileSync('src/App.tsx','utf8');test('includes semantic navigation, search, live status and labelled detail controls',()=>{for(const marker of ['aria-label="Explorer sections"','role="search"','aria-live="polite"','role="alert"','aria-label="Evidence detail"','aria-label="Close detail"'])assert.ok(source.includes(marker),marker)});
