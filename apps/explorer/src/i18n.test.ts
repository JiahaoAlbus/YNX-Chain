import test from'node:test';import assert from'node:assert/strict';import{locales,requiredKeys,translate}from'./i18n.js';test('all supported locales resolve every required key without blanks',()=>{assert.equal(locales.length,12);for(const locale of locales)for(const key of requiredKeys)assert.ok(translate(locale,key).trim(),`${locale}:${key}`)});test('Arabic uses Intl without throwing',()=>assert.ok(new Intl.NumberFormat('ar').format(6423)));
