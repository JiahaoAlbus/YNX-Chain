import{readFileSync}from'node:fs';import test from'node:test';import assert from'node:assert/strict';const source=readFileSync('src/App.tsx','utf8'),i18n=readFileSync('src/i18n.ts','utf8');test('Explorer component contract includes all truthful domains',()=>{for(const label of ['Blocks','Transactions','Accounts','Contracts','Validators','Resources','Tokens','Governance','Trust','Analytics'])assert.ok(source.includes(label),label);assert.ok(source.includes('No estimated TPS'));assert.ok(i18n.includes('Upstream unavailable'));assert.ok(source.includes('sourceLinks(snapshot)'))});
