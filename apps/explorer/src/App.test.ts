import{readFileSync}from'node:fs';import test from'node:test';import assert from'node:assert/strict';const source=readFileSync(new URL('./App.tsx',import.meta.url),'utf8');test('Explorer component contract includes all truthful domains',()=>{for(const label of ['Blocks','Transactions','Accounts','Contracts','Validators','Resources','Tokens','Governance','Trust','Analytics'])assert.ok(source.includes(label));assert.ok(source.includes('No estimated TPS'));assert.ok(source.includes('Upstream unavailable'));assert.ok(source.includes('sourceLinks(snapshot)'))});
