# Quant Web provider-only Wallet handoff

Commit `5a4b7a75` consumes the accepted shared Provider discovery/connect reducer from `98c6d5d784d212df8981a53b17118a511e246ad2` (handoff `d3831c300560507f64a50e73117bab7b85926d9a`). Web discovers EIP-6963/EIP-1193 only, and a successful connection requires the selected provider, an approved account, and the provider's `eth_chainId` response of `0x1917`. It performs switch → 4902 add → re-switch, closes the reducer chooser and clears its pending intent only after that provider verification. Refresh uses `eth_accounts` and `eth_chainId`, while account/chain/disconnect events update the reducer. Browser RPC fetch is not a connection prerequisite; an accepted RPC-probe failure preserves the connected Standard Wallet. Product Session remains independently DEGRADED, and Research/Paper guest access plus official YNX Wallet and MetaMask links remain available.

`product-release.json` and `public-product-metadata.json` now withhold the legacy macOS and Windows ZIP download declarations. A direct runtime probe still returned HTTP 200 for both legacy ZIP URLs, so the remote withdrawal is not complete and needs the exact deployment authority. A valid replacement must be a current-source DMG with verified macOS launches, or a current-source Windows EXE/MSIX with verified Windows installation; neither exists yet.

Source scan, four focused unit tests, three browser tests, and the production Wallet bundle pass. This is source/build evidence only: public deployment, installation, real account approval, callback, Product Session, signing, orders, strategy execution, and ComputerControl remain false.
