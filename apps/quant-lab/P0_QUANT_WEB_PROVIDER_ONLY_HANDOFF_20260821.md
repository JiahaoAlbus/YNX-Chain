# Quant Web provider-only Wallet handoff

This historical handoff is superseded for Provider authority by `P0_QUANT_PROVIDER_AUTHORITY_CONSUMPTION_20260822.md`. The only authoritative Provider source binding is Wallet/Auth commit `98c6d5d784d212df8981a53b17118a511e246ad2`, repository tree `51a60a362d4ad5dd748bcdefb101f71b1d9e0cee`, and evidence `c3ab255c32bdeb9c8e056882c315f8ad43c29c7f`. Web discovers EIP-6963/EIP-1193 only, and a successful connection requires the selected provider, an approved account, and the provider's `eth_chainId` response of `0x1917`. The YNX action accepts only an exact discovered YNX Wallet candidate; MetaMask is independently discovered and never relabeled or routed through YNX Wallet. It performs switch → 4902 add → re-switch, closes the reducer chooser and clears its pending intent only after that provider verification. Refresh uses `eth_accounts` and `eth_chainId`, while account/chain/disconnect events update the reducer. Browser RPC fetch is not a connection prerequisite; an accepted RPC-probe failure preserves the connected Standard Wallet. Product Session remains independently DEGRADED, and Research/Paper guest access plus official YNX Wallet and MetaMask links remain available.

`product-release.json` and `public-product-metadata.json` now withhold the legacy macOS and Windows ZIP download declarations. A direct runtime probe still returned HTTP 200 for both legacy ZIP URLs, so the remote withdrawal is not complete and needs the exact deployment authority. A local current-source macOS arm64 UDZO DMG candidate was built from `5a4b7a75`, SHA-256 `45385ef09eb4681facbe1d08846e99623afdbf86df5faf139df0109ff92487ea`, 8,287,144 bytes; its ad-hoc signature verified and it mounted and launched twice with the expected version response. It is not hosted, production signed/notarized, or installed into Applications. A Windows EXE/MSIX with verified Windows installation does not exist yet.

Source scan, four focused unit tests, three browser tests, and the production Wallet bundle pass. This is source/build evidence only: public deployment, installation, real account approval, callback, Product Session, signing, orders, strategy execution, and ComputerControl remain false.

Public runtime audit at `2026-08-21T11:41:15Z`: `https://quant.ynxweb4.com/` returned HTTP 200 (19,299 bytes, SHA-256 `7030c610e5830b3c69b831caa8bdd5ff5bc3559ac83d340db6d40a9914802763`), but `/api/version` reported `443286487e057d78cb6b1a686d14bb37be8b3c23`, not this checkpoint. It is therefore an observed historical runtime, not source-bound public evidence.
