# Quant publication lease request — shared reducer candidate

**State:** `PENDING_CENTRAL_SINGLE_USE_LEASE`; **scope:** `apps/quant-lab/**` only.

Quant checkpoint `debc0dc218562e8ab0ac8a82e88d0a396e2d7669` / tree `0dda790c431a4b7ca5dc60fa0b786814707e73a2` consumes the authoritative provider reducer and exposes provider details, disconnect and switching. Central must freeze a new source-bound runtime archive (server executable and served Web asset inventory) before deployment; the current public runtime must not be attributed to this source.

The lease must freshly bind target/architecture, service/ExecStart/WorkingDirectory, environment, Caddy, state, active release/symlink, current binary/assets and public `/`, `/api/version`, `/health` responses with bytes/SHA. It must name immutable stage/backup/release paths, exact deploy/public-verifier/rollback command bytes and preserve a rollback receipt for every fresh binding.

Only non-sensitive public UI validation is in scope after deployment: guest view, distinct YNX/MetaMask chooser, no blank tab and details/disconnect controls. Account approval, signing, EIP-712, strategy execution, testnet orders and Product Session remain forbidden and false.
