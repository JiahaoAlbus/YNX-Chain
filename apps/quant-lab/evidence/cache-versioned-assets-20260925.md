# Quant cache-versioned runtime candidate — source only

- Implementation source: `d30f950733b0d938f9b20fce078b4eb6f24e53aa`, tree `688c039e462c71cfdbee6ccfb16be10a01f3a09f`, based on main `a2ac7f196d4aff9951b9ae94e27d8fde446859f0`.
- HTML binds `styles.css`, `wallet-auth.js`, `i18n.js`, and `app.js` by full content SHA-256 query. The runtime packager checks the final built `wallet-auth.js` bytes. The server sends `Cache-Control: no-store` for HTML and wallet callback fallbacks.
- Local Chrome cache-enabled old-to-new fixture uses a real HTTP server and browser cache, not Playwright route interception. Its old-wallet fixture is synthetic and does not assert old-public source identity.
- `npm test --prefix apps/quant-lab`: 20/20. `npm run test:browser --prefix apps/quant-lab`: 6/6. `npm run test:wallet-flow --prefix apps/quant-lab`: 16/16 after the test server was corrected to resolve URL pathnames independently of hash queries. `go test ./apps/quant-lab/... ./internal/quantlab/...`: pass. `node apps/quant-lab/scripts/verify-versioned-assets.mjs`: pass. Shared Wallet source verifier: pass.
- Reproducible Linux amd64 candidate: `/tmp/ynx-quant-d30f950733b0-a.tar.gz` and `-b.tar.gz`, each 3,630,477 bytes, SHA-256 `e9f2d8924e511f918c51cbde23437424aad1517d84ab2b64795744538addf9c2`. `cmp` and clean-extract `SHA256SUMS` pass. Binary `ynx-quantd` is ELF64 x86-64, 8,114,360 bytes, SHA-256 `cfb1a1b403d8c4ba121e0956e65a4810f727bd80d62c2d8d8ac3997431b079f8`.
- Earlier Mac-host packages from the same worktree were invalid Mach-O despite a Linux label. They are superseded and must not be deployed. The packager now fixes GOOS/GOARCH/CGO and rejects a non-ELF x86-64 binary.
- Deployment/public source binding: **false**. Public Quant `/api/version` was observed on old commit `26112825ef68a70cdeab5862186c1d05bd96c5e7`; this candidate has not been switched or verified publicly. Multi-instance state, real account approval, signature, order, trade, installed application, and Product Session completion remain unproved.
- Next action belongs to the independent Quant release controller: fresh read-only runtime/rollback binding and multi-instance readiness decision, exact single-use lease, artifact transfer with SHA readback, rollback-first switch, `/api/version`/HTML/asset byte proof, then direct browser lifecycle. Rollback pointer must be freshly read before a switch; do not infer one from this local candidate.
