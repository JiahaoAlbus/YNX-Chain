import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {fileURLToPath} from 'node:url';const root=fileURLToPath(new URL('../',import.meta.url));
test('Quant Web uses v2 provider-only standard wallet transport',async()=>{const [html,wallet,lock]=await Promise.all(['web/index.html','web/wallet-auth-entry.js','package-lock.json'].map(file=>readFile(root+file,'utf8')));for(const marker of ['launchWebAuthorization','provider-ready','eth_requestAccounts','wallet_switchEthereumChain','wallet_addEthereumChain',"'0x1917'",'Product Session remains DEGRADED'])assert.match(wallet,new RegExp(marker));for(const prohibited of ['ynxwallet://authorize','window.open','<iframe','location.assign','location.href=','createProductWalletConnection'])assert.doesNotMatch(wallet,new RegExp(prohibited.replaceAll('/','\\/')));for(const id of ['connect-wallet','connect-metamask','install-wallet','install-metamask'])assert.match(html,new RegExp(`id="${id}"`));assert.match(lock,/safe-launcher-v2/);});
