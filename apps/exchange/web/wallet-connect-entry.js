import {launchWebAuthorization} from './node_modules/@ynx-chain/wallet-auth/src/authorize-launcher.js';
import {WALLET_PROVIDER_KIND} from './node_modules/@ynx-chain/wallet-auth/src/wallet-provider-discovery.js';
import {createStandardWalletConnectState,reduceStandardWalletConnectState,STANDARD_WALLET_CHAIN_ID,STANDARD_WALLET_CONNECT_STATUS,STANDARD_WALLET_RPC_PROBE_TRANSPORT} from './node_modules/@ynx-chain/wallet-auth/src/standard-wallet-connect-state.js';

const YNX_CHAIN_HEX=STANDARD_WALLET_CHAIN_ID;
const downloads=Object.freeze({ynxWallet:'https://www.ynxweb4.com/dapp/download',metaMask:'https://metamask.io/download/'});
let walletState=createStandardWalletConnectState(),boundProvider=null,boundListeners=null;
function emit(){if(typeof window!=='undefined')window.dispatchEvent(new CustomEvent('ynx-exchange-standard-wallet-state',{detail:walletState}));return walletState}
function transition(event){walletState=reduceStandardWalletConnectState(walletState,event);return emit()}
function pendingIntent(){return crypto.randomUUID().replaceAll('-','')}
function safeCode(error){const code=String(error?.code||'WALLET_CONNECT_FAILED').toUpperCase().replace(/[^A-Z0-9_]/g,'_');return /^[A-Z][A-Z0-9_]{2,63}$/.test(code)?code:'WALLET_CONNECT_FAILED'}
function result(status=walletState.status){return Object.freeze({status,account:walletState.account,chainId:walletState.chainId,providerKind:walletState.providerKind,authority:walletState.authority,connectionState:walletState,downloads})}
function detach(){if(!boundProvider||!boundListeners)return;for(const [event,listener] of Object.entries(boundListeners)){try{if(typeof boundProvider.removeListener==='function')boundProvider.removeListener(event,listener);else boundProvider.off?.(event,listener)}catch{}}boundProvider=null;boundListeners=null}
function bind(provider){if(provider===boundProvider)return;detach();const accountsChanged=accounts=>{try{transition({type:'ACCOUNTS_CHANGED',accounts})}catch{}};const chainChanged=chainId=>{try{transition({type:'CHAIN_CHANGED',chainId})}catch{}};const disconnect=()=>{try{transition({type:'PROVIDER_DISCONNECT'})}catch{}};for(const [event,listener] of Object.entries({accountsChanged,chainChanged,disconnect})){try{provider.on?.(event,listener)}catch{}}boundProvider=provider;boundListeners={accountsChanged,chainChanged,disconnect}}

export async function connectProvider(provider,providerKind,authority){
  if(walletState.status===STANDARD_WALLET_CONNECT_STATUS.CONNECTED)return result('standard-connected');
  transition({type:'BEGIN',pendingIntent:pendingIntent()});transition({type:'PROVIDER_SELECTED',providerKind});
  try{try{await provider.request({method:'wallet_switchEthereumChain',params:[{chainId:YNX_CHAIN_HEX}]});}catch(error){if(error?.code!==4902)throw error;await provider.request({method:'wallet_addEthereumChain',params:[{chainId:YNX_CHAIN_HEX,chainName:'YNX Testnet',nativeCurrency:{name:'YNX Test Token',symbol:'YNXT',decimals:18},rpcUrls:['https://rpc.ynxweb4.com/'],blockExplorerUrls:['https://explorer.ynxweb4.com/']}]});await provider.request({method:'wallet_switchEthereumChain',params:[{chainId:YNX_CHAIN_HEX}]});}const chainId=await provider.request({method:'eth_chainId'});if(chainId!==YNX_CHAIN_HEX){transition({type:'FAIL',code:'WRONG_NETWORK'});return result('wrong-chain');}const accounts=await provider.request({method:'eth_requestAccounts'});if(!Array.isArray(accounts)||typeof accounts[0]!=='string'||!accounts[0])throw Object.assign(new Error('The standard Wallet returned no account.'),{code:'WALLET_NOT_AUTHORIZED'});transition({type:'ACCOUNT_APPROVED',account:accounts[0]});transition({type:'CHAIN_CONFIRMED',chainId});if(walletState.status!==STANDARD_WALLET_CONNECT_STATUS.CONNECTED)return result('wrong-chain');bind(provider);return result('standard-connected');}catch(error){transition({type:'FAIL',code:safeCode(error)});throw error}
}
async function discover(){return launchWebAuthorization(undefined,{scope:window,waitMs:160})}
async function connect(kind=WALLET_PROVIDER_KIND.YNX){const launch=await discover();const candidate=kind===WALLET_PROVIDER_KIND.METAMASK?launch.discovery?.metamask:launch.discovery?.ynx;if(launch.status!=='provider-ready'||!candidate?.provider)return Object.freeze({status:'unsupported',detail:launch.detail==='PROVIDER_DISCOVERY_AMBIGUOUS'?'PROVIDER_DISCOVERY_AMBIGUOUS':kind===WALLET_PROVIDER_KIND.METAMASK?'METAMASK_NOT_INJECTED':'YNX_WALLET_NOT_INJECTED',downloads,connectionState:walletState});return connectProvider(candidate.provider,candidate.kind,candidate.authority)}
async function restore(){const launch=await discover();if(launch.status!=='provider-ready'||!launch.providerCandidate?.provider)return result('not-restored');const provider=launch.providerCandidate.provider;try{const [accounts,chainId]=await Promise.all([provider.request({method:'eth_accounts'}),provider.request({method:'eth_chainId'})]);transition({type:'RESTORE',providerKind:launch.providerCandidate.kind,accounts,chainId});if(walletState.status===STANDARD_WALLET_CONNECT_STATUS.CONNECTED)bind(provider);return result(walletState.status===STANDARD_WALLET_CONNECT_STATUS.CONNECTED?'standard-connected':'wrong-chain')}catch(error){return Object.freeze({status:'not-restored',detail:safeCode(error),connectionState:walletState,downloads})}}
function reportAcceptedRpcProbe(status,code){return transition({type:status==='ready'?'RPC_PROBE_READY':'RPC_PROBE_DEGRADED',probeTransport:STANDARD_WALLET_RPC_PROBE_TRANSPORT,...(status==='ready'?{}:{code:safeCode({code})})})}
export function disconnect(){detach();return transition({type:'DISCONNECT'})}
if(typeof window!=='undefined')window.YNXExchangeWebWallet=Object.freeze({connect,connectYNX:()=>connect(WALLET_PROVIDER_KIND.YNX),connectMetaMask:()=>connect(WALLET_PROVIDER_KIND.METAMASK),restore,disconnect,reportAcceptedRpcProbe,state:()=>walletState,downloads});
