import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const html=fs.readFileSync(new URL('../web/index.html',import.meta.url),'utf8');const css=fs.readFileSync(new URL('../web/styles.css',import.meta.url),'utf8');const js=fs.readFileSync(new URL('../web/app.js',import.meta.url),'utf8');
test('truthful venue and unsupported-route disclosures are visible',()=>{for(const phrase of ['TESTNET ONLY','No third-party price, liquidity, public volume, users or counterparties','Cross-chain · unavailable','non-withdrawable venue credit'])assert.match(html,new RegExp(phrase.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'))) });
test('responsive and reduced-motion foundations exist',()=>{assert.match(css,/@media\(max-width:700px\)/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);assert.match(html,/name="viewport"/)});
test('accessibility landmarks, labels, live status and keyboard focus exist',()=>{for(const marker of ['<main id="main"','aria-label="Primary"','aria-live="polite"','Skip to trading workspace','focus-visible'])assert.ok(html.includes(marker)||css.includes(marker),marker)});
test('AI is bounded and cannot execute an order',()=>{assert.match(html,/AI can draft values only/);assert.match(js,/provider_unavailable|Provider unavailable/);const fn=js.slice(js.indexOf('async function requestAI'),js.indexOf('function renderAIState'));assert.doesNotMatch(fn,/\/v1\/orders|place_order/)});
test('legacy browser sessions are absent and record rendering avoids unsafe HTML',()=>{assert.doesNotMatch(js,/sessionStorage|localStorage|session_token|auth\/challenges|auth\/sessions/);assert.match(js,/requireCentralSession/);assert.match(js,/textContent=v/) });
test('solvency surface distinguishes liabilities from committed custody evidence',()=>{for(const phrase of ['Assets, liabilities & withdrawal capacity','Liability Merkle root','committed Testnet custody evidence','Other accounts are never exposed'])assert.match(html,new RegExp(phrase));assert.match(js,/\/v1\/solvency/);assert.match(js,/Committed custody assets/);assert.match(js,/assetsMicro==null\?'Unavailable'/);assert.match(js,/liability-proof\?asset=YNXT/)});
test('UltraLiquidity UI compares real complete routes without auto execution',()=>{for(const phrase of ['UltraLiquidity route','Native CLOB + committed consensus DEX','Quote only · never auto-executes','Compare executable costs'])assert.match(html,new RegExp(phrase.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(js,/\/v1\/liquidity\/quote/);assert.match(js,/No complete executable route/);const fn=js.slice(js.indexOf('async function quoteLiquidity'),js.indexOf('function renderActivity'));assert.doesNotMatch(fn,/\/orders|swaps\/exact/)});
test('browser Wallet login is real, device-bound and survives a second opening',()=>{assert.match(html,/Open YNX Wallet/);assert.match(html,/Install YNX Wallet/);for(const marker of ['createProductDeviceIdentity','encodeRequestDeepLink','verifyAuthorization','createGatewayChallenge','createProductSessionProof','indexedDB.open'])assert.match(js,new RegExp(marker));assert.match(js,/X-YNX-Product-Session-Proof/);assert.doesNotMatch(js,/Bearer/)});
test('perpetual UI exposes real risk, order book and owned margin without synthetic counterparties',()=>{for(const phrase of ['MARGIN & PERPETUAL','Oracle-gated Testnet derivatives','No synthetic counterparty','fails closed','Positions & risk'])assert.match(html,new RegExp(phrase.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(js,/\/v1\/risk/);assert.match(js,/\/v1\/perpetual\/orderbook/);assert.match(js,/snapshot\?\.margin/);assert.match(js,/requireCentralSession/)});
