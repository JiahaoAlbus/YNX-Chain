# Exchange cache-versioned runtime candidate — source only

- Implementation source: `d30f950733b0d938f9b20fce078b4eb6f24e53aa`, tree `688c039e462c71cfdbee6ccfb16be10a01f3a09f`, based on main `a2ac7f196d4aff9951b9ae94e27d8fde446859f0`.
- HTML binds `styles.css`, `wallet-connect.js`, and `app.js` by full content SHA-256 query. `app.js` binds all three module imports (`market-data.js`, `order-preview.js`, `private-session.js`) likewise. The runtime packager checks the final packaged bytes, not only source files. The server sends `Cache-Control: no-store` for HTML and SPA fallbacks.
- Local Chrome cache-enabled old-to-new fixture uses a real HTTP server and browser cache, not Playwright route interception. This is local browser evidence, not public wallet/account approval.
- `npm test --prefix apps/exchange`: 60/60. `npm run test:browser --prefix apps/exchange`: 5/5. `go test ./apps/exchange/... ./internal/exchangeproduct/...`: pass. `node apps/exchange/web/verify-versioned-assets.mjs`: pass. Shared Wallet source verifier: pass.
- Reproducible Linux amd64 candidate: `/tmp/ynx-exchange-d30f950733b0-a.tar.gz` and `-b.tar.gz`, each 3,890,271 bytes, SHA-256 `d04dcc47f455d844e8094e4490eac8105b87b70b043155cdd4af1752a666d64c`. `cmp` and clean-extract `SHA256SUMS` pass. Binary `ynx-exchanged` is ELF64 x86-64, 8,646,840 bytes, SHA-256 `70f754f3389c96b009202575a877ed553b6bd0d7263d05b0b550fc7b4bdc4ad2`.
- Earlier Mac-host packages from the same worktree were invalid Mach-O despite a Linux label. They are superseded and must not be deployed. The packager now fixes GOOS/GOARCH/CGO and rejects a non-ELF x86-64 binary.
- Deployment/public source binding: **false**. Public Exchange `/api/version` was observed on old commit `d736a87831fe83249ce7adfeae7a21efbc283b3e`; this candidate has not been switched or verified publicly. No account approval, signature, order, trade, installer, or Product Session completion is claimed.
- Next action belongs to the independent Exchange release controller: fresh read-only runtime/rollback binding, exact single-use lease, artifact transfer with SHA readback, rollback-first switch, `/api/version`/HTML/asset byte proof, then direct browser lifecycle. Rollback pointer must be freshly read before a switch; do not infer one from this local candidate.
