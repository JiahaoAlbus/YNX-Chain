# Exchange schema-v10 preservation checkpoint (source only)

- Owner branch: `codex/exchange-v10-preservation-20260925`; starting checkpoint `68b69608bfac5cc8bed60853d989076fff801cdd`.
- The d30 Exchange deployment failed before switching public runtime because the candidate had replaced the live schema-v10/36-field state reader with a schema-v1-only reader. The old d736 public runtime remained active; this checkpoint is not a deployment or trading result.
- Restored the historical Exchange schema-v10 order, conditional, OCO, TWAP, scale, margin/perpetual, risk, liquidity, quant-adapter, audit and migration domain from `d736a8783`, while retaining the newer guest market snapshot/SSE, trading-rule and settlement-invariant source.
- `internal/exchangeproduct/schema_v10_compatibility_test.go` creates a synthetic populated 36-field state, checks exact round-trip and no-write startup, then rejects tampering. It does not use or disclose production state.
- PostgreSQL startup now detects the previously released integrity-CAS and revision-CAS table layouts; an unsupported layout fails closed. A public startup refuses to auto-migrate an existing older state; that requires a separate reviewed migration. Finance read keeps a persistent PostgreSQL nonce guard and rejects an address without an economic record. Live PostgreSQL integration remains unverified without an isolated test database.
- The server entry point defaults to public mode and keeps Strategy Vault execution evidence false. Public reads are available; order, admin credit, and other financial writes as well as background TWAP/dead-man/oracle execution remain gated. An environment string is not accepted as custody evidence.
- Local checks: `go test ./internal/exchangeproduct ./apps/exchange/server`; `go test -race ./internal/exchangeproduct ./apps/exchange/server`; `go build ./apps/exchange/server`; `npm ci --ignore-scripts` for the Exchange Web and Wallet Auth local dependencies, then `npm test` in `apps/exchange` (60/60) and `npm run test:browser` (5/5). Browser checks used local fixtures and are not public Wallet approval evidence.
- Isolated old/new/old compatibility check on a **synthetic** 36-field schema-v10 file (`10,500` bytes, SHA-256 `a3afe6e0fc0ba0a4155d12a146c67d6dfe45797f6de904b03cd0bf8aeab18669`): old d736 binary SHA-256 `33e11702780c45753a949be51766d1adfad878e401a6fc786a01ad8902e84620` returned local `/api/health` 200 and `/api/v1/markets` 200; new candidate binary SHA-256 `f17614609a930189497de4620ec0c383c199f1ff1dfbc32a488370f6b46507c2` returned `/api/health` 200, `/api/ready` 503 `degraded_single_host`, `/api/v1/markets` 200 and `/api/v1/market-data/snapshot` 200. Restarting the old binary again returned health/markets 200. File SHA-256 stayed byte-identical after all three startups. This is isolated localhost evidence, not public deployment or a real-account test.
- Isolated local PostgreSQL 16-alpine (image ID `c05eced0bdb4`) used two separate disposable databases. Historical integrity-CAS layout passed `TestPostgresStateRepositoryBootstrapAndCAS` and `TestFinanceReadPostgresCrossInstanceNonceAndPersistedAccount`; later revision-CAS layout passed `TestPostgreSQLStateStoreMultiInstanceCASAndRestartRecovery` and the same cross-instance Finance nonce test. Each was run against real PostgreSQL, not an in-memory mock. The temporary container was stopped and removed; no production database was accessed.
- Not proved: safe public migration of a populated revision-CAS schema-v1 table to v10 (public startup intentionally refuses it); installed or source-bound public runtime; wallet approval; order/settlement on public Testnet. No release artifact is frozen by this checkpoint.
- Rollback: no production change occurred. To abandon this candidate, retain the old public d736 release; do not replace its state file or table with a schema-v1 projection.
