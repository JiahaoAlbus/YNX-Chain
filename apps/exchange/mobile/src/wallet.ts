import * as Crypto from "expo-crypto";
import * as Linking from "expo-linking";
import * as SecureStore from "expo-secure-store";
import {p256} from "@noble/curves/nist.js";
import {bytesToHex,hexToBytes} from "@noble/hashes/utils.js";
import {canonicalJSON,createGatewayChallenge,createProductSessionProof,encodeProductSessionProofHeader,encodeRequestDeepLink,httpBodyDigest,parseCallbackURL,requestDigest,signGatewayChallenge,verifyAuthorization,type AuthorizationRequest,type AuthorizationResponse,type CentralWalletSession} from "@ynx-chain/wallet-auth";
import {BUNDLE_ID,CALLBACK,CLIENT_ID,SCOPES} from "./wallet-config";

const DEVICE="ynx-exchange-device-p256-v1", PENDING="ynx-exchange-wallet-request-v1", SESSION="ynx-exchange-central-session-v1";
function encodeBase64url(bytes:Uint8Array){return btoa(String.fromCharCode(...bytes)).replace(/=+$/g,"").replace(/\+/g,"-").replace(/\//g,"_")}
export type CentralSession=CentralWalletSession;
async function deviceSecret(){let value=await SecureStore.getItemAsync(DEVICE);if(value&&/^[0-9a-f]{64}$/.test(value)&&p256.utils.isValidSecretKey(hexToBytes(value)))return value;for(;;){const bytes=await Crypto.getRandomBytesAsync(32);if(p256.utils.isValidSecretKey(bytes)){value=bytesToHex(bytes);await SecureStore.setItemAsync(DEVICE,value);return value}}}
export async function beginWalletSignIn(){const secret=await deviceSecret();const now=new Date();const request:AuthorizationRequest={version:"1",nonce:encodeBase64url(await Crypto.getRandomBytesAsync(24)),chainId:"ynx_6423-1",requestingProduct:"exchange",productClientId:CLIENT_ID,bundleId:BUNDLE_ID,productDeviceAlgorithm:"p256-sha256",productDeviceKey:encodeBase64url(p256.getPublicKey(hexToBytes(secret),true)),callback:CALLBACK,scopes:[...SCOPES],purpose:"Sign in to the YNX-owned deterministic testnet exchange; authorize only reviewed actions and never share a recovery key",issuedAt:now.toISOString(),expiresAt:new Date(now.getTime()+5*60_000).toISOString()};await SecureStore.setItemAsync(PENDING,JSON.stringify(request));await Linking.openURL(encodeRequestDeepLink(request));return request}
export async function finishWalletSignIn(url:string):Promise<CentralSession>{const raw=await SecureStore.getItemAsync(PENDING);if(!raw)throw new Error("No pending Wallet request exists.");const request=JSON.parse(raw) as AuthorizationRequest,now=new Date();const approval=verifyAuthorization(parseCallbackURL(url,CALLBACK),{...request,requestDigest:requestDigest(request),now}) as AuthorizationResponse;const gateway=process.env.EXPO_PUBLIC_YNX_EXCHANGE_GATEWAY_URL?.replace(/\/$/,"");if(!gateway)throw new Error("Wallet approval is valid, but Central Gateway routing is unavailable.");const challenge=createGatewayChallenge(approval,{challenge:encodeBase64url(await Crypto.getRandomBytesAsync(24)),expiresAt:new Date(Math.min(Date.parse(approval.expiresAt),now.getTime()+60_000)).toISOString()},now);const completion=signGatewayChallenge(challenge,secretBase64(await deviceSecret()));const envelope=await post(`${gateway}/v1/wallet/sessions/complete`,{authorizationRequest:request,walletApproval:approval,gatewayCompletion:completion});if(!envelope?.ok||!envelope.result)throw new Error("Central Gateway did not return a canonical Product Session.");const session=envelope.result as CentralSession;await SecureStore.setItemAsync(SESSION,JSON.stringify(session));await SecureStore.deleteItemAsync(PENDING);return session}
export async function restoreSession(){const raw=await SecureStore.getItemAsync(SESSION);if(!raw)return null;try{const v=JSON.parse(raw) as CentralSession;if(Date.parse(v.expiresAt)>Date.now())return v}catch{}await SecureStore.deleteItemAsync(SESSION);return null}
export async function signOut(){await SecureStore.deleteItemAsync(SESSION)}
export async function productSessionProof(session:CentralSession,scope:string){const body=canonicalJSON({requiredScopes:[scope]}),now=new Date(),proof=createProductSessionProof(session,{method:"POST",path:"/v1/wallet/sessions/introspect",bodyDigest:httpBodyDigest(body),nonce:encodeBase64url(await Crypto.getRandomBytesAsync(24)),issuedAt:now.toISOString(),expiresAt:new Date(Math.min(Date.parse(session.expiresAt),now.getTime()+30_000)).toISOString()},secretBase64(await deviceSecret()));return encodeProductSessionProofHeader(proof)}
function secretBase64(value:string){return encodeBase64url(hexToBytes(value))}
async function post(url:string,body:unknown){const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:canonicalJSON(body)});const value:any=await response.json().catch(()=>({}));if(!response.ok)throw new Error(value.error?.message||`Central Gateway returned ${response.status}`);return value}
