{
  "schemaVersion": 1,
  "schema": "security-platform/secret-inventory.schema.json",
  "source": "operator-maintained metadata; no value material",
  "asOf": "2026-07-25T06:30:00Z",
  "status": "not-configured",
  "requiredSecretTypes": [
    "validator-key",
    "faucet-key",
    "deploy-key",
    "treasury-key",
    "provider-credential",
    "database-credential",
    "api-credential",
    "artifact-signing-key",
    "mobile-signing-key",
    "tls-key",
    "backup-encryption-key",
    "recovery-key"
  ],
  "separationRules": [
    "one secret id maps to one purpose",
    "validator keys are never deploy credentials",
    "treasury keys are never available to CI",
    "provider credentials are never delivered to browsers",
    "artifact signing keys are never business signing keys",
    "backup encryption keys and recovery keys use separate recovery boundaries"
  ],
  "valueMaterialStored": false,
  "secrets": [],
  "blockedBy": [
    "production secret manager selection",
    "named owners and access approvers",
    "environment-specific workload identity",
    "rotation and recovery acceptance evidence"
  ]
}
