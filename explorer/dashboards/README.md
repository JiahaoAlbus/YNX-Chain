# dashboards

Explorer operational metrics are exposed by `ynx-explorerd` at `/metrics` and scraped by Prometheus. Dashboards should track RPC height, indexed height, sync lag, transaction visibility, and upstream health before any public Explorer availability claim.
